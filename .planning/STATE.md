---
gsd_state_version: 1.0
milestone: v1.1
milestone_name: "Milestone: GiftMaison visual refresh"
status: executing
stopped_at: Completed 17-07-backend-search-rescope-PLAN.md
last_updated: "2026-05-30T13:37:20.223Z"
last_activity: 2026-05-30
progress:
  total_phases: 17
  completed_phases: 16
  total_plans: 92
  completed_plans: 87
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-20)

**Core value:** Gift givers can reliably reserve and purchase gifts without duplicates — the reservation-to-purchase flow must be seamless and trustworthy.
**Current focus:** Phase 17 — discover-feature-with-community-popular-products-and-ai-powered-web-search-via-gemini

## Current Position

Phase: 17
Plan: Not started
Status: Ready to execute
Last activity: 2026-05-30

Progress: [█████████░] community-popular path shipped + working; search path being re-architected.

### Phase 17 search pivot (2026-05-28)

UAT-6 found Gemini `google_search` grounding hallucinates product titles onto real
product IDs (e.g. Montessori-toy title served as an Esprit T-shirt at eMAG /pd/DKT00YMBM/).
Decision: re-architect the search path — Gemini does intent extraction + query
generation + explanations ONLY; real products come from **Google Custom Search JSON API**
(no scraping, no hallucinated products). Full spec: `17-SEARCH-V2-SPEC.md`.

Locked decisions: formal re-plan · search-path only (community-popular untouched) ·
flat product list UI · Google CSE setup as a human-action checkpoint.

**Done + staying:** `discoverPopular`, popularItems triggers + backfill (17-02/03/04),
Stores decommission (17-01), Discover screen scaffolding + retailerName (17-05).
**Being replaced:** `discoverSearch` internals, `geminiClient` (drop grounding),
`enrichImages.ts` (CSE returns images), `promptTemplate.ts`, `retailers.ts`.
**UAT-6 is the open driver** — do NOT mark Phase 17 verified/complete until search v2 ships + re-UATs.

Next: `/gsd:plan-phase 17` (research + plan the search re-scope) after `/clear`.

Discover layout toggle (quick 260530-ncw) shipped + visually verified on emulator-5554 — session-local 1-col ⇄ 2-col, GridView / ViewAgenda icons, en+ro content descriptions.

**Phase 15 (web invite-landing) remains parked** — resume blocked on Identity Platform upgrade. linkInviteOnSignup must target pendingInvitedUsers per Phase 16 D-14 coupling (15-CONTEXT.md stanza appended 2026-05-24).

**v1.1 milestone:** all 14 phases that were planned for execution are now complete + verified. Phase 15 is the only outstanding phase, parked deliberately.

### Plan 14-04 closed (2026-05-22, with deferrals)

**Final tally:** 7/10 tasks PASS + 2 deferred + 1 close-out done.

**UAT items closed:** Items 1-7 ALL PASS in both Chrome and Safari (Pass 1
only — Pass 2 deferred). See
`.planning/phases/14-web-fallback-live-deploy-guest-uat/14-04-UAT-RESULTS.md`
"Plan 14-04 — Final Tally" section.

**5 silent production bugs caught and fixed during the plan:**

1. Cloud Tasks queue-name mismatch — `bf4ca31`
2. Cloud Tasks dispatch missing OIDC token — `d0c7516`
3. Cloud Tasks region default mismatch — `7ffb380`
4. `signInWithPopup` cross-origin opener channel restricted by browser policy — `3218a49`
5. `authDomain` origin mismatch with serving origin — `47c1bfa`

Plus post-PASS polish: `useAuth.isReady` gate to kill post-redirect flash — `1ac9f39`.

**Deferred to follow-up todos in `.planning/todos/pending/`:**

- `2026-05-22-uat-pass-2-recruited-giver-web-fallback-items-2-3.md` — Task 8
  recruited-giver Pass 2 of UAT items 2+3. Risk LOW (Pass 1 PASS in both
  browsers).

- `2026-05-22-wire-android-app-check-and-flip-enforcement.md` — Task 9 App
  Check enforcement flip (originally a must_have). Android app has NO App
  Check provider wired (grep-verified); flipping enforcement today would
  403-reject every Android request. Wire Android first, THEN flip
  Storage → Functions → Firestore per D-04.

**Plus pre-existing pending follow-up:**

- `2026-05-21-invite-email-cta-link-stripped-in-gmail-mobile.md` — SPF/DKIM
  for transactional emails; surfaced during UAT-5 invitee setup.

**Three folded todos closed by Phase 14:** Moved `pending/` → `completed/`:
`2026-04-20-register-firebase-web-app-and-deploy-real-web-config.md`,
`2026-04-20-fix-functions-tsconfig-and-env-handling-to-unblock-firebase-deploy.md`,
`2026-04-28-deploy-phase-12-storage-rules.md`.

**Next orchestrator step:** Phase 14 verification (`verify_phase_goal`). Do
NOT mark Phase 14 complete in ROADMAP until verifier passes — only Plan
14-04 is closed.

## Performance Metrics

**Velocity:**

- Total plans completed: 0
- Average duration: —
- Total execution time: 0 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| - | - | - | - |

**Recent Trend:**

- Last 5 plans: —
- Trend: —

*Updated after each plan completion*
| Phase 01-firebase-foundation P01 | 15 | 2 tasks | 9 files |
| Phase 01-firebase-foundation P03 | 3 | 2 tasks | 6 files |
| Phase 01-firebase-foundation P02 | 8min | 2 tasks | 6 files |
| Phase 02-android-core-auth P01 | 3min | 2 tasks | 17 files |
| Phase 02-android-core-auth P02 | 6min | 2 tasks | 15 files |
| Phase 02-android-core-auth P03 | 24min | 2 tasks | 11 files |
| Phase 02-android-core-auth P04 | 4min | 2 tasks | 6 files |
| Phase 03-registry-item-management P02 | 5min | 2 tasks | 4 files |
| Phase 03-registry-item-management P01 | 4min | 2 tasks | 13 files |
| Phase 03-registry-item-management P03 | 12 | 2 tasks | 18 files |
| Phase 03-registry-item-management P05 | 8min | 2 tasks | 16 files |
| Phase 04-reservation-system P01 | 2min | 2 tasks | 9 files |
| Phase 04-reservation-system P02 | 1min | 2 tasks | 3 files |
| Phase 04-reservation-system P03 | 2min | 2 tasks | 9 files |
| Phase 04-reservation-system P04 | 3min | 3 tasks | 9 files |
| Phase 04-reservation-system P05 | 8min | 2 tasks | 7 files |
| Phase 04-reservation-system P06 | 4min | 3 tasks | 9 files |
| Phase 05-web-fallback P01 | 8min | 2 tasks | 21 files |
| Phase 05-web-fallback P02 | 2min | 2 tasks | 4 files |
| Phase 05-web-fallback P03 | 2min | 2 tasks | 14 files |
| Phase 05-web-fallback P04 | 10min | 3 tasks | 13 files |
| Phase 05-web-fallback P05 | 3min | 2 tasks | 9 files |
| Phase 05-web-fallback P06 | 7min | 2 tasks | 13 files |
| Phase 05-web-fallback P07 | 7min | 2 tasks | 8 files |
| Phase 06-notifications-email-flows P00 | 7min | 3 tasks | 11 files |
| Phase 06-notifications-email-flows P03 | 4min | 3 tasks | 14 files |
| Phase 06-notifications-email-flows P01 | 7min | 2 tasks | 14 files |
| Phase 06-notifications-email-flows P02 | 10min | 2 tasks | 5 files |
| Phase 06 P05 | 4min | 3 tasks | 9 files |
| Phase 06-notifications-email-flows P04 | 12min | 3 tasks | 15 files |
| Phase 07-romanian-store-browser P00 | 7min | 3 tasks | 13 files |
| Phase 07-romanian-store-browser P01 | 8min | 2 tasks | 12 files |
| Phase 07-romanian-store-browser P02 | 3min | 2 tasks | 9 files |
| Phase 07-romanian-store-browser P03 | 12min | 3 tasks | 8 files |
| Phase 08-giftmaison-design-foundation P01 | 5min | 2 tasks | 8 files |
| Phase 09-shared-chrome-status-ui P01 | 2min | 2 tasks | 5 files |
| Phase 09-shared-chrome-status-ui P02 | 3min | 2 tasks | 6 files |
| Phase 09-shared-chrome-status-ui P03 | 4min | 2 tasks | 5 files |
| Phase 10-onboarding-home-redesign P01 | 2 | 2 tasks | 6 files |
| Phase 10-onboarding-home-redesign P02 | 45min | 2 tasks | 8 files |
| Phase 10 P03 | 4min | 2 tasks | 6 files |
| Phase 10-onboarding-home-redesign P04 | 3min | 2 tasks | 8 files |
| Phase 11-registry-detail-create-add-item-redesign P01 | 8min | 2 tasks | 7 files |
| Phase 11-registry-detail-create-add-item-redesign P02 | 4min | 2 tasks | 8 files |
| Phase 11 P03 | 5min | 2 tasks | 8 files |
| Phase 11 P04 | 4min | 2 tasks | 6 files |
| Phase 11 P05 | 5min | 2 tasks | 11 files |
| Phase 12-registry-cover-photo-themed-placeholder P01 | 8min | 3 tasks | 19 files |
| Phase 12-registry-cover-photo-themed-placeholder P03 | 10min | 3 tasks | 7 files |
| Phase 12-registry-cover-photo-themed-placeholder P02 | 9min | 4 tasks | 52 files |
| Phase 12-registry-cover-photo-themed-placeholder P04 | 11min | 3 tasks | 12 files |
| Phase 12-registry-cover-photo-themed-placeholder P05 | 12min | 3 tasks | 4 files |
| Phase 13 P00 | 2 min | 3 tasks | 3 files |
| Phase 13-web-fallback-visual-refresh P03 | 3min | 3 tasks | 4 files |
| Phase 13-web-fallback-visual-refresh P01 | 3min | 2 tasks | 7 files |
| Phase 13-web-fallback-visual-refresh P02 | 8min | 2 tasks | 6 files |
| Phase 13-web-fallback-visual-refresh P06 | 4min | 4 tasks | 8 files |
| Phase 13-web-fallback-visual-refresh P04 | 8min | 3 tasks | 7 files |
| Phase 13-web-fallback-visual-refresh P05 | 6min | 3 tasks | 6 files |
| Phase 14-web-fallback-live-deploy-guest-uat P01 | 90min | 7 tasks | 2 files |
| Phase 14-web-fallback-live-deploy-guest-uat P03 | 10min | 2 tasks | 0 files |
| Phase 14 P02 | 3min | 5 tasks | 5 files |
| Phase quick/260521-myv-update-shareable-registry-link-from-r-re P01 | 2min | 2 tasks | 3 files |
| Phase quick/260522-iew P01 | 2min | 2 tasks | 1 files |
| Phase quick/260522-iqv P01 | 3min | 2 tasks | 4 files |
| Phase 16-android-notifications-inbox-invite-accept-decline P01 | 9min | 2 tasks | 10 files |
| Phase 16-android-notifications-inbox-invite-accept-decline P03 | 3min | 1 tasks | 4 files |
| Phase 16 P02 | 4min | 2 tasks | 6 files |
| Phase 16-android-notifications-inbox-invite-accept-decline P04-invite-response-sheet-and-viewmodel | 6min | 3 tasks | 6 files |
| Phase 16-android-notifications-inbox-invite-accept-decline P05 | 7min | 3 tasks | 4 files |
| Phase 17 P01 | 12min | 3 tasks | 13 files |
| Phase 17 P02 | 4min | 2 tasks | 15 files |
| Phase 17-discover-feature-with-community-popular-products-and-ai-powered-web-search-via-gemini P03 | 3min | 2 tasks | 4 files |
| Phase 17-discover-feature-with-community-popular-products-and-ai-powered-web-search-via-gemini P04 | 4min | 2 tasks | 5 files |
| Phase 17 P05 | 11min | 4 tasks | 19 files |
| Phase 17 P07 | 38min | 4 tasks | 12 files |

## Accumulated Context

### Roadmap Evolution

- Phase 12 added: Registry Cover Photo & Themed Placeholder
- Phase 13 added: Web Fallback Live Deploy for guest experience (2026-04-30)
- Phase 13 renamed → "Web Fallback Visual Refresh"; Phase 14 added: "Web Fallback Live Deploy + Guest UAT" (2026-04-30, scope split — visual refresh per upcoming web design handoff, then deploy+UAT)
- Phase 15 added: Web Invite-Landing + Magic-Link Guest Flow (2026-05-22)
- Phase 15 parked 2026-05-24 — user pivoting to mobile work; resume with /gsd:execute-phase 15 (Identity Platform upgrade needed first)
- Phase 16 added: Android Notifications Inbox + Invite Accept/Decline (2026-05-24) — strict accept-gate model introduces new `pendingInvitedUsers` field; coupling note: when Phase 15 resumes, `linkInviteOnSignup` must target `pendingInvitedUsers` instead of `invitedUsers`
- Phase 17 added: Discover feature — "From the community" (popular products from existing registries) + "From the web" (Gemini 2.5 Flash with Google Search grounding, Romanian retailers, 30-day cache) (2026-05-26); replaces "Stores" bottom-nav entry; introduces backend endpoints `GET /discover/popular` + `POST /discover/search` (rate-limited 20/hr/user); GEMINI_API_KEY backend-only, never exposed to Android client

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- Init: Firebase over SQLite — real-time sync required for shared registries
- Init: Cloud Tasks (not cron) for reservation expiry — per-reservation precision required
- Init: Affiliate URL transformation in Cloud Functions only — never in APK; allows merchant rule updates without Play Store release
- Init: EMAG catalog phase deliberately last — no confirmed public catalog API; manual URL add is the primary path
- [Phase 01-firebase-foundation]: firebase-functions/v2 import pattern (2nd gen Cloud Functions) — 1st gen deprecated
- [Phase 01-firebase-foundation]: Emulator Suite singleProjectMode=true on ports Auth=9099, Functions=5001, Firestore=8080, Hosting=5000, UI=4000
- [Phase 01-firebase-foundation]: Feature-namespaced key convention established: app_, common_, auth_, registry_, reservation_ prefixes prevent key collision across features
- [Phase 01-firebase-foundation]: assetlinks.json uses PLACEHOLDER values — will be updated in Phase 2 (package name) and Phase 5 (SHA-256 fingerprint)
- [Phase 01-firebase-foundation]: invitedUsers map (not array) for O(1) membership check in Firestore security rules
- [Phase 01-firebase-foundation]: reservations collection hard-deny (allow read, write: if false) — Admin SDK bypasses rules for Cloud Functions
- [Phase 01-firebase-foundation]: users delete disabled — account deletion requires backend business logic, not raw client delete
- [Phase 02-android-core-auth]: KSP over KAPT for Hilt — AGP 9 built-in Kotlin makes KAPT incompatible; KSP 2.3.6 is the only viable annotation processor path
- [Phase 02-android-core-auth]: AppCompatActivity (not ComponentActivity) — required for AppCompatDelegate.setApplicationLocales() locale switching (I18N-UX-03)
- [Phase 02-android-core-auth]: Static seed color #6750A4, no dynamic color — ensures consistent brand identity across all Android versions including pre-Android 12
- [Phase 02-android-core-auth]: No org.jetbrains.kotlin.android plugin — AGP 9.1.0 has Kotlin built in; adding the plugin causes duplicate plugin application error
- [Phase 02-android-core-auth]: Zero Firebase imports in domain layer — pure Kotlin interfaces only, keeping domain testable without Android/Firebase dependencies
- [Phase 02-android-core-auth]: callbackFlow with awaitClose for Firebase AuthStateListener bridging — ensures listener cleanup on Flow cancellation
- [Phase 02-android-core-auth]: runCatching wraps Firebase suspend calls in AuthRepositoryImpl — converts exceptions to Result.failure keeping FirebaseExceptions out of domain
- [Phase 02-android-core-auth]: drop(1) on ObserveAuthStateUseCase Flow prevents auth screen flash on session restore
- [Phase 02-android-core-auth]: rememberViewModelStoreNavEntryDecorator absent from Navigation3 1.0.1 stable; hiltViewModel() uses Activity ViewModelStoreOwner
- [Phase 02-android-core-auth]: SignOutUseCase added to domain layer (was missing from Plan 02 use case list)
- [Phase 02-android-core-auth]: SharingStarted.Eagerly for SettingsViewModel.currentLocale — immediate emission on ViewModel init ensures stored locale reflects before any UI subscriber attaches
- [Phase 03-registry-item-management]: inviteToRegistry uses email: prefix for non-user invite keys to prevent collision with Firebase UIDs in invitedUsers map
- [Phase 03-registry-item-management]: fetchOgMetadata returns null fields on fetch failure so Android client can fall back to manual entry without HttpsError overhead
- [Phase 03-registry-item-management]: node-html-parser chosen over cheerio for smaller bundle size in Node.js 22 Cloud Functions runtime
- [Phase 03-registry-item-management]: AffiliateUrlTransformer uses placeholder affiliate IDs that must be replaced with BuildConfig fields before production
- [Phase 03-registry-item-management]: merchantRules map pattern in AffiliateUrlTransformer enables merchant extensibility without changing core logic
- [Phase 03-registry-item-management]: FirestoreDataSource mirrors FirebaseAuthDataSource pattern: callbackFlow + awaitClose for real-time Firestore observation
- [Phase 03-registry-item-management]: AffiliateUrlTransformer applied in ItemRepositoryImpl.addItem (data layer), not in AddItemUseCase, to keep affiliate logic out of domain
- [Phase 03-registry-item-management]: Registry screens (Plan 04 work) built as part of Plan 05 — parallel executor ran before Plan 04 dependency
- [Phase 03-registry-item-management]: deepLinkRegistryId extracted in MainActivity.onCreate and passed to AppNavigation for REG-08 cold-start deep link support
- [Phase 03-registry-item-management]: InviteBottomSheet resets inviteSent via resetInviteSent() — allows sending multiple invites per bottom sheet session
- [Phase 04-reservation-system]: createReservation stub omits CloudTasksClient import — dependency pre-wired in package.json but import deferred to Plan 02 to keep stub minimal
- [Phase 04-reservation-system]: releaseReservation throws Error (not HttpsError) since onTaskDispatched handlers do not use HttpsError
- [Phase 04-reservation-system]: createReservation wraps CloudTasksClient.createTask in try/catch so emulator build works without Cloud Tasks; cloudTaskName stored as empty string in transaction, updated via post-transaction .update()
- [Phase 04-reservation-system]: Guard transaction pattern for releaseReservation: status==active check + now>expiresAt check before any writes — idempotent handler survives Cloud Tasks retry
- [Phase 04-reservation-system]: GuestPreferencesDataStore uses DataStore name='guest_prefs' — unique Context extension property prevents runtime IllegalStateException from duplicate DataStore initialization
- [Phase 04-reservation-system]: GuestPreferencesDataStoreTest uses in-memory fake (not Robolectric) — verifies repository contract without Android framework; DataStore persistence verified manually per 04-VALIDATION.md
- [Phase 04-reservation-system]: ReservationRepositoryImpl payload includes giverId as nullable — supports both anonymous guests (null) and authenticated users passing Firebase UID
- [Phase 04-reservation-system]: ReservationEvent as Channel<BUFFERED> in ViewModel -- one-shot events collected in LaunchedEffect(Unit) so Intent fires exactly once per reservation
- [Phase 04-reservation-system]: Item domain model lacks expiresAt field -- countdown deferred, showing only Reserved label; no domain model changes made in Plan 04
- [Phase 04-reservation-system]: ReReserveDeepLink NavEntry navigates to HomeKey with Phase 6 TODO -- placeholder route exists so email stub URL is well-formed (RES-08 partial)
- [Phase 04-reservation-system]: doc.getTimestamp('expiresAt') used for reading server-written Timestamps — POJO auto-mapping to Long fails silently
- [Phase 04-reservation-system]: expiresAt excluded from Item.toMap()/toUpdateMap() — client must never write this field (D-08: no client-side timer authority)
- [Phase 04-reservation-system]: item.expiresAt?.let{} pattern for countdown display — backward compat with legacy reserved items that predate the field
- [Phase 04-reservation-system]: ReservationDeepLinkBus SharedFlow(replay=1) chosen over adding autoReserveItemId to RegistryDetailKey — avoids Navigation3 @Serializable default-value complications
- [Phase 04-reservation-system]: resolveReservation Cloud Function has no auth guard — giver arrives from email link unauthenticated (guest flow); returns only opaque navigation IDs, no PII
- [Phase 05-web-fallback]: Tailwind v3 (3.4.19) pinned for web scaffold — v4 changed config format to CSS-first; all UI-SPEC examples use v3 tailwind.config.ts syntax
- [Phase 05-web-fallback]: i18next ^26.0 — npm-verified current version; CLAUDE.md reference to v24 is stale per RESEARCH.md
- [Phase 05-web-fallback]: assetlinks.json migrated to web/public/.well-known/ — emptyOutDir: true deletes hosting/public/ on each Vite build; web/public/ is Vite static asset source
- [Phase 05-web-fallback]: ES module import hoisting means firebase.ts singletons are constructed before debug token code runs, but initializeAppCheck is called in module body before createRoot — network calls only fire when components read/write, after App Check is active
- [Phase 05-web-fallback]: VITE_RECAPTCHA_SITE_KEY absence in dev skips App Check with console.warn — no crash, frictionless local dev without reCAPTCHA registration
- [Phase 05-web-fallback]: i18n import order in main.tsx: import './i18n' at line 6, import App at line 7 — guarantees i18next initialized synchronously before any React component mounts
- [Phase 05-web-fallback]: React Router v7 import surface: from 'react-router' not 'react-router-dom' — v7 merged dom into main package; all Plan 04+ hooks import from 'react-router'
- [Phase 05-web-fallback]: Legacy seed sync: web/i18n/*.json kept byte-identical to web/src/i18n/*.json — prevents drift for any tool referencing the legacy seed path
- [Phase 05-web-fallback]: data: undefined|null|Registry semantics in useRegistryQuery — undefined=loading skeleton, null=not-found OR permission-denied (WEB-D-13+D-14), Registry=success
- [Phase 05-web-fallback]: Test pattern: await waitFor(handle not null) before firing onNext — useEffect registers callbacks asynchronously; calling before registration yields null data
- [Phase 05-web-fallback]: ItemCard.reserveSlot + ItemGrid.renderReserve extension points — Plan 06 injects real ReserveButton; Plan 04 renders disabled placeholder with min-h-[48px] touch target
- [Phase 05-web-fallback]: authProviders.ts catches auth/popup-closed-by-user and auth/cancelled-popup-request silently returning null — caller no-ops instead of showing error toast
- [Phase 05-web-fallback]: AuthModal Dialog.Description added as sr-only element — Radix warns when no description present; both modals have accessible descriptions
- [Phase 05-web-fallback]: httpsCallable called at module level (not inside hook) — callable is singleton; test must not reset httpsCallableMock in beforeEach
- [Phase 05-web-fallback]: useActiveReservation uses React state only (not localStorage) — tab-scoped UX acceptable for 30-min window per CONTEXT.md discretion
- [Phase 05-web-fallback]: RegistryPage/App tests: vi.mock all reservation + auth imports to avoid firebase/auth initialization in jsdom test environment
- [Phase 05-web-fallback]: useResolveReservation calls httpsCallable inside hook (not module level) — only used on ReReservePage, no singleton benefit; test mocks apply at call time without beforeEach reset constraint
- [Phase 05-web-fallback]: autoReserveFiredRef.current set before GuestIdentityModal open — prevents double-fire between setState calls; same ref prevents StrictMode double-effect
- [Phase 06-notifications-email-flows]: firebase-functions-test requires --legacy-peer-deps for npm install due to peer dep range declaration on firebase-admin (functional at runtime with admin@13.x)
- [Phase 06-notifications-email-flows]: mail + notifications_failures collections hard-deny all client access; fcmTokens subcollection owner-only via isSignedIn() + uid match
- [Phase 06-notifications-email-flows]: relaxed=true required for MockK mocking of final Firebase classes on Java 25 — Byte Buddy inline instrumentation does not support Java 25 bytecode
- [Phase 06-notifications-email-flows]: FCM token registration silently no-ops for anonymous users (return@runCatching Unit) — tokens are tied to user accounts
- [Phase 06-notifications-email-flows]: sendEmail validates to+subject presence and throws Error; locale defaults to en for both expiry and invite emails when recipient locale unknown (D-14 fallback)
- [Phase 06-notifications-email-flows]: invitePush handles both FCM throw and batch.commit cleanup failure independently, each writing to notifications_failures — best-effort with no error propagation
- [Phase 06-notifications-email-flows]: confirmPurchase has no auth guard — guest givers (anonymous) may confirm purchase per CONTEXT.md guest access constraint
- [Phase 06-notifications-email-flows]: Cloud Task deleteTask called after transaction commits in confirmPurchase — never inside runTransaction (Pitfall 2)
- [Phase 06-notifications-email-flows]: notifyOnPurchase defaults true when absent from registry doc — opt-out is explicit, opted-in by default
- [Phase 06]: useConfirmPurchase uses plain useState/useCallback (not TanStack useMutation) — httpsCallable created inside confirm() callback so test mocks bind at call time without beforeEach reset constraint
- [Phase 06]: ConfirmPurchaseBanner requires ConfirmPurchaseBanner mock in all RegistryPage test files to prevent transitive firebase.ts init failure in jsdom
- [Phase 06-notifications-email-flows]: MessagingHandler extracted as plain class for unit-testability — GiftRegistryMessagingService delegates; @AndroidEntryPoint(FirebaseMessagingService::class) + Hilt_GiftRegistryMessagingService required by KSP
- [Phase 06-notifications-email-flows]: SnackbarMessage sealed interface (Resource/Push) in RegistryDetailViewModel unifies confirm-purchase + FCM push snackbar routing; NotificationBus re-emitted via VM to keep Compose unaware of singleton bus
- [Phase 06-notifications-email-flows]: hasActiveReservation derived from combine(items, observeGuestIdentity) — no new DataStore reservation ID key needed for banner visibility
- [Phase 07-romanian-store-browser]: tsconfig include expanded to ['src', 'scripts'] — seed script in functions/scripts/ was excluded from compilation scope; build would fail without this change
- [Phase 07-romanian-store-browser]: Placeholder 1x1 transparent WebP files (34 bytes each) committed for all 9 store logos — real retailer logos must be supplied before v1.0 production release
- [Phase 07-romanian-store-browser]: StoresModule created as separate @Module (not added to DataModule) — keeps Phase 7 bindings discoverable and avoids touching DataModule which other phases may also modify
- [Phase 07-romanian-store-browser]: Manual snapshot.get('stores') cast over toObject — Firestore POJO mapper does not reliably map top-level arrays; confirmed by Research Pattern 2
- [Phase 07-romanian-store-browser]: DataStore name 'last_registry_prefs' verified unique against existing names (guest_prefs, language_prefs, onboarding_prefs) per Research Pitfall 3
- [Phase 07-romanian-store-browser]: StoreListKey declared as data class (not data object) to carry optional preSelectedRegistryId — enables registry-aware navigation from RegistryDetail without a separate nav key
- [Phase 07-romanian-store-browser]: entry<StoreBrowserKey> intentionally deferred to Plan 03 — tapping a store card is no-op in intermediate state, Navigation3 renders no content for unregistered keys without crashing
- [Phase 07-romanian-store-browser]: WebView ref held in Composable (remember mutableStateOf) not ViewModel — prevents Activity context leak; LaunchedEffect(homepageUrl) triggers initial load to avoid recomposition reloads in update lambda
- [Phase 07-romanian-store-browser]: External scheme blocking uses Toast inside shouldOverrideUrlLoading (MVP); Snackbar follow-up requires dedicated StateFlow — documented as known deviation
- [Phase 07-romanian-store-browser]: Add-to-list button disabled when registryId is null (Home-FAB entry path) — guards AddItemKey dispatch; D-10 registry picker is the follow-up
- [Phase 08-giftmaison-design-foundation]: Wave 0 TDD RED pattern: every phase requirement ships with an automated failing test before any implementation exists; Waves 1-2 must flip tests RED→GREEN to prove the handoff contract is satisfied
- [Phase 08-giftmaison-design-foundation]: Compose Google Fonts BOM-pinning: compose-ui-text-google-fonts alias declared without version.ref so BOM 2026.03.00 resolves it to 1.10.5 alongside other androidx.compose.ui:* modules
- [Phase 08-giftmaison-design-foundation]: Wordmark is unit-tested as a pure-Kotlin factory wordmarkAnnotatedString(ink, accent): AnnotatedString so Wave 2 must factor the @Composable GiftMaisonWordmark over this testable helper
- [Phase 08-giftmaison-design-foundation]: TypographyTest includes an explicit Pitfall 2 guard (everyRole_disablesFontPadding_pitfall2) asserting PlatformTextStyle(includeFontPadding = false) on all 10 type roles — catches future em-lineHeight regressions
- [Phase 09-shared-chrome-status-ui]: Wave 0 pure-Kotlin unit tests only: Compose UI test scaffolding deferred; unit tests cover logic; visual verification via StyleGuidePreview in Plan 04
- [Phase 09-shared-chrome-status-ui]: PulsingDot constants exposed as top-level const vals for Compose-framework-free unit testing; Plan 02 contract is PULSING_DOT_DEFAULT_PERIOD_MS=1400L + alpha 1f/0.5f + scale 1f/0.85f
- [Phase 09-shared-chrome-status-ui]: Asymmetric domain→display mapping pinned by test: ItemStatus.AVAILABLE→OPEN, ItemStatus.RESERVED→RESERVED, ItemStatus.PURCHASED→GIVEN (PURCHASED→GIVEN is highest-risk API pitfall)
- [Phase 09-shared-chrome-status-ui]: NavVisibility.kt stub created in Plan 02 to unblock BottomNavVisibilityTest compilation — all unit tests share a single compile step; correct showsBottomNav() logic available for Plan 03 to build on
- [Phase 09-shared-chrome-status-ui]: showsBottomNav() kept in NavVisibility.kt (Plan 02 stub) rather than merged into GiftMaisonBottomNav.kt — no duplicate declaration, tests already passing
- [Phase 09-shared-chrome-status-ui]: Icons.Outlined.KeyboardArrowRight used instead of Icons.AutoMirrored.Filled.ChevronRight — ChevronRight not in project Material Icons extended set
- [Phase 09-shared-chrome-status-ui]: showsBottomNav() import replaces local predicate — semantic change from persistent-everywhere to HomeKey+RegistryDetailKey only (intentional Phase 9 cutover per D-03)
- [Phase 09-shared-chrome-status-ui]: SuggestionChip imports kept in RegistryDetailScreen — file uses SuggestionChip for registry.occasion label in header; only inline ItemStatusChip composable deleted
- [Phase 10-onboarding-home-redesign]: Calendar-based startOfTodayMs (not LocalDate) for minSdk 23 compat — LocalDate.atStartOfDay() requires API 26
- [Phase 10-onboarding-home-redesign]: AUTH_SCREEN_DEFAULT_IS_SIGN_UP_MODE = true flips current false per CONTEXT.md D-02 (first-run new user creates account)
- [Phase 10-onboarding-home-redesign P02]: Canvas over layered Box for ConcentricRings — rings anchor at corner Offset(width, 0) and natural bounds-clip achieves handoff overflow:hidden
- [Phase 10-onboarding-home-redesign P02]: Provisional string refs: auth_google_sign_in_button (GoogleBanner) + auth_settings_title (AvatarButton) — both exist in strings.xml; Plans 03/04 rewire to final keys
- [Phase 10-onboarding-home-redesign P02]: FocusedFieldCaret: 1.1 s InfiniteRepeatableSpec RepeatMode.Reverse, opacity-only — distinct from PulsingDot 1.4 s opacity+scale
- [Phase 10]: AuthUiState.kt stubs from Plan 02 were correct (firstName/lastName + AUTH_SCREEN_DEFAULT_IS_SIGN_UP_MODE=true) — no edit needed in Plan 03
- [Phase 10]: Scaffold/SnackbarHost dropped from AuthScreen — inline warn-banner (colors.warn 0.15 alpha) replaces snackbar per RESEARCH.md Pattern 2
- [Phase 10]: AUTH_SCREEN_DEFAULT_IS_SIGN_UP_MODE = true exposed as top-level const so AuthFormStateTest can assert the flip without reading Compose state
- [Phase Phase 10-onboarding-home-redesign]: onNavigateToNotifications kept as no-op default in RegistryListScreen — inbox still reachable via deep link; bell placement deferred to Phase 11 per CONTEXT.md
- [Phase Phase 10-onboarding-home-redesign]: Tab index uses Int via rememberSaveable mutableIntStateOf(0), not sealed class, per RESEARCH.md Pitfall 3
- [Phase 11]: Wave 0 test package = impl package: tests in com.giftregistry.ui.registry.detail etc. so unqualified symbol references work once Plan 02 ships
- [Phase 11]: HeroToolbarAlpha Pitfall 1 guard pinned: firstVisibleItemIndex >= 1 must short-circuit to 1f to prevent toolbar flash-to-transparent when hero scrolls off-screen
- [Phase 11]: Legacy alias map pinned: Baby shower -> Baby, Anniversary -> Housewarming for backward compat with pre-Phase-11 Firestore docs
- [Phase 11]: ADD_ITEM_MODE_DEFAULT_ORDINAL exposed as top-level const val per Phase 10 rememberSaveable Int-backed state pattern; ordinal=0 maps to PasteUrl
- [Phase 11]: isAffiliateDomain inserted between extractDomain() and noMatch() in AffiliateUrlTransformer — additive, merchantRules stays private, transform() unchanged
- [Phase 11]: OccasionCatalog legacy aliases use lowercase keys so storageKeyFor() normalises via .lowercase() — handles any casing variant from legacy Firestore docs
- [Phase 11]: ShareBanner.onShared is a non-suspend lambda; caller wraps scope.launch to call showSnackbar — compile error auto-fixed
- [Phase 11]: onSkip default no-op on CreateRegistryScreen: EditRegistryKey entry backward-compat with zero changes
- [Phase 11]: Skip-save pattern: set 'Untitled draft' placeholder before onSave() to pass VM title.length 3..50; isDraft classified by itemCount==0
- [Phase 11]: addAnotherMode flag in Composable (not ViewModel) separates save+reset from save+pop — keeps ViewModel pure; flag resets after LaunchedEffect branch fires
- [Phase 11]: LaunchedEffect(selectedTab) resets selectedTabIndex before calling onNavigateToBrowseStores — ensures re-entry shows PasteUrl tab
- [Phase 12]: Wave 0 RED uses stub-bodies pattern (compile-but-fail-on-assertion) — Plans 02/03/04 flip RED→GREEN by replacing stub bodies, never editing tests
- [Phase 12]: RegistryRepository.newRegistryId() added as default-method (returns empty string) — D-07 hook without forcing edits to RegistryRepositoryImpl + FakeRegistryRepository
- [Phase 12]: D-07 + Pitfall 2 ordering pinned via coVerifyOrder { uploadCover; createRegistry } in CreateRegistryViewModelCoverTest — Plan 04 must satisfy via VM impl, NEVER edit the test
- [Phase 12]: firebase-storage 22.0.1 resolved via Firebase BoM 34.11.0 (no KTX); Storage Emulator port 9199 wired; storage.rules deferred to Plan 02
- [Phase 12]: CoverImageProcessorTest @Ignored at class level — Robolectric not on testRuntimeClasspath; Plan 02 picks fake-Bitmap or instrumented variant for D-06 size invariants
- [Phase 12-registry-cover-photo-themed-placeholder]: HeroImageOrPlaceholder accepts optional ColorFilter parameter — applied ONLY to AsyncImage branch so gradient placeholder pops at full brightness on dark-ink primary card
- [Phase 12-registry-cover-photo-themed-placeholder]: CoverPhotoPickerInline + CoverPhotoPickerSheet are string-resource-agnostic — caller (Plan 04) passes stringResource(...) values via parameters, keeping primitives reusable in @Preview
- [Phase 12-registry-cover-photo-themed-placeholder]: Pitfall 6 honored: 3-stop dark overlay stays at RegistryDetailHero call site gated on imageUrl != null; HeroImageOrPlaceholder owns ONLY the gradient+glyph fallback, never the dark overlay
- [Phase 12-registry-cover-photo-themed-placeholder]: Pitfall 1 imageUrl roundtrip fixed in RegistryDto + RegistryRepositoryImpl.toMap/toUpdateMap/toDomain — cover URL persists through Firestore create/update/observe
- [Phase 12-registry-cover-photo-themed-placeholder]: RegistryRepositoryImpl.newRegistryId() wired via firestore.collection('registries').document().id with FirebaseFirestore injected as third constructor param — D-07 enabler for Plan 12-04 upload-then-write
- [Phase 12-registry-cover-photo-themed-placeholder]: StorageDataSource split holds the D-05 path schema; StorageRepositoryImpl is a thin runCatching wrapper — keeps FirebaseExceptions out of the domain (Phase 02 D-08)
- [Phase 12-registry-cover-photo-themed-placeholder]: 36 placeholder JPEGs generated with Pillow (1280x720, ~12 KB each, GiftMaison palette base ± 12 brightness per index) — CONTEXT D-02 fallback acceptable; curation follow-up todo logged
- [Phase 12-registry-cover-photo-themed-placeholder]: storage.rules cross-service rules use firestore.get() to mirror firestore.rules canReadRegistry — same legacy-doc defaults for visibility/invitedUsers; deploy deferred to Plan 12-05 human checkpoint
- [Phase 12-registry-cover-photo-themed-placeholder]: CreateRegistryViewModel onSave pre-mints registryId via registryRepository.newRegistryId(), uploads BEFORE Firestore write, returns early on failure (zero orphan documents)
- [Phase 12-registry-cover-photo-themed-placeholder]: RegistryRepositoryImpl.createRegistry honours pre-set registry.id via new FirestoreDataSource.createRegistryWithId; legacy id='' callers fall through to auto-mint
- [Phase 12-registry-cover-photo-themed-placeholder]: D-13 owner-only tap on RegistryDetailHero implemented via nullable onCoverTap callback — guests pass null, clickable(enabled = false) is a no-op (no ripple, no pressed state)
- [Phase 12-registry-cover-photo-themed-placeholder]: CreateRegistryViewModelCoverTest contract assertions (coVerifyOrder, exactly=0, failure assertions) preserved verbatim; only Wave 0 fail() markers + simulated state replaced with VM-driven exercise
- [Phase 12-registry-cover-photo-themed-placeholder]: Cover-photo selection rehydration on Detail VM placed in second init { } block AFTER val registry — Kotlin runs init blocks in source order, before subsequent property initializers
- [quick-260428-tx9]: M3 Compose DatePickerDialog / AlertDialog{TimePicker} replace framework android.app dialogs on CreateRegistryScreen — framework dialogs read the system Android theme (default green/teal) bypassing MaterialTheme.colorScheme; M3 Compose pickers inherit colorScheme.primary (= gm.accent terracotta via LightColorScheme) with zero `colors=` overrides
- [quick-260428-tx9]: rememberDatePickerState UTC-midnight ↔ local-Calendar Long: convert symmetrically at the picker call site (decode UTC to civil Y/M/D; re-encode locally preserving prevHour/prevMin) — Bucharest UTC+2/+3 off-by-one guard
- [quick-260428-tx9]: M3 has no TimePickerDialog composable; documented standard is AlertDialog { TimePicker(state) } with confirmButton/dismissButton TextButtons calling viewModel.setEventTime(state.hour, state.minute)
- [quick-260428-tx9]: stringResource(android.R.string.ok / .cancel) over new strings.xml keys for picker buttons — system labels are translated by the platform on RO devices, no app keys needed
- [quick-260428-v0q]: `Any?.showsBottomNav()` inverted from visible-whitelist (HomeKey + RegistryDetailKey only — Plan 09-02 D-03) to hidden-whitelist (null, AuthKey, OnboardingKey, ReReserveDeepLink only) — bottom nav is now persistent on every authenticated destination; default-true makes any future post-auth nav key show the bar without an explicit add; AppNavigation.kt:128 consumer call site untouched; pinned by 14-case BottomNavVisibilityTest
- [Phase 12-registry-cover-photo-themed-placeholder]: Plan 12-05: 4 StyleGuidePreview @Preview sections shipped (HeroImageOrPlaceholder hero+card, CoverPhotoPickerInline 3 states, CoverPhotoPickerSheet inline-body, RegistryCard placeholder regression check) — Phase 12 visual contracts now reviewable in Studio without booting an emulator
- [Phase 12-registry-cover-photo-themed-placeholder]: Plan 12-05: storage rules live deploy DEFERRED via structured todo (.planning/todos/pending/2026-04-28-deploy-phase-12-storage-rules.md) — user resume signal was 'approved — storage deploy skipped'; production cover-photo upload traffic blocked until the deploy lands; preset selection paths unaffected
- [Phase 12-registry-cover-photo-themed-placeholder]: Plan 12-05: 12-VALIDATION.md signed off (status=approved, nyquist_compliant=true, wave_0_complete=true); Per-Task Verification Map has 15 rows; 7 of 8 Wave 0 RED suites GREEN; all 16 Decision IDs satisfied; Pitfalls 1+2+5+6+7 pinned
- [Phase quick-260507-uzv]: Owner-only UI affordances on RegistryDetailScreen gated via two-layer pattern: (1) trigger callback set to null at the call site (e.g. `onOverflow = if (isOwner) (...) else null`), AND (2) downstream menu/sheet Box wrapped in `if (isOwner) { ... }` for defence in depth — mirrors the existing D-13 `onCoverTap` nullable-callback convention, no new auth abstraction, no VM/strings/server-rule changes
- [Phase quick-260507-veb]: Three more owner-only RegistryDetailScreen affordances (top-bar Share OpenInNew icon, ShareBanner pill, AddItemTopCta '+ Add an item' CTA) gated via the same viewModel.isOwner StateFlow already used by quick-260507-uzv (kebab) + Phase 12 D-13 (cover-photo tap); RegistryDetailHero.onShare signature: () -> Unit -> (() -> Unit)? = null with IconButton wrapped in if (onShare != null) — third sibling to onOverflow + onCoverTap nullable callbacks; ShareBanner + AddItemTopCta LazyColumn items wrapped in if (isOwner) { item(...) { ... } } so non-owners never register those keys; Spacer(Modifier.weight(1f)) retained between Back and the now-conditional Share IconButton (kebab stays far-right for owners); zero changes to ViewModel, AuthRepository, ShareBanner, firestore.rules, storage.rules, Cloud Functions, strings.xml; no new tests (RegistryDetailViewModelIsOwnerTest already pins isOwner contract); StyleGuidePreview.kt's two RegistryDetailHero call sites also use named args and compile cleanly under the parameter reorder; Task 2 device verification deferred and combined with the still-pending quick-260507-uzv Task 2
- [Phase quick-260507-vrp]: Invitee dual-mode UI on EditItemScreen — RegistryItemRow Modifier.clickable(onClick = onTap) for row-level tap + showOverflow Boolean gate for kebab; EditItemViewModel grows 5 new constructor deps (AuthRepository, ObserveRegistryUseCase, ReserveItemUseCase, ConfirmPurchaseUseCase, GuestPreferencesRepository) all pre-provided in existing Hilt graph (no module changes); EditItemViewModel.isOwner is a verbatim mirror of RegistryDetailViewModel.isOwner (combine + Eagerly + initial-false + .catch{emit(false)}); ReservationEvent re-declared privately INSIDE EditItemViewModel rather than promoted to a top-level domain type — both VMs feed identical UI side effects, the 4-line duplication is cheaper than touching every test that names the type; snackbar payload on EditItemViewModel is raw Int resId NOT the SnackbarMessage sealed type from RegistryDetailViewModel — EditItemScreen does not consume FCM push events so the simpler payload keeps the new code surface small; giverId on the Android invitee path = authRepository.currentUser?.uid (signed-in invitees have a UID for analytics; the public web giver flow keeps passing null because anonymous); itemFlow added as separate reactive Item? observation (NOT folded into the existing init-block firstOrNull lookup) so status-based button gates update reactively when the Cloud Function flips RESERVED -> PURCHASED; performReservation + onConfirmPurchase mirror RegistryDetailViewModel verbatim (getGuestIdentity check, sheet on null, ReserveItemUseCase, setActiveReservationId persist on success, ShowConflictError on failure; confirm-purchase emits R.string success/error resId, clears activeReservationId on success); EditItemScreen Column body branches on if (isOwner) — owner = full-edit form verbatim, invitee = same 5 fields with enabled=false + Reserve/Mark-as-purchased Buttons gated by currentItem?.status; two LaunchedEffect(Unit) collectors mirror RegistryDetailScreen.kt:137-155 (Intent.ACTION_VIEW / sheet open / snackbar for events; snackbar + onBack() for confirm-purchase success); GuestIdentitySheet re-imported from ui.registry.detail (no extraction); 9 new tests GREEN (4 isOwner + 5 reservation, latter using turbine.test for the Channel/SharedFlow); zero touches to firestore.rules / storage.rules / Cloud Functions / GuestPreferencesRepository / ReservationRepository / ReserveItemUseCase / ConfirmPurchaseUseCase / AuthRepository / nav keys / strings.xml (EN+RO empty diff); Task 4 device verification combined with quick-260507-uzv + quick-260507-veb pending device passes
- [Phase 13]: [Phase 13-web-fallback-visual-refresh P00]: Tailwind gm.* tokens resolve to var(--gm-*) custom properties (D-07 indirection) — v1.2 occasion cascade can swap values at registry-detail root with zero class renames
- [Phase 13]: [Phase 13-web-fallback-visual-refresh P00]: Single shared @keyframes gm-pulse re-declared inside prefers-reduced-motion @media block — Tailwind animate-gm-pulse utility resolves to whichever definition wins; simpler than UI-SPEC's two-name variant, equivalent in behaviour
- [Phase 13]: [Phase 13-web-fallback-visual-refresh P00]: D-01 visual-only scope envelope anchored — Phase 13 ships visual-layer code only; no SSR/slug routing/cookies/OG-fn/re-reserve/4-occasion-cascade/backend changes; diff scope confined to web/tailwind.config.ts + web/src/index.css + web/index.html
- [Phase 13]: [Phase 13-web-fallback-visual-refresh P00]: Phase 5 legacy palette (primary/surface/destructive/outline) retained in tailwind.config.ts during cutover so all 107 existing tests stay green; legacy removal audit deferred to Phase 14
- [Phase 13-web-fallback-visual-refresh]: Plan 13-03: Banner heading shipped as triple (banner_heading_pre/emphasis/post) and banner subline as quadruple (banner_subline_pre/retailer/separator/countdown) replacing the v1.0 single-interpolated-string design — Plan 13-05 StickyReserveBanner can wrap item names in italic Display spans and render countdown digits in a separately-testable element without .replace() chains; mirrors detail_headline_* triple convention; trailing/leading whitespace lives inside _pre/_post slots so consumer concatenates without manual whitespace handling
- [Phase 13-web-fallback-visual-refresh]: Plan 13-03: web_auth.headline_pre kept empty string ('') because both EN and RO headlines start with the italic span (Pick up / Continuă) — empty pre slot is the contract that pinpoints whether the italic emphasis renders first; web_reserve.banner_subline_separator='  ·  ' (two-space middle-dot two-space) lives as own slot so RO can swap separator without touching pre/post
- [Phase 13-web-fallback-visual-refresh]: Plan 13-03: Used literal Unicode characters (not \u escapes) in new web_* namespaces — the existing file inconsistently mixed literal and escaped chars (e.g. ro.json had literal 'Redirecționare' alongside escaped 'încarcă'); after Task 1 verify passed cleanly with literal chars, kept literal chars throughout for readability — JSON parsers handle both identically; future plans should not regress to mixed encoding
- [Phase 13-web-fallback-visual-refresh]: Plan 13-03: 104-key parity between en.json and ro.json verified by node flatKeys diff; legacy seed at web/i18n/*.json byte-identical to web/src/i18n/*.json via plain cp (Phase 5 D-locked decision); future plans MUST run diff -q web/src/i18n/*.json web/i18n/*.json AND flatKeys parity check before committing any i18n change
- [Phase 13]: [Phase 13-web-fallback-visual-refresh P01]: 6 GiftMaison atoms (Wordmark/Pill/Btn/Field/PulseDot/MonoCaption) shipped as pure-presentational React components in web/src/components/giftmaison/ — Tailwind utilities only, no inline rgb/hex except documented bg-[oklch(...)] arbitrary values for Pill ok/warn tones; PulseDot halo via shadow-[0_0_0_Npx_var(--gm-accentSoft)] preserves D-07 occasion-cascade indirection; Field forwardRef enables react-hook-form register() spread; Btn drops Phase 5 focus:ring-* in favour of focus-visible:outline-gm-accent per UI-SPEC; barrel index.ts merged with parallel Plan 02 chrome exports (TopNav/Footer/StickyMobileBar) — intended end-state for Phase 13
- [Phase 13-web-fallback-visual-refresh]: Plan 13-02: TopNav.onSignInClick optional callback unifies modal (RegistryPage) + route (AuthScreen) sign-in flows in one component
- [Phase 13-web-fallback-visual-refresh]: Plan 13-02: Footer ships i18n defaultValue fallbacks for web_footer.copyright/terms/privacy — renders correctly before Plan 03 keys land, defaultValue branches become unreachable post-Plan-03
- [Phase 13-web-fallback-visual-refresh]: Plan 13-02: StickyMobileBar uses Tailwind arbitrary value pb-[calc(env(safe-area-inset-bottom)+12px)] for safe-area inset — Tailwind 3.4 square-bracket arbitrary utility supports env() in calc()
- [Phase 13-web-fallback-visual-refresh]: Plan 13-02: LanguageSwitcher single-button toggle (EN / RO active highlight) preserved over prototype's two-button shape — keeps Phase 5 5-test contract anchored on click→language change green, fewer DOM nodes
- [Phase 13-web-fallback-visual-refresh]: Plan 13-02: AppRootPage chosen as first design-system canary — minimal traffic + zero feature dependencies = lowest-risk first end-to-end render of Plan 00 tokens + Plan 01 atoms + Plan 02 chrome
- [Phase 13-web-fallback-visual-refresh]: Plan 13-06: Two-surface auth pattern shipped — full-page <AuthScreen /> at /sign-in (split lg:grid-cols-[520px_1fr] with EditorialPhoto desktop-only, StickyMobileBar guest skip on mobile, navigate(-1) post-success) AND restyled in-page <AuthModal /> preserving Phase 5 API verbatim (open/onOpenChange/onContinueAsGuest props; signInEmail/signUpEmail/signInWithGoogle wiring); both surfaces consume same atom anatomy (Btn/Field/MonoCaption barrel imports + tab switcher with border-b-2 gm-ink underline)
- [Phase 13-web-fallback-visual-refresh]: Plan 13-06: GuestSkipCard does NOT pin itself — atom is decoupled from breakpoint logic; AuthScreen renders it twice (hidden lg:block desktop inline + StickyMobileBar sm:hidden mobile-pinned) so a single React node covers both viewports without prop drilling; D-05 'must be reachable in 1 tap' satisfied via the StickyMobileBar wrapper
- [Phase 13-web-fallback-visual-refresh]: Plan 13-06: Self-host /auth-editorial.jpg (Unsplash photo-1513694203232-719a280e022f, 1200x801 progressive JPEG ~92 KB) at web/public/ — production has no runtime images.unsplash.com dependency, Vite emits to hosting/public/ on every build; og-default.png shipped as real Pillow-rendered 1200x630 placeholder with italic giftmaison wordmark + terracotta accent period (NOT 1x1 transparent fallback) since Pillow was available
- [Phase 13-web-fallback-visual-refresh]: Plan 13-06: AuthModal.test.tsx adds vi.mock('../../../firebase') + vi.mock('../useAuth') because the restyled modal imports atoms via the giftmaison barrel which transitively imports TopNav -> useAuth -> firebase/auth (jsdom auth/invalid-api-key crash); plan's verify chain mandated the literal barrel import, so test-side mock was the resolution path (not barrel-bypass); pattern reusable for any future component pulling the giftmaison barrel into a jsdom test
- [Phase 13-web-fallback-visual-refresh]: Plan 13-04: ItemCard.tsx imports atoms directly from giftmaison/Pill / /PulseDot / /MonoCaption (not the barrel) — barrel re-exports TopNav→useAuth→firebase/auth, which crashes ItemCard.test.tsx in jsdom without firebase mocks; direct imports keep the unit-test import graph firebase-free
- [Phase 13-web-fallback-visual-refresh]: Plan 13-04: Hero headline (RegistryHeader) kept upright (no italic-accent emphasis span) — UI-SPEC D-13 reads italic-accent as wordmark + screens 02/03 only; the screens with explicit pre/emphasis/post i18n triples (Plans 13-05 / 13-06) own the italic emphasis, RegistryHeader honours D-13 literally
- [Phase 13-web-fallback-visual-refresh]: Plan 13-04: Lifted-filter-state pattern — RegistryPage owns useState<ItemFilter>('all'); FilterChips controlled via active+onChange; ItemGrid receives filter prop and applies item.status === filter predicate inline; counts via useMemo(itemsQ.data) drive optional FilterChips badges + ProgressStrip totalChosen
- [Phase 13-web-fallback-visual-refresh]: Plan 13-04: ItemCard purchased status keeps Pill at top-left (NOT bottom-left per the ASCII contract) — outer opacity-[0.55] + image grayscale carry the purchased signal; consistency across statuses preserves grid visual rhythm; Plan 07 may revisit if visual review surfaces a regression
- [Phase 13-web-fallback-visual-refresh]: [Phase 13-05] Minute-cached aria-live string instance via lastMinutesRef + cachedAriaTextRef pair — text-node identity preserved between minute ticks so screen readers re-announce only on minute flip
- [Phase 13-web-fallback-visual-refresh]: [Phase 13-05] Native <details> element for HowTimerWorks mobile collapsible — chosen over useState for zero-JS accessibility-built-in primitive; lg:hidden summary + hidden lg:block heading split keeps desktop always-open
- [Phase 13-web-fallback-visual-refresh]: [Phase 13-05] ReservationBanner.tsx kept as deprecated re-export of StickyReserveBanner — 4 test suites vi.mock the path, deferred deletion to Phase 14 cleanup
- [Phase 13-web-fallback-visual-refresh]: [Phase 13-05] ConfirmPurchaseBanner gains optional minutesLeft prop (default 30) — confirm_body interpolates {{minutes}}, ReserveDetailSection passes live countdown.minutes
- [Phase 13]: GuestIdentityModal uses barrel import (not direct atom import) — its existing test (GuestIdentityModal.test.tsx) is firebase-free from Phase 5, probing only form behaviour; no firebase mock needed unlike AuthModal.test.tsx
- [Phase 13]: ToastProvider severity pattern: left-border accent (border-l-4 gm-accent/warn/line) rather than full-card colouring — keeps card legible on paper bg while communicating severity; success=terracotta, error=amber, neutral=faint
- [Phase 14-web-fallback-live-deploy-guest-uat]: Fix-forward rollback: firebase hosting:rollback restores broken blank-page bundle — always redeploy corrected build instead
- [Phase 14-web-fallback-live-deploy-guest-uat]: App Check stays monitor-only on first deploy: VITE_RECAPTCHA_SITE_KEY empty per D-04; Plan 14-04 wires site key after monitor-mode verification
- [Phase 14-web-fallback-live-deploy-guest-uat]: Firestore guest-read PERMISSION_DENIED deferred to Plan 14-03: stale prod rules lack visibility.get default; Wave 1 goal (non-blank page) fully met
- [Phase 14-web-fallback-live-deploy-guest-uat]: firestore:rules get subcommand unavailable in installed firebase-tools; deploy-log grep gate + SHA256 fingerprint used as parity fallback for deployed rules verification
- [Phase 14-web-fallback-live-deploy-guest-uat]: 403 on registries/heTs42jyX1XPDtBSJbR3 is NOT caused by stale rules (defensive visibility.get defaults were already deployed); root cause is registry visibility/existence — deferred to Plan 14-04 UAT inspection
- [Phase 14]: Committed functions/.env with PUBLIC_WEB_BASE_URL (public Firebase Hosting URL — not a secret); enables firebase deploy --only functions from a fresh clone
- [Phase 14]: healthCheck in us-central1 is a pre-existing condition — region is immutable in Cloud Run; documented not fixed
- [Phase quick/260521-myv-update-shareable-registry-link-from-r-re]: Android share URL flipped /r/{id} → /registry/{id} to match canonical web fallback route; no redirect added (web never had /r/:id)
- [Phase quick/260522-iew]: Added both reservations composite indexes simultaneously (signed-in + guest path) — same query shape, both would have hit FAILED_PRECONDITION
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Wave 0 RED scaffolding: 5 Android tests + 2 Functions tests + modified inviteToRegistry + rules + composite index — production handlers/enum/VM intentionally absent so compile/module errors ARE the RED state
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Pattern 8 verified: firestore.rules need NO change for pendingInvitedUsers — isInvited() reads invitedUsers only; 4 new D-18/D-19 rules tests pass against existing rules
- [Phase 16-android-notifications-inbox-invite-accept-decline]: declineInvite idempotency contract: throw failed-precondition NO_PENDING_INVITE when uid is in neither pending nor invited — symmetric with acceptInvite. Plan 16-02 can flip to no-op success with a one-line test change if UX demands
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-03 reused existing AppModule.provideFirebaseFunctions Hilt binding for NotificationRepositoryImpl — no new module needed.
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-03 preserved Notification data class signature verbatim (D-26) — 4 new payload keys flow through existing Map<String, String?> with no breaking change.
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-03 Rule 3 fix: added placeholder when-branches in NotificationsScreen.kt (toIcon, localizedTitle, localizedBody) for 3 new enum values — Plan 16-05 replaces with proper UI.
- [Phase 16]: buildEnrichedInvitePayload returns eventDateMs as number (not String) to match Wave 0 test contract; writeNotification payload accepts number
- [Phase 16]: NotificationType union in writeNotification.ts extended with invite_accepted_self/invite_accepted/invite_declined as a Rule 3 blocking type-level fix
- [Phase 16]: FieldPath used consistently for all pendingInvitedUsers/invitedUsers writes — belt-and-suspenders that survives future email-keyed entries
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-04: Host-in-screen sheet pattern (parent ViewModel owns sheet visibility StateFlow + tap-time pure-Kotlin predicate decides sheet vs navigate) — avoids sheet-as-NavKey back-stack pollution; predicate is JVM-unit-testable.
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-04: Action-tagged Error state — State.Error(action: Action, messageKey: String) lets UI render localized copy at render time without baking strings into the VM; VM stays pure-JVM-testable (no Robolectric needed).
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-04: Stub-string + LocalizationParityTest pattern — when a UI plan ships before its strings plan, stub keys in BOTH locales with English placeholder copy. Key parity (not copy parity) is the test contract; keeps build green and preserves downstream plan's copy-ownership.
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-04: Sheet hosted OUTSIDE the Scaffold (after closing brace) so the ModalBottomSheet scrim covers the bottom nav. Chrome contract since Phase 12 — sheets ALWAYS hoist above the Scaffold.
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-05: Deferred InviteResponseSheet @Preview to on-device UAT in Plan 16-06 — ModalBottomSheet does not render statically in @Preview without a complex harness (same precedent as Phase 12 CoverPhotoPickerSheetPreview)
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-05: Removed D-02/D-08 negative-coverage comment text from production KDoc so grep-only verification (! grep -E 'Badge\(|BadgedBox|pendingCount') passes — the constraint is enforced by code shape, not by comment search
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-06: App Check init order at Application.onCreate — FirebaseApp.initializeApp → installAppCheckProviderFactory (debug for emulator, PlayIntegrity for release) → THEN any Firebase product reads; reusable pattern for v1.0+ phases shipping App Check-enforced callables
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-06: FCM token registration via FirebaseAuth.addAuthStateListener in Application.onCreate (NOT onNewToken-only) — onNewToken fires only on install + token rotation; post-install sign-ins leave users/{uid}/fcmTokens empty. Canonical pattern for any future phase depending on FCM push for signed-in users
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-06 Task 7: clamp createdAtMs to minOf(createdAtMs, now) at every DateUtils.getRelativeTimeSpanString call site reading Firestore-serverTimestamp Longs — prevents Romanian future-tense ("peste 0 minute") rendering when server clock is microseconds ahead of device clock. EN-CLDR is symmetric so bug only manifests in locales with distinct past/future prepositions
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-06 Task 8: JetBrainsMonoFamily switched from GoogleFont async to bundled TTFs (v2.304 Medium + SemiBold, OFL-licensed) — third application of the 260427-lnq cutover pattern after InstrumentSerifFamily; resolved per-glyph fallback inconsistency on NotificationsScreen timestamps. Future font families should ship bundled from day one
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-06: Mid-UAT deviation fix workflow — small UAT-surfaced bugs (Tasks 4, 5, 7, 8 — bell entry point, FCM-token-on-signin, PESTE clamp, JetBrains Mono bundle) committed atomically with deviation notes in commit messages rather than routing through /gsd:plan-phase --gaps. Each ~30-100 LoC, single root cause. Pattern reusable for future polish that emerges mid-execution
- [Phase 16-android-notifications-inbox-invite-accept-decline]: Plan 16-06: UAT runs against PRODUCTION backend on both emulator + physical (-Puse_emulator=false) — production parity outweighs emulator friction because deployed callables + Cloud Tasks behavior only match reality with real backend. Documented as task #12 to add -Puse_emulator=false flag note to CLAUDE.md
- [Phase 17]: Plan 17-01: Stores capability fully decommissioned — 32 files deleted (Android source/tests, drawables, Functions seed). AddItemMode collapsed to { PasteUrl, Manual }. nav_stores_tab string deliberately retained until Plan 17-05 lock-step rename (build-safety departure from D-42).
- [Phase 17]: Two-phase nav slot rotation pattern: Plan 17-01 empties slot 2 (deletes Stores keys, leaves onStores callback as empty no-op stub); Plan 17-05 wires Discover + deletes the surviving nav_stores_tab string in the same commit as the GiftMaisonBottomNav rename. Avoids broken-build window.
- [Phase 17]: geminiClient.ts intentionally NOT unit-tested per D-50 precedent — pure-function tests cover the parse/normalize contracts; network call verified end-to-end via Plan 17-06 smoke.
- [Phase 17]: cacheKey extracted to its own file (functions/src/discover/cacheKey.ts) rather than inline in search.ts — keeps the rate-limit/cache key derivation independently testable + reusable from triggers.
- [Phase 17-discover-feature-with-community-popular-products-and-ai-powered-web-search-via-gemini]: TTL-deadline write pattern: every TTL-managed Firestore field stores (now + TTL_MS) so the doc survives the full window; never use serverTimestamp() on TTL fields
- [Phase 17-discover-feature-with-community-popular-products-and-ai-powered-web-search-via-gemini]: Callable handler split pattern: export both xHandler(request) (testable) and x = onCall(opts, xHandler) (deploy artifact) — enables unit tests without full onCall wiring
- [Phase 17-discover-feature-with-community-popular-products-and-ai-powered-web-search-via-gemini]: discoverPopular L1 cache (module-scope, 1h TTL) means new popularItems writes from triggers (plan 17-04) will not surface for up to 1h on warm Function instances — acceptable per D-21
- [Phase 17-discover-feature-with-community-popular-products-and-ai-powered-web-search-via-gemini]: popularItems triggers use runTransaction (not batch) so the registryIds-membership idempotency guard and the delete-on-zero check are atomic with the FieldValue.increment write
- [Phase 17-discover-feature-with-community-popular-products-and-ai-powered-web-search-via-gemini]: Backfill aggregates in-memory via Map<productId, Aggregate> with first-observed denorm fields and registryIds: Set<string>; gracefully skips malformed URLs without aborting the scan
- [Phase 17-discover-feature-with-community-popular-products-and-ai-powered-web-search-via-gemini]: Deploy ordering for plan 17-06: run backfill BEFORE deploying triggers (D-22) — otherwise live triggers race with batched writes and produce double-counts
- [Phase 17]: Reuse AppModule.provideFirebaseFunctions (europe-west3) for Discover repository — no per-feature region override.
- [Phase 17]: Coil 3 (coil3.compose.AsyncImage) namespace used app-wide; placeholder drawable uses aapt:attr gradient inside vector (minSdk 23 ok).
- [Phase 17]: Lock-step nav slot rotation completed: GiftMaisonBottomNav slot 2 renamed (Stores→Discover) and nav_stores_tab deleted in same commit.
- [Phase 17]: 17-07: Serper.dev /shopping chosen as CSE fallback after CSE 403; hostname post-filter in code replaces PSE pre-filter
- [Phase 17]: 17-07: cseQuery renamed to searchQuery in INTENT_SCHEMA and 3 files for provider-agnosticism
- [Phase 17]: 17-07: runSearchPipeline exported with injectable PipelineDeps to enable unit-testing without Firestore

### Pending Todos

- [2026-04-20 general] Register Firebase Web app and deploy real web config — `https://gift-registry-ro.web.app` blank because no Web app registered + missing `web/.env.local` → `initializeApp({})` throws. See `.planning/todos/pending/2026-04-20-register-firebase-web-app-and-deploy-real-web-config.md`
- [2026-04-20 ui] Group registries by ownership and clarify invitee permissions — after shared-with-me query shipped, add "Your lists" / "Shared with you" section headers on Home + audit Detail screen affordances (FAB, Invite, Edit, Delete) for invitee-aware hide/disable. See `.planning/todos/pending/2026-04-20-group-registries-by-ownership-and-clarify-invitee-permissions.md`
- [2026-04-20 tooling] Fix functions tsconfig and env handling to unblock firebase deploy — `functions/tsconfig.json` produces `lib/src/*.js` instead of `lib/*.js`; `defineString` balks in non-interactive deploys. Blocks production deploy of quick-260420-ozb notifications work. See `.planning/todos/pending/2026-04-20-fix-functions-tsconfig-and-env-handling-to-unblock-firebase-deploy.md`
- [2026-05-21 api] Fix URL import for JS-rendered retailer pages — lego.com (and likely nike.com / apple.com / zara.com / adidas.com / hm.com / zalando.ro) are Next.js SPAs behind Akamai bot protection; `fetchOgMetadata.ts` returns all-null because the static HTML has no OG/JSON-LD tags. UX-degradation only (manual entry still works). Defer until multiple user reports justify the engineering cost. See `.planning/todos/pending/2026-05-21-fix-url-import-for-js-rendered-retailer-pages.md`

### Blockers/Concerns

- ~~Phase 7 (EMAG Catalog): EMAG has no confirmed public product catalog API~~ — **Resolved 2026-04-19**: Phase 7 re-scoped to "Romanian Store Browser" (in-app WebView + persistent Add-to-list CTA). Retires ITEM-03/04 and introduces STORE-01..04.
- ~~Phase 4 (Reservation): Cloud Tasks cancellation API~~ — **Resolved**: `cloudTaskName` pattern verified and working (used in createReservation + confirmPurchase with NOT_FOUND swallowing).

### Quick Tasks Completed

| # | Description | Date | Commit | Status | Directory |
|---|-------------|------|--------|--------|-----------|
| 260419-ubj | Onboarding carousel (3 slides) before auth screen | 2026-04-19 | e1aeb0f |  | [260419-ubj-onboarding-carousel-3-slides-before-auth](./quick/260419-ubj-onboarding-carousel-3-slides-before-auth/) |
| 260420-gat | Harden firestore.rules against missing fields on legacy registry docs | 2026-04-20 | 05207a6 |  | [260420-gat-harden-firestore-rules-against-missing-f](./quick/260420-gat-harden-firestore-rules-against-missing-f/) |
| 260420-gv2 | Gate Firebase emulator wiring on `-Puse_emulator` Gradle property | 2026-04-20 | 9c96e55 |  | [260420-gv2-gate-firebase-emulator-wiring-on-puse-em](./quick/260420-gv2-gate-firebase-emulator-wiring-on-puse-em/) |
| 260420-hcw | Wire Coil image rendering — preview in AddItemScreen + thumbnails in item list | 2026-04-20 | 75d4e61 |  | [260420-hcw-wire-coil-image-rendering-preview-in-add](./quick/260420-hcw-wire-coil-image-rendering-preview-in-add/) |
| 260420-hgb | Collapse two language pickers into one — single picker drives both app UI locale and Firestore preferredLocale for emails | 2026-04-20 | f069c8e |  | [260420-hgb-remove-the-email-language-setting-from-s](./quick/260420-hgb-remove-the-email-language-setting-from-s/) |
| 260420-hua | Bottom nav (Home, Add list, Browse stores, Preferences) + direct-create FAB on home | 2026-04-20 | be2f66b |  | [260420-hua-bottom-nav-home-add-list-browse-stores-p](./quick/260420-hua-bottom-nav-home-add-list-browse-stores-p/) |
| 260420-iic | Fix stale-UID bug — make RegistryListViewModel reactive to authState | 2026-04-20 | 4ff3973 |  | [260420-iic-fix-stale-uid-bug-make-registrylistviewm](./quick/260420-iic-fix-stale-uid-bug-make-registrylistviewm/) |
| 260420-iro | Persistent bottom nav across all screens + RegistryDetail FAB direct-opens Add Item form | 2026-04-20 | 9a842f4 |  | [260420-iro-persistent-bottom-nav-across-all-screens](./quick/260420-iro-persistent-bottom-nav-across-all-screens/) |
| 260420-jlg | Invite bottom sheet — show success confirmation and auto-dismiss instead of silent flip | 2026-04-20 | 2f17c7e |  | [260420-jlg-invite-bottom-sheet-show-success-confirm](./quick/260420-jlg-invite-bottom-sheet-show-success-confirm/) |
| 260420-nh8 | Fix email invite URL + deploy hosting and 3 functions to gift-registry-ro | 2026-04-20 | 41ed064 |  | [260420-nh8-fix-email-invite-url-and-deploy-firebase](./quick/260420-nh8-fix-email-invite-url-and-deploy-firebase/) |
| 260420-o6w | Show shared-with-me registries — add invitedUsers query + merge/dedupe/sort in repository | 2026-04-20 | d86577d |  | [260420-o6w-show-shared-with-me-registries-query-reg](./quick/260420-o6w-show-shared-with-me-registries-query-reg/) |
| 260420-ozb | Persistent in-app notifications inbox — top-bar bell, 5 event sources via writeNotification helper, EN+RO strings | 2026-04-20 | cbc8b9a |  | [260420-ozb-add-persistent-in-app-notifications-inbo](./quick/260420-ozb-add-persistent-in-app-notifications-inbo/) |
| 260421-lwi | Fix typography letter-spacing units (em→sp) — Phase 8 Typography used `.em` for letter-spacing but handoff JSX values are pixel-equivalent; fixed all 10 TextStyle roles + 8 TypographyTest assertions | 2026-04-21 | 7743d35 |  | [260421-lwi-fix-typography-letter-spacing-units-em-t](./quick/260421-lwi-fix-typography-letter-spacing-units-em-t/) |
| 260421-moi | Fix bottom nav label truncation — weight all 5 slots evenly, remove hardcoded FAB slot width, add maxLines/softWrap guards; remove duplicate RegistryListScreen legacy FAB | 2026-04-21 | c1a1f65 |  | [260421-moi-fix-bottom-nav-label-truncation-weight-s](./quick/260421-moi-fix-bottom-nav-label-truncation-weight-s/) |
| 260421-moi | Fix bottom-nav label truncation + remove duplicate RegistryListScreen FAB — 5-slot equal-width via Modifier.weight(1f), softWrap=false label guards, legacy Scaffold FAB removed | 2026-04-21 | 6139b9c |  | [260421-moi-fix-bottom-nav-label-truncation-weight-s](./quick/260421-moi-fix-bottom-nav-label-truncation-weight-s/) |
| 260427-gld | Fix wordmark letterSpacing em→sp + decouple ConcentricRings from GoogleBanner height — matchParentSize overlay pattern restores Auth screen wordmark legibility and ~68 dp banner height | 2026-04-27 | 82d8125 |  | [260427-gld-fix-wordmark-letterspacing-em-to-sp-and-](./quick/260427-gld-fix-wordmark-letterspacing-em-to-sp-and-/) |
| 260427-gxu | Bundle Instrument Serif TTFs as GMS-first/bundled-fallback + fix Auth headline to 2-span (ink line 1, accent line 2 incl. period) — OFL licence in assets/licenses/; 6/6 AuthHeadlineTest pass | 2026-04-27 | d4fccf8 |  | [260427-gxu-fix-auth-headline-colors-line-2-should-b](./quick/260427-gxu-fix-auth-headline-colors-line-2-should-b/) |
| 260427-lnq | Reorder InstrumentSerifFamily — bundled fonts promoted to sole entries, GMS async entries removed; synchronous serif rendering app-wide | 2026-04-21 | abd6028 |  | [260427-lnq-reorder-instrumentseriffamily-to-put-bun](./quick/260427-lnq-reorder-instrumentseriffamily-to-put-bun/) |
| 260427-lwz | Fix bottom nav clipping + FAB optical alignment — Row content height 56 dp -> 72 dp so HOME/STORES/LISTS/YOU labels and ADD caption render fully; FAB 22 dp lift preserved | 2026-04-27 | b21e24e |  | [260427-lwz-fix-bottom-nav-clipping-and-fab-alignmen](./quick/260427-lwz-fix-bottom-nav-clipping-and-fab-alignmen/) |
| 260427-n67 | Align ADD label with other nav labels — FabSlot mirrors NavItemSlot column (wrap-content + 44 dp inner Box scaffold + 4 dp Spacer); FAB rendered at 54 dp via requiredSize, 22 dp lift preserved via offset; Task 2 human-verify outstanding | 2026-04-27 | 5fe7c87 |  | [260427-n67-align-add-label-with-other-nav-labels-mi](./quick/260427-n67-align-add-label-with-other-nav-labels-mi/) |
| 260427-nkn | Drop FAB lift — plus icon must sit below the bar's top border line; removed `.offset(y = -22.dp)` from FabSlot + unused `offset` import; FAB now flush within 72 dp bar (FAB top ~4 dp below gray border, no protrusion); KDocs updated to record handoff override per user feedback; Task 2 human-verify outstanding | 2026-04-27 | d4d9a4a |  | [260427-nkn-drop-fab-lift-plus-icon-must-sit-below-t](./quick/260427-nkn-drop-fab-lift-plus-icon-must-sit-below-t/) |
| 260428-iny | Trim Add sheet from 4 rows to 2 (New registry + Add an item); AddItemKey gains nullable registryId + fromAddSheet flag; AddItemScreen renders Material3 ExposedDropdownMenuBox registry picker as first field when fromAddSheet=true (with empty-state link to CreateRegistryKey); CreateRegistry → AddItem chain keeps picker hidden; 4 new EN+RO strings added, 6 deprecated strings removed; sheet bug-fix preservation (corners, scrim, drag-handle, no shadow) confirmed; Task 3 human-verify outstanding | 2026-04-28 | 5752adf |  | [260428-iny-trim-add-sheet-to-2-options-new-registry](./quick/260428-iny-trim-add-sheet-to-2-options-new-registry/) |
| 260428-s3b | Fix Event Date + Time fields on Create/Edit Registry — InteractionSource pattern replaces broken Modifier.clickable on date field (was no-op due to TextField pointer-input swallow); add TimePickerDialog wired via same pattern with runtime gate inside collector (enabled=true so InteractionSource fires); ViewModel gets eventTimeSet StateFlow + setEventTime(hour, minute) encoding into existing eventDateMs Long; edit-mode hydration flips eventTimeSet=true on non-midnight Calendar-decoded timestamps; 5 new VM unit tests pin the contract; no domain/persistence/string changes; Task 3 human-verify outstanding | 2026-04-28 | c133ac5 |  | [260428-s3b-fix-event-date-time-fields-on-createregi](./quick/260428-s3b-fix-event-date-time-fields-on-createregi/) |
| 260428-tx9 | Replace framework android.app.DatePickerDialog / TimePickerDialog with Material3 Compose DatePickerDialog + DatePicker(state) and AlertDialog { TimePicker(state) } on Create/Edit Registry — pickers now inherit GiftMaison terracotta via MaterialTheme.colorScheme.primary (LightColorScheme.primary = gm.accent in Theme.kt:36) instead of bleeding the Android system Material green/teal; UTC↔local-Calendar conversion in DatePickerDialog confirmButton guards against Bucharest UTC+2/+3 off-by-one civil-day; @OptIn(ExperimentalMaterial3Api::class) at @Composable level; OK/Cancel via stringResource(android.R.string.ok / .cancel) — zero new strings.xml keys; all s3b behaviour preserved verbatim (InteractionSource trigger, hour/minute preservation on re-pick, eventTimeSet StateFlow + setEventTime VM API, 24h locale awareness, edit-mode round-trip); Task 2 human-verify outstanding | 2026-04-28 | 08c66da |  | [260428-tx9-replace-framework-datepickerdialog-timep](./quick/260428-tx9-replace-framework-datepickerdialog-timep/) |
| 260428-v0q | Make bottom nav persistent across all post-auth destinations — invert `Any?.showsBottomNav()` from 2-key visible-whitelist (HomeKey + RegistryDetailKey only) to 4-case hidden-whitelist (null, AuthKey, OnboardingKey, ReReserveDeepLink); fixes user's reported bug where tapping YOU hid the chrome on Settings, plus extends the same contract to Notifications, Stores, and all forms (CreateRegistry, EditRegistry, AddItem, EditItem); BottomNavVisibilityTest pins all 14 nav-key cases (4 hidden, 10 visible — every key in AppNavKeys.kt); zero touches to AppNavigation.kt, AppNavKeys.kt, GiftMaisonBottomNav.kt, or string resources; Task 2 human-verify outstanding (8 device scenarios) | 2026-04-28 | a486ca5 |  | [260428-v0q-make-bottom-nav-persistent-across-all-po](./quick/260428-v0q-make-bottom-nav-persistent-across-all-po/) |
| 260507-uce | Fix AddItemScreen "Choose a registry" picker to show only ACTIVE registries — `AddItemViewModel.registriesForPicker` now applies `Registry.isActive(startOfTodayMs())` via `.map { }` BEFORE `.catch.stateIn`, reusing the same `TabFilters.kt` predicate that powers the Lists screen Active tab (single source of truth, no new convention); past registries (e.g. "test", "A&V Wedding", "Secret Santa") no longer appear; `todayMs` recomputed per emission so screens kept open across midnight re-evaluate naturally; filter sits before `.catch` so error fallback still emits an empty list (drives the "Create a registry first" empty-state); ViewModel-layer fix (not repository) preserves `RegistryListViewModel`'s Past tab; new `AddItemViewModelPickerFilterTest` (4/4 GREEN) pins contract via local-helper-mirroring-production-filter pattern modelled on `TabFilterPredicateTest`; zero touches to TabFilters/RegistryListScreen/RegistryListViewModel/AddItemScreen/strings.xml; Task 2 human-verify outstanding (device walkthrough on bug-reproducing account) | 2026-05-07 | 65dec78 |  | [260507-uce-fix-add-item-registry-dropdown-showing-p](./quick/260507-uce-fix-add-item-registry-dropdown-showing-p/) |
| 260507-uzv | Hide owner-only overflow menu actions (Edit / Share / Invite / Delete + kebab) from non-owners on RegistryDetailScreen — `RegistryDetailHero.onOverflow` signature: `() -> Unit` → `(() -> Unit)? = null` (mirrors existing `onCoverTap` D-13 nullable convention in same file); kebab IconButton wrapped in `if (onOverflow != null)`; call site passes `onOverflow = if (isOwner) ({ overflowMenuExpanded = true }) else null` (mirrors existing D-13 cover-tap line two below); entire DropdownMenu Box wrapped in `if (isOwner) { ... }` for defence in depth; reuses pre-existing `RegistryDetailViewModel.isOwner` StateFlow (Phase 12 D-13) — no VM/AuthRepository/Firestore-rules/Cloud-Functions/strings.xml touches; new `RegistryDetailViewModelIsOwnerTest` (4/4 GREEN) pins contract: match→true, mismatch→false, null-registry→false, null-user→false; Eagerly + initial-false + .catch{emit(false)} means non-owner UI is the safe default during load (no flash of owner-only items); top-bar Share icon, hero share-pill, ShareBanner, D-13 cover-photo tap, ConfirmPurchaseBanner all unchanged per spec; Task 2 device verification outstanding (owner sees kebab + 4 actions; invitee sees neither) | 2026-05-07 | ed1f180 |  | [260507-uzv-hide-owner-only-overflow-menu-actions-fr](./quick/260507-uzv-hide-owner-only-overflow-menu-actions-fr/) |
| 260507-veb | Hide three more owner-only affordances on RegistryDetailScreen (top-bar Share OpenInNew icon, ShareBanner "Tap to copy or share" pill, "+ Add an item" primary CTA) from non-owners — `RegistryDetailHero.onShare` signature: `() -> Unit` → `(() -> Unit)? = null` (third sibling to `onOverflow` + `onCoverTap` nullable callbacks in the same file); top-bar Share IconButton wrapped in `if (onShare != null)`; `RegistryDetailScreen` call site passes `onShare = if (isOwner) onShareTap else null` (matches sibling `onOverflow` + `onCoverTap` conditionals); ShareBanner LazyColumn item + AddItemTopCta LazyColumn item wrapped in `if (isOwner) { item(key=..) { ... } }` (standard Compose pattern — non-owners never register those keys); reuses the same `viewModel.isOwner` StateFlow already pinned by `RegistryDetailViewModelIsOwnerTest` (quick-260507-uzv) — no new VM logic, no new tests, no string changes, no security-rule / Cloud-Function changes; Spacer(Modifier.weight(1f)) retained between Back and Share so kebab stays far-right for owners (no UX regression); StyleGuidePreview.kt's two RegistryDetailHero call sites also use named args and compile cleanly under the parameter reorder; Task 2 device verification deferred + combined with quick-260507-uzv Task 2 (owner sees all three; invitee sees none) | 2026-05-07 | 1f563db |  | [260507-veb-hide-top-bar-share-icon-share-link-pill-](./quick/260507-veb-hide-top-bar-share-icon-share-link-pill-/) |
| 260507-vrp | Invitees can tap item rows on RegistryDetailScreen to open EditItemScreen and reserve / mark-as-purchased — `RegistryItemRow` gains required `onTap: () -> Unit` (outer Row wraps `Modifier.clickable(onClick = onTap)` between `.fillMaxWidth()` and `.drawBehind {}` so divider draws on top of ripple) + `showOverflow: Boolean = true` (kebab `Box { IconButton + DropdownMenu }` wrapped in `if (showOverflow)`); `RegistryDetailScreen` call site passes `onTap = { onNavigateToEditItem(item.id) }` and `showOverflow = isOwner` (reuses pre-existing `isOwner` StateFlow); `EditItemViewModel` gains 5 new constructor deps (AuthRepository, ObserveRegistryUseCase, ReserveItemUseCase, ConfirmPurchaseUseCase, GuestPreferencesRepository) all already provided in the existing Hilt graph; `isOwner` StateFlow line-for-line mirrors `RegistryDetailViewModel.isOwner` (combine + Eagerly + initial-false + `.catch{emit(false)}`); private nested `ReservationEvent` sealed interface re-declared (NOT promoted to top-level type) with same 3 subtypes — `OpenRetailer / ShowGuestSheet / ShowConflictError`; `performReservation` mirrors `RegistryDetailViewModel.performReservation` line-for-line (getGuestIdentity check, sheet on null, ReserveItemUseCase with `giverId = currentUser?.uid`, `setActiveReservationId` + `OpenRetailer` on success, `ShowConflictError(code)` on failure); `onConfirmPurchase` mirrors `RegistryDetailViewModel.onConfirmPurchase` (raw `Int` resId snackbar payload, no SnackbarMessage sealed type — EditItemScreen does not consume FCM push events; `setActiveReservationId(null)` on success); `EditItemScreen` Column body now branches on `if (isOwner)` — owner = full-edit form verbatim; invitee = same 5 fields with `enabled=false` + Reserve / Mark-as-purchased Buttons gated by `currentItem?.status` (canReserve = AVAILABLE, canConfirmPurchase = RESERVED && activeReservationId != null); two `LaunchedEffect(Unit)` collectors mirror `RegistryDetailScreen.kt:137-155` (Intent.ACTION_VIEW for OpenRetailer, sheet open for ShowGuestSheet, snackbar for conflict; snackbar + onBack() on confirm-purchase success); `GuestIdentitySheet` re-imported from `ui.registry.detail` and rendered when `showGuestSheet` is true; new `EditItemViewModelIsOwnerTest` (4/4 GREEN) + `EditItemViewModelReservationTest` (5/5 GREEN, turbine.test for Channel/SharedFlow); zero touches to firestore.rules / storage.rules / Cloud Functions / GuestPreferencesRepository / ReservationRepository / ReserveItemUseCase / ConfirmPurchaseUseCase / AuthRepository / nav keys / strings.xml (EN+RO empty diff); Hilt graph compiles cleanly (`:app:assembleDebug` green); Task 4 device verification combined with the still-pending quick-260507-uzv + quick-260507-veb device passes | 2026-05-07 | 3c4b663 |  | [260507-vrp-invitee-can-tap-item-rows-to-view-detail](./quick/260507-vrp-invitee-can-tap-item-rows-to-view-detail/) |
| 260510-o7w | Fix web `mapRegistrySnapshot` reading wrong Firestore field names (`name`/`occasionType`/`eventDate`-Timestamp) — pre-existing Phase 5 schema mismatch surfaced by Phase 13 hero. Now reads Android-canonical `title`/`occasion`/`eventDateMs` (Long ms). 4 new vitest cases pin the contract; existing `useRegistryQuery.test.ts` stub updated to match. 111/111 web tests pass. Web `Registry` interface naming unchanged — only mapper read path | 2026-05-10 | b0b7d24 |  | [260510-o7w-fix-web-registryheader-showing-empty-tit](./quick/260510-o7w-fix-web-registryheader-showing-empty-tit/) |
| 260510-oja | Fix Android `ItemDto` schema mismatch causing registry-detail crash when an item is reserved — added `reservedBy: String?` + `reservedAt: Timestamp?`, retyped `expiresAt: Long?` → `Timestamp?` to match Cloud Function `createReservation.ts` canonical writer; `ItemRepositoryImpl.toDomain` converts `Timestamp.toDate().time` → `Long?` ms at the data→domain boundary so `Item.expiresAt: Long?` UI contract (StatusChip / ReservedChip) is preserved verbatim; `FirestoreDataSource.observeItems` dead `getTimestamp("expiresAt")` workaround removed (matches RegistryDto pattern). 6 new schema regression tests (`ItemDtoSchemaTest`) pin DTO field types + mapper conversion; 330/330 JVM suite GREEN; `:app:assembleDebug` clean; user-confirmed crash repro silent on rebuilt APK. Domain `Item.kt`, Cloud Function code, web mapper, and reservation business logic untouched | 2026-05-07 | c28bffb, 1ed563b | Verified | [260510-oja-fix-android-itemdto-schema-mismatch-add-](./quick/260510-oja-fix-android-itemdto-schema-mismatch-add-/) |
| 260510-sai | Configurable `-PemulatorHost=<ip>` Gradle property plumbed into `BuildConfig.FIREBASE_EMULATOR_HOST` (debug variant, default `10.0.2.2`; release variant hardcoded literal as defense-in-depth); four DI call sites (AppModule × 3 + StorageModule × 1) now read host from BuildConfig instead of hardcoded `"10.0.2.2"`. Scope expanded to 3 commits because human-verify on physical device (serial WCR0219729000994) surfaced TWO additional blockers the planner couldn't anticipate: (a) Android default cleartext-HTTP block (fixed via debug `network_security_config.xml` permitting cleartext to RFC1918 ranges 10/8, 192.168/16, 172.16/12 — release stays TLS-only), (b) Firebase emulators bound to `127.0.0.1` rejecting non-loopback (fixed via `firebase.json` per-emulator `host: "0.0.0.0"`). User confirmed Google + email/password sign-in end-to-end on hardware against host emulator at `192.168.1.10`. AVD dev loop unchanged when flag omitted. Three layers (BuildConfig + cleartext + bind) now aligned — without all three, traffic fails silently | 2026-05-12 | 62515e5 |  | [260510-sai-add-gradle-property-pemulatorhost-for-co](./quick/260510-sai-add-gradle-property-pemulatorhost-for-co/) |
| 260510-v4v | Replace two silent catch blocks in AuthScreen's Continue-with-Google flow with a four-branch chain: `GetCredentialCancellationException` (silent — sheet dismissal stays no-op) → `GetCredentialException` (log at WARN under tag `AuthScreen` with `e::class.simpleName` + message + propagate localized error via new `AuthViewModel.setError`) → kotlinx `CancellationException` (re-throw — preserves structured concurrency) → `Exception` (log + propagate). Adds public `AuthViewModel.setError(message)` mirroring existing `clearError()`. New localized key `auth_error_google_signin_failed` in EN + RO. No behavior change to email/password, guest, or sign-up paths. Net diff small: ~18 LOC AuthScreen, ~4 LOC AuthViewModel, +1 line each strings.xml. This fix is the foundation that made the 260510-sai physical-device debugging session possible — without v4v's logging, the cleartext + 0.0.0.0-bind layers underneath would still be invisible silent failures | 2026-05-12 | 626f716 |  | [260510-v4v-fix-continue-with-google-silent-failure-](./quick/260510-v4v-fix-continue-with-google-silent-failure-/) |
| 260510-noi | Wire `Registry.description` through Android Add/Edit Registry form's UI — added multi-line OutlinedTextField (`minLines=3, maxLines=5`) below the Place field on `CreateRegistryScreen.kt` bound to `viewModel.description` via `collectAsStateWithLifecycle`; soft 500-char cap at INPUT via `it.take(500)`; reuses existing `registry_description_label` / `_hint` (EN + RO already shipped). NO DTO/mapper/strings/VM/web changes — pre-flight grep confirmed every layer below the composable (Registry.description, RegistryDto.description, toMap/toUpdateMap, VM ifBlank-on-save, edit-mode hydration, EN+RO strings, web mapper + RegistryHeader render) already shipped from prior phases. Field was silently dropped only because no UI rendered it. TDD regression-pin: `CreateRegistryViewModelDescriptionTest` (3 tests — typed → onSave, blank → null, edit-mode hydration) passed immediately on RED gate (intentional pattern — locks existing contract before refactor risk). User confirmed via real device usage | 2026-05-12 | 44f8fb8 |  | [260510-noi-add-description-field-to-android-add-reg](./quick/260510-noi-add-description-field-to-android-add-reg/) |
| 260510-pdp | Fix reservation expiry not firing in local Firebase emulator — Firebase Emulator Suite ships no Cloud Tasks emulator, so `onTaskDispatched`-based `releaseReservation` was never invoked locally and items stayed `reserved` forever past the 30-min mark. Extracted `releaseReservationCore({ reservationId, db })` from the production `onTaskDispatched` wrapper (signature unchanged) so it can be invoked directly; wired an emulator-only `setTimeout` fallback inside `createReservation.ts`'s Cloud-Tasks-catch block, gated on `process.env.FUNCTIONS_EMULATOR === "true"` with `timer.unref?.()` for clean emulator shutdown. Production code path verified untouched — wrapper still delegates verbatim. Idempotency safety: `releaseReservationCore` short-circuits on `status !== "active"`, so a late-firing timer post-confirmPurchase is a safe no-op. Out-of-scope discovery committed separately: `functions/package.json` `main: lib/index.js` was wrong (tsconfig produces `lib/src/*.js`) — repaired to `lib/src/index.js` so the loader finds the compiled entry. 3 new `createReservation.test.ts` cases pin fallback contract (emulator+enqueue-throws → fires; production+enqueue-throws → does not; emulator+enqueue-success → does not); 78/78 functions tests GREEN; tsc clean. Known limitation documented: emulator restart loses in-process timers (workaround: manually advance status or clean restart); production durable via Cloud Tasks. User confirmed end-to-end emulator walkthrough — item flipped back to `available` with `[createReservation] Emulator fallback: scheduling release …` log line firing per new reservation | 2026-05-10 | 8672900, 2df463f, 889a27f | Verified | [260510-pdp-fix-reservation-expiry-not-firing-in-loc](./quick/260510-pdp-fix-reservation-expiry-not-firing-in-loc/) |
| 260512-wt8 | Android Add Item — debounced (700ms) auto-fetch of product OG metadata when a valid http/https URL settles in the URL field, removing the need to tap the fetch button (manual icon-tap retained as retry affordance that bypasses de-dup). `AddItemViewModel` gains a `collectLatest` pipeline over the `url` StateFlow that gates on `isValidProductUrl(s)` (`java.net.URI` parse, http/https schemes only, non-blank host) → 700ms debounce → de-dup against `lastFetchedUrl` → `fetchMetadata()`; `lastFetchedUrl` is seeded with `initialUrl.trim()` BEFORE the auto-fetch flow subscribes so Store-Browser deep-links don't double-fire; `collectLatest` cancels in-flight fetches when the URL changes (no stale overwrite). Trailing IconButton swapped from `Icons.Default.Refresh` → `Icons.Outlined.CloudDownload` with new localized `contentDescription` via new `item_url_fetch_button_cd` ("Fetch product details" / "Preia detaliile produsului"). 7 new `AddItemViewModelAutoFetchTest` cases pin contract (invalid URL no-fetch, non-http scheme no-fetch, valid https fires once after 700ms, rapid edits debounce to single fetch, repeat URL de-duped, initialUrl single-fire on init, manual onFetchMetadata() bypasses de-dup); 7/7 GREEN; full `:app:testDebugUnitTest` BUILD SUCCESSFUL; `:app:compileDebugKotlin` clean. User confirmed 11-scenario on-device UAT (paste auto-fetch, type-debounce, validity gate, de-dup, manual retry, cancellation under rapid edits, Romanian contentDescription, store-browser deep-link no-double-fire, failure path retry, save path, icon visual swap) | 2026-05-12 | f80a2f6, ea7c843, 20add21 | Verified | [260512-wt8-on-mobile-app-when-adding-a-url-for-a-pr](./quick/260512-wt8-on-mobile-app-when-adding-a-url-for-a-pr/) |
| 260512-vlg | Add user-menu dropdown with Sign out to web header — TopNav's static avatar `<div>` (no click handler) becomes an interactive `DropdownMenu.Trigger` from a new `UserMenu.tsx` atom built on `@radix-ui/react-dropdown-menu@^2.1.0` (on-pattern with existing Radix family — Dialog 1.1.15 + Toast 1.2.0). Single "Sign out" item invokes `signOut()` from `features/auth/authProviders`; `useAuth`'s `onAuthStateChanged` subscriber then reactively flips `user → null`, TopNav auto-renders the "Sign in" ghost button — NO `useNavigate` on sign-out (intentional: a giver mid-flow on `/registry/:id` stays on that page, which is anonymous-accessible by design). Radix defaults supply all keyboard a11y: Esc closes, click-outside closes, focus returns to trigger, Tab/arrow nav, `role="menu"` ARIA. Two i18n keys added: `auth.user_menu_label` ("Account menu for {{name}}" / "Meniu cont pentru {{name}}") and `auth.sign_out` ("Sign out" / "Deconectează-te"). Plan auto-fixed one deviation — i18n keys had to land in BOTH `web/i18n/` (legacy path) AND `web/src/i18n/` (active runtime path) because the i18n module loads from `web/src/i18n/`; tests caught raw key fallthrough before commit. 4 new Vitest specs in `UserMenu.test.tsx` (aria-label, open, signOut call, error-toast path) using the same `vi.hoisted` + ToastProvider stub pattern as `AuthModal.test.tsx`. Full web suite 115/115 GREEN, TypeScript clean. User confirmed end-to-end in browser: menu opens, Esc + click-outside dismiss, Sign out swaps TopNav to "Sign in" CTA within ~1s, EN/RO toggle swaps strings live | 2026-05-12 | 359ba1f, 1b54ed4 | Verified | [260512-vlg-add-user-menu-with-sign-out-to-web-heade](./quick/260512-vlg-add-user-menu-with-sign-out-to-web-heade/) |
| 260513-fk1 | Remove auto-open of retailer URL on successful reservation (guests and signed-in users both) — `useCreateReservation` mutation hook's internal `onSuccess` called `window.open(data.affiliateUrl, '_blank', 'noopener,noreferrer')` BEFORE delegating to caller-supplied `options.onSuccess`, originating from Phase 5 design tags `WEB-D-07 + WEB-04`. User reported this as unwanted: guests should stay on the registry page; the existing "Continue to retailer" `<a href={active.affiliateUrl}>` in `StickyReserveBanner` is the deliberate user-initiated path. Fix deletes the 3-line `if (data.affiliateUrl) { window.open(...) }` block; the hook becomes a pure data mutator and the `options.onSuccess?.(data, variables)` delegation stays intact (callers `RegistryPage.autoReserveMutation`, `ReserveButton`, `GuestIdentityModal` submit path continue to set `useActiveReservation.active` which renders the banner). `data.affiliateUrl` remains in the response shape and flows into `setActive({ affiliateUrl })` so the banner CTA keeps working — affiliate revenue path preserved, just user-initiated. TDD: existing test `'opens affiliateUrl in a new tab on success (WEB-D-07)'` inverted to assert `window.open` is NOT called — confirmed RED before deletion, GREEN after. Toast copy updated EN+RO across all 4 i18n files (`web/src/i18n/{en,ro}.json` + `web/i18n/{en,ro}.json`) from "Gift reserved! Redirecting to retailer…" / "Cadou rezervat! Redirecționare către magazin…" → "Gift reserved! Continue to retailer when you're ready." / "Cadou rezervat! Continuă către magazin când ești gata." — copy no longer promises a redirect that does not happen. Phase 5 `05-UI-SPEC.md` WEB-D-07 row revised in place (inline `revised 2026-05-13` annotation; older decision-history docs left as historical record). 116/116 web tests + `tsc --noEmit` clean; `git diff --stat` confirms exactly 7 files touched + zero protected files (useActiveReservation, StickyReserveBanner, ReserveButton, RegistryPage, GuestIdentityModal, ConfirmPurchaseBanner, ReservationBanner). User confirmed in browser: reserve as guest → no auto-tab, banner appears, manual "Continue to retailer" still opens new tab; same for signed-in; Romanian copy reads correctly | 2026-05-13 | ffd59bd | Verified | [260513-fk1-remove-guest-reserve-auto-open-of-retail](./quick/260513-fk1-remove-guest-reserve-auto-open-of-retail/) |
| 260512-x5d | Make reserved-by-me items navigate back to ReserveDetailSection persistently across refresh / new tab / guest path — three-layer fix. **Backend (new Cloud Functions):** `releaseReservationCallable` (manual giver-initiated release with ownership check: signed-in via `auth.uid === reservation.giverId`, guest via `payload.giverEmail === reservation.giverEmail && giverId === null`; reuses extracted `releaseReservationCore` with new `skipNotYetExpiredGuard=true` flag; cancels the scheduled Cloud Task with NOT_FOUND swallow per `confirmPurchase` pattern) + `hydrateActiveReservation` callable (queries `reservations` collection for `registryId + status === 'active'` + ownership match; returns null if none — best-effort, never blocks page render); 15 backend tests across 2 new test files. **Web hooks:** `useReleaseReservation` (mirrors `useConfirmPurchase` httpsCallable-inside-callback pattern — signed-in sends only `reservationId`, guest sends + `giverEmail`) + `useActiveReservationHydration(registryId)` (gates on `authReady`, refuses to clobber a fresh in-session reservation set by `useCreateReservation.onSuccess` via `if (active) return`, uses `lastKeyRef` for cleanup-safe refetch on user/email/registry change, signed-in path sends ONLY `registryId` defence-in-depth). **Wiring:** `RegistryPage` calls `useActiveReservationHydration(id)` on mount; `ReserveDetailSection` gets stable anchor `id="reserve-detail-section"`; ItemCard renders the reserved-banner row as a `<button>` when a parent click handler is provided (smooth-scroll to anchor on click — only fires when `item.reservedBy === effectiveEmail` per D-06); `StickyReserveBanner` Release CTA swapped from `clear()` → `useReleaseReservation.release(reservationId, giverEmailToSend)` with success-toast-then-`clear()` and error-toast effects (deduped via refs). 3 new i18n keys (`reservation.release_success`/`release_error` + `web_reserve.release_aria_busy`) synced across all 4 i18n files (EN+RO × `web/src/i18n/` + `web/i18n/`). 116/116 web tests + functions suite green; `tsc --noEmit` exits 0. Initial human-verify surfaced 3 bugs — bug 2 (confirm-purchase didn't clear active context) split out and fixed as quick-260513-0kn; bugs 1 & 3 (release no-op + guest refresh no banner) traced to emulator staleness — restarting the functions emulator with the new callables built resolved both. User confirmed Release works end-to-end in browser (release CTA flips reservation to expired server-side + item to available + cancels Cloud Task + clears local context + success toast) | 2026-05-13 | f184fd7, 7c68d40, 90abfba | Verified | [260512-x5d-make-reserved-by-me-items-navigate-back-](./quick/260512-x5d-make-reserved-by-me-items-navigate-back-/) |
| 260513-0kn | Fix confirm-purchase not clearing active reservation on web — `ConfirmPurchaseBanner` flipped the item to PURCHASED server-side (via `confirmPurchase` callable, which atomically transitions `reservation.status` and `item.status`) but never cleared the React `useActiveReservation` context. Result: sticky banner + ReserveDetailSection (including its "I completed the purchase" CTA) remained visible after success, even though the item card flipped via Firestore snapshot. Pre-existing latent bug, surfaced by quick-260512-x5d's hydration (which kept the banner reachable across refreshes). Fix mirrors the `StickyReserveBanner` release-success effect exactly: inside the existing `useEffect(status === 'success' …)` block (already one-shot-guarded by `successToastedRef`), add a single `clear()` call to the destructured `useActiveReservation` context. `useConfirmPurchase` hook stays a pure status/error state machine — context-clearing belongs where context is consumed. TDD: new vitest spec in `ConfirmPurchaseBanner.test.tsx` asserts `clear()` is called exactly once on the status transition idle→pending→success; new module-level `vi.mock('../useActiveReservation', …)` satisfies all 5 pre-existing tests without test-file refactor; 6/6 ConfirmPurchaseBanner specs pass + 116/116 across the whole web suite + `tsc --noEmit` exits 0. Exactly two files diffed (banner + its test). User confirmed in browser: reserve → confirm-purchase → banner + detail section disappear within ~1s with no refresh | 2026-05-13 | 87464b8 | Verified | [260513-0kn-fix-confirm-purchase-not-clearing-active](./quick/260513-0kn-fix-confirm-purchase-not-clearing-active/) |
| 260513-fnm | Add "Check other products in the registry" text-link CTA to the web giver-flow purchase-step screen — accent-coloured (`text-gm-accent` = #C8623A) text-link rendered directly below `ConfirmPurchaseBanner` in `ReserveDetailSection.tsx`, right-aligned with `-mt-2` so it visually pairs with the "I completed the purchase" accent button above it. Click handler `document.getElementById('registry-list-section')?.scrollIntoView({ behavior: 'smooth', block: 'start' })` (mirrors the existing `renderReservedByMeClick` scroll-up pattern in RegistryPage; same-route, no navigation). Stable scroll anchor `id="registry-list-section"` added to the item-list `<section>` in `RegistryPage.tsx`. Styling: `font-body text-[12.5px] leading-[1.45]` to match HowTimerWorks body copy + `underline underline-offset-[3px] decoration-[1px] hover:decoration-2` + standard focus-visible outline; no new component primitive, inline `<button>` element. i18n key `web_reserve.check_other_products_cta` ("Check other products in the registry" / "Vezi alte produse din registru") synced to BOTH `web/src/i18n/{en,ro}.json` (the React runtime path — initial commit missed this, caused button to render the raw key) AND `web/i18n/{en,ro}.json` (legacy duplicate, per quick-260512-vlg convention). 3 commits — feat (initial ghost-Btn + scroll anchor + wrong-path i18n) → fix (i18n key in runtime path) → style (ghost-Btn → accent text-link below confirm CTA after user-annotated screenshot feedback). 5/5 i18n parity tests pass + `tsc --noEmit` clean. User confirmed final placement, color, font/size, and scroll behaviour all correct | 2026-05-13 | 07ba565, 6493b1c, f8700e5 | Verified | [260513-fnm-add-check-other-products-in-the-registry](./quick/260513-fnm-add-check-other-products-in-the-registry/) |
| 260513-g9g | Add per-item reserve-detail route `/registry/:id/item/:itemId` so each of multiple concurrent reservations on the same registry is reachable (not just the most-recent one held by single-slot `useActiveReservation`). User-locked decisions (via --discuss): URL scheme is item-id (matches Android EditItemKey pattern, deep-linkable, survives reservation lifecycle); existing in-page `ReserveDetailSection` + `StickyReserveBanner` kept as-is for most-recent reservation on the list page; post-reserve auto-navigates to the new dedicated page (replaces the just-removed-in-fk1 auto-open-retailer behavior). **Backend (new):** `getReservationForItem` callable mirrors `hydrateActiveReservation` ownership rules (signed-in uid match, guest giverEmail+null giverId) with an extra `itemId` equality filter; returns active reservation or null; 9 jest specs. **Web hook (new):** `useReservationForItem(registryId, itemId)` — route-scoped, does NOT touch `useActiveReservation` context; same authReady/identity gates as `useActiveReservationHydration` with key-based StrictMode guard; 7 vitest specs. **Page (new):** `ItemReservePage.tsx` at `/registry/:id/item/:itemId` renders 5 states (loading / item-not-found / not-yours / expired / reserved-by-me detail). Reuses `ConfirmPurchaseBanner`, `HowTimerWorks`, `useReleaseReservation`, `useCountdown`. Release-success and confirm-success both navigate back to `/registry/:id`. **Wiring:** `App.tsx` route added; `ItemCard` reserved-banner row swapped from scroll-to-anchor to `<button>` calling parent's `onReservedByMeClick` (which `RegistryPage` populates with `navigate(/registry/:id/item/:itemId)` ONLY when `item.reservedBy === effectiveEmail` per D-06); `RegistryPage.autoReserveMutation.onSuccess` + `ReserveButton.onSuccess` both append `navigate(/registry/:id/item/:itemId)` after setActive+toast (auto-navigate site count: 3). 3 new i18n keys per the new page (loading_label / not-yours / item-not-found / expired / page_caption / headline_pre/emphasis/post / back_to_registry / item_pill / time_label) synced EN+RO across `web/src/i18n/` + `web/i18n/`. Initial human-verify surfaced 2 bugs split into follow-ups: quick-260513-hon (transition-detector for navigate-back race) and quick-260513-i6l (empty-affiliateUrl silent skip in lookup callables). User confirmed end-to-end after both fixes: two reservations on same registry, click each card → URL changes to `/item/<itemId>` with that item's countdown/CTAs; release/confirm both navigate back; not-yours/not-found/expired states all render correctly | 2026-05-13 | 4754f10, 285ec68, 90abfba | Verified | [260513-g9g-add-per-item-reserve-detail-route-so-eac](./quick/260513-g9g-add-per-item-reserve-detail-route-so-eac/) |
| 260513-hon | Fix `ItemReservePage` navigate-back-on-item-status-flip effect firing on stale initial snapshot — the effect was meant to bounce the user back to `/registry/:id` when their item transitioned `reserved → purchased` (via confirm-purchase) or `reserved → available` (via release). Bug: the effect only checked the current value, so on initial mount when `useItemsQuery` snapshot was stale and still showed `status === 'available'` (the few hundred ms before the Firestore listener propagated the just-completed reservation write), the effect fired immediately and bounced the user off the just-loaded reserve-detail page back to the registry list. Symptoms: (a) guest auto-reserve appearing to land on "not yours" because the page rendered briefly and snapped back; (b) reserving a second item: URL flashed to `/item/B` then snapped back to `/registry/:id`. **Fix:** introduced `prevStatusRef` transition-detector — only fire navigate-back when `prevStatusRef.current === 'reserved'` AND current is `'purchased'` or `'available'`. Stale `'available'` on initial render becomes the seed value for `prevStatusRef` and is NOT treated as a transition. The existing one-shot `itemStatusNavigatedRef` guard preserved so navigate fires at most once per mount. 3 new vitest specs added covering stale-on-mount no-fire, real reserved→available fires once, real reserved→purchased fires once. 141/141 web tests + `tsc --noEmit` clean; exactly 2 files diffed (ItemReservePage.tsx + its test). User confirmed in browser: guest auto-reserve now lands and stays on `/item/X` with full detail UI; release/confirm regression behavior preserved | 2026-05-13 | 06f20be | Verified | [260513-hon-fix-itemreservepage-navigate-back-firing](./quick/260513-hon-fix-itemreservepage-navigate-back-firing/) |
| 260513-i6l | Drop empty-affiliateUrl silent-skip in reservation lookup callables + originalUrl fallback — `getReservationForItem` (lines 100-104) and `hydrateActiveReservation` (lines 91-94) both had a `if (!affiliateUrl) return { active: null }` short-circuit intended for legacy data. This silently broke any reservation made on a non-EMAG item (e.g. IKEA URL-imported items added via `fetchOgMetadata` which doesn't populate `affiliateUrl`): the callable returned null, `useReservationForItem` reported `lookupStatus='empty'`, `ItemReservePage` rendered the "This isn't your reservation" not-yours branch. The release flow earlier didn't expose this because `useActiveReservation` was populated DIRECTLY from `useCreateReservation.onSuccess`'s in-flight response (which carried the empty `affiliateUrl` too, but the context was set unconditionally) — the dedicated page was the first surface to round-trip through the callable. **Fix (backend):** dropped the silent-skip in both callables; both now compute `effectiveAffiliateUrl = (reservation.affiliateUrl as string) || (item.originalUrl as string) || ""` after the item doc lookup that was already happening (extended to read `originalUrl`). Tests inverted from "empty → null" into "empty + originalUrl → active with affiliateUrl=originalUrl"; added "empty + empty → active with affiliateUrl=''". **Fix (frontend defensive UX):** when `active.affiliateUrl === ""`, `ItemReservePage` and `StickyReserveBanner` both hide the "Continue to retailer" anchor entirely (no broken link, no disabled placeholder). Release + Mark-as-purchased + countdown + Back-to-registry all work without a retailer URL. New `StickyReserveBanner.test.tsx` created (no test existed for it) covering hide-CTA case. 89 backend + 147 web tests pass; `tsc --noEmit` clean. `createReservation.ts` and all client hooks (useCreateReservation, useActiveReservationHydration, useReservationForItem) untouched — fix is purely read-time. User confirmed in browser: reserve an IKEA item as guest → lands on `/item/X` with full detail UI (NOT "not yours"); EMAG regression-check confirmed "Continue to retailer" still appears when affiliateUrl is populated | 2026-05-13 | e7f1c34, b2bff4b | Verified | [260513-i6l-drop-empty-affiliateurl-skip-in-reservat](./quick/260513-i6l-drop-empty-affiliateurl-skip-in-reservat/) |
| 260513-h36 | Save-your-spot account-creation upsell modal — replaces `GuestIdentityModal` as the primary modal a guest sees when tapping Reserve, offering to create a free email+password account in one step instead of reserving as a guest. **Initial design (Tasks 1+2):** post-reservation upsell on `ItemReservePage` with sessionStorage per-reservation dismissal — implemented but user verification surfaced the intent was the opposite (account upsell BEFORE reserving, not after). **Pivot (Task 3, commit 2d2bcdb):** restructured to pre-reservation. New `SaveYourSpotModal` collects `firstName + lastName + email + password` in one form (pre-fills from `useGuestIdentity` localStorage); primary CTA "Create account & reserve →" validates all fields, calls `signUpEmail` (Firebase `createUserWithEmailAndPassword`), then `ReserveButton` reserves under the new user's uid; secondary "Continue as guest" validates name+email only and reserves with `giverId=null`. Persists name/email to `useGuestIdentity` on either path so subsequent reserves pre-fill. `ReserveButton.tsx` now opens `SaveYourSpotModal` for anonymous users (was `GuestIdentityModal`); signed-in users still skip the modal entirely. `ItemReservePage.tsx` integration from Tasks 1+2 removed (post-reservation mount, eligibility effect, sessionStorage dismissal — all gone). `GuestIdentityModal.tsx` kept in place — still used by `RegistryPage` for the deep-link `?autoReserve=…` auto-reserve path; not removed. **Critical design note:** this codebase has NO anonymous Firebase auth path; reservation backend keys on `giverEmail` with `giverId=null` for guests. So `signUpEmail` + reserve under new uid is sufficient — no `linkWithCredential` or anonymous→permanent conversion needed. 21 i18n keys (`save_your_spot.*`) added to both `web/src/i18n/{en,ro}.json` with parity verified. Firebase auth error codes (`auth/email-already-in-use`, `auth/weak-password`, `auth/invalid-email`) mapped to user-friendly inline errors. **Tests:** 2 ReserveButton specs updated for new modal title/CTAs ("Save your spot?" / "Continue as guest"); 3 new specs added for account-creation path (success, weak-password validation, email-already-in-use). 144/144 web tests + `tsc --noEmit` clean. User confirmed in browser: reserve as fresh guest → SaveYourSpotModal pops up; "Continue as guest" reserves with giverId=null and lands on ItemReservePage as anon; "Create account & reserve" with valid password signs the user up and reserves under their new uid; signed-in users skip the modal; RO locale labels render correctly | 2026-05-13 | 09decf9, 8b7ff00, 2d2bcdb | Verified | [260513-h36-add-save-your-spot-account-creation-upse](./quick/260513-h36-add-save-your-spot-account-creation-upse/) |
| 260513-j8a | Clear `useActiveReservation` context on release from `ItemReservePage` — sticky banner on registry page lingered after release because `ItemReservePage`'s release-success `useEffect` fired `showToast` + `navigate(/registry/:id)` but never called `useActiveReservation().clear()`. Consequence: after releasing a reservation from the dedicated per-item page, the user landed back on `RegistryPage` with `StickyReserveBanner` still rendering the just-released reservation (running countdown, "Continue to retailer" CTA — all phantom). The same pattern was already applied to `StickyReserveBanner.tsx:65` (release-from-banner) and `ConfirmPurchaseBanner` (quick-260513-0kn for confirm-purchase) — `ItemReservePage` was the third surface that needed the same one-line fix. **Fix:** added `useActiveReservation` import, destructured `clear` (aliased as `clearActiveReservation` for readability), called it inside the existing release-success useEffect alongside the toast + navigate, and added `clearActiveReservation` to the effect's dep array. JSDoc note updated to document the unconditional-clear tradeoff (same as ConfirmPurchaseBanner: if user has a concurrent reservation, `useActiveReservationHydration` on RegistryPage re-resolves it). **TDD:** new P-06b spec in `ItemReservePage.test.tsx` mocks `useReleaseReservation` to drive idle→pending→success transition; asserts `activeMock.clear` is called exactly once; confirmed RED before fix, GREEN after. 148/148 web tests + `tsc --noEmit` clean. Exactly 2 files diffed (ItemReservePage.tsx + its test). User confirmed in browser: click Eliberează rezervarea → toast + navigate to registry; sticky banner does NOT linger | 2026-05-13 | 586f85c | Verified | [260513-j8a-fix-release-from-itemreservepage-not-cle](./quick/260513-j8a-fix-release-from-itemreservepage-not-cle/) |
| 260513-ge9 | Fix web registry PROGRESS tile duplicate count + hide Share CTA from non-owners — `web_hero.progress_copy` was `"{{n}} of {{total}} chosen"` while the tile ALSO rendered `{totalChosen}` in a separate big-numeral display span above, producing "2 2 of 3 chosen". Fix: dropped `{{n}}` placeholder from the i18n template AND removed the `n: totalChosen` arg from the t() call. Body now reads "of N chosen" (EN) / "din N alese" (RO) with the large numeral above unchanged. i18n synced across all 4 files (`web/src/i18n/{en,ro}.json` runtime + `web/i18n/{en,ro}.json` mirror) per quick-260512-vlg convention. **Share CTA gating (first web isOwner pattern):** added `isOwner?: boolean` prop to `ProgressStrip` (defaults to `false`), gated the `<Btn>` render on it; `RegistryPage` computes `const isOwner = !!user && registryQ.data?.ownerId === user.uid` inline (one consumer — not extracted to a hook) and passes through. Guests (`user === null`) and non-owners both evaluate to `false` → Share button completely absent (no placeholder, no disabled state). Loading state (`registryQ.data === undefined`) also safe — optional chain short-circuits to false. New `ProgressStrip.test.tsx` with 4 vitest specs locks both behaviours (body copy positive + Share visible-for-owner + Share hidden-for-guest + zero-state sanity); 120/120 web tests + `tsc --noEmit` clean. User confirmed in browser: owner sees Share + "of N chosen", guest+non-owner see no Share, RO locale reads correctly, zero state renders without JS error | 2026-05-13 | b682190 | Verified | [260513-ge9-web-registry-progress-tile-remove-duplic](./quick/260513-ge9-web-registry-progress-tile-remove-duplic/) |
| 260525-gqs | Fix URL metadata fetch error in Add Item → Paste URL — `fetchOgMetadata` now follows HTML `<meta http-equiv="refresh">` redirects (up to 3 hops, same-origin guard) so EMAG stale-slug product URLs (e.g. iPhone 16 → renamed iPhone 17 Pro Max SKU at the same `/pd/CODE/` path) auto-fill title + image instead of returning the empty "No details found" fallback. Root cause: EMAG re-slugs URLs whenever a SKU title is edited and serves an HTML refresh stub instead of an HTTP 301/302 — Node `fetch()` follows HTTP redirects automatically but does NOT follow HTML meta-refresh, so the Cloud Function was parsing the tiny redirect-stub HTML (no OG tags) and returning all-null. New `extractMetaRefreshTarget(root)` helper handles content-attribute variants (`0;url=/x`, `0; URL='/x'`, `0;url="/x"`); 3-hop loop with `new URL(target, currentUrl).origin` same-origin guard prevents open-redirect abuse; `fetchOgMetadataHandler` extracted as a named export so tests can invoke it directly without firebase-functions-test harness. 9/9 jest specs (1 happy path, 5 meta-refresh variants/hops/cross-origin, 1 empty page, 1 network error) GREEN; full functions suite 107/110 (3 unrelated pre-existing `createReservation` failures). Deployed to `gift-registry-ro` / europe-west3 / nodejs22 via single-function deploy (no other functions, rules, hosting, or indexes touched). **Task 4 — UX string improvement (added during checkpoint):** during on-device verification user tested other retailers (Answear, Altex) and hit the same "No details found" message; investigation showed those sit behind Akamai/Cloudflare bot protection that rejects all server-side fetches by TLS/JA3 fingerprint regardless of User-Agent — fundamentally different problem, phase-sized to address (Puppeteer / scraping API / client-side fetch). To avoid misleading users into thinking the URL is invalid, rewrote the fallback string EN+RO from "No details found for that URL — fill in below." → "Couldn't auto-fill from this site — enter details manually below." (RO: "Nu am putut completa automat pentru acest site — introdu detaliile manual mai jos."); LocalizationParityTest still passes. **Out of scope (filed as follow-up):** EMAG price extraction (lives in `EM.used_offers` JS globals, not OG tags) + broader WAF-protected retailer support. User confirmed on device: stale-slug EMAG URL auto-fills title + image; non-EMAG URLs now show honest fallback message | 2026-05-26 | f3c1ce4, f041eee, 10e086a, d835a06 | Verified | [260525-gqs-fix-url-metadata-fetch-error-in-add-item](./quick/260525-gqs-fix-url-metadata-fetch-error-in-add-item/) |
| 260512-wn3 | Eliminate Android cold-start AuthScreen flash (~1s) for cached signed-in users — fix required two layers because the planner's initial hypothesis was incomplete. **Data layer:** new `AuthStateEvent` sealed type (`Initial(user)` vs `Changed(user)`) emitted by `FirebaseAuthDataSource.authStateFlow` via a `seenFirst` flag inside `callbackFlow`; `AuthViewModel.init` keeps `_authState = Loading` when `Initial(null)` arrives (cold-start gap where Firebase hasn't read disk cache yet), only emitting `Unauthenticated` on `Changed(null)` (settled signed-out). 12 `AuthViewModelTest` regression tests pin the sequence (Initial(null)→Changed(user) never flashes Unauthenticated). **UI layer (follow-up, root cause confirmed by physical-device test):** `AppNavigation.kt`'s `backStack = mutableStateListOf<Any>(AuthKey)` initial value caused a Compose race — when `authUiState` transitioned `Loading → Authenticated`, recomposition rendered `NavDisplay(backStack.last() = AuthKey)` BEFORE the existing `LaunchedEffect(authUiState)` could clear+swap to `HomeKey`. Under cold-start main-thread pressure (logcat: "Skipped 35+ frames"), that one-frame window stretched to ~1 second. Added a unified `isLoading || isMidTransition` gate that holds `CircularProgressIndicator` while `isAuthRoot != (authUiState is Unauthenticated)`, collapsing the race to zero visible frames. Covers both directions (cold-start cached restore + sign-out from Home). Zero `mutableStateListOf<Any>(AuthKey)` change — initial value left intact; the gate handles reconciliation. User confirmed on physical device: cold-start with cached user → spinner → HomeKey directly, NO AuthScreen visible | 2026-05-13 | 845f147, 67b0251, 4e9e613 | Verified | [260512-wn3-fix-auth-screen-flash-on-startup-for-alr](./quick/260512-wn3-fix-auth-screen-flash-on-startup-for-alr/) |
| 260519-08n | j5j/ke1 follow-up: pass navigation state in the `navigate-on-status-flip` useEffect of `ItemReservePage` — closes a race that defeated j5j/ke1 entirely. **Discovery via in-browser diag logs:** user retest after ke1 still showed sticky banner + ReserveDetailSection + tile RESERVED after release. localStorage-persisted diag logs (added then stripped during diagnosis) revealed the timeline — click→pending at T+0, `[DIAG RegistryPage] render fullState: null` at T+132ms, `[DIAG inline-release AWAIT_OK]` at T+134ms. The navigate to `/registry/:id` fired BEFORE `release()` even resolved, AND with no navigation state. **Root cause:** `ItemReservePage` has TWO useEffects that can navigate to `/registry/:id` after a release: (a) the release-success effect (line ~232 — passes nav state per j5j); (b) the navigate-on-status-flip effect (line ~268 — watches `itemsQ` snapshot for `prev === 'reserved' && (current === 'available' \|\| current === 'purchased')` and navigates WITHOUT state, added by quick-260513-hon as a transition detector for confirm-purchase + cross-tab/Cloud-Tasks release). The Firestore `onSnapshot` listener fires faster than `useReleaseReservation`'s `setStatus('success')` promise chain, so (b) wins the race and navigates WITHOUT state → RegistryPage's j5j/ke1 `ignoreReservationId`/`ignoreItemId` guards have nothing to match against → hydration re-populates shared context → banner + ReserveDetailSection appear. **Fix:** extend the status-flip effect's `navigate(/registry/:id)` to pass `{ state: { recentReleasedReservationId: active.reservationId, recentReleasedItemId: active.itemId } }` (same shape as j5j's release-success navigate). Either racer now suppresses correctly because the state is identical. **Tests:** new K-22 regression in `ItemReservePage.test.tsx` mirrors K-21's ProbeDestination pattern — sets up reserved-by-me detail render, mutates the items snapshot to flip the item status from 'reserved' to 'available', asserts the navigate captures the expected state payload. 207/207 web tests + `tsc --noEmit` clean (was 206 → +1). **Executor deviations (documented in SUMMARY):** worktree was based on `683a122` (8 commits behind main) — merged main before starting; the planner's claim that "source fix and diag logs already exist in worktree" was false (they existed only in the orchestrator's main checkout, not the worktree) — executor turned the task into a clean RED → GREEN TDD cycle by applying the source fix in Task 2. Zero scope creep — deliverables match the plan's must_haves exactly. Diag scaffolding from the orchestrator's main checkout was discarded via `git checkout --` before FF-merging the worktree branch. Zero touches to backend, i18n, or any other component | 2026-05-19 | 2681626, f8919ee, 9f9dde7 | Verified | [260519-08n-j5j-follow-up-status-flip-race-pass-navi](./quick/260519-08n-j5j-follow-up-status-flip-race-pass-navi/) |
| 260518-ke1 | Harden release-from-`ItemReservePage` race guard with `ignoreItemId` — j5j follow-up. User retested after j5j and reported the bug persists (sticky banner + ReserveDetailSection + tile RESERVED state still re-appear on `/registry/:id` after release). Network tab confirmed `hydrateActiveReservation` returns `{ active: { reservationId: 'X', ... } }` AFTER the release — meaning the backend IS returning an active reservation that ISN'T the just-released one (different `reservationId`). **Most plausible cause:** stale-active reservation rows accumulated in Firestore from earlier test sessions where the emulator's `setTimeout` auto-expiry fallback (quick-260510-pdp) was lost across emulator restarts — those rows stay marked `status='active'` forever, so hydration legitimately returns them on every page load. j5j's `ignoreReservationId` only suppresses the EXACT reservation ID just released; a stale older reservation on the SAME item slips through because its `reservationId` doesn't match (different ID, same `itemId`). The same scenario can manifest in production when Cloud Tasks fails to fire auto-expiry. **Fix (2 source files + 2 test files):** (a) `useActiveReservationHydration.ts` — extend options arg from `{ ignoreReservationId? }` to `{ ignoreReservationId?; ignoreItemId? }`. After backend resolves with `r.data.active`, BEFORE `set(...)`, combine both checks: `if (options?.ignoreReservationId === r.data.active.reservationId \|\| options?.ignoreItemId === r.data.active.itemId) { setStatus('empty'); return }`. Logical OR — either match suppresses. Both option keys added to effect dep array as stable strings (no object identity issues). JSDoc updated to document `ignoreItemId` semantics: covers the case where the backend returns a DIFFERENT reservationId for the same item (stale-active row from prior session where Cloud Tasks failed to auto-expire); without this guard the banner re-appears after release for the same item the user just thought they released. (b) `RegistryPage.tsx` — at the j5j hydration call site (line 58), add `ignoreItemId: recentReleasedItemId` alongside the existing `ignoreReservationId: recentReleasedReservationId`. `recentReleasedItemId` is already in scope from j5j (read from `location.state`, already passed to the `itemsForRender` memo) — just thread the same value into hydration too. `ItemReservePage.tsx` already passes both IDs in navigate state via j5j — no change needed. **Tests (4 new, 2 files):** H-NEW-03: `ignoreItemId` match → `set` NOT called, status='empty'. H-NEW-04: `ignoreItemId` mismatch → `set` called normally (regression guard, ensures no over-suppression). H-NEW-05: both options set + match-by-either → suppress (proves logical OR semantics). R-NEW-04: MemoryRouter initial entry with `state: { recentReleasedItemId: 'X' }` → hydration spy called with `{ ignoreItemId: 'X' }` (locks future drift in the threading from `location.state` → hydration call). 206/206 web tests + `tsc --noEmit` clean (was 202 → +4). Zero touches to `ItemReservePage.tsx`, `useActiveReservation.ts`, `useReservationForItem.ts`, `useReleaseReservation.ts`, `useItemsQuery.ts`, `StickyReserveBanner.tsx`, `ReserveDetailSection.tsx`, `ItemCard.tsx`, `ItemGrid.tsx`, app/, functions/, backend, or i18n. Worktree was at current main HEAD pre-Task 1, no rebase needed; clean FF. User-confirmed in browser pending; user also doing a clean-slate DB reset in parallel to clear accumulated stale-active reservations from earlier test sessions. The combined j5j + ke1 fix now suppresses both (a) the exact just-released reservation (composite-index lag) AND (b) any stale-active reservation on the same item (Cloud Tasks failure / emulator restart) — production-resilient | 2026-05-18 | 3b874be, 8fba738, 8285726 |  | [260518-ke1-harden-release-race-guard-with-ignoreite](./quick/260518-ke1-harden-release-race-guard-with-ignoreite/) |
| 260518-j5j | Eliminate release-from-`ItemReservePage` race that made `StickyReserveBanner` + `ReserveDetailSection` + tile RESERVED state re-appear for hundreds of ms on `/registry/:id` — k37/iux/k4f/ku3/lsi/oiy follow-up. User-reported (confirmed): bug DOES NOT occur when Safari Web Inspector is open — textbook timing-race signal (devtools slows JS, closes the race window). The oiy fix (optimistic items-cache patch before navigate) was insufficient because **two parallel races** ran on RegistryPage re-mount: (1) **hydration race (dominant)** — `useActiveReservationHydration` fires immediately; its backend callable `hydrateActiveReservation` queries Firestore with `status === 'active'` filter; the just-released reservation IS marked `status='expired'` server-side (release transaction commits before callable resolves), BUT the composite index (registryId + giverEmail + giverId + status + orderBy createdAt) takes a few ms to propagate, so hydration briefly returns the just-released reservation → `set(reservation)` re-populates the shared `useActiveReservation` context → sticky banner + `ReserveDetailSection` render; (2) **snapshot race (secondary)** — even with the oiy optimistic items-cache patch, the new `useItemsQuery` `onSnapshot` listener (set up on RegistryPage re-mount) may fire FIRST with Firestore client-cache data (stale `item.status='reserved'`) before the server-sync snapshot fires with fresh data → cached-snapshot overwrites the oiy patch → tile briefly shows RESERVED. **Strategy:** pass React Router navigation state carrying the released reservation/item IDs; RegistryPage uses it to suppress the racy operations on first arrival. **Fix (3 source files):** (a) `ItemReservePage.tsx` release-success useEffect — `navigate(/registry/:id, { state: { recentReleasedReservationId: active.reservationId, recentReleasedItemId: itemId } })` (replaces bare navigate; `active` from k4f derivation is in scope because the Release button requires it). (b) `useActiveReservationHydration.ts` — new optional 2nd arg `options?: { ignoreReservationId?: string }`; after backend callable resolves with `r.data.active`, BEFORE `set(r.data.active)`, if `options?.ignoreReservationId === r.data.active.reservationId` then `setStatus('empty')` + early-return (treats stale-index hit as null). Effect deps include the stable string key. (c) `RegistryPage.tsx` — `useLocation()` at top of component body; read `location.state` once; pass `{ ignoreReservationId: recentReleasedReservationId }` to `useActiveReservationHydration(id, ...)`; new `itemsForRender` `useMemo` (deps `[itemsQ.data, recentReleasedItemId]`) that returns `itemsQ.data` identity when no override applies and otherwise maps the released item to `status='available'` + null reservedBy/reservedAt/expiresAt; pass `itemsForRender` to `ItemGrid` AND to the counts memo so the progress strip + FilterChips counts match the grid. Returns `undefined` when `itemsQ.data === undefined` (loading state preserved). **Tests (3 files, 6 new specs):** (a) `useActiveReservationHydration.test.ts` (NEW FILE — didn't exist) — H-NEW-01: ignore matches → `set` NOT called, status flips to 'empty'; H-NEW-02: ignore mismatch → `set` called normally (regression guard). (b) `ItemReservePage.test.tsx` K-21: navigate is called with second arg `{ state: { recentReleasedReservationId, recentReleasedItemId } }`. Uses probe-destination-component pattern reading `useLocation()` and dumping `location.state` into a `data-state` attribute (rejected file-wide `react-router` mock because it would break the K-20 destination-route assertion). (c) `RegistryPage.test.tsx` (at `web/src/features/registry/__tests__/RegistryPage.test.tsx`, NOT `web/src/pages/__tests__/` as initial scope assumed) — R-NEW-01: MemoryRouter initial entry with `state: { recentReleasedItemId: 'X' }` and items cache with item X status='reserved' → ItemCard for X renders as 'available' (override); R-NEW-02: no state → items unchanged (regression guard); R-NEW-03: hydration spy called with `ignoreReservationId` matching the state value. 202/202 web tests + `tsc --noEmit` clean (was 196 → +6). Zero touches to `useReservationForItem.ts`, `useActiveReservation.ts`, `useReleaseReservation.ts`, `useItemsQuery.ts`, `StickyReserveBanner.tsx`, `ItemCard.tsx`, `ItemGrid.tsx`, app/, functions/, backend, or i18n. User-confirmed in browser pending | 2026-05-18 | f90da85, c0aef4c, 3e456ed |  | [260518-j5j-fix-release-from-itemreservepage-race-pa](./quick/260518-j5j-fix-release-from-itemreservepage-race-pa/) |
| 260516-oiy | Fix stale registry-list tile after release-from-`ItemReservePage` — k37/iux/k4f/ku3/lsi follow-up exposed during user retest. **Reproduction:** signed-out user reserves item, navigates to detail page, clicks Release → lands back on `/registry/:id` where the just-released item still shows the `RESERVED` pill + in-card reserved-by-me countdown banner until manual refresh. **Root cause:** snapshot-lag on the React Query cache. `web/src/features/registry/useItemsQuery.ts` uses a Firestore `onSnapshot` listener set up inside `useEffect` with cleanup that unsubscribes on unmount. When user navigates from `RegistryPage` → `ItemReservePage`, RegistryPage unmounts and the listener tears down. The items cache (`['registry', registryId, 'items']`) stays frozen at the moment of navigation with the now-target item still marked `status: 'reserved'`. After release-success on ItemReservePage: `clearActiveReservation()` correctly nulls the shared banner context (sticky banner stays hidden, confirmed); `navigate('/registry/:id')` re-mounts RegistryPage; the new onSnapshot listener takes ~100-500ms to fire its first snapshot in the emulator. During that window, `useItemsQuery` returns the FROZEN cache and `ItemCard` renders the stale reserved state. Backend already correctly marks the reservation `expired` and the item `available` before the callable returns — `releaseReservationCallable` uses a transactional update via `releaseReservationCore` that commits before the callable resolves; this is purely a client-side cache freshness issue across a route unmount. `StickyReserveBanner` does NOT re-appear (shared context already cleared; `useActiveReservationHydration` backend call correctly returns null for the expired reservation). **Fix:** purely client-side, one file. In `ItemReservePage.tsx`, added `useQueryClient` import from `@tanstack/react-query` + a destructure inside the component; inside the existing release-success `useEffect` (immediately BEFORE the `navigate(...)` call), call `queryClient.setQueryData<Item[]>(['registry', id, 'items'], (old) => old?.map((it) => it.id === itemId ? { ...it, status: 'available' as const, reservedBy: null, reservedAt: null, expiresAt: null } : it) ?? old)` to optimistically patch the released item in-place. Added `queryClient` + `itemId` to the effect deps. Inline comment documents WHY (snapshot listener tears down across the navigation; cache stays frozen; without the patch the registry list shows the stale reserved state for ~100-500ms until the new listener's first fire). Type-only import of `Item` retained (used solely as a type parameter on `setQueryData<Item[]>`). The eventual real snapshot listener overwrites the cache with fresh data — optimistic patch and real data agree on the item being `available`, no flicker. **Tests:** new K-20 spec appended to `ItemReservePage.test.tsx` — mocks `useQueryClient` via `vi.hoisted` + `vi.mock('@tanstack/react-query', async (importOriginal) => ({ ...await importOriginal(), useQueryClient: () => queryClientMock }))` preserving the real `QueryClient`/`QueryClientProvider` consumed by `renderPage`. Drives `useReleaseReservation` mock through `idle → pending → success`; asserts `setQueryData` is called with `['registry', registryId, 'items']`; when the captured updater function is invoked with a fake items array containing the target item with `status='reserved' / reservedBy='user@x' / reservedAt + expiresAt set` plus one unrelated item, the returned array has the target item's `status === 'available'`, `reservedBy === null`, `reservedAt === null`, `expiresAt === null`, and the unrelated item unchanged. Call-order asserted via `mock.invocationCallOrder` on `setQueryData` vs `activeMock.clear` (strict superset proof that the patch fires before navigate, since `clear` sits between them in the effect body). 196/196 web tests + `tsc --noEmit` clean (was 195 → +1). Zero touches to `useItemsQuery.ts`, `StickyReserveBanner.tsx`, `ConfirmPurchaseBanner.tsx`, `useReleaseReservation.ts`, app/, functions/, any other web component, any i18n file, or backend. Worktree rebased onto current main HEAD pre-Task 1 per orchestrator instruction (lbf/lsi precedent showed worktree-base drift causes merge headaches); merge cleanup required removing a duplicate untracked PLAN.md before FF-merging cleanly. User-confirmed in browser pending. Follow-up candidate: same optimistic-patch pattern could be applied to `ConfirmPurchaseBanner` (item → 'purchased') and `StickyReserveBanner` release path (though the latter stays on /registry/:id without unmounting the listener, so the bug doesn't manifest there) | 2026-05-16 | 2a7a623, 584675d, f1891d0 |  | [260516-oiy-optimistic-items-cache-patch-on-release-](./quick/260516-oiy-optimistic-items-cache-patch-on-release-/) |
| 260516-lsi | Make product blade on `ItemReservePage` clickable — clicking anywhere on the hero card opens the merchant's product page in a new tab. Affects BOTH blade renderings: `ItemDetailHero` used by all three browse branches (BROWSE_AVAILABLE / BROWSE_RESERVED_BY_OTHER / BROWSE_PURCHASED, lines 534-565) AND the reserved-item hero inside `renderReservedByMeDetail` (lines 654-708). Previously non-interactive `<div>` — users couldn't click the hero image/title/price to jump to retailer. **URL priority:** `affiliateUrl` (commission flow — matches existing "Continue to retailer" CTAs in `StickyReserveBanner.tsx:144-153` and `ItemReservePage.tsx:719-727`); fall back to `originalUrl`. If BOTH empty/missing, render as `<div>` (graceful degradation — no broken navigation, no #-href). For reserved-by-me hero, `active.affiliateUrl || item.originalUrl` (sibling `item` in scope; `active` shape has no `originalUrl`). **Anchor attrs:** `target="_blank"` + `rel="noopener noreferrer"` (Mozilla-recommended security: prevents tab-nabbing + blocks `Referer` leak — matches existing CTA pattern). **Visual affordance:** `cursor-pointer hover:border-gm-accent transition-colors` + Btn.tsx-line-58 focus-visible ring (`focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gm-accent`). Border-only shift to accent stroke on hover (bg untouched, mirrors Pill accent-on-border idiom). **A11y:** new i18n key `web_reserve.item_page.blade_link_aria` interpolating `{{retailer}}` from `item.merchantDomain ?? 'retailer'`. EN: "Open product page at {{retailer}}"; RO: "Deschide pagina produsului pe {{retailer}}". Synced across ALL FOUR i18n files (`web/i18n/{en,ro}.json` + `web/src/i18n/{en,ro}.json`) per the established convention from quick-260513-fk1 / quick-260512-vlg. **HTML safety:** pre-impl source scan confirmed no nested anchors inside either blade body (the existing "Continue to retailer" anchor lives in the lower button row OUTSIDE the blade per line 719-727); no `<a>`-inside-`<a>` invalidity. **Tests:** 5 new specs appended to `ItemReservePage.test.tsx` — K-15 (BROWSE_AVAILABLE + affiliateUrl set → `<a>` href === affiliateUrl, target=_blank, rel includes both noopener+noreferrer); K-16 (BROWSE_AVAILABLE + affiliateUrl="" + originalUrl set → `<a>` href === originalUrl, fallback path); K-17 (BROWSE_PURCHASED + both URLs empty → `<div>` not `<a>`, graceful degradation); K-18 (BROWSE_RESERVED_BY_OTHER aria-label uses blade_link_aria interpolating merchantDomain); K-19 (Reserved-by-me hero + active.affiliateUrl set → `<a>` href === active.affiliateUrl; nested time-to-purchase card preserved inside the blade; lower-row "Continue to retailer" CTA regression-guarded). 195/195 web tests + `tsc --noEmit` clean (was 190 → +5). Worktree rebased onto current main HEAD before Task 1 (per orchestrator instruction — lbf cherry-pick precedent showed worktree-base drift causes merge headaches); FF-merged cleanly back. Zero Android / backend / other-component / new-files changes. User-confirmed in browser pending | 2026-05-16 | d54f7e2, 5b6c88f, f95119b | Verified | [260516-lsi-make-product-blade-on-itemreservepage-cl](./quick/260516-lsi-make-product-blade-on-itemreservepage-cl/) |
| 260516-lbf | Fix web product tiles silently dropping shop / price / currency — schema mismatch between Android writer and web mapper, surfaced during k37 verification when user reported "Only the title and image are displayed." **Root cause:** `app/src/main/java/com/giftregistry/data/registry/ItemRepositoryImpl.kt:76-87` `toMap` / `toUpdateMap` write only `{title, originalUrl, affiliateUrl, imageUrl, price, notes, status, createdAt, updatedAt}` — with `price` as `String?` holding the formatted display string from `fetchOgMetadata` (e.g. `"459,00 RON"`, `"€19.99"`). NO `currency`, NO `merchantDomain`, NO `priceAmount`/`priceCurrency`. The web mapper `web/src/lib/firestore-mapping.ts:77-86` expected `price` as number (`typeof === 'number'` → returned `null` for strings), `currency` as string (never written → `null`), `merchantDomain` as string (never written → `null`). Result: every web-rendered item showed only image + title; `ItemCard` (shop line + price), `ItemReservePage` BROWSE_AVAILABLE / BROWSE_RESERVED_BY_OTHER / BROWSE_PURCHASED (hero shop + price), and `renderReservedByMeDetail` all conditionally render those fields — when they're null, the lines vanish silently. Sibling precedent: quick-260510-o7w fixed an analogous mismatch for `RegistryDto`. **Fix scope (web-only):** two new exported helpers in `firestore-mapping.ts` + `mapItemSnapshot` rewrite. (1) `parsePriceString(raw: string): { amount: number \| null; currency: string \| null }` — mirrors `functions/src/registry/fetchOgMetadata.ts:71-108` server-side parsing client-side. Extracts first numeric run via `/\d[\d.,]*/`, normalizes thousand/decimal separators (right-most of `.` and `,` is the decimal when both present; comma followed by 3 digits = thousands; otherwise comma = Romanian-locale decimal). Currency aliases: minimal subset of the Cloud Function map — `€→EUR`, `$→USD`, `£→GBP`, `lei→RON`, `ron→RON`, `eur→EUR`, `usd→USD` (extend on demand). Handles `"459,00 RON"` → `{459, RON}`, `"€19.99"` → `{19.99, EUR}`, `"$1,299.99"` → `{1299.99, USD}`, `"299"` → `{299, null}`, `"1,234"` (3-digit tail) → `{1234, null}` (thousands not decimal), `"abc"` / `""` → `{null, null}`. (2) `deriveMerchantDomain(d): string \| null` — priority: explicit `d.merchantDomain` (string) wins for legacy/future structured docs; otherwise `new URL(d.originalUrl).hostname.replace(/^www\./, '')` with try/catch for invalid URLs. (3) `mapItemSnapshot` now branches on `typeof d.price`: number → `price = d.price`, `currency = d.currency ?? null` (legacy/future-compatible); non-empty string → `parsePriceString(d.price)` → `price = parsed.amount`, `currency = parsed.currency ?? d.currency ?? null`; anything else → both null. `merchantDomain` always goes through `deriveMerchantDomain` so the URL-derived fallback is automatic. JSDoc block above `mapItemSnapshot` documents the schema mismatch + links to quick-260510-o7w precedent so a future reader doesn't try to re-tighten the types. **Tests:** 19 new specs appended to existing `web/src/lib/__tests__/firestore-mapping.test.ts` (sibling to the existing `mapRegistrySnapshot` suite, same `makeSnap`/`makeItemSnap` pattern) — 8 `parsePriceString` unit tests (all edge cases above) + 6 `deriveMerchantDomain` unit tests (explicit-wins, www-strip, missing/malformed URL handling) + 5 `mapItemSnapshot` integration tests (Android-style doc lights up all three fields; legacy/future structured doc still wins; null `originalUrl` → null merchantDomain; etc.). **Verification:** 190/190 web tests + `tsc --noEmit` clean (was 171 → +19). UI lights up automatically on browser reload — `ItemCard`, `ItemReservePage` BrowseShell/ItemDetailHero, and `renderReservedByMeDetail` already render the fields conditionally; no UI code touched. Worktree was based on an earlier snapshot before k37/iux/k4f/ku3 — cherry-picked 88aba0c → d5cda99 → 84a6efb cleanly onto main (no conflicts because lbf only touched files outside the k37 stack). Zero Android / backend / i18n / UI / new-files changes. Follow-up candidate: Android writer could be updated to persist `priceAmount` + `priceCurrency` + `merchantDomain` as structured fields so the parsing-on-read is no longer needed for new items; current fix means legacy data also works without backfill | 2026-05-16 | 47e8af7, 9d150d7, 20f0897 | Verified | [260516-lbf-fix-web-product-tiles-missing-shop-price](./quick/260516-lbf-fix-web-product-tiles-missing-shop-price/) |
| 260516-ku3 | Fix `StickyReserveBanner` not clearing on second release in the same page mount — k37/iux/k4f follow-up exposed by multi-reservation user flow. **Reproduction (user-reported):** signed-out user reserves item A → reserves item B → shared `useActiveReservation` now holds B (most recent wins). Returns to `RegistryPage`; sticky banner shows B. Clicks Release → backend cancels B; toast fires; `clear()` empties shared context; `useActiveReservationHydration` re-resolves to A (still-active concurrent reservation per quick-260512-x5d); banner re-appears with A. Clicks Release on A → backend cancels A successfully — BUT page does NOT update; banner stays mounted with A's countdown ticking, until manual refresh. **Root cause:** `StickyReserveBanner.tsx` lines 50-67 use two refs (`releaseSuccessToastedRef`, `releaseErrorToastedForRef`) as one-shot guards in the release-success / release-error `useEffect`s. Both are set on the first success and **NEVER reset**. The component stays mounted across releases (unlike `ItemReservePage`, where the release-success effect navigates back to `/registry/:id` and unmounts so refs are recreated next mount — same ref pattern exists there but bug doesn't manifest). Second release: `releaseStatus` flips to `'success'` again, but the `!releaseSuccessToastedRef.current` guard is `false`, so the entire success branch (toast + `clear()`) is skipped silently. **Fix:** added a new `useEffect` immediately BEFORE the success effect (source order matters for React's declaration-order effect firing) that resets BOTH refs whenever `releaseStatus === 'pending'`. `useReleaseReservation.release()` always calls `setStatus('pending')` at the start of each call (`useReleaseReservation.ts:33`), so 'pending' is the reliable reset signal for "a new release attempt is starting." Effect deps `[releaseStatus]` only. Identical-message error after a success now also re-fires the error toast (latent issue acknowledged but not user-reported — covered by the same one-line fix at no extra cost). **Test (new sequential-release spec in `StickyReserveBanner.test.tsx`):** renders with active=B, drives release mock through `idle → pending → success`, asserts toast + `clear()` called once; then mutates `activeMock.active` to A, rerenders, drives release through the lifecycle again, asserts call counts now both equal 2. RED before fix (`expected "spy" to be called 2 times, but got 1`) → GREEN after. **TDD discipline:** failing test committed first (495945d), fix committed second (6f50c03) — same shape as the k4f / iux predecessors. 171/171 web tests + `tsc --noEmit` clean. Zero touches to `ItemReservePage.tsx`, `useReleaseReservation.ts`, `useActiveReservation.ts`, `useActiveReservationHydration.ts`, any backend, or any i18n. User-confirmed in browser pending | 2026-05-16 | 495945d, 6f50c03, a12a411 | Verified | [260516-ku3-fix-stickyreservebanner-not-clearing-on-](./quick/260516-ku3-fix-stickyreservebanner-not-clearing-on-/) |
| 260516-k4f | Fix `ItemReservePage` not transitioning to reserved-by-me detail after Reserve from BROWSE_AVAILABLE — k37/iux follow-up. **Root cause:** `useReservationForItem` (per-page backend lookup) and `useActiveReservation` (shared React context) are independent state systems. k37's Reserve mutation `onSuccess` called `setActive(...)` to seed the shared context, but the page rendered off `useReservationForItem.active` which never observed the shared context. So after Reserve: shared context updated ✓, lookup hook stayed null ✗, Firestore listener flipped `item.status` to `'reserved'`, page rendered BROWSE_RESERVED_BY_OTHER ("Already reserved by another guest") for the user's OWN just-made reservation. The k37 comment at ItemReservePage.tsx:94-100 confidently claimed `"the next render will pick it up via useReservationForItem and naturally transition into the reserved-by-me detail branch — NO manual navigate needed"` — false; that's the bug. **Fix:** ONE new computation line in `ItemReservePage.tsx` — `const active = (sharedActive && sharedActive.itemId === itemId) ? sharedActive : lookupActive`. Destructure renames: line 86 `active → lookupActive`; line 92 expanded to `const { active: sharedActive, set: setActive, clear: clearActiveReservation } = useActiveReservation()`. Hooks reordered: `useActiveReservation()` moved before `useCountdown(active?.expiresAtMs ?? null)` so `sharedActive` is in scope for the derivation (Rules-of-Hooks-safe — both called unconditionally every render). Every downstream consumer (countdown, `sawNonExpiredRef` tracking effect, navigate-on-status-flip effect, iux `effectiveActive` derivation, branches 4/5, `renderReservedByMeDetail`) reads the derived `active` unchanged — the new `const active` shadows at the same scope as the old destructure. **iux compatibility preserved:** `effectiveActive` still wraps the derived `active`, so a shared-context-sourced reservation that drifts past 30 minutes during the same page mount still short-circuits to the legitimate "Your time ran out" branch (rare; covered by existing K-09). **itemId guard prevents cross-page leak:** when shared context has an active for a DIFFERENT itemId (e.g., user navigates from one detail page to another concurrent reservation), the derivation falls back to `lookupActive` — locked in by new K-14. JSDoc block + misleading inline comment both rewritten to document the new derivation. **Tests:** K-12 (Reserve→detail transition: BROWSE_AVAILABLE → mock onSuccess fires `setActive` → next render → `item-reserve-detail` present, `item-reserve-available` absent), K-13 (hydration regression: shared empty + lookup set + matching itemId → detail renders), K-14 (cross-page leak guard: shared set + itemId mismatch → derivation uses lookup). `activeMock.active = null` added to `beforeEach` as cross-test leak safety net. 170/170 web tests + `tsc --noEmit` clean. Zero backend / i18n / new-files touches. User confirmed release flow works independently on retest | 2026-05-16 | 93eca0d, 7a10591, 775ba78 | Verified | [260516-k4f-fix-itemreservepage-not-transitioning-to](./quick/260516-k4f-fix-itemreservepage-not-transitioning-to/) |
| 260516-iux | Fix `ItemReservePage` rendering "Your time ran out" + infinite-loading for anonymous viewers — k37 follow-up. Two distinct bugs surfaced during k37 human-verify; both blocked the new BROWSE_AVAILABLE branch from being reachable for not-signed-in users. **Bug A — truly anonymous (no Firebase user, no `guestIdentity` in localStorage):** `useReservationForItem.ts` short-circuited (`if (!user && !identity) return`) without touching its local `status`, leaving it at `'idle'` forever; `ItemReservePage`'s `lookupStatus === 'idle'` gate then held the loading state indefinitely. Fix: set `status = 'empty'` + `active = null` with a stable `__anon__\|registryId\|itemId` lastKeyRef key (distinct from signed-in/guest key shapes, so a mid-render sign-in re-triggers fetch normally). **Bug B — anonymous-with-stale-guest-identity + stale "active" reservation in Firestore (the user-reported screenshot):** `ItemReservePage`'s expired branch (`if (active && countdown?.expired)`) fired on initial mount whenever a backing reservation's `expiresAtMs` was already in the past — common after emulator restart killed the auto-release `setTimeout` from quick-260510-pdp, or for any legacy stale-active row. Viewer with no mental model of having reserved saw "Your time ran out." Initial planner scope gated only branch 4 (expired); user pushed back because branch 5 (reserved-by-me detail) would still match `active`-truthy and render `00:00` countdown + Release + Confirm CTAs — same confusion, different page. Final fix: derived `effectiveActive = (active && countdown?.expired && !sawNonExpiredRef.current) ? null : active`; branches 4 AND 5 both read `effectiveActive`, so stale-expired-on-mount falls all the way through to the `item.status === 'available' \| 'reserved' \| 'purchased'` browse branches. `sawNonExpiredRef` flips to `true` the first time `active && !countdown?.expired` is observed → in-session expirations still render "Your time ran out" correctly. The two `useEffect`s (sawNonExpiredRef tracking + navigate-on-status-flip at lines 131-142) deliberately keep the real `active` reference so legitimate transitions out of 'reserved' (confirm-purchase, Cloud Tasks release) still fire navigation; only the render-time branch matching uses `effectiveActive`. JSDoc state-machine block at lines 27-37 rewritten to document the new derivation rule. 5 new tests (U-08 + K-08/09/10/11) + P-05 rewritten (it had asserted the exact buggy stale-on-mount behaviour). 167/167 web tests + `tsc --noEmit` clean. Zero backend / i18n / new-files / new-routes touches. User screenshot bug reproduced + fix confirmed in browser pending | 2026-05-16 | 836442b, 5ef542b, b43cf47 | Verified | [260516-iux-fix-itemreservepage-rendering-your-time-](./quick/260516-iux-fix-itemreservepage-rendering-your-time-/) |
| 260513-k37 | Improve web product tiles + add product detail page — (1) tile redesign: entire `ItemCard` becomes a `<Link>` to `/registry/:id/item/:itemId` with vertical stack (image / shop = raw `merchantDomain` faint mono / product name truncated to 60 chars + `...` via new `truncateTitle` helper / bold price). Inline Reserve button removed from card body — Reserve now lives only on the detail page. Reserved-by-me banner-row remains a `<button>` invoking parent's `onReservedByMeClick` (clicking inside it doesn't double-fire the outer Link via `e.stopPropagation()`). `SkeletonCard`, `ItemGrid`, `RegistryPage` updated to match new layout + click model (`renderReserve` removed; `renderReservedByMeClick` retained per D-06 navigation rule). (2) `ItemReservePage` extended with three new browse-state branches: BROWSE_AVAILABLE shows hero image + shop + full untruncated name + price + owner notes (`item.notes`) + "Reserve this gift" CTA wired to `useCreateReservation` direct mutation (no round-trip through RegistryPage's autoReserveItemId query param); BROWSE_RESERVED_BY_OTHER renders view-only detail with "already reserved by another guest" message and no Reserve CTA (D-06: zero reserver-name surface); BROWSE_PURCHASED renders view-only "already purchased" detail with no CTA. State-machine priority: `active` reservation (reserved-by-me) wins over `item.status === 'reserved'` so the viewer's own reservation never falls into the reserved-by-other branch. Existing reserved-by-me detail (countdown, release, confirm-purchase, expired) unchanged. 10 new i18n keys synced across all 4 i18n files (EN+RO × `web/src/i18n/` + `web/i18n/`). 162/162 web tests pass (was 155 — +7 K-tests for new behaviour); `tsc --noEmit` clean; `npm run build` clean. Task 3 human-verify outstanding (10-step browser walkthrough) | 2026-05-13 | 6e2a2cb, fc9e1f2, 8c1f4cb, 8c28555 | Verified | [260513-k37-improve-web-product-tiles-add-product-de](./quick/260513-k37-improve-web-product-tiles-add-product-de/) |
| 260521-myv | Flip Android share URL from `/r/{id}` to `/registry/{id}` to match canonical web fallback route — `ShareUrl.kt` builder + 5 `ShareUrlTest` assertions + `ShareBanner.kt` display-format comment. Web, Cloud Functions, email templates, and AndroidManifest deep-link intent-filter already used `/registry/{id}` (verified via repo grep — zero remaining `web.app/r/` matches). No `/r/:id` → `/registry/:id` redirect added in `firebase.json` or `web/src/App.tsx` (deliberate — the web SPA never had a `/r/:id` route, so old `/r/`-shaped Android shares already 404'd; adding a redirect would only legitimize a pattern that should die). `:app:testDebugUnitTest --tests ShareUrlTest` 5/5 GREEN + `:app:assembleDebug` BUILD SUCCESSFUL. Optional device smoke outstanding (tap ShareBanner pill → paste URL into Chrome → RegistryPage loads, not NotFoundPage) | 2026-05-21 | db41de8, 045d2fa, f3a2f1a |  | [260521-myv-update-shareable-registry-link-from-r-re](./quick/260521-myv-update-shareable-registry-link-from-r-re/) |
| 260522-iew | Add missing Firestore composite indexes for `hydrateActiveReservation` Cloud Function and deploy — function in `functions/src/reservation/hydrateActiveReservation.ts` was throwing `FAILED_PRECONDITION: The query requires an index` on every web/app refresh (Cloud Logging error group `CPjI86GD2qez9wE`, Cloud Run service `hydrateactivereservation` in `europe-west3`). Two new composite indexes added to `firestore.indexes.json`: signed-in path (`registryId` ASC + `giverId` ASC + `status` ASC + `createdAt` DESC) and guest path (`registryId` ASC + `giverEmail` ASC + `giverId` ASC + `status` ASC + `createdAt` DESC) — equality fields first, orderBy DESCENDING last per Firestore composite-index rules. Pre-existing indexes preserved verbatim (`items` `status+addedAt`, `reservations` `status+expiresAt`). Deployed via `firebase deploy --only firestore:indexes --project gift-registry-ro` (exit 0); 4 indexes confirmed present in production via `firebase firestore:indexes --json`. Indexes build asynchronously — small `reservations` collection means READY within ~1 min; user can confirm in Firebase Console → Firestore → Indexes. Zero function/rules/test touches per guardrails | 2026-05-22 | df534c9, bc25b58 |  | [260522-iew-add-missing-firestore-composite-indexes-](./quick/260522-iew-add-missing-firestore-composite-indexes-/) |
| 260522-iqv | Fix "Registry not available" flash (~1s) on web registry page refresh — `useRegistryQuery.ts:55-56` and `useItemsQuery.ts:35-36` queryFns were coercing the empty cache via `?? null` / `?? []`, resolving the TanStack Query to `success` with `data === null` (or `[]`) before the `onSnapshot` `useEffect` had even fired once. `RegistryPage.tsx:246-248` then rendered `<NotFoundPage />` (i18n `registry.not_found_title` = "Registry not available") for ~1s until the first snapshot arrived and overwrote the cache. Fix: replace the coercion fallback with `new Promise<T>(() => {})` so TanStack Query stays in `pending` state with `data === undefined` until `setQueryData(...)` from `onSnapshot` transitions it to `success` — brings impl in line with the JSDoc contract on `useRegistryQuery.ts:7-19`. Added symmetric JSDoc to `useItemsQuery`. 3 new TDD regression tests pin the contract (initial mount → `data === undefined`; after `setQueryData(key, value)` → `data === value`; `setQueryData(key, null)` → `data === null` still works for genuine not-found). All 4 plan-specified canary suites green (`useRegistryQuery.test.ts`, `useItemsQuery.test.ts`, `RegistryPage.test.tsx`, `App.test.tsx`) — 63 tests passing. `npx tsc --noEmit` clean. Zero touches to `RegistryPage.tsx` / `queryClient.ts` / i18n / onSnapshot callbacks / mapping helpers / queryKey shapes per guardrails. 3 pre-existing `firebase.test.ts` App Check init failures from Phase 14-04 logged as deferred (out of scope, confirmed pre-existing). Manual next step: `cd web && npm run build && firebase deploy --only hosting` to live-verify | 2026-05-22 | f99b248, d756b35, 4908e03 |  | [260522-iqv-fix-registry-not-available-flash-on-web-](./quick/260522-iqv-fix-registry-not-available-flash-on-web-/) |
| 260530-ncw | Discover screen layout toggle — session-local 1-col ⇄ 2-col with icon-only button in top-right. Single `LazyVerticalGrid` with `GridCells.Fixed(layoutMode.columnCount)` used for both modes (no LazyColumn/LazyVerticalGrid swap); section headers + search bar span full width via `GridItemSpan(maxLineSpan)`; shimmer placeholders respect column count. `DiscoverLayoutMode` sealed interface in `DiscoverUiState.kt` (OneColumn/TwoColumns), `toggleLayoutMode()` on `DiscoverViewModel`, no DataStore persistence (default = OneColumn on each app launch). `Icons.Outlined.GridView` shown in 1-col, `Icons.Outlined.ViewAgenda` shown in 2-col (the user's "half a square with a full square under it" description). 2 new localized content descriptions (en + ro). `DiscoverProductCard.kt` unchanged — `fillMaxWidth` + `maxLines=2` adapt naturally to half-width cells. `StyleGuidePreview.kt` gains a 2-col `@Preview` alongside the existing 1-col. Visual verification on emulator-5554 PASS (toggle visible top-right, icon swaps correctly, both popular + search sections reflow, no persistence across relaunch) | 2026-05-30 | 8969c31, a982d40, aece860, 9c858a0 | Verified | [260530-ncw-add-a-layout-toggle-icon-button-to-the-d](./quick/260530-ncw-add-a-layout-toggle-icon-button-to-the-d/) |

## Session Continuity

Last session: 2026-05-28T21:22:00.741Z
Stopped at: Completed 17-07-backend-search-rescope-PLAN.md
Resume file: None
Next action: v1.1 milestone is functionally complete (15/16 phases done; Phase 15 deliberately parked). Recommended next steps: (a) /gsd:audit-milestone to formally close v1.1, OR (b) /gsd:execute-phase 15 to unpark Phase 15 (requires Identity Platform upgrade), OR (c) /gsd:check-todos to review the 5 pending follow-ups (incl. Task #10 Huawei sign-in todo, Task #12 -Puse_emulator=false doc, Task #15 foreground push polish, Task #16 sheet visual polish, retrofitting App Check on inviteToRegistry).
