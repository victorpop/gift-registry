---
phase: null (quick-task chain)
task: awaiting user verification of 260519-08n
total_tasks: 10 quick tasks completed in this chain
status: awaiting_user_verification
last_updated: 2026-05-20T13:34:15.921Z
---

<current_state>
Long verification chain for the k37 web product tiles + product detail page work. Bug surfaced during retest: after a guest reserves an item then clicks Release on the detail page, the sticky banner + ReserveDetailSection + tile RESERVED state still appeared on /registry/:id until manual refresh.

Final fix landed as quick-260519-08n: the `navigate-on-status-flip` useEffect on `ItemReservePage` (line ~268) was navigating without the j5j/ke1 navigation state, winning the race against the release-success effect (Firestore onSnapshot fires before useReleaseReservation's setStatus('success')). Now both effects pass identical `{ recentReleasedReservationId, recentReleasedItemId }` state — whichever wins, RegistryPage's hydration guards suppress correctly.

207/207 web tests + `tsc --noEmit` clean. Ready for user re-test.
</current_state>

<completed_work>
- 260513-k37: web product tiles + product detail page — **Verified**
- 260516-iux: anonymous-viewer hydration + stale-expired gate — **Verified**
- 260516-k4f: derived `active` (Reserve→detail transition) — **Verified**
- 260516-ku3: StickyReserveBanner sequential-release ref reset — **Verified**
- 260516-lbf: web mapper Android-schema adapter (shop/price/currency) — **Verified**
- 260516-lsi: clickable product blade — **Verified**
- 260516-oiy: optimistic items-cache patch on release — complete (pending re-verify after 08n)
- 260518-j5j: pass nav state from release-success — complete (pending re-verify after 08n)
- 260518-ke1: ignoreItemId hardening — complete (pending re-verify after 08n)
- **260519-08n** (latest): pass nav state in navigate-on-status-flip effect — **AWAITING USER VERIFICATION**
</completed_work>

<remaining_work>
- User hard-refresh Safari + re-test the full release flow. If clean → flip 08n STATE row to Verified.
- Optional cleanup: ~10 untracked PLAN.md files in `.planning/quick/` from past tasks that didn't get committed (likely a gsd-tools bug, pre-existing).
- Optional follow-up: persist priceAmount/priceCurrency/merchantDomain on Android side so web mapper doesn't need to parse Android's display-format price string for new items.
</remaining_work>

<decisions_made>
- Progressive hardening of release race: ignoreReservationId (j5j) → ignoreItemId (ke1) → extend status-flip effect to pass nav state (08n). Each iteration closed a discovered race path.
- Status-flip useEffect (added by quick-260513-hon for confirm-purchase + cross-tab/Cloud-Tasks release transitions) now passes the same navigation state as the release-success effect. Either racer suppresses correctly.
- All diagnostic console + localStorage logging was discarded after diagnosis (grep DIAG|diagLog|__DIAG__ returns zero in committed code).
- Auto-reserve flow (RegistryPage.tsx:91) and direct-mutation flow (ItemReservePage.tsx:54) both now feed the same release path with state-passing navigation.
</decisions_made>

<blockers>
- None. All blockers from earlier in the chain (race conditions, schema mismatches, stale-active reservations) have been addressed.
</blockers>

<context>
The user was patient through a long debug arc. Critical breakthrough: bug doesn't occur when Safari Web Inspector is open → classic timing race. localStorage-persisted diag logs revealed exact timeline: release click → status='pending' at T+0, RegistryPage render with fullState=null at T+132ms, release() promise resolves at T+134ms. The navigate at T+132ms was firing from the status-flip useEffect (Firestore onSnapshot beat the release callable). Without state, j5j/ke1 guards had nothing to match.

The 6-line fix: extend status-flip navigate to pass `{ state: { recentReleasedReservationId: active.reservationId, recentReleasedItemId: active.itemId } }`. Now identical to release-success's navigate.

Dev server (Vite) at http://localhost:5173 was running (task bu2vwhggx) during the session — may still be alive when resuming.
</context>

<next_action>
On resume:
1. Ask the user if they verified 260519-08n. If yes ("approved"), flip the STATE row's Status column from blank to "Verified" via Edit + commit via gsd-tools.
2. If user reports the bug still exists, ask for fresh logs (localStorage __DIAG__ approach would need re-adding) — but the source fix is well-tested by K-22 regression, so it should work.
3. If approved, the entire k37 verification chain (10 quick tasks) is closed. Suggest cleanup of untracked PLAN.md files or move to next user-requested work.
</next_action>
