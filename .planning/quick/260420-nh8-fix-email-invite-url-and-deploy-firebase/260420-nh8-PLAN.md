---
phase: quick-260420-nh8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/src/registry/inviteToRegistry.ts
  - functions/src/notifications/onPurchaseNotification.ts
  - functions/src/reservation/releaseReservation.ts
  - functions/src/config/publicUrls.ts
  - functions/src/__tests__/emailTemplates.test.ts
  - hosting/public/**
autonomous: false
requirements:
  - QUICK-NH8-01  # Replace placeholder giftregistry.app with real Firebase Hosting domain via config constant
  - QUICK-NH8-02  # Deploy Firebase Hosting so the invite URL resolves to the live React app
  - QUICK-NH8-03  # Deploy Cloud Functions so the URL fix takes effect in production invite/purchase/expiry emails

must_haves:
  truths:
    - "Cloud Functions no longer hardcode the placeholder domain giftregistry.app in any email link"
    - "The invite email registry link, purchase notification link, and expiry re-reserve link all use https://gift-registry-ro.web.app as their base URL"
    - "Visiting https://gift-registry-ro.web.app/ returns the deployed React web fallback (not a Firebase welcome page)"
    - "Visiting https://gift-registry-ro.web.app/registry/{someId} serves the SPA and the React router resolves RegistryPage"
    - "The base URL is sourced from a single config module so future migrations are a one-line change"
  artifacts:
    - path: "functions/src/config/publicUrls.ts"
      provides: "Single source of truth for the public web base URL and URL builders for registryUrl / reReserveUrl"
      exports: ["publicWebBaseUrl", "buildRegistryUrl", "buildReReserveUrl"]
    - path: "functions/src/registry/inviteToRegistry.ts"
      provides: "Invite email uses buildRegistryUrl(registryId) instead of hardcoded domain"
      contains: "buildRegistryUrl"
    - path: "functions/src/notifications/onPurchaseNotification.ts"
      provides: "Purchase notification email uses buildRegistryUrl(registryId)"
      contains: "buildRegistryUrl"
    - path: "functions/src/reservation/releaseReservation.ts"
      provides: "Expiry email uses buildReReserveUrl(reservationId)"
      contains: "buildReReserveUrl"
    - path: "hosting/public/index.html"
      provides: "Freshly-built React SPA shell from web/ build (not a stale Vite scaffold)"
      contains: "<div id=\"root\""
  key_links:
    - from: "functions/src/registry/inviteToRegistry.ts"
      to: "functions/src/config/publicUrls.ts"
      via: "import { buildRegistryUrl } from '../config/publicUrls'"
      pattern: "buildRegistryUrl\\("
    - from: "https://gift-registry-ro.web.app/registry/:id"
      to: "web/src/pages/RegistryPage.tsx"
      via: "React Router path '/registry/:id' in web/src/App.tsx (already wired)"
      pattern: "path: '/registry/:id'"
---

<objective>
Replace the placeholder `giftregistry.app` domain in Cloud Functions email links with the real Firebase Hosting default domain (`gift-registry-ro.web.app`), centralize the base URL in a config module, and ship a working web fallback by building the React app and deploying both hosting and functions.

Purpose: Today, every invite email, purchase-notification email, and expiry/re-reserve email sends recipients to a domain that is not owned. Even after fixing the domain, the hosting site currently serves a stale Vite scaffold page — there is no deployed React web fallback. Both problems must be fixed together, or the invite flow still appears broken to end users.

Output: Cloud Functions source + deployed Functions that use `gift-registry-ro.web.app`, plus a freshly-built and deployed React SPA at that domain so the email links actually resolve to the web fallback.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@firebase.json
@.firebaserc
@functions/src/registry/inviteToRegistry.ts
@functions/src/notifications/onPurchaseNotification.ts
@functions/src/reservation/releaseReservation.ts
@web/vite.config.ts
@web/src/App.tsx

<interfaces>
<!-- Key facts the executor needs. Extracted from the codebase — no exploration required. -->

Firebase project ID (from .firebaserc):
  default = "gift-registry-ro"
  => Default Firebase Hosting domain = https://gift-registry-ro.web.app
  (Alt domain https://gift-registry-ro.firebaseapp.com resolves to the same site;
   prefer the .web.app form — it is the canonical default shown in the Firebase console.)

Current hardcoded URLs (all must be replaced):
  functions/src/registry/inviteToRegistry.ts:93
    const registryUrl = `https://giftregistry.app/registry/${registryId}`;
  functions/src/notifications/onPurchaseNotification.ts:114
    const registryUrl = `https://giftregistry.app/registry/${registryId}`;
  functions/src/reservation/releaseReservation.ts:86
    const reReserveUrl = `https://giftregistry.app/reservation/${rid}/re-reserve`;

Web routes (from web/src/App.tsx — already match the URL path format):
  '/'                              -> AppRootPage
  '/registry/:id'                  -> RegistryPage         (matches /registry/{registryId})
  '/reservation/:id/re-reserve'    -> ReReservePage        (matches /reservation/{rid}/re-reserve)
  '*'                              -> NotFoundPage

Vite build output (web/vite.config.ts):
  outDir = ../hosting/public
  emptyOutDir = true
  => `npm --prefix web run build` writes straight into hosting/public (which is
     what firebase.json hosting.public points at). No manual copy step needed.

Firebase config pattern: No prior `defineString`/`params` usage in functions/src.
  We are introducing the first one in this plan; use `firebase-functions/params`
  (2nd gen standard). The fallback `default` means the code works in unit tests
  and local emulator without needing env setup.

Test fixtures that reference the old domain (cosmetic only — tests don't assert
the hostname, but updating them keeps fixtures self-consistent):
  functions/src/__tests__/emailTemplates.test.ts:14, 64, 106
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralize the public web base URL and replace all hardcoded giftregistry.app references</name>
  <files>functions/src/config/publicUrls.ts, functions/src/registry/inviteToRegistry.ts, functions/src/notifications/onPurchaseNotification.ts, functions/src/reservation/releaseReservation.ts, functions/src/__tests__/emailTemplates.test.ts, functions/src/__tests__/publicUrls.test.ts</files>
  <behavior>
    - `publicWebBaseUrl()` returns `https://gift-registry-ro.web.app` by default (in unit tests and local emulator with no env override set).
    - `publicWebBaseUrl()` returns the env-provided value when `PUBLIC_WEB_BASE_URL` is configured via Firebase params.
    - `buildRegistryUrl('abc123')` returns `https://gift-registry-ro.web.app/registry/abc123` (no trailing slash on base, single slash join).
    - `buildReReserveUrl('res1')` returns `https://gift-registry-ro.web.app/reservation/res1/re-reserve`.
    - The builders strip a trailing slash from the base if one is accidentally configured (defense so a mis-set env like `https://example.com/` doesn't produce `//registry/abc`).
    - `grep -R "giftregistry.app" functions/src` returns zero matches in production source (`functions/src/**`, excluding tests is OK — see edge cases).
    - Existing inviteToRegistry unit tests still pass unchanged (they don't assert the URL string).
    - emailTemplates.test.ts fixture URLs updated to the new domain for self-consistency; template outputs still contain the fixture URL unchanged, so assertions that check `html.includes(registryUrl)` still pass.
    Edge cases:
    - Test files under `functions/src/__tests__/` may also contain the old string in fixture inputs — update those fixture strings to the new domain so future greps are clean.
    Non-goals:
    - Do NOT change the email templates themselves (`email/templates/invite.ts`, `expiry.ts`, `purchase.ts`) — they already accept the URL as a parameter.
    - Do NOT add a custom domain to Firebase Hosting; default `.web.app` domain is sufficient for QUICK-NH8.
  </behavior>
  <action>
    1. Create `functions/src/config/publicUrls.ts`:

       ```typescript
       import { defineString } from "firebase-functions/params";

       // Single source of truth for the public web base URL (the Firebase Hosting
       // site for the React web fallback). Override at deploy time by setting
       // PUBLIC_WEB_BASE_URL in the Functions environment; defaults to the
       // Firebase-assigned hosting domain for the `gift-registry-ro` project.
       const PUBLIC_WEB_BASE_URL = defineString("PUBLIC_WEB_BASE_URL", {
         default: "https://gift-registry-ro.web.app",
       });

       /** Returns the public web base URL with any trailing slash stripped. */
       export function publicWebBaseUrl(): string {
         return PUBLIC_WEB_BASE_URL.value().replace(/\/$/, "");
       }

       /** Builds the public URL for a registry page, e.g. `${base}/registry/{id}`. */
       export function buildRegistryUrl(registryId: string): string {
         return `${publicWebBaseUrl()}/registry/${registryId}`;
       }

       /** Builds the public URL for a re-reserve page, e.g. `${base}/reservation/{id}/re-reserve`. */
       export function buildReReserveUrl(reservationId: string): string {
         return `${publicWebBaseUrl()}/reservation/${reservationId}/re-reserve`;
       }
       ```

    2. In `functions/src/registry/inviteToRegistry.ts`:
       - Add `import { buildRegistryUrl } from "../config/publicUrls";` near the top.
       - Replace line 93: `const registryUrl = buildRegistryUrl(registryId);`

    3. In `functions/src/notifications/onPurchaseNotification.ts`:
       - Add `import { buildRegistryUrl } from "../config/publicUrls";`.
       - Replace line 114 to use `buildRegistryUrl(registryId)`.

    4. In `functions/src/reservation/releaseReservation.ts`:
       - Add `import { buildReReserveUrl } from "../config/publicUrls";`.
       - Replace line 86 to use `buildReReserveUrl(rid)`.

    5. In `functions/src/__tests__/emailTemplates.test.ts` fixtures (lines ~14, ~64, ~106):
       - Replace `https://giftregistry.app` → `https://gift-registry-ro.web.app` in the three fixture URL constants. Assertions unchanged — they compare the fixture string to itself via `html.includes(fixtureUrl)`.

    6. Add `functions/src/__tests__/publicUrls.test.ts` verifying:
       - Default value ends with `gift-registry-ro.web.app` (no trailing slash).
       - `buildRegistryUrl('abc')` == `https://gift-registry-ro.web.app/registry/abc`.
       - `buildReReserveUrl('r1')` == `https://gift-registry-ro.web.app/reservation/r1/re-reserve`.

       Note on `defineString` in tests: `firebase-functions/params` returns the
       `default` value when `.value()` is called outside a deployed Functions
       context, which is exactly what we want in unit tests. No test env setup
       required.

    7. Sanity check: run `grep -R "giftregistry.app" functions/src` — must return zero hits.

    Addresses: QUICK-NH8-01 (URL fix via config constant).
  </action>
  <verify>
    <automated>cd functions &amp;&amp; npm run build &amp;&amp; npm test -- --run 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - functions/src/config/publicUrls.ts exists with three exports.
    - All three production files import from it; no `giftregistry.app` remains in `functions/src/**`.
    - `functions/` TypeScript build is clean.
    - All existing Functions unit tests still pass, plus the new publicUrls test.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the React web app and stage it into hosting/public</name>
  <files>hosting/public/**, .gitignore</files>
  <action>
    1. From repo root, run `cd web &amp;&amp; npm install` if node_modules is missing (it appears present — skip if so).

    2. Run the production build:

       ```
       cd web &amp;&amp; npm run build
       ```

       This executes `tsc -b &amp;&amp; vite build`. Per web/vite.config.ts, Vite writes
       output to `../hosting/public` and empties the directory first.

    3. Smoke-check the output:

       ```
       ls hosting/public
       head -20 hosting/public/index.html
       ```

       Expected: `index.html`, `assets/` directory with hashed JS/CSS bundles,
       and `index.html` contains a `<div id="root"></div>` plus `<script type="module" src="/assets/…js">`.

       DO NOT deploy yet — this task only prepares the artifact. Deployment
       happens in Task 4 after checkpoint approval. The executor should commit
       the Functions source changes from Task 1 before the deploy step, but
       MUST NOT commit `hosting/public/` build artifacts (see .gitignore — if
       it's not ignored yet, add `hosting/public/` to .gitignore in this task
       as part of the commit, since build output should not be tracked).

    4. `.gitignore` check: If `hosting/public/` is currently tracked or partly
       tracked, add `hosting/public/` to `.gitignore` (keeping
       `hosting/public/.well-known/assetlinks.json` tracked via a negated
       pattern, since that file is a source artifact referenced in firebase.json
       headers):

       ```
       hosting/public/*
       !hosting/public/.well-known/
       hosting/public/.well-known/*
       !hosting/public/.well-known/assetlinks.json
       ```

       After `vite build`, `emptyOutDir: true` will have wiped
       `hosting/public/.well-known/` — copy it back from `web/public/.well-known/`
       if present there, OR if `web/public/.well-known/assetlinks.json` is Vite's
       source-of-truth (per Phase 05 decision in STATE.md: "assetlinks.json
       migrated to web/public/.well-known/"), then the build itself will emit it
       to `hosting/public/.well-known/assetlinks.json` and nothing extra is
       needed. Verify:

       ```
       cat hosting/public/.well-known/assetlinks.json
       ```

       If missing, that means Vite didn't copy it — investigate before Task 4
       (do not deploy a site that would break Android App Links).

    Addresses: QUICK-NH8-02 (hosting build step).
  </action>
  <verify>
    <automated>ls hosting/public/index.html hosting/public/.well-known/assetlinks.json &amp;&amp; grep -q 'id="root"' hosting/public/index.html &amp;&amp; ls hosting/public/assets/*.js | head -1</automated>
  </verify>
  <done>
    - `hosting/public/index.html` contains a root div and a hashed `/assets/*.js` script tag (not a Vite scaffold welcome page).
    - `hosting/public/assets/` contains hashed JS and CSS bundles.
    - `hosting/public/.well-known/assetlinks.json` is present.
    - `.gitignore` correctly excludes `hosting/public/` build output but preserves `assetlinks.json`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pre-deploy checkpoint — confirm remote deploy</name>
  <files>(no files — human verification gate before live deploy)</files>
  <action>Pause execution and surface the deploy scope to the user. Do NOT run `firebase deploy` until the user resumes with "deploy". This gate exists because Task 4 modifies remote state (production Firebase Hosting + three Cloud Functions in project `gift-registry-ro`). No automated work happens in this task.</action>
  <what-built>
    - Cloud Functions source updated: URL now sourced from `publicUrls.ts` (default `https://gift-registry-ro.web.app`).
    - React web app built locally into `hosting/public/`.
    - No remote state changed yet.
  </what-built>
  <how-to-verify>
    The next task performs a **live deploy to Firebase** (modifies remote state for both Hosting and Functions in the `gift-registry-ro` production project). Before proceeding, confirm:

    1. You are signed in as the intended Firebase account: run `firebase login:list` — the active account should own the `gift-registry-ro` project.
    2. You accept that this deploy will:
       - Publish the React SPA to `https://gift-registry-ro.web.app` (visible to anyone with the link).
       - Replace the currently-deployed `inviteToRegistry`, `onPurchaseNotification`, and `releaseReservation` Cloud Functions (brief cold-start latency may affect any in-flight invocations — very low volume for this project).
    3. Optional local smoke (skip if Functions emulator isn't already running): `firebase emulators:start --only hosting` then visit `http://localhost:5002/` — should serve the built React app. This only exercises the hosting build; the URL fix is covered by Task 1 unit tests.
  </how-to-verify>
  <verify>Human approval only — no automated check. Proceed to Task 4 only after user types "deploy".</verify>
  <done>User has approved the deploy scope and typed "deploy" (or an equivalent resume signal).</done>
  <resume-signal>Type "deploy" to proceed with `firebase deploy`, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Deploy Hosting + Functions and verify the live URL</name>
  <files>(remote — no local file changes)</files>
  <action>
    1. Deploy both hosting and the three updated functions. Scope functions to only the ones changed to minimize blast radius:

       ```
       firebase deploy --only hosting,functions:inviteToRegistry,functions:onPurchaseNotification,functions:releaseReservation --project gift-registry-ro
       ```

       If the CLI refuses partial Functions deploy (e.g. due to shared build), fall back to:

       ```
       firebase deploy --only hosting,functions --project gift-registry-ro
       ```

       Capture the deploy output — in particular the "Hosting URL" line.

    2. Live verification of the web app:

       ```
       curl -sI https://gift-registry-ro.web.app/ | head -5
       curl -s https://gift-registry-ro.web.app/ | grep -c 'id="root"'
       curl -sI https://gift-registry-ro.web.app/registry/nonexistent-id | head -5
       ```

       Expected:
       - `/` returns `HTTP/2 200` and the body contains the root div (grep count > 0).
       - `/registry/anything` also returns `HTTP/2 200` (SPA rewrite in firebase.json sends all paths to `/index.html`).
       - `Content-Type: text/html` in both cases.

    3. Live verification of Cloud Functions deployment:
       - List deployed functions and confirm the three updated ones show a recent "Updated" timestamp:

         ```
         firebase functions:list --project gift-registry-ro | grep -E 'inviteToRegistry|onPurchaseNotification|releaseReservation'
         ```

       - (Optional but recommended) Trigger a real invite from the Android app or the emulator to a test email address you control, and confirm the email body contains `https://gift-registry-ro.web.app/registry/…` — not `giftregistry.app`. If this requires coordinated manual testing, log it in the SUMMARY as a follow-up instead of blocking the plan.

    4. Record in the SUMMARY:
       - The deployed hosting URL exactly as printed by the CLI.
       - The three function names + their deployed region (europe-west3).
       - Any non-fatal deploy warnings.

    Addresses: QUICK-NH8-02 (hosting deploy) and QUICK-NH8-03 (functions deploy).
  </action>
  <verify>
    <automated>curl -sI https://gift-registry-ro.web.app/ | head -1 | grep -q '200' &amp;&amp; curl -s https://gift-registry-ro.web.app/ | grep -q 'id="root"'</automated>
  </verify>
  <done>
    - `firebase deploy` succeeded for hosting and the three functions (CLI exit 0, no red error lines).
    - `https://gift-registry-ro.web.app/` serves the React SPA shell (HTTP 200 + root div).
    - `https://gift-registry-ro.web.app/registry/{anyId}` returns HTTP 200 (SPA rewrite working).
    - The three Cloud Functions show a fresh deploy timestamp in `functions:list`.
    - SUMMARY records the deployed URL, the deployed function names, and any warnings.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, these observable truths must hold:

1. `grep -R "giftregistry.app" functions/src` returns zero matches (excluding node_modules).
2. All existing Functions tests pass + the new `publicUrls.test.ts` passes.
3. `curl -s https://gift-registry-ro.web.app/` returns HTML containing `id="root"`.
4. `curl -sI https://gift-registry-ro.web.app/registry/anything` returns HTTP 200.
5. `firebase functions:list` shows `inviteToRegistry`, `onPurchaseNotification`, `releaseReservation` all updated within the current session.
</verification>

<success_criteria>
- Registry invite emails, purchase notification emails, and expiry re-reserve emails all contain `https://gift-registry-ro.web.app/...` URLs.
- A recipient clicking the invite link lands on the live React web app at `https://gift-registry-ro.web.app/registry/{id}` (not a 404, not a Firebase welcome page, not a dead domain).
- The public base URL is owned by a single config module — a future migration to a custom domain is a one-line change + redeploy.
- Remote Firebase Hosting and the three updated Cloud Functions are deployed successfully, and the deploy URL is recorded in the SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/quick/260420-nh8-fix-email-invite-url-and-deploy-firebase/260420-nh8-SUMMARY.md` with:
- The deployed Hosting URL (as reported by the CLI).
- The three Cloud Function names deployed and their region.
- Confirmation that the grep for `giftregistry.app` in `functions/src` returns zero hits.
- Any warnings from `firebase deploy` worth noting for future quick tasks.
</output>
