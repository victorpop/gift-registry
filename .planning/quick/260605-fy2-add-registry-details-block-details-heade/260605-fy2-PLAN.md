---
phase: quick-260605-fy2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/giftregistry/ui/registry/detail/RegistryDetailsSection.kt
  - app/src/main/java/com/giftregistry/ui/registry/detail/RegistryDetailScreen.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/res/values-ro/strings.xml
autonomous: true
requirements: [FY2-01]
must_haves:
  truths:
    - "When eventDateMs, description, AND eventLocation are all empty, no Details block (and no DETAILS header) renders anywhere on the screen."
    - "When eventDateMs is set, a pill shows the date ONLY (e.g. 'May 16') with a leading calendar icon — never a time."
    - "When description is non-blank, a description paragraph renders under the DETAILS header."
    - "When eventLocation is non-blank, a rounded card with a pin icon + the location string (as-is, wrapping if long) renders."
    - "The Details block appears between the hero header and the stats strip for BOTH owners and guests."
  artifacts:
    - path: "app/src/main/java/com/giftregistry/ui/registry/detail/RegistryDetailsSection.kt"
      provides: "RegistryDetailsSection composable + private formatEventDate helper"
      contains: "fun RegistryDetailsSection"
    - path: "app/src/main/res/values/strings.xml"
      provides: "English DETAILS header string"
      contains: "registry_details_section_title"
    - path: "app/src/main/res/values-ro/strings.xml"
      provides: "Romanian DETAILS header string"
      contains: "registry_details_section_title"
  key_links:
    - from: "RegistryDetailScreen.kt LazyColumn"
      to: "RegistryDetailsSection"
      via: "new item(key = \"details\") between item(key = \"hero\") and item(key = \"stats\")"
      pattern: "item\\(key = \"details\"\\)"
---

<objective>
Add a "Details" block to the registry detail screen, inserted between the hero header and the stats strip. The block shows a DETAILS section header plus up to three sub-elements: a date-only pill, a description paragraph, and a location card. Each sub-element renders only when its underlying `Registry` field is populated; if all three are empty the entire block (header included) renders nothing.

Purpose: Surface registry event context (when / where / what) directly under the hero so both owners and gift-givers see it without opening edit.
Output: One new composable file, one new localized string key (en + ro), and a single LazyColumn item insertion. Pure presentation — no ViewModel / model / state changes.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Screen to modify (insert ONE LazyColumn item between "hero" and "stats")
@app/src/main/java/com/giftregistry/ui/registry/detail/RegistryDetailScreen.kt

# Style precedent: section labels (monoCaps + inkFaint), edge padding, gap tokens, LazyColumn item composable shape
@app/src/main/java/com/giftregistry/ui/registry/detail/StatsStrip.kt

<interfaces>
<!-- Executor should use these directly — no codebase exploration needed. -->

Registry model (app/src/main/java/com/giftregistry/domain/model/Registry.kt) — fields relevant here:
```kotlin
val eventDateMs: Long? = null    // encodes date+time; we use DATE ONLY
val eventLocation: String? = null // single nullable string, render as-is
val description: String? = null
```

Already collected in RegistryDetailScreen (~line 77), pass straight through:
```kotlin
val registry by viewModel.registry.collectAsStateWithLifecycle()   // Registry?
```
Do NOT touch the ViewModel — `registry` already carries description / eventLocation / eventDateMs.

Design tokens (read via GiftMaisonTheme.colors / .typography / .spacing / .shapes):
- colors: paper, ink, inkSoft, inkFaint, line, accent, accentInk, accentSoft, paperDeep
- typography: displayS (section title fallback), bodyM (body/desc), bodyXS, bodyMEmphasis, monoCaps (small-caps labels — used for the DETAILS header to mirror StatsStrip stat labels)
- spacing: edge (16dp horizontal), gap8, gap12, gap16, gap4
- shapes: pill (date pill), radius12 / radius14 (location card)

Existing string keys to REUSE (both values/ and values-ro/ exist) — do NOT recreate:
- registry_description_label, registry_event_location_label, registry_event_date_label
(These are labels; you may use them as accessibility contentDescription / icon labels. The visible DETAILS section header needs a NEW key — see Task 1.)

Date formatter precedent (RegistryCard.kt ~lines 41-47 uses SimpleDateFormat("MMM d, yyyy", Locale.getDefault())):
```kotlin
// Date-ONLY pill: drop the year to match the mockup ("May 16")
SimpleDateFormat("MMM d", Locale.getDefault()).format(Date(eventDateMs))
```
Add this as a private helper in the new file — do NOT expose RegistryCard's private one.

StatsStrip stat-label style to mirror for the DETAILS header:
```kotlin
Text(text = label, style = typography.monoCaps, color = colors.inkFaint)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the DETAILS section-header string (en + ro)</name>
  <files>app/src/main/res/values/strings.xml, app/src/main/res/values-ro/strings.xml</files>
  <action>
Add ONE new string key in BOTH locale files, placed next to the existing registry detail label keys (near `registry_event_location_label`, around line 80).

In `app/src/main/res/values/strings.xml`:
```xml
<string name="registry_details_section_title">Details</string>
```

In `app/src/main/res/values-ro/strings.xml`:
```xml
<string name="registry_details_section_title">Detalii</string>
```

Store the value as natural Title-case ("Details" / "Detalii"), NOT hardcoded uppercase. The mockup's all-caps look is achieved at render time via the `monoCaps` typography style (which already renders small-caps), matching how StatsStrip stat labels are stored Title-case but rendered as small caps. Do NOT add `textAllCaps` to the string value and do NOT reuse `registry_event_date_label` / `registry_event_location_label` / `registry_description_label` for the header — those are field labels, not the section title.

Reuse the existing three field-label keys ONLY if you need an icon contentDescription (accessibility) inside the new composable.
  </action>
  <verify>
    <automated>grep -q 'registry_details_section_title' app/src/main/res/values/strings.xml && grep -q 'registry_details_section_title' app/src/main/res/values-ro/strings.xml && echo OK</automated>
  </verify>
  <done>Key `registry_details_section_title` exists in both values/strings.xml ("Details") and values-ro/strings.xml ("Detalii"). No other strings changed.</done>
</task>

<task type="auto">
  <name>Task 2: Create RegistryDetailsSection composable and insert it between hero and stats</name>
  <files>app/src/main/java/com/giftregistry/ui/registry/detail/RegistryDetailsSection.kt, app/src/main/java/com/giftregistry/ui/registry/detail/RegistryDetailScreen.kt</files>
  <action>
**A. Create `RegistryDetailsSection.kt`** in package `com.giftregistry.ui.registry.detail` (same package/directory as StatsStrip.kt — follow that file's organization).

Signature:
```kotlin
@Composable
internal fun RegistryDetailsSection(
    description: String?,
    eventLocation: String?,
    eventDateMs: Long?,
    modifier: Modifier = Modifier,
)
```

Behavior (LOCKED decisions — implement exactly):
1. Compute three booleans at the top:
   - `val hasDate = eventDateMs != null`
   - `val hasDescription = !description.isNullOrBlank()`
   - `val hasLocation = !eventLocation.isNullOrBlank()`
2. **Empty-handling gate:** if `!hasDate && !hasDescription && !hasLocation`, `return` immediately — render NOTHING (no Column, no header). This is what makes the entire block (header included) disappear when all three fields are empty.
3. Otherwise render a `Column(Modifier.fillMaxWidth().padding(horizontal = spacing.edge, vertical = spacing.gap12))` with `verticalArrangement = Arrangement.spacedBy(spacing.gap12)` containing, in order:
   - **DETAILS header:** `Text(stringResource(R.string.registry_details_section_title), style = typography.monoCaps, color = colors.inkFaint)` — mirrors the StatsStrip stat-label style so it reads as the small-caps "DETAILS" header.
   - **Date pill** (only `if (hasDate)`): a `Row` clipped to `shapes.pill`, `background(colors.accentSoft)`, padded (`horizontal = spacing.gap12, vertical = spacing.gap8`), `verticalAlignment = Alignment.CenterVertically`. Inside: a small leading calendar/event icon (`Icons.Outlined.CalendarToday` or `Icons.Outlined.Event`) tinted `colors.accent` with `contentDescription = stringResource(R.string.registry_event_date_label)`, a `Spacer(Modifier.width(spacing.gap8))`, then `Text(formatEventDate(eventDateMs!!), style = typography.bodyXS or bodyMEmphasis, color = colors.accent)`. DATE ONLY — never render the time.
   - **Description paragraph** (only `if (hasDescription)`): `Text(description!!, style = typography.bodyM, color = colors.inkSoft)`. Wraps naturally; no maxLines clamp.
   - **Location card** (only `if (hasLocation)`): a `Row` clipped to `shapes.radius12`, `background(colors.paperDeep)` (soft surface), optional `border(1.dp, colors.line, shapes.radius12)`, padded (`spacing.gap12`), `verticalAlignment = Alignment.Top` (so the pin stays top-aligned when the string wraps to multiple lines). Inside: a pin icon (`Icons.Outlined.Place` or `Icons.Outlined.LocationOn`) tinted `colors.inkSoft` with `contentDescription = stringResource(R.string.registry_event_location_label)`, a `Spacer(Modifier.width(spacing.gap8))`, then `Text(eventLocation!!, style = typography.bodyM, color = colors.ink)` rendered AS-IS (do NOT split on comma; do NOT fabricate a two-line city/address layout; let it wrap naturally).

Add a private helper in the SAME file:
```kotlin
private fun formatEventDate(eventDateMs: Long): String =
    SimpleDateFormat("MMM d", Locale.getDefault()).format(Date(eventDateMs))
```
(Pattern `"MMM d"` — no year — to match the mockup "May 16". Localized via `Locale.getDefault()`.)

Read tokens via `val colors = GiftMaisonTheme.colors`, `val typography = GiftMaisonTheme.typography`, `val spacing = GiftMaisonTheme.spacing`, `val shapes = GiftMaisonTheme.shapes` at the top of the composable (StatsStrip pattern). Use only Material3 / Compose Foundation imports already used elsewhere in this package. Use Material icons from `androidx.compose.material.icons.outlined.*` (verify the chosen icons exist; if `CalendarToday` / `Event` / `Place` / `LocationOn` is unavailable in the bundled icon set, fall back to a known-good outlined icon — but prefer calendar for date and pin for location).

**B. Insert the LazyColumn item** in `RegistryDetailScreen.kt`, BETWEEN `item(key = "hero") { ... }` (ends ~line 214) and `item(key = "stats") { StatsStrip(items = items) }` (~line 216). Add exactly:
```kotlin
item(key = "details") {
    RegistryDetailsSection(
        description = registry?.description,
        eventLocation = registry?.eventLocation,
        eventDateMs = registry?.eventDateMs,
    )
}
```
Notes:
- `registry` is the nullable `Registry?` already collected at ~line 77 — use safe-call `registry?.field`. When `registry` is null all three args are null → the section's empty gate returns nothing (correct: no block before data loads).
- Do NOT gate on `isOwner` — this block shows to everyone (LOCKED decision 5). Place it OUTSIDE any `if (isOwner)` block, as an unconditional `item(...)` exactly like "hero" and "stats".
- Do NOT modify, reorder, or wrap any other item (hero, stats, share, filter, add-item-cta, confirm-purchase-banner, item list, bottom-nav-padding).
- No new import is needed for `RegistryDetailsSection` (same package).
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin -q 2>&1 | tail -20</automated>
  </verify>
  <done>
- `RegistryDetailsSection.kt` exists with the composable + private `formatEventDate` helper.
- `RegistryDetailScreen.kt` has a new `item(key = "details")` positioned between "hero" and "stats", calling `RegistryDetailsSection` with `registry?.description / registry?.eventLocation / registry?.eventDateMs`, NOT gated on `isOwner`.
- `./gradlew :app:compileDebugKotlin` succeeds (BUILD SUCCESSFUL).
- Reasoning check for empty-handling (cannot run UI here): trace the four cases by reading the code — (a) all three null/blank → early `return`, nothing renders; (b) only date → header + pill; (c) only description → header + paragraph; (d) only location → header + card. Confirm each `if (hasX)` guards exactly its sub-element and the early `return` precedes the Column.
  </done>
</task>

</tasks>

<verification>
- `./gradlew :app:compileDebugKotlin` succeeds.
- `registry_details_section_title` present in BOTH `values/strings.xml` and `values-ro/strings.xml`.
- New `item(key = "details")` sits between "hero" and "stats" in the LazyColumn; no other item touched.
- ViewModel, UI state, `Registry` model, hero, stats, tabs, and item list are unchanged (git diff limited to the 4 files in `files_modified`).
- Empty-handling reasoning: with all three fields null/blank, `RegistryDetailsSection` returns before composing any UI (header included), so nothing renders. Each populated field independently surfaces its sub-element.
</verification>

<success_criteria>
- Details block renders between hero and stats for owners AND guests.
- Date pill shows date-only (`MMM d`) with a leading calendar icon, only when `eventDateMs != null`.
- Description paragraph renders only when `description` is non-blank, wrapping naturally.
- Location card (pin icon + single `eventLocation` string as-is) renders only when `eventLocation` is non-blank.
- Whole block (DETAILS header included) is absent when all three fields are empty.
- Exactly one new localized string key added (en + ro); all other strings, the ViewModel, and the domain model untouched.
- Project compiles.
</success_criteria>

<output>
After completion, create `.planning/quick/260605-fy2-add-registry-details-block-details-heade/260605-fy2-SUMMARY.md`
</output>
