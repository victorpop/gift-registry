---
phase: quick-260605-h1d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestore.rules
  - tests/rules/firestore.rules.test.ts
autonomous: true
requirements: [QUICK-260605-h1d]
must_haves:
  truths:
    - "A signed-in PENDING invitee (private registry, pendingInvitedUsers[uid]==true) CAN read the registry DOC"
    - "A PENDING-only invitee still CANNOT read the items subcollection"
    - "A true outsider (in neither invitedUsers nor pendingInvitedUsers) is still DENIED the registry doc"
    - "Owner and post-accept (invitedUsers) invitee still CAN read"
  artifacts:
    - path: firestore.rules
      provides: "New isPendingInvited() helper + OR clause on registry-doc read (line 50 only)"
      contains: "function isPendingInvited"
    - path: tests/rules/firestore.rules.test.ts
      provides: "Updated D-18 describe block encoding the new pending-read-doc / no-items policy"
      contains: "pendingInvitedUsers read scope"
  key_links:
    - from: "firestore.rules registries/{registryId} read rule"
      to: "isPendingInvited(resource.data)"
      via: "OR clause on line 50"
      pattern: "canReadRegistry\\(resource.data\\) \\|\\| isPendingInvited\\(resource.data\\)"
    - from: "firestore.rules registries/{registryId}/items/{itemId} read rule"
      to: "canReadRegistry only (UNCHANGED)"
      via: "no isPendingInvited reference under items"
      pattern: "items.*canReadRegistry"
---

<objective>
Loosen Firestore security rules so a PENDING registry invitee can read the registry
DOCUMENT (enabling the invite-response sheet's live fetch of description/location/date)
WITHOUT exposing the items subcollection. Update the rules unit tests to encode the new
intended behavior.

Purpose: After quick-260605-ge6, the invite-response bottom sheet fetches the live
Registry client-side, but `isInvited()` only checks `invitedUsers` — a pending invite
lives in the SEPARATE `pendingInvitedUsers` map, so the read is denied and nothing shows.
The user chose to LOOSEN the rules (over a payload/Cloud-Functions change).

Output: `firestore.rules` with a new `isPendingInvited()` helper ORed onto the
registry-doc read rule only; updated `tests/rules/firestore.rules.test.ts` D-18 block.
No Android, Cloud Functions, or other rule-block changes.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@firestore.rules
@tests/rules/firestore.rules.test.ts

<interfaces>
<!-- Current rule logic the executor must extend (NOT rewrite). -->

firestore.rules — existing helpers (lines 30-46):
```
function isInvited(registryData) {
  return isSignedIn() &&
         registryData.get('visibility', 'public') == 'private' &&
         registryData.get('invitedUsers', {})[request.auth.uid] == true;
}

function canReadRegistry(registryData) {
  return isOwner(registryData) ||
         isPublicRegistry(registryData) ||
         isInvited(registryData);
}
```

firestore.rules — the two reads that share canReadRegistry():
```
// line 50 — registry DOC read (CHANGE THIS ONE)
allow read: if canReadRegistry(resource.data);

// lines 56-58 — items read (LEAVE UNCHANGED)
match /items/{itemId} {
  allow read: if canReadRegistry(
    get(/databases/$(database)/documents/registries/$(registryId)).data
  );
  ...
}
```

Test seeding helpers (tests/rules/firestore.rules.test.ts):
```
seedRegistry(id, data)  // withSecurityRulesDisabled setDoc on registries/{id}
// items seeded inline via testEnv.withSecurityRulesDisabled (see lines 265-277)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isPendingInvited() helper and OR it onto the registry-DOC read rule only</name>
  <files>firestore.rules</files>
  <action>
Add a NEW helper function `isPendingInvited(registryData)` adjacent to the existing
`isInvited()` helper (after line 36, before `canReadRegistry`). Mirror the defensive
`.get(key, default)` style and the WHY-comment convention already in the file:

```
function isPendingInvited(registryData) {
  // Quick 260605-h1d: A pending invite stores the uid in `pendingInvitedUsers`
  // (separate from `invitedUsers`) until the acceptInvite Cloud Function promotes
  // it. The invite-response sheet fetches the live registry doc to show
  // description/location/date, so pending invitees need DOC read access.
  // Scoped to the registry DOC read ONLY (see line 50) — the items subcollection
  // read still uses canReadRegistry(), so pending invitees CANNOT read items.
  return isSignedIn() &&
         registryData.get('visibility', 'public') == 'private' &&
         registryData.get('pendingInvitedUsers', {})[request.auth.uid] == true;
}
```

Then change the registry-DOC read rule (currently line 50) from:
  `allow read: if canReadRegistry(resource.data);`
to:
  `allow read: if canReadRegistry(resource.data) || isPendingInvited(resource.data);`

CRITICAL — do NOT touch:
- `canReadRegistry()` itself (lines 38-46) — must stay byte-identical.
- The items read rule (lines 56-58) — must stay byte-identical so pending invitees
  remain unable to read items.
- Any other rule block.

Self-review / code-trace after editing (record in SUMMARY):
- Pending invitee (private, pendingInvitedUsers[uid]==true): DOC read → isPendingInvited
  true → SUCCEEDS. Items read → canReadRegistry only (isOwner=false, isPublic=false,
  isInvited=false because uid not in invitedUsers) → FAILS. ✓
- True outsider (in neither map): DOC read → both false → FAILS. ✓
- Owner: canReadRegistry → isOwner true → SUCCEEDS (unchanged). ✓
- Post-accept invitee (invitedUsers[uid]==true): canReadRegistry → isInvited true →
  SUCCEEDS (unchanged). ✓
  </action>
  <verify>
    <automated>grep -q "function isPendingInvited" firestore.rules && grep -q "canReadRegistry(resource.data) || isPendingInvited(resource.data)" firestore.rules && grep -q "allow read: if canReadRegistry(" firestore.rules</automated>
  </verify>
  <done>
`isPendingInvited()` helper exists; line-50 registry-doc read ORs it on; `canReadRegistry()`
and the items read rule (line 56) are byte-identical to before. Code-trace of all four
cases documented in SUMMARY. NOTE: emulator rule-test + production deploy are run
post-merge by the orchestrator — executor does NOT run them.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update the D-18 rules unit tests to encode the new pending-read-doc / no-items policy</name>
  <files>tests/rules/firestore.rules.test.ts</files>
  <action>
Edit the `describe("pendingInvitedUsers read scope (D-18)")` block (~lines 485-540).

(a) Rewrite the header comment block (~485-494). Remove the "no rule edit needed"
/ "do NOT grant read" wording. State the NEW policy: per quick-260605-h1d, a pending
invitee (private registry, `pendingInvitedUsers[uid]==true`) CAN read the registry DOC
(so the invite-response sheet can fetch description/location/date) but CANNOT read items.
Keep the D-19 note that post-accept (invitedUsers) invitees still read.

(b) Test "non-owner cannot read a registry doc with pendingInvitedUsers populated"
(~497-506): REFRAME into a true-outsider denial. Keep
`pendingInvitedUsers: { "stranger-uid": true }` in the seed, but authenticate as a
DIFFERENT uid that is in NEITHER map (e.g. "outsider-uid"), keep `assertFails`. Rename
to something like "true outsider (in neither map) cannot read registry doc".

(c) Test "invitee with ONLY pending entry (no invitedUsers entry) cannot read registry"
(~508-517): FLIP `assertFails` → `assertSucceeds` and rename to reflect that a
pending-only invitee CAN now read the registry DOC (e.g. "pending-only invitee CAN read
the registry doc"). Keep the same seed (private, invitedUsers: {},
pendingInvitedUsers: { "invitee-uid": true }) and auth as "invitee-uid".

(d) Keep "owner CAN read registry with pendingInvitedUsers populated" (~519-528) and
"D-19: invitee promoted to invitedUsers (post-accept) CAN read" (~530-539) as-is
(both `assertSucceeds`).

(e) ADD a NEW test that locks the items scoping: a pending-only invitee CANNOT read an
ITEM. Seed a private registry with `pendingInvitedUsers: { "pending-uid": true }` and
`invitedUsers: {}`, then seed an item doc via `testEnv.withSecurityRulesDisabled`
(mirror the inline pattern at lines 265-277), authenticate as "pending-uid", and
`assertFails(getDoc(doc(db, "registries", id, "items", itemId)))`. Name it e.g.
"pending-only invitee CANNOT read items (scope guard)". This proves items were NOT opened.

Keep all other describe blocks untouched.
  </action>
  <verify>
    <automated>npx --prefix tests/rules tsc --noEmit -p tests/rules/tsconfig.json 2>/dev/null && echo TSC_OK || grep -q "pending-only invitee CANNOT read items" tests/rules/firestore.rules.test.ts</automated>
  </verify>
  <done>
D-18 header rewritten to new policy; test (b) is a true-outsider denial (assertFails);
test (c) flipped to assertSucceeds; owner + D-19 tests unchanged; NEW pending-only
items-denial test added (assertFails on an items getDoc). If `tsc` is unavailable in the
worktree, a grep confirms the new test landed and the SUMMARY notes tsc was skipped.
NOTE: the emulator suite (`npm run test:emulator`) is run post-merge by the orchestrator
from the MAIN checkout — the executor does NOT run it here (no node_modules / emulator in
the worktree) and must say so explicitly in the SUMMARY.
  </done>
</task>

</tasks>

<verification>
In-worktree checks the executor CAN perform (do NOT run the emulator / deploy):
- `grep -n "isPendingInvited" firestore.rules` shows the new helper + the OR clause on the
  registry-doc read (and NOT on the items read).
- `canReadRegistry()` body and the items read rule are unchanged (diff shows only the new
  function + the single OR on line 50).
- The four logic cases trace correctly (pending→doc SUCCEEDS / items FAILS; outsider→FAILS;
  owner→SUCCEEDS; post-accept→SUCCEEDS).
- The D-18 test block matches the four edits + the new items-denial test.
- Optional `tsc --noEmit` if tooling resolves in the worktree.

Post-merge (ORCHESTRATOR, NOT executor):
- `npm run test:emulator` from the main checkout.
- `firebase deploy --only firestore:rules` to production.
</verification>

<success_criteria>
- `firestore.rules` grants registry-DOC read to pending invitees via a new
  `isPendingInvited()` helper ORed onto line 50 only; items read path unchanged.
- `tests/rules/firestore.rules.test.ts` D-18 block encodes: pending→doc SUCCEEDS,
  outsider→FAILS, owner/post-accept→SUCCEEDS, pending→items FAILS.
- Only the two scoped files changed. Committed. Emulator tests + deploy deferred to the
  orchestrator post-merge (stated in SUMMARY).
</success_criteria>

<output>
After completion, create
`.planning/quick/260605-h1d-allow-pending-invitees-to-read-the-regis/260605-h1d-SUMMARY.md`.
In it, explicitly state: (1) the code-trace of all four rule cases, (2) whether `tsc` ran
in the worktree, and (3) that `npm run test:emulator` and the production
`firebase deploy --only firestore:rules` were NOT run here and must be run by the
orchestrator from the MAIN checkout after merge.
</output>
