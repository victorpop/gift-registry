---
quick_id: 260513-k37
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/features/registry/ItemCard.tsx
  - web/src/features/registry/ItemGrid.tsx
  - web/src/features/registry/SkeletonCard.tsx
  - web/src/pages/RegistryPage.tsx
  - web/src/pages/ItemReservePage.tsx
  - web/i18n/en.json
  - web/i18n/ro.json
  - web/src/features/registry/__tests__/ItemCard.test.tsx
  - web/src/pages/__tests__/ItemReservePage.test.tsx
autonomous: true
requirements:
  - K37-01  # Tile redesign: image / shop / name (truncated 60ch) / price; entire tile is a link
  - K37-02  # Extend ItemReservePage to handle browse states (available, reserved-by-someone-else, purchased)
  - K37-03  # Available-state detail UI with Reserve CTA via direct useCreateReservation
  - K37-04  # Preserve all existing reserved-by-me/expired/release/confirm flows + D-06
  - K37-05  # Add i18n entries (en + ro) for every new visible string
  - K37-06  # Update ItemCard.test.tsx + ItemReservePage.test.tsx for new layout and browse states

must_haves:
  truths:
    - "On the registry page, each product tile shows: image on top, then a small uppercase shop line (merchantDomain), then the product name (truncated to 60 chars + '...' if longer), then a bold price."
    - "Clicking anywhere on a product tile (available, reserved, or purchased) navigates to /registry/:id/item/:itemId."
    - "The reserved-by-me banner row inside a tile still navigates to the detail page (no regression)."
    - "Purchased tiles keep the existing opacity/grayscale treatment AND are clickable."
    - "Visiting /registry/:id/item/:itemId for an available item shows hero image, shop, full untruncated product name, price, owner notes, and a Reserve button — NOT the 'not yours' message."
    - "Clicking Reserve on the available-state detail page goes through useCreateReservation directly (no round-trip through RegistryPage's autoReserveItemId param) and on success navigates the user to the reserved-by-me detail view."
    - "Visiting the detail page for an item reserved by someone else shows the item info (image, shop, name, price, notes) plus a clear 'already reserved by another guest' state with NO Reserve CTA and NO reserver identity (D-06)."
    - "Visiting the detail page for a purchased item shows item info + notes, no Reserve CTA, no giver name (D-06)."
    - "Reserved-by-me detail (with countdown, release, confirm-purchase, expired) renders unchanged — existing P-01..P-11 tests continue to pass."
    - "Every new visible string is read via i18n and exists in BOTH web/i18n/en.json and web/i18n/ro.json."
  artifacts:
    - path: "web/src/features/registry/ItemCard.tsx"
      provides: "Redesigned tile: clickable wrapper, image / shop / truncated name / price; reserveSlot prop removed; reserved-by-me click handler retained."
    - path: "web/src/features/registry/SkeletonCard.tsx"
      provides: "Skeleton placeholders match new vertical stack (shop / name / price)."
    - path: "web/src/pages/RegistryPage.tsx"
      provides: "Stops passing renderReserve/ReserveButton into ItemGrid (cards no longer have inline Reserve)."
    - path: "web/src/pages/ItemReservePage.tsx"
      provides: "New browse-state branches: available (with Reserve CTA), reserved-by-someone-else (view-only), purchased (view-only). Existing reserved-by-me/expired branches untouched."
    - path: "web/i18n/en.json"
      provides: "New web_pill.shop_label (optional) / web_reserve.item_page.* keys for available, reserved-by-other, purchased browse states + Reserve CTA + notes section."
    - path: "web/i18n/ro.json"
      provides: "Romanian translations matching every new en key."
    - path: "web/src/features/registry/__tests__/ItemCard.test.tsx"
      provides: "Updated tests: assert tile is a link to /registry/:id/item/:itemId; assert shop line / truncated 60-char name / price stack; remove reserve-slot rendering assertions for the inline Reserve button (cards no longer render a Reserve CTA)."
    - path: "web/src/pages/__tests__/ItemReservePage.test.tsx"
      provides: "New tests: K-01 available-state detail renders Reserve CTA + notes; K-02 Reserve click calls useCreateReservation; K-03 reserved-by-someone-else view-only; K-04 purchased view-only; existing P-01..P-11 tests continue to pass."
  key_links:
    - from: "web/src/features/registry/ItemCard.tsx"
      to: "/registry/:id/item/:itemId"
      via: "react-router <Link> (or <a> with onClick navigate) wrapping the article"
      pattern: "Link to=.*item"
    - from: "web/src/features/registry/ItemCard.tsx"
      to: "60-char truncation helper"
      via: "trim then slice(0, 60) + '...' when length > 60"
      pattern: "\\.slice\\(0, 60\\)"
    - from: "web/src/pages/ItemReservePage.tsx"
      to: "useCreateReservation"
      via: "direct mutation on Reserve click in available-state branch"
      pattern: "useCreateReservation"
    - from: "web/src/pages/RegistryPage.tsx"
      to: "ItemGrid"
      via: "ItemGrid no longer receives renderReserve (or receives undefined) — tiles are bare links now"
      pattern: "ItemGrid"
---

<objective>
Redesign the web product tile and extend ItemReservePage so the whole tile becomes a link to a per-item detail page that works for every status (available, reserved-by-me, reserved-by-other, purchased). Reuse the existing /registry/:id/item/:itemId route — no new routes.

Purpose: improve scannability of the registry list (cleaner vertical stack, no inline Reserve button) and give givers a richer view-before-reserve experience that surfaces owner notes and the full product name. Keep all current reservation flows (release, confirm, expired) working unchanged.

Output: updated ItemCard + ItemGrid + RegistryPage + SkeletonCard, extended ItemReservePage with three new branches, new i18n keys in en + ro, and updated tests for both the card and the detail page.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@web/src/features/registry/ItemCard.tsx
@web/src/features/registry/ItemGrid.tsx
@web/src/features/registry/SkeletonCard.tsx
@web/src/pages/RegistryPage.tsx
@web/src/pages/ItemReservePage.tsx
@web/src/lib/firestore-mapping.ts
@web/src/features/reservation/ReserveButton.tsx
@web/src/features/reservation/useCreateReservation.ts
@web/src/features/reservation/useReservationForItem.ts
@web/src/features/reservation/useActiveReservation.ts
@web/src/features/registry/__tests__/ItemCard.test.tsx
@web/src/pages/__tests__/ItemReservePage.test.tsx
@web/i18n/en.json
@web/i18n/ro.json
@web/src/App.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From web/src/lib/firestore-mapping.ts:
```typescript
export type ItemStatus = 'available' | 'reserved' | 'purchased'

export interface Item {
  id: string
  title: string
  imageUrl: string | null
  price: number | null
  currency: string | null
  notes: string | null          // owner notes — surfaced on detail page
  status: ItemStatus
  reservedBy: string | null     // email — used for "is this MY reservation?" check, NEVER displayed (D-06)
  reservedAt: Date | null
  expiresAt: Date | null
  affiliateUrl: string
  originalUrl: string
  merchantDomain: string | null // raw domain string used as "Shop" label as-is
}
```

From web/src/features/reservation/useCreateReservation.ts:
```typescript
export interface CreateReservationRequest {
  registryId: string
  itemId: string
  giverName: string
  giverEmail: string
  giverId: string | null
}
export interface CreateReservationResponse {
  reservationId: string
  affiliateUrl: string
  expiresAtMs: number
}
export function useCreateReservation(options?: { onSuccess?, onError? }): UseMutationResult
```

From web/src/features/reservation/useActiveReservation.ts:
```typescript
export interface ActiveReservation {
  reservationId: string
  itemId: string
  itemName: string
  affiliateUrl: string
  merchantDomain: string | null
  expiresAtMs: number
}
// useActiveReservation().set(...) is what we call on Reserve-success to seed the
// reserved-by-me detail view; useReservationForItem will then resolve it too.
```

From web/src/features/registry/ItemGrid.tsx (current contract — will be simplified):
```typescript
interface Props {
  items: Item[]
  renderReserve?: (item: Item) => React.ReactNode      // REMOVE in this plan
  filter?: ItemFilter
  renderReservedByMeClick?: (item: Item) => (() => void) | undefined  // KEEP
}
```

From web/src/features/registry/ItemCard.tsx (current contract — will change):
```typescript
interface Props {
  item: Item
  reserveSlot?: React.ReactNode                  // REMOVE
  onReservedByMeClick?: () => void               // KEEP (still navigates to detail page)
}
```

Constraints derived from prior decisions:
- D-06: NEVER render reserver or giver name anywhere (read Item.reservedBy only to compare against effectiveEmail; never display it).
- WEB-D-10: NEVER read invitedUsers; registry doc null → 404 (unaffected by this plan).
- The autoReserveItemId flow already exists in RegistryPage.tsx but the user-decision says: prefer DIRECT mutation from ItemReservePage (no round-trip through RegistryPage).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redesign ItemCard (and SkeletonCard) — entire tile is a link, vertical stack of shop / truncated name / price</name>
  <files>
    web/src/features/registry/ItemCard.tsx,
    web/src/features/registry/SkeletonCard.tsx,
    web/src/features/registry/ItemGrid.tsx,
    web/src/pages/RegistryPage.tsx,
    web/src/features/registry/__tests__/ItemCard.test.tsx,
    web/i18n/en.json,
    web/i18n/ro.json
  </files>
  <behavior>
    Updated ItemCard tests assert:
    - Tile is wrapped in a react-router Link whose href === `/registry/${registryId}/item/${item.id}` (test must pass registryId as a new prop).
    - For an available item: visible elements in order — image (alt=title), shop line (merchantDomain rendered as MonoCaption faint), product name h3, bold price. NO inline Reserve button rendered (querying for testid="reserve-slot" returns null).
    - Truncation: when `item.title` is 70 characters of "a", the rendered name h3 textContent === 60 'a's + "..." (length 63).
    - Truncation: when `item.title` is exactly 60 characters, rendered text === the full 60 chars (no ellipsis).
    - Truncation: when `item.title` is "  short title   " (with trailing/leading whitespace), rendered text has the trailing whitespace trimmed before any ellipsis logic; for a long title with trailing spaces the slice happens AFTER trim so the visible ellipsis is not preceded by spaces.
    - Reserved-by-me banner row still renders inside the card AND still fires onReservedByMeClick when clicked (unchanged from current behavior).
    - Purchased: outer wrapper still has opacity-[0.55] class AND the wrapper is still a Link (not a plain div), proving purchased tiles remain clickable.
    - D-06: no reserver email or name appears in the DOM for reserved or purchased states.

    Updated SkeletonCard test (or visual structure assertion): three stacked bars approximating shop (small, mono-width) / name (wider) / price (medium-bold).
  </behavior>
  <action>
    1. Add a `truncateTitle(title: string, limit = 60): string` helper at the top of ItemCard.tsx (or in a sibling util file). Spec:
       ```ts
       function truncateTitle(title: string, limit = 60): string {
         const trimmed = title.trim()
         if (trimmed.length <= limit) return trimmed
         return trimmed.slice(0, limit).trimEnd() + '...'
       }
       ```
       (The inner `.trimEnd()` prevents "abc   ..." when the cut lands mid-whitespace.)

    2. Change `ItemCard` Props:
       - REMOVE `reserveSlot?: React.ReactNode`.
       - ADD `registryId: string` (required) — needed to construct the Link href.
       - KEEP `onReservedByMeClick?: () => void`.

    3. Replace the outer `<article>` with a react-router `<Link to={`/registry/${registryId}/item/${item.id}`}>` that contains the article-shaped div. Apply existing classes to the inner element. The Link must:
       - Have `aria-label={t('web_pill.tile_aria', { title: truncateTitle(item.title) })}` (new i18n key).
       - Carry `data-testid="item-card"` and `data-status={item.status}` (preserve existing test selectors).
       - Apply the opacity-[0.55] class on the inner wrapper when purchased (visual treatment unchanged).

    4. Rebuild the body layout (vertical stack, gap-2, p-4):
       - Shop line: `<MonoCaption size="micro" tone="faint">{item.merchantDomain ?? ''}</MonoCaption>` — render only if `merchantDomain` non-empty. Keep the existing uppercase styling that MonoCaption provides.
       - Product name: `<h3 className="font-body text-[15px] font-medium text-gm-ink leading-[1.25] tracking-[-0.2px] m-0">{truncateTitle(item.title)}</h3>` — for the detail page we will render the FULL title; this is the only truncation site.
       - Price: keep existing price span (data-testid="price") — render the price number bold (already `font-medium`; bump to `font-semibold` if styling guidance permits) with currency as small mono on the side. Keep the `data-testid="price"` selector intact for current tests.

    5. REMOVE the inline Reserve button block (`{isAvailable && <div data-testid="reserve-slot">…</div>}`) entirely. The new design has NO inline Reserve CTA on the tile — the whole tile is now the affordance.

    6. KEEP the in-card reserved-by-me banner button block exactly as-is (the existing onReservedByMeClick handler) — it still renders inside the Link without interfering, because clicking it ALSO ends up navigating to the same detail page (existing handler already does `navigate(/registry/:id/item/:itemId)`). To avoid double-navigation: render the banner as a `<button>` and call `e.preventDefault(); e.stopPropagation()` inside its onClick before calling `onReservedByMeClick`. (The Link wrapper's onClick should still allow the button's click to take precedence.)

    7. Update SkeletonCard.tsx to reflect the new stack: replace the title+inline-price-row with three stacked bars (shop micro / name / price) matching the new visual hierarchy. Keep aspect-[4/3] sm:aspect-[16/10] image area unchanged.

    8. Update ItemGrid.tsx:
       - REMOVE the `renderReserve?: (item: Item) => React.ReactNode` prop and stop forwarding it.
       - ADD `registryId: string` prop and forward it to each ItemCard.
       - KEEP `renderReservedByMeClick`.

    9. Update RegistryPage.tsx:
       - Stop passing `renderReserve={(item) => <ReserveButton .../>}` to ItemGrid.
       - Pass `registryId={registryQ.data!.id}` to ItemGrid instead.
       - The `import ReserveButton from '../features/reservation/ReserveButton'` line may stay (still used elsewhere — check) or be removed if it becomes unreferenced. Run a quick grep before deciding.

    10. Add i18n keys (BOTH en.json and ro.json):
       - `web_pill.tile_aria` → en: "Open details for {{title}}" / ro: "Deschide detalii pentru {{title}}".
       (No other new strings introduced by this task — shop label is just merchantDomain raw.)

    11. Update web/src/features/registry/__tests__/ItemCard.test.tsx:
       - Wrap renders in `<MemoryRouter>` (or use existing react-router mock) so `<Link>` works. The current file already mocks `useNavigate`; extend to allow real `<Link>` rendering.
       - Update `makeItem()` callers to pass `registryId="reg1"` as a new required prop to `<ItemCard>` (or wrap in a small helper).
       - DELETE the three tests that assert reserve-slot rendering (`renders reserve-slot when status is available`, the two `does NOT render reserve-slot when…` and the `uses custom reserveSlot when provided` test). These behaviours no longer exist.
       - ADD tests:
         * "wraps the article in a Link to /registry/:id/item/:itemId" — assert `getByTestId('item-card').closest('a')` href ends with `/registry/reg1/item/item-1`.
         * "renders shop line (merchantDomain) above product name" — assert shop text "store" appears before "Coffee Grinder" in DOM order (use `compareDocumentPosition`).
         * "truncates titles longer than 60 chars with trailing '...'" — title = "a".repeat(70), expect rendered h3 text === "a".repeat(60) + "...".
         * "does NOT truncate titles ≤ 60 chars" — title = "a".repeat(60), expect rendered text === "a".repeat(60).
         * "trims trailing whitespace before adding ellipsis" — title = "a".repeat(58) + "   bcd", verify no whitespace immediately precedes the "..." in the rendered text.
         * "purchased tile is still wrapped in a Link (clickable)" — assert closest('a') exists for status='purchased'.
         * "reserved-by-me banner click prevents Link navigation and calls onReservedByMeClick" — fire userEvent.click on the banner button; assert callback called once, and the surrounding Link is not navigated (use a memory router and assert location did NOT change OR mock navigate and assert not-called).

    12. Run `npm --prefix web test -- --run ItemCard` and confirm all tests pass. Update snapshots if any.
  </action>
  <verify>
    <automated>npm --prefix web test -- --run ItemCard.test ItemGrid 2>&1 | tail -40</automated>
  </verify>
  <done>
    - ItemCard renders as a `<Link>` to /registry/:id/item/:itemId for every status.
    - Body is a vertical stack: shop (MonoCaption faint, merchantDomain raw) → product name h3 (truncated to 60 chars + '...' when needed, trailing whitespace trimmed) → price (bold).
    - No inline Reserve button is rendered anywhere inside the tile.
    - Reserved-by-me banner still works and does not double-navigate.
    - Purchased tiles still apply opacity-[0.55] AND are wrapped in a Link.
    - SkeletonCard mirrors the new shop / name / price stack.
    - ItemGrid + RegistryPage updated to drop renderReserve and pass registryId.
    - New i18n key `web_pill.tile_aria` present in en.json AND ro.json.
    - All ItemCard tests pass; no test asserts the now-removed reserve-slot.
    - No D-06 violations introduced (no test or code path renders reserver email/name).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ItemReservePage with browse-state branches (available + reserved-by-other + purchased) and direct Reserve mutation</name>
  <files>
    web/src/pages/ItemReservePage.tsx,
    web/src/pages/__tests__/ItemReservePage.test.tsx,
    web/i18n/en.json,
    web/i18n/ro.json
  </files>
  <behavior>
    New tests (added to ItemReservePage.test.tsx; existing P-01..P-11 must keep passing unchanged):

    - K-01 (available browse state — no active reservation): items loaded, item.status === 'available', useReservationForItem returns { status: 'empty', active: null }. ItemReservePage MUST render the new available-state UI (hero image, shop=merchantDomain, FULL product name untruncated, price, notes section with item.notes, and a Reserve button) and MUST NOT render the old `item-reserve-not-yours` panel for available items.

    - K-02 (Reserve click): in the K-01 setup, clicking the Reserve button calls useCreateReservation's mutate with `{ registryId, itemId, giverName, giverEmail, giverId }` derived from `useAuth()`/`useGuestIdentity()` exactly the way ReserveButton.tsx derives them (signed-in: user.displayName||email.split('@')[0]; guest with stored identity: identity.firstName+lastName / identity.email / giverId=null; anonymous w/o identity: opens SaveYourSpotModal-equivalent OR navigates to /registry/:id?autoReserveItemId=:itemId fallback — pick the SIMPLER path, see Action step 4).

    - K-03 (reserved-by-someone-else browse state): items loaded, item.status === 'reserved', item.reservedBy === 'someone-else@x.com' (NOT current viewer's email), useReservationForItem returns { status: 'empty', active: null }. Page renders a view-only detail UI (image, shop, full name, price, notes) plus a clearly labelled "already reserved by another guest" status — using a new i18n key, e.g. `web_reserve.item_page.reserved_by_other_title` / `_body`. NO Reserve CTA. NO reserver email in DOM (D-06 — search the DOM for "someone-else@x.com" and assert not found).

    - K-04 (purchased browse state): items loaded, item.status === 'purchased', useReservationForItem returns { status: 'empty', active: null }. Page renders view-only detail UI (image, shop, full name, price, notes) plus a clearly labelled "already purchased" status — new i18n key `web_reserve.item_page.purchased_title` / `_body`. NO Reserve CTA. NO giver name in DOM (D-06).

    - K-05 (notes optional): when item.notes is null, the notes block is omitted from the available/reserved-by-other/purchased detail UI; when notes is a string, it renders inside a labelled section (new i18n key `web_reserve.item_page.notes_label`).

    - K-06 (regression guard — reserved-by-me unchanged): re-run existing P-04 (happy reserved-by-me) — must still pass with the new code path; specifically the priority order must be: active != null → reserved-by-me detail (regardless of item.status), so a reserved-by-me viewer never falls into the K-03 branch.

    - K-07 (regression guard — expired unchanged): existing P-05 (expired) must still pass.
  </behavior>
  <action>
    1. Restructure the state machine in ItemReservePage.tsx. Replace the current "if (!active) → not-yours" branch with a status-aware browse-state dispatcher. New priority order (top to bottom — first match wins):

       ```
       if (!id || !itemId)                              → null (unchanged)
       if (items undefined || lookupStatus idle/load)   → loading (unchanged)
       if (!item)                                       → item-not-found (unchanged)
       if (active && countdown.expired)                 → expired (unchanged)
       if (active)                                      → reserved-by-me detail (unchanged — happy path)
       // --- new browse branches below ---
       if (!active && item.status === 'available')      → BROWSE_AVAILABLE branch
       if (!active && item.status === 'reserved')       → BROWSE_RESERVED_BY_OTHER branch
       if (!active && item.status === 'purchased')      → BROWSE_PURCHASED branch
       // fallback (should not be reachable):
       default                                          → existing not-yours panel
       ```

       Note: The expired check must come BEFORE the reserved-by-me happy path (it already does) but only fires when `active != null && countdown?.expired`. Do NOT change this.

    2. Build a shared `ItemDetailHero` sub-component (inline in ItemReservePage.tsx — no new file needed) that renders the common view-only layout used by all three browse branches:
       - Hero image (item.imageUrl) — same aspect ratio as the reserved-by-me hero card (sm:w-[160px] sm:h-[160px] flex-row layout, or stack on mobile). REUSE the existing markup from the current reserved-by-me detail hero card; extract it into a small inner component or render fn taking `{ item, statusPillKey }` props.
       - Shop line: MonoCaption faint, `{item.merchantDomain ?? ''}` raw.
       - Full product name (NO truncation — this is the detail page; deliberately untruncated per spec).
       - Price line: `{item.price} {item.currency}` (same formatting as reserved-by-me detail).
       - Notes section: when `item.notes` non-null, render a labelled block (label via i18n `web_reserve.item_page.notes_label`, body = item.notes). Use existing typography classes (font-body text-[15px] text-gm-inkSoft leading-[1.55]).
       - Status pill at the top — driven by `statusPillKey`:
         * available → `web_pill.available` (neutral tone)
         * reserved-by-other → new key `web_pill.reserved_by_other` (accent tone, NO PulseDot)
         * purchased → `web_pill.purchased` (ok tone)
       - The hero MUST NOT render any reserver/giver name anywhere (D-06).

    3. BROWSE_AVAILABLE branch UI:
       - Page chrome: TopNav + back-to-registry Link + Footer (same as not-found/expired branches).
       - Headline: new i18n key `web_reserve.item_page.available_title` (e.g., en: "Reserve this gift" / ro: "Rezervă acest cadou").
       - ItemDetailHero with statusPillKey='web_pill.available'.
       - Reserve CTA button — see step 4 for behavior.
       - NO countdown, NO release, NO confirm UI.

    4. Reserve CTA implementation (DIRECT mutation, per user decision — no round-trip through RegistryPage):
       - Import `useCreateReservation`, `useAuth`, `useGuestIdentity`, `useActiveReservation`, `useToast` (most already imported).
       - Wire a `useCreateReservation` mutation INSIDE ItemReservePage with `onSuccess` that:
         a. Calls `useActiveReservation().set({ reservationId, itemId, itemName: item.title, affiliateUrl, merchantDomain: item.merchantDomain, expiresAtMs })`.
         b. Calls `showToast(t('reservation.success'), 'success')`.
         c. Does NOT call `navigate(...)` — the page is ALREADY on /registry/:id/item/:itemId; on next render `useReservationForItem` will hydrate `active` and the reserved-by-me happy path will take over. (Add a comment explaining this — distinguishes from RegistryPage's autoReserveMutation which DOES navigate.)
         d. onError: existing `mapHttpsErrorToI18nKey` toast pattern.
       - Reserve button click handler (mirror ReserveButton.tsx's `handleClick`):
         * If `user`: derive giverName from `user.displayName || email.split('@')[0] || 'Guest'`, giverEmail = `user.email ?? ''`, giverId = `user.uid`, call `mutate(...)`.
         * Else if `identity` (guest with stored identity): derive giverName = `${identity.firstName} ${identity.lastName}`.trim(), giverEmail = identity.email, giverId = null, call `mutate(...)`.
         * Else (truly anonymous): fallback path — navigate to `/registry/${id}?autoReserveItemId=${itemId}` so the existing RegistryPage SaveYourSpot/GuestIdentity flow handles identity capture (this is the simpler path: avoids importing and rendering a SaveYourSpotModal inside ItemReservePage). After navigation, RegistryPage's existing auto-reserve effect will fire and, on success, its onSuccess navigates back to /registry/:id/item/:itemId. NOTE in code: "Anonymous-no-identity is the only path that round-trips through RegistryPage — all other reserves are direct."
       - Disable the Reserve button + show a spinner while `mutation.isPending`.

    5. BROWSE_RESERVED_BY_OTHER branch UI:
       - Page chrome + back link + Footer (same shell).
       - ItemDetailHero with statusPillKey='web_pill.reserved_by_other'.
       - Status message block (new i18n keys `web_reserve.item_page.reserved_by_other_title` and `_body`):
         * Title en: "Already reserved by another guest" / ro: "Deja rezervat de alt invitat".
         * Body en: "Someone else has this gift on hold right now. Head back to the registry to pick a different one." / ro: appropriate translation.
       - NO Reserve CTA, NO reserver name (D-06).
       - testid `item-reserve-reserved-by-other`.

    6. BROWSE_PURCHASED branch UI:
       - Page chrome + back link + Footer.
       - ItemDetailHero with statusPillKey='web_pill.purchased'.
       - Status message block (new i18n keys `web_reserve.item_page.purchased_title` and `_body`):
         * Title en: "Already purchased" / ro: "Deja cumpărat".
         * Body en: "This gift has been given. Head back to the registry to see what's still available." / ro: appropriate translation.
       - NO Reserve CTA, NO giver name (D-06).
       - testid `item-reserve-purchased`.

    7. Add the following i18n keys to BOTH web/i18n/en.json AND web/i18n/ro.json (under existing `web_reserve.item_page` and `web_pill` namespaces — do NOT introduce new top-level keys):
       - `web_pill.reserved_by_other` — en: "RESERVED" (uppercase; same word as `web_pill.reserved`, kept separate for clarity if styling diverges) / ro: "REZERVAT".
       - `web_reserve.item_page.available_title` — en: "Reserve this gift" / ro: "Rezervă acest cadou".
       - `web_reserve.item_page.available_subline` — en: "30-minute timer starts the moment you reserve." / ro: "Cronometrul de 30 de minute pornește în momentul rezervării."
       - `web_reserve.item_page.reserve_cta` — en: "Reserve this gift →" / ro: "Rezervă acest cadou →" (reuse phrasing similar to `web_hero.reserve_cta`).
       - `web_reserve.item_page.reserved_by_other_title` — see step 5.
       - `web_reserve.item_page.reserved_by_other_body` — see step 5.
       - `web_reserve.item_page.purchased_title` — see step 6.
       - `web_reserve.item_page.purchased_body` — see step 6.
       - `web_reserve.item_page.notes_label` — en: "From the registry owner" / ro: "De la persoana care a creat lista".
       - `web_reserve.item_page.shop_label` — en: "Shop" / ro: "Magazin" (optional — only add if you decide to label the merchantDomain line with a prefix; otherwise drop this key and keep the line as raw MonoCaption).

       VERIFY: every new key has BOTH locales. Run `python3 -c 'import json; en=json.load(open("web/i18n/en.json")); ro=json.load(open("web/i18n/ro.json")); def keys(d,p=""): out=[]; [out.extend(keys(v,p+k+".")) if isinstance(v,dict) else out.append(p+k) for k,v in d.items()]; return out' …` (or just grep both files for each new key).

    8. Update web/src/pages/__tests__/ItemReservePage.test.tsx:
       - Add hoisted mock for `useCreateReservation` (similar to existing release/confirm mocks):
         ```ts
         const createReservationMock = vi.hoisted(() => ({ mutate: vi.fn(), isPending: false as boolean }))
         vi.mock('../../features/reservation/useCreateReservation', () => ({
           useCreateReservation: (opts: { onSuccess?: Function; onError?: Function }) => {
             createReservationMock.opts = opts
             return createReservationMock
           },
         }))
         ```
       - In beforeEach, reset createReservationMock.mutate, isPending, opts.
       - Add tests K-01..K-05 per Behavior section. K-02 must:
         * Set `useReservationForItem` → { status: 'empty', active: null }, items = [makeItem({ status: 'available' })].
         * Render the page, click the Reserve button (queryByRole('button', { name: /reserve/i })).
         * Assert `createReservationMock.mutate` called with `{ registryId: 'reg1', itemId: 'it1', giverName: …, giverEmail: 'u1@x.com', giverId: 'u1' }`.
       - K-03 / K-04 must assert `screen.queryByText('someone-else@x.com')` is null (D-06 regression guard).
       - DO NOT modify P-01..P-11 tests. Run the full file and confirm zero regressions.

    9. Run `npm --prefix web test -- --run ItemReservePage` and confirm all tests pass.
  </action>
  <verify>
    <automated>npm --prefix web test -- --run ItemReservePage.test 2>&1 | tail -60</automated>
  </verify>
  <done>
    - ItemReservePage handles five mutually-exclusive states correctly: loading, not-found, expired, reserved-by-me detail, AND the three new browse states (available, reserved-by-other, purchased).
    - Available branch renders Reserve CTA that calls useCreateReservation directly; on success, useActiveReservation.set() seeds the context and the next render naturally falls into the reserved-by-me detail branch (no manual navigate).
    - Anonymous-no-identity path falls back to /registry/:id?autoReserveItemId=:itemId (existing flow); all other paths are direct mutations.
    - Reserved-by-other and Purchased branches show view-only detail (image, shop, full untruncated name, price, notes when present) — NO Reserve CTA, NO reserver/giver name in DOM (D-06 enforced by test assertion).
    - Notes section shows `item.notes` under a labelled block when non-null; absent otherwise.
    - All new visible strings come from i18n; every new key exists in BOTH en.json AND ro.json.
    - Existing P-01..P-11 tests pass unchanged; K-01..K-05 tests pass.
    - Full web test suite green: `npm --prefix web test -- --run` exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — visual + interaction check on running web app</name>
  <what-built>
    Tile redesign (vertical stack with shop / truncated name / price, entire tile clickable) and extended ItemReservePage with three new browse states (available with Reserve CTA, reserved-by-other view-only, purchased view-only) plus full notes display. Existing reserved-by-me flows (countdown, release, confirm-purchase, expired) are preserved.
  </what-built>
  <how-to-verify>
    Prereq: run `npm --prefix web run dev` and `firebase emulators:start --only firestore,functions,auth` if needed for emulator data, then open the dev server URL.

    1. **Tile layout (registry page)** — Open any registry with at least one available item.
       - Confirm the tile shows, top to bottom: image, small uppercase shop line (the raw merchantDomain), product name, bold price. NO Reserve button visible inside the tile.
       - Find a tile with a long title (>60 chars). If none exists, add one via the Android app or edit Firestore directly. Confirm the visible name ends in "..." and is exactly 60 chars + "..." (count if necessary). Confirm no trailing whitespace immediately before the "...".

    2. **Whole-tile click** — Click an available tile. Confirm browser navigates to /registry/:id/item/:itemId and the available-state detail UI loads.

    3. **Available detail state** — On the detail page for an available item:
       - Hero image, shop label (raw merchantDomain), FULL product name (NOT truncated), price visible.
       - If the item has `notes` in Firestore, the notes section is visible with the i18n label.
       - "Reserve this gift" CTA is present and enabled.

    4. **Reserve flow (signed-in)** — Sign in, click Reserve. Expect:
       - Toast success.
       - Page automatically transitions (next render) into the reserved-by-me detail view (countdown, release, confirm CTAs visible). NO URL change required.
       - D-06 check: nowhere on the page is your email or display name shown next to the reservation.

    5. **Reserve flow (anonymous)** — Sign out, hard-refresh, click an available tile. Click Reserve.
       - You should be navigated to /registry/:id?autoReserveItemId=:itemId and the existing GuestIdentityModal opens. Submit name+email. Expect toast success, then navigation back to /registry/:id/item/:itemId showing reserved-by-me detail.

    6. **Reserved-by-someone-else view** — Sign in as User A and reserve an item. Sign out, open a private/incognito window as User B (or different guest identity) and navigate directly to /registry/:id/item/:itemId for that item.
       - Confirm a view-only detail UI: image, shop, full name, price, notes (if any), and a clearly labelled "Already reserved by another guest" message. NO Reserve button. NO trace of User A's email/name in the DOM (Inspect → Ctrl+F for User A's email returns 0 matches).

    7. **Purchased view** — Find or create a purchased item, navigate to its detail page.
       - Confirm view-only UI with "Already purchased" message. NO Reserve. NO giver name.

    8. **Reserved-by-me regression** — As User A, navigate to /registry/:id/item/:itemId for the item you reserved in step 6. Confirm:
       - The reserved-by-me detail loads (countdown, release CTA, ConfirmPurchaseBanner, Continue-to-retailer link).
       - Release button works → toast → navigates back to /registry/:id.
       - (If you have time) Reserve another item, then click "I completed the purchase" in ConfirmPurchaseBanner → confirm success → navigates back to /registry/:id.

    9. **Romanian locale check** — Switch the browser language to Romanian (or call the locale switcher if surfaced). Reload the detail page in each of the three new states and confirm:
       - The new strings render in Romanian (not en fallback, not the raw key).
       - No console warnings about missing translation keys.

    10. **Console** — Throughout the above, the browser console must be free of new errors/warnings introduced by this change.
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 steps pass, or describe any failures (which step, what you saw, what you expected).</resume-signal>
</task>

</tasks>

<verification>
- `npm --prefix web test -- --run` exits 0 (full web test suite green).
- `npm --prefix web run lint` exits 0 (no new lint errors).
- `npm --prefix web run build` exits 0 (no new TypeScript errors).
- Manual: all 10 checkpoint steps in Task 3 pass.
- Grep check: `grep -nr "reservedBy" web/src --include="*.tsx" --include="*.ts" | grep -v test` — confirm Item.reservedBy is only read for the "is this MY reservation?" comparison, never rendered as text content (D-06).
- Both i18n files contain every new key: `for key in web_pill.tile_aria web_pill.reserved_by_other web_reserve.item_page.available_title web_reserve.item_page.available_subline web_reserve.item_page.reserve_cta web_reserve.item_page.reserved_by_other_title web_reserve.item_page.reserved_by_other_body web_reserve.item_page.purchased_title web_reserve.item_page.purchased_body web_reserve.item_page.notes_label; do grep -q "$key" web/i18n/en.json && grep -q "$key" web/i18n/ro.json && echo "OK $key" || echo "MISSING $key"; done` — every line prints OK.
</verification>

<success_criteria>
- Product tiles on /registry/:id render in the new layout (image / shop / truncated name / price), entire tile is a link, no inline Reserve button.
- /registry/:id/item/:itemId handles all five item-and-viewer combinations correctly: available (Reserve CTA via direct mutation), reserved-by-me (existing detail unchanged), reserved-by-other (view-only, no CTA), purchased (view-only, no CTA), expired (existing unchanged).
- D-06 invariant holds: no reserver or giver email/name appears anywhere on tiles or detail page DOM in any state.
- Every new visible string is read via `t(...)` and exists in BOTH web/i18n/en.json and web/i18n/ro.json.
- All previously-passing tests continue to pass (P-01..P-11 unmodified); ItemCard tests updated for new layout; new K-01..K-05 detail-page tests added and pass.
- Full web build + lint + test pipeline green.
</success_criteria>

<output>
After completion, create `.planning/quick/260513-k37-improve-web-product-tiles-add-product-de/260513-k37-SUMMARY.md` describing: the new tile layout, the new ItemReservePage branches and their priority order, the direct-mutation vs. round-trip path for Reserve, the new i18n keys added (en+ro), and the new + updated tests.
</output>
