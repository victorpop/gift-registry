---
quick_id: 260519-08n
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/pages/ItemReservePage.tsx
  - web/src/pages/RegistryPage.tsx
  - web/src/features/reservation/useActiveReservation.ts
  - web/src/features/reservation/useActiveReservationHydration.ts
  - web/src/pages/__tests__/ItemReservePage.test.tsx
autonomous: true
requirements:
  - QUICK-260519-08n: j5j/ke1 follow-up — pass navigation state in the navigate-on-status-flip effect on ItemReservePage AND strip the diagnostic localStorage logging that was added during diagnosis.

must_haves:
  truths:
    - "When ItemReservePage's navigate-on-status-flip effect (line ~267-289) fires, it passes location.state.recentReleasedReservationId AND state.recentReleasedItemId to /registry/:id so RegistryPage's j5j/ke1 hydration + items-shape override guards can suppress the race on EITHER racer (release-success effect OR snapshot-driven status-flip effect)."
    - "All diagnostic localStorage push + console.log noise added during diagnosis is removed from ItemReservePage.tsx, RegistryPage.tsx, useActiveReservation.ts, and useActiveReservationHydration.ts."
    - "useActiveReservation.ts is restored to its pre-diag shape: no diagLog helper, no diagLog import/export, set/clear are minimal useCallback wrappers around setActive."
    - "Test suite is green at 207 tests (206 existing + 1 new K-22) under web `npm test -- --run`."
    - "`tsc --noEmit` passes from web/."
  artifacts:
    - path: "web/src/pages/__tests__/ItemReservePage.test.tsx"
      provides: "K-22 regression test that the navigate-on-status-flip effect passes navigation state"
      contains: "K-22"
    - path: "web/src/pages/ItemReservePage.tsx"
      provides: "navigate-on-status-flip effect with state payload (already applied) + no diag code"
      pattern: "recentReleasedReservationId: active.reservationId"
    - path: "web/src/pages/RegistryPage.tsx"
      provides: "no diag imports/calls; diagLog removed from useActiveReservation import"
    - path: "web/src/features/reservation/useActiveReservation.ts"
      provides: "minimal context provider — no diagLog helper or export"
    - path: "web/src/features/reservation/useActiveReservationHydration.ts"
      provides: "j5j/ke1 suppression logic intact; no diag console.log or localStorage push"
  key_links:
    - from: "web/src/pages/ItemReservePage.tsx navigate-on-status-flip effect (~line 267-289)"
      to: "web/src/pages/RegistryPage.tsx j5j/ke1 hydration + itemsForRender override (lines 45-76, 215-240)"
      via: "navigate(`/registry/${id}`, { state: { recentReleasedReservationId, recentReleasedItemId } })"
      pattern: "recentReleasedItemId: active.itemId"
---

<objective>
j5j/ke1 follow-up. The bug persisted after j5j (pass nav state from release-success effect) and ke1 (add ignoreItemId guard) because ItemReservePage has TWO racing useEffects that navigate back to /registry/:id on release:

1. The release-success effect at ~line 206 (j5j fix already passes state).
2. The navigate-on-status-flip effect at ~line 267 (watches itemsQ snapshot; fires when item.status flips out of 'reserved').

Firestore's snapshot listener for items consistently fires faster than useReleaseReservation's setStatus('success'). The navigate-on-status-flip effect WINS the race, navigates WITHOUT state, and RegistryPage's j5j/ke1 suppression guards have nothing to match against → StickyReserveBanner + ReserveDetailSection + the reserved-card flash all reappear briefly.

**The source fix is ALREADY APPLIED** (manual edit during diagnosis on 2026-05-19). Lines 283-288 of `web/src/pages/ItemReservePage.tsx` now read:

```ts
navigate(`/registry/${id}`, {
  state: {
    recentReleasedReservationId: active.reservationId,
    recentReleasedItemId: active.itemId,
  },
})
```

This plan does TWO things:
1. Add a K-22 regression test in ItemReservePage.test.tsx that asserts the navigate-on-status-flip effect passes state, so the fix can't silently regress. (Task 1 — RED-first.)
2. Strip ALL diagnostic localStorage/console logging that was added during the diagnosis. (Task 2 — GREEN, also includes the source-fix commit since it's not yet committed.)

Purpose: Close the j5j/ke1 chain decisively. Both racers now pass state, RegistryPage's suppression layer becomes race-immune, and the codebase is restored to a clean state with no diag scaffolding leaking into production.

Output: 1 new test (K-22) + diag-stripped source across 4 files. No new files. No behavior change beyond what's described.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Source files (fix already in source; diag must be stripped)
@web/src/pages/ItemReservePage.tsx
@web/src/pages/RegistryPage.tsx
@web/src/features/reservation/useActiveReservation.ts
@web/src/features/reservation/useActiveReservationHydration.ts

# Test files
@web/src/pages/__tests__/ItemReservePage.test.tsx
@web/src/features/registry/__tests__/RegistryPage.test.tsx

<interfaces>
<!-- Mock & helper shapes the executor needs for the K-22 test. Extracted from ItemReservePage.test.tsx. -->

Existing mocks in ItemReservePage.test.tsx (lines 16-105):
- itemsQueryMock.useItemsQuery — overridable via .mockReturnValue({ data: Item[] | undefined })
- reservationForItemMock.useReservationForItem — overridable via .mockReturnValue({ status, active })
- activeMock — { active, set, clear } stand-in for useActiveReservation context
- ACTIVE_RES constant (lines 111-118) — { reservationId: 'res1', itemId: 'it1', itemName, affiliateUrl, merchantDomain, expiresAtMs }
- makeItem(overrides) helper (lines 120-137) — default Item with status='reserved', reservedBy='user@example.com'
- ItemReservePageWithForceUpdate + rerenderSame() — used by K-09/P-09/P-10 to force re-render after mutating mock returns

Probe-destination pattern (K-21, lines 990-1030 of the test file):
```tsx
const ProbeDestination = () => {
  const loc = useLocation()
  return <div data-testid="registry-page" data-state={JSON.stringify(loc.state)} />
}
```
This is the pattern K-22 should reuse — wire a memory router whose /registry/:id route renders the probe, navigate via the status-flip effect (NOT the release-success effect), then assert location.state.

Imports already present (line 8): `createMemoryRouter, RouterProvider, useLocation`.

Key trigger for the status-flip effect (ItemReservePage.tsx lines 266-289):
- Requires `active` truthy (sharedActive matching itemId OR lookupActive)
- Requires `prevStatusRef.current === 'reserved'` THEN currentItemStatus flips to 'available' OR 'purchased'
- Requires itemStatusNavigatedRef.current still false
- Fires navigate(...) with state payload

To trigger in test:
1. Mount with itemsQ.data = [makeItem({ status: 'reserved' })] AND reservationForItem returning ACTIVE_RES
2. Mutate itemsQ mock to return [makeItem({ status: 'available' })] or 'purchased'
3. rerenderSame() to flush
4. Assert ProbeDestination received state = { recentReleasedReservationId: 'res1', recentReleasedItemId: 'it1' }

The release-success effect must NOT fire — keep releaseMock.status = 'idle' (the beforeEach default).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: K-22 (RED) — write regression test for navigate-on-status-flip effect passing nav state</name>
  <files>web/src/pages/__tests__/ItemReservePage.test.tsx</files>
  <behavior>
    K-22: navigate-on-status-flip effect (ItemReservePage.tsx ~line 267-289) passes navigation state
    when item.status flips from 'reserved' to 'available' or 'purchased'.

    Test case A — status flip to 'available':
      - Setup: items mock returns [makeItem({ status: 'reserved' })], reservationForItem returns ACTIVE_RES.
        releaseMock.status stays 'idle' (default) so the OTHER racer (release-success effect) does NOT fire.
      - Mount with the ProbeDestination pattern (K-21 pattern, lines 990-1018) — a memory router whose
        /registry/:id route renders a probe component that reads useLocation() and exposes
        loc.state as data-state on a [data-testid="registry-page"] element.
      - prevStatusRef captures 'reserved' on first render.
      - Mutate itemsQ mock to return [makeItem({ status: 'available' })] and call rerenderSame().
      - The status-flip effect MUST fire and navigate with state.
      - Assert: JSON.parse(probe.getAttribute('data-state')) === { recentReleasedReservationId: 'res1',
        recentReleasedItemId: 'it1' }.

    (One case is sufficient — the 'purchased' branch shares the same code path.)

    This test is a GREEN-now test if the executor runs it against the current source (the fix IS applied).
    To prove the test is MEANINGFUL, the executor should:
      1. Run the test once — it passes (because lines 283-288 already include the state payload).
      2. Temporarily revert lines 283-288 of ItemReservePage.tsx to just `navigate(\`/registry/${id}\`)`.
      3. Re-run the test — it must FAIL (data-state will be 'null' or undefined).
      4. Restore the state payload.
      5. Confirm GREEN.
    This is the only way to validate the test would have caught the bug. The TDD spirit is preserved
    even though the source fix landed before the test.
  </behavior>
  <action>
    Open `web/src/pages/__tests__/ItemReservePage.test.tsx`. Add a new test block AFTER the existing K-21
    test (after line ~1030, before the closing `})` of the `describe('ItemReservePage', ...)` on line 1031).

    Mirror the K-21 ProbeDestination pattern (lines 990-1030) EXACTLY for router/render setup. The only
    differences from K-21:
      - DO NOT set `releaseMock.status = 'success'` — leave it at the beforeEach default of 'idle'.
      - Set `itemsQueryMock.useItemsQuery` to return `[makeItem({ status: 'reserved' })]` for the FIRST
        render so prevStatusRef captures 'reserved'.
      - Use `ItemReservePageWithForceUpdate` (already in scope at line 144) for the /registry/:id/item/:itemId
        route so rerenderSame() can flush the post-mutation re-render. Capture the rerenderSame handle by
        binding to _forceUpdateHandle the same way renderPage() does (lines 175-180) — or just reuse
        renderPage() with extraRoutes parameter and pass the ProbeDestination as extraRoutes:
        `renderPage('reg1', 'it1', [{ path: '/registry/:id', element: <ProbeDestination /> }])`.
        NOTE: renderPage() ALREADY registers /registry/:id (line 165). The extraRoutes override is the cleaner path:
        modify the call to use renderPage's third argument — but observe that renderPage at line 165 already
        defines /registry/:id as a plain div. The cleanest approach is to inline the router build (like K-21
        does at lines 1006-1018) rather than fight renderPage's defaults.
      - After mount: wrap the mock mutation + rerenderSame in `await act(async () => { ... })`:
        ```ts
        await act(async () => {
          itemsQueryMock.useItemsQuery.mockReturnValue({ data: [makeItem({ status: 'available' })] })
          _forceUpdateHandle?.()
        })
        ```
      - Then `await waitFor(() => { expect(screen.getByTestId('registry-page')).toBeInTheDocument() })`.
      - Then read data-state and assert:
        ```ts
        const stateAttr = screen.getByTestId('registry-page').getAttribute('data-state')
        expect(stateAttr).not.toBeNull()
        const state = JSON.parse(stateAttr!)
        expect(state).toEqual({
          recentReleasedReservationId: 'res1',
          recentReleasedItemId: 'it1',
        })
        ```

    Test title: `'K-22: navigate-on-status-flip effect navigates with state.recentReleasedReservationId + state.recentReleasedItemId (08n)'`

    Do NOT modify any existing tests. Do NOT touch any other file in this task.

    After writing the test, verify it passes against the current source:
      `cd web && npm test -- --run src/pages/__tests__/ItemReservePage.test.tsx`
    Expected: K-22 passes (because the source fix is already applied at lines 283-288).

    OPTIONAL meaningfulness check (recommended, NOT required for completion):
      - Temporarily change lines 283-288 of ItemReservePage.tsx back to `navigate(\`/registry/${id}\`)`.
      - Re-run K-22 — confirm it FAILS with a JSON-parse-on-null or `expect(state).toEqual(...)` mismatch.
      - Restore lines 283-288 to the state-payload version.
      - Re-run — confirm GREEN.
    Commit ONLY the test addition in this task. Source-fix + diag-strip lands in Task 2.
  </action>
  <verify>
    <automated>cd web && npm test -- --run src/pages/__tests__/ItemReservePage.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    K-22 test exists in web/src/pages/__tests__/ItemReservePage.test.tsx. Running the file alone shows
    207 — wait, this file's own count goes up by 1: K-22 added means this file goes from N to N+1.
    Importantly: K-22 PASSES against current source (because the fix is already applied).
    Test file committed with message: `test(quick-260519-08n-01): add K-22 regression for navigate-on-status-flip state payload`
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip all diagnostic logging + commit the source fix; verify 207 tests + tsc green</name>
  <files>
    web/src/pages/ItemReservePage.tsx,
    web/src/pages/RegistryPage.tsx,
    web/src/features/reservation/useActiveReservation.ts,
    web/src/features/reservation/useActiveReservationHydration.ts
  </files>
  <action>
    This task removes all diagnostic localStorage/console scaffolding added during diagnosis AND
    formalizes the navigate-on-status-flip state-payload fix (the source change at lines 283-288 of
    ItemReservePage.tsx is already on disk but unstaged — this task commits it alongside the diag strip).

    **DIAG MAP — strip these exact blocks:**

    **A) `web/src/pages/ItemReservePage.tsx`** — three diag blocks to remove:

      A1. Release-success effect FIRED diag (lines ~207-215, top of the release-success useEffect):
          Remove the entire `try { ... } catch { /* ignore */ }` block that pushes
          `'[DIAG release-success effect FIRED]'` to localStorage. The effect should start directly
          with the `if (releaseStatus === 'success' && !releaseSuccessHandledRef.current) {` guard.

      A2. Release navigating-with-state diag (lines ~227-240, inside the release-success effect after
          `clearActiveReservation()`):
          Remove the `// eslint-disable-next-line no-console` + `console.log('[DIAG release] navigating with state', {...})`
          AND the subsequent `try { ... } catch { /* ignore */ }` block that pushes
          `'[DIAG release] navigating with state'` to localStorage.
          The navigate() call at line ~241 stays exactly as-is. Result: `clearActiveReservation()`
          is immediately followed by `navigate(...)`.

      A3. Inline-release click/await diag (lines ~807-830, inside the Btn onClick async handler):
          The handler currently wraps three diag blocks around the `await release(...)` call:
            - `'[DIAG inline-release CLICK]'` push (~807-816)
            - `'[DIAG inline-release AWAIT_OK]'` push (~819-823)
            - `'[DIAG inline-release AWAIT_ERR]'` push (~824-830)
          Restore the handler to its pre-diag shape: a single line
            `onClick={() => release(active.reservationId, giverEmailToSend)}`
          (NOT async, NOT try/catch — the useReleaseReservation hook handles its own error state, and
          the release-error useEffect at line ~251 already toasts on releaseError changes — this was
          the pre-diag behavior; consult git log for `web/src/pages/ItemReservePage.tsx` to confirm
          the original handler shape, but it was a one-line non-async onClick before the diag wrapping).

      A4. KEEP (do NOT remove): lines 283-288 — the navigate-on-status-flip state payload. This is
          the fix. The comment at lines 277-282 explains why and SHOULD be retained.

    **B) `web/src/pages/RegistryPage.tsx`** — remove diag wiring:

      B1. Import (line 19):
          Change `import { useActiveReservation, diagLog } from '../features/reservation/useActiveReservation'`
          to `import { useActiveReservation } from '../features/reservation/useActiveReservation'`.

      B2. Diag render-time calls (lines 51-63):
          Remove the comment `// diag: persist to localStorage so logs survive navigation`
          and BOTH `diagLog('[DIAG RegistryPage] render', {...})` and
          `diagLog('[DIAG RegistryPage] itemsQ', {...})` blocks entirely.

    **C) `web/src/features/reservation/useActiveReservation.ts`** — revert to pre-diag shape:

      C1. Remove the diagLog helper function (lines 20-36) — entire `function diagLog(label, data) {...}`.

      C2. Inside ActiveReservationProvider:
          - Remove `diagLog('[DIAG ctx.set]', r)` from the `set` useCallback (line 41). The body becomes
            `setActive(r)`.
          - Remove `diagLog('[DIAG ctx.clear]', null)` from the `clear` useCallback (line 45). The body
            becomes `setActive(null)`.

      C3. Remove `export { diagLog }` at line 52.

      Final shape of the file: header imports, ActiveReservation interface, Ctx interface,
      ActiveReservationContext, ActiveReservationProvider (with one-line set / one-line clear), and
      useActiveReservation hook. No diag artifacts.

    **D) `web/src/features/reservation/useActiveReservationHydration.ts`** — remove hydration diag block:

      D1. Lines ~97-113 (inside the `.then((r) => { ... })` block):
          Remove the entire `diagPayload` const declaration, the
          `// eslint-disable-next-line no-console` + `console.log('[DIAG hydration] backend response', diagPayload)`,
          and the `try { ... } catch { /* ignore */ }` localStorage push.
          The `.then` body should resume directly with the `if (r.data?.active) {` guard (line ~114) —
          the j5j/ke1 suppression logic from line 114 onward MUST stay exactly intact.

    **VERIFY (run from `web/` directory):**

      1. TypeScript: `cd web && npx tsc --noEmit` — must exit 0.
      2. Full test suite: `cd web && npm test -- --run 2>&1 | tail -30` — must show ALL tests pass.
         Expected count: 207 (206 prior + 1 new K-22). If count differs, investigate before commit.
      3. Sanity grep — confirm no diag artifacts remain:
         `grep -rn "DIAG\|diagLog\|__DIAG__" web/src/` — must return zero matches.

    **COMMITS:**

      Single commit for Task 2 covering source fix + diag strip:
        `fix(quick-260519-08n-02): pass nav state in navigate-on-status-flip effect + strip diag logging`

      Body should mention:
        - The navigate-on-status-flip effect (ItemReservePage.tsx ~line 283-288) now passes
          recentReleasedReservationId + recentReleasedItemId, closing the race window that j5j/ke1
          alone couldn't cover.
        - All diagnostic localStorage/console.log scaffolding removed from 4 files.
        - useActiveReservation.ts restored to pre-diag minimal shape (no diagLog helper/export).
        - K-22 (added in -01) is the regression guard.
        - Test count: 207 passing, tsc --noEmit green.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npm test -- --run 2>&1 | tail -15 && grep -rn "DIAG\|diagLog\|__DIAG__" src/ ; echo "grep exit=$?"</automated>
  </verify>
  <done>
    - All four source files are diag-free (grep returns zero matches; exit code 1).
    - `npx tsc --noEmit` exits 0 from web/.
    - `npm test -- --run` reports 207 passed, 0 failed.
    - useActiveReservation.ts has no diagLog helper, no diagLog export, set/clear are one-line useCallbacks.
    - useActiveReservationHydration.ts retains j5j/ke1 suppression but has no [DIAG hydration] block.
    - ItemReservePage.tsx lines 283-288 still pass state in the navigate-on-status-flip effect.
    - RegistryPage.tsx imports only `useActiveReservation` (no diagLog).
    - Single commit landed with the message above.
  </done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` exits 0.
- `cd web && npm test -- --run` reports 207 passing, 0 failing.
- `grep -rn "DIAG\|diagLog\|__DIAG__" web/src/` returns zero matches (exit 1).
- `git log --oneline -3` shows the two new commits in order (test first, fix+strip second).
- Visual smoke (NOT required for completion, useful as a sanity check the user may run manually):
  reserve an item from RegistryPage → navigate to /registry/:id/item/:itemId → click Release.
  RegistryPage should display the just-released item as 'available' immediately with NO sticky banner
  re-appearance, NO ReserveDetailSection flash, and NO reserved-card flicker.
</verification>

<success_criteria>
1. K-22 test exists and passes — regression guard for the navigate-on-status-flip state payload.
2. All four affected source files are diag-free.
3. The navigate-on-status-flip effect passes navigation state (already on disk; commit makes it permanent).
4. tsc + tests green at 207.
5. Two commits land: K-22 test (task 1) + source-fix-plus-diag-strip (task 2).
6. No changes outside the five files listed in `files_modified`.
7. D-06 preserved (no reserver/giver name/email rendered anywhere new).
8. Constraint compliance: no touches to useReservationForItem.ts, useReleaseReservation.ts,
   useItemsQuery.ts, StickyReserveBanner.tsx, ConfirmPurchaseBanner.tsx, ItemCard.tsx, ItemGrid.tsx,
   app/, functions/, backend, i18n.
</success_criteria>

<output>
After completion, create `.planning/quick/260519-08n-j5j-follow-up-status-flip-race-pass-navi/260519-08n-SUMMARY.md`.
</output>
