---
phase: 02-android-core-auth
plan: 05
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - I18N-01
  - I18N-03
must_haves:
  truths:
    - "Full auth flow works end to end on emulator"
    - "Language switching works immediately"
    - "Guest mode allows bypassing auth"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete auth flow and language switching on an Android emulator.

Purpose: Confirms all auth paths work end-to-end before moving to Phase 3. Catches visual, interaction, and integration issues that unit tests cannot detect.
Output: User confirmation that Phase 2 is complete.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build debug APK and verify compilation</name>
  <files></files>
  <read_first>
    app/build.gradle.kts (verify build config),
    firebase.json (emulator port config)
  </read_first>
  <action>
    Build the debug APK to verify all code from Plans 01-04 compiles together:
    ```bash
    cd /Users/victorpop/ai-projects/gift-registry && ./gradlew assembleDebug
    ```
    If build fails, diagnose and fix compilation errors. Common issues:
    - Missing imports between packages
    - Navigation3 API changes (verify against RESEARCH.md patterns)
    - Hilt component graph issues (missing @InstallIn or @Inject)

    Start Firebase Auth emulator for testing:
    ```bash
    cd /Users/victorpop/ai-projects/gift-registry && firebase emulators:start --only auth &
    ```

    Install APK on running emulator:
    ```bash
    ./gradlew installDebug
    ```

    Report build result and emulator status.
  </action>
  <verify>
    <automated>cd /Users/victorpop/ai-projects/gift-registry && ./gradlew assembleDebug 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew assembleDebug` exits with BUILD SUCCESSFUL
    - APK file exists at app/build/outputs/apk/debug/app-debug.apk
  </acceptance_criteria>
  <done>Debug APK builds successfully and is ready for manual testing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2: email sign-up/sign-in, Google OAuth, guest mode, Navigation3 auth gating, Settings with language switching, and guest conversion bottom sheet. Running against Firebase Auth emulator.
  </what-built>
  <how-to-verify>
    Prerequisites: Android emulator running, Firebase Auth emulator on port 9099.

    **Auth Flow (AUTH-01, AUTH-02, AUTH-04):**
    1. Launch app -- verify loading spinner appears briefly, then auth screen
    2. Verify two tabs: "Sign In" and "Create Account"
    3. Switch to "Create Account" -- verify email, password, confirm password fields
    4. Create account: test@example.com / password123 / password123 -- tap Create Account
    5. Verify redirect to Home screen with "Gift Registry" title
    6. Sign out (tap Sign Out button on Home) -- verify return to auth screen
    7. Sign in with same credentials on "Sign In" tab -- verify success
    8. Kill and relaunch app -- verify goes directly to Home (no auth flash) (AUTH-04)

    **Error Handling:**
    9. Try wrong password -- verify inline error message
    10. Try short password on sign up (e.g., "123") -- verify weak password error
    11. Try signing up with existing email -- verify email exists error

    **Guest Flow (AUTH-05):**
    12. Sign out, then tap "Continue as Guest" -- verify redirect to Home
    13. Verify guest mode works without any form input

    **Google OAuth (AUTH-03):**
    14. Tap "Sign in with Google" -- verify Credential Manager bottom sheet appears
    (May fail on emulator without Google Play Services -- expected behavior)

    **Language Switching (I18N-01, I18N-03):**
    15. Navigate to Settings (gear icon on Home)
    16. Tap Language -- verify dialog with English/Romanian radio buttons
    17. Select "Romana" -- tap "Change Language"
    18. Verify app recreates and shows Romanian strings ("Autentificare" etc.)
    19. Switch back to English -- verify English strings return
    20. Kill and relaunch -- verify language preference persisted

    **Guest Conversion (AUTH-06):**
    21. The GuestConversionBottomSheet component exists but is triggered by Phase 4. Verify the composable compiles (it does if build succeeded). No manual test needed now.

    Type "approved" if tests pass, or describe issues.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Human verification covers all phase requirements:
- AUTH-01: Step 4
- AUTH-02: Step 7
- AUTH-03: Step 14
- AUTH-04: Step 8
- AUTH-05: Steps 12-13
- AUTH-06: Component exists (build verification)
- I18N-01: Steps 17-18
- I18N-03: Steps 15-20
</verification>

<success_criteria>
- User confirms auth flow works end-to-end
- User confirms language switching works with persistence
- User confirms session persists across restarts
- User confirms no auth screen flash on restart
</success_criteria>

<output>
After completion, create `.planning/phases/02-android-core-auth/02-05-SUMMARY.md`
</output>
