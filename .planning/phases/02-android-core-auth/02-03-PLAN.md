---
phase: 02-android-core-auth
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - app/src/main/java/com/giftregistry/ui/auth/AuthUiState.kt
  - app/src/main/java/com/giftregistry/ui/auth/AuthViewModel.kt
  - app/src/main/java/com/giftregistry/ui/auth/AuthScreen.kt
  - app/src/main/java/com/giftregistry/ui/navigation/AppDestinations.kt
  - app/src/main/java/com/giftregistry/ui/navigation/AppNavigation.kt
  - app/src/main/java/com/giftregistry/MainActivity.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/res/values-ro/strings.xml
  - app/src/test/java/com/giftregistry/MainDispatcherRule.kt
  - app/src/test/java/com/giftregistry/ui/auth/AuthViewModelTest.kt
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
must_haves:
  truths:
    - "User sees a tabbed auth screen with Sign In and Create Account tabs"
    - "User can enter email and password to sign up or sign in"
    - "User can tap Sign in with Google button"
    - "User can tap Continue as Guest to enter anonymous mode"
    - "Authenticated user is auto-redirected past auth screen"
    - "Auth state persists across process death (no flash of auth screen)"
  artifacts:
    - path: "app/src/main/java/com/giftregistry/ui/auth/AuthScreen.kt"
      provides: "Tabbed auth UI with sign in, sign up, Google, guest"
      min_lines: 100
    - path: "app/src/main/java/com/giftregistry/ui/auth/AuthViewModel.kt"
      provides: "Auth state management and action dispatch"
      contains: "@HiltViewModel"
    - path: "app/src/main/java/com/giftregistry/ui/navigation/AppNavigation.kt"
      provides: "Navigation3 NavDisplay with auth gating"
      contains: "NavDisplay"
    - path: "app/src/test/java/com/giftregistry/ui/auth/AuthViewModelTest.kt"
      provides: "ViewModel unit tests with FakeAuthRepository"
      min_lines: 50
  key_links:
    - from: "AppNavigation.kt"
      to: "AuthViewModel.kt"
      via: "hiltViewModel() + auth state observation"
      pattern: "collectAsStateWithLifecycle"
    - from: "AuthScreen.kt"
      to: "AuthViewModel.kt"
      via: "event callbacks"
      pattern: "viewModel\\."
    - from: "MainActivity.kt"
      to: "AppNavigation.kt"
      via: "setContent composable call"
      pattern: "AppNavigation"
---

<objective>
Build the auth UI screen, ViewModel, Navigation3 gating, and ViewModel unit tests.

Purpose: This is the primary user-facing deliverable of Phase 2 -- the auth flow that users interact with. Depends on the domain/data layers from Plan 02.
Output: Working auth screen with email/password sign-up and sign-in, Google sign-in button, guest access, navigation gating, and ViewModel tests.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-android-core-auth/02-RESEARCH.md
@.planning/phases/02-android-core-auth/02-UI-SPEC.md
@.planning/phases/02-android-core-auth/02-CONTEXT.md

@app/src/main/res/values/strings.xml
@app/src/main/res/values-ro/strings.xml

<interfaces>
<!-- Key types from Plan 02 that this plan depends on -->

From domain/model/User.kt:
```kotlin
data class User(
    val uid: String,
    val email: String?,
    val displayName: String?,
    val isAnonymous: Boolean
)
```

From domain/auth/AuthRepository.kt:
```kotlin
interface AuthRepository {
    val authState: Flow<User?>
    val currentUser: User?
    suspend fun signUpWithEmail(email: String, password: String): Result<User>
    suspend fun signInWithEmail(email: String, password: String): Result<User>
    suspend fun signInWithGoogle(idToken: String): Result<User>
    suspend fun signInAnonymously(): Result<User>
    suspend fun linkEmailToAnonymous(email: String, password: String): Result<User>
    fun signOut()
}
```

Use cases (all @Inject constructor with operator fun invoke):
- SignUpUseCase(email, password): Result<User>
- SignInEmailUseCase(email, password): Result<User>
- SignInGoogleUseCase(idToken): Result<User>
- SignInAnonymousUseCase(): Result<User>
- ObserveAuthStateUseCase(): Flow<User?>

From data/auth/FakeAuthRepository.kt:
```kotlin
class FakeAuthRepository : AuthRepository {
    var shouldFail = false
    var failureException: Exception = Exception("Auth failed")
    fun emitUser(user: User?)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AuthUiState, AuthViewModel, MainDispatcherRule, and ViewModel unit tests</name>
  <files>
    app/src/main/java/com/giftregistry/ui/auth/AuthUiState.kt,
    app/src/main/java/com/giftregistry/ui/auth/AuthViewModel.kt,
    app/src/test/java/com/giftregistry/MainDispatcherRule.kt,
    app/src/test/java/com/giftregistry/ui/auth/AuthViewModelTest.kt
  </files>
  <read_first>
    app/src/main/java/com/giftregistry/domain/auth/AuthRepository.kt (interface contract),
    app/src/main/java/com/giftregistry/domain/model/User.kt (domain model),
    app/src/test/java/com/giftregistry/data/auth/FakeAuthRepository.kt (test double),
    app/src/main/java/com/giftregistry/domain/auth/SignUpUseCase.kt (use case signature),
    app/src/main/java/com/giftregistry/domain/auth/ObserveAuthStateUseCase.kt (use case signature),
    .planning/phases/02-android-core-auth/02-UI-SPEC.md (error messages and interaction states),
    .planning/phases/02-android-core-auth/02-RESEARCH.md (Pitfall 4: auth state race on process restart)
  </read_first>
  <behavior>
    - Test 1: Initial state is Loading (not Idle) to prevent auth screen flash per Pitfall 4
    - Test 2: When auth state emits a non-null User, uiState transitions to Authenticated
    - Test 3: When auth state emits null after init delay, uiState transitions to Unauthenticated
    - Test 4: signUp with valid input succeeds and sets state to Authenticated
    - Test 5: signUp with failure sets error message in form state
    - Test 6: signIn with valid input succeeds and sets state to Authenticated
    - Test 7: signIn with failure sets error message in form state
    - Test 8: continueAsGuest succeeds and sets state to Authenticated with isAnonymous true
    - Test 9: signOut resets state to Unauthenticated
  </behavior>
  <action>
    Create `app/src/test/java/com/giftregistry/MainDispatcherRule.kt`:
    ```kotlin
    package com.giftregistry

    import kotlinx.coroutines.Dispatchers
    import kotlinx.coroutines.ExperimentalCoroutinesApi
    import kotlinx.coroutines.test.UnconfinedTestDispatcher
    import kotlinx.coroutines.test.resetMain
    import kotlinx.coroutines.test.setMain
    import org.junit.rules.TestWatcher
    import org.junit.runner.Description

    @OptIn(ExperimentalCoroutinesApi::class)
    class MainDispatcherRule : TestWatcher() {
        val testDispatcher = UnconfinedTestDispatcher()
        override fun starting(description: Description) { Dispatchers.setMain(testDispatcher) }
        override fun finished(description: Description) { Dispatchers.resetMain() }
    }
    ```

    Create `ui/auth/AuthUiState.kt`:
    ```kotlin
    package com.giftregistry.ui.auth

    sealed interface AuthUiState {
        data object Loading : AuthUiState
        data object Unauthenticated : AuthUiState
        data class Authenticated(val uid: String, val isAnonymous: Boolean) : AuthUiState
    }

    data class AuthFormState(
        val email: String = "",
        val password: String = "",
        val confirmPassword: String = "",
        val isLoading: Boolean = false,
        val errorMessage: String? = null
    )
    ```

    Create `ui/auth/AuthViewModel.kt` annotated with `@HiltViewModel`:
    - Inject use cases: `SignUpUseCase`, `SignInEmailUseCase`, `SignInGoogleUseCase`, `SignInAnonymousUseCase`, `ObserveAuthStateUseCase`
    - `private val _authState = MutableStateFlow<AuthUiState>(AuthUiState.Loading)` -- starts Loading per Pitfall 4
    - `val authState: StateFlow<AuthUiState> = _authState.asStateFlow()`
    - `private val _formState = MutableStateFlow(AuthFormState())`
    - `val formState: StateFlow<AuthFormState> = _formState.asStateFlow()`
    - In `init` block: launch coroutine collecting `observeAuthStateUseCase()`. On first emission, if user is non-null set Authenticated immediately. If user is null, delay 500ms then re-check -- if still null set Unauthenticated. On subsequent emissions, map directly (non-null = Authenticated, null = Unauthenticated). This handles Pitfall 4 (Firebase emits null before restoring session).
    - `fun updateEmail(email: String)` -- updates formState.email, clears errorMessage
    - `fun updatePassword(password: String)` -- updates formState.password, clears errorMessage
    - `fun updateConfirmPassword(confirmPassword: String)` -- updates formState.confirmPassword
    - `fun clearError()` -- sets formState.errorMessage to null
    - `fun signUp()` -- validates: email not blank, password.length >= 8, password == confirmPassword. If validation fails set errorMessage. Otherwise set isLoading=true, call signUpUseCase, on failure set errorMessage + isLoading=false.
    - `fun signIn()` -- validates: email not blank, password not blank. Set isLoading=true, call signInEmailUseCase, on failure set errorMessage + isLoading=false.
    - `fun signInWithGoogle(idToken: String)` -- set isLoading=true, call signInGoogleUseCase, on failure set errorMessage + isLoading=false.
    - `fun continueAsGuest()` -- set isLoading=true, call signInAnonymousUseCase, on failure set errorMessage + isLoading=false.
    - `fun signOut()` -- call repository.signOut() directly (inject AuthRepository for this one operation, or add a dedicated use case). Simplest: inject `AuthRepository` alongside use cases and call `repository.signOut()`.

    Create `AuthViewModelTest.kt` using JUnit 4 + coroutines-test + Turbine:
    - `val fakeRepo = FakeAuthRepository()`
    - Construct real use cases with fakeRepo: `SignUpUseCase(fakeRepo)`, `SignInEmailUseCase(fakeRepo)`, etc.
    - Construct `AuthViewModel` manually passing all use cases and the fakeRepo
    - `@get:Rule val mainDispatcherRule = MainDispatcherRule()`
    - Test the 9 behaviors listed. Use Turbine `.test { }` for Flow assertions.
    - For Test 3 (Unauthenticated after delay): use `advanceTimeBy(600)` and `runCurrent()` with `StandardTestDispatcher` -- or accept that with `UnconfinedTestDispatcher` the delay completes immediately and test the final state.
  </action>
  <verify>
    <automated>cd /Users/victorpop/ai-projects/gift-registry && ./gradlew testDebugUnitTest --tests "*.AuthViewModelTest" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - AuthUiState.kt contains `sealed interface AuthUiState`
    - AuthUiState.kt contains `data object Loading`, `data object Unauthenticated`, `data class Authenticated`
    - AuthFormState contains fields: email, password, confirmPassword, isLoading, errorMessage
    - AuthViewModel.kt contains `@HiltViewModel`
    - AuthViewModel.kt contains `MutableStateFlow<AuthUiState>(AuthUiState.Loading)`
    - AuthViewModel.kt contains `fun signUp()`
    - AuthViewModel.kt contains `fun signIn()`
    - AuthViewModel.kt contains `fun signInWithGoogle(idToken: String)`
    - AuthViewModel.kt contains `fun continueAsGuest()`
    - AuthViewModel.kt does NOT contain `MutableLiveData`
    - AuthViewModel.kt does NOT contain `import com.google.firebase`
    - AuthViewModelTest.kt contains `FakeAuthRepository`
    - AuthViewModelTest.kt contains at least 5 `@Test` functions
    - MainDispatcherRule.kt contains `Dispatchers.setMain`
    - All tests pass when run via `./gradlew testDebugUnitTest --tests "*.AuthViewModelTest"`
  </acceptance_criteria>
  <done>AuthViewModel manages auth state and form state. All 9 test cases pass using FakeAuthRepository. ViewModel has zero Firebase imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create AuthScreen, Navigation3 gating, wire MainActivity, and add string resources</name>
  <files>
    app/src/main/java/com/giftregistry/ui/auth/AuthScreen.kt,
    app/src/main/java/com/giftregistry/ui/navigation/AppDestinations.kt,
    app/src/main/java/com/giftregistry/ui/navigation/AppNavigation.kt,
    app/src/main/java/com/giftregistry/MainActivity.kt,
    app/src/main/res/values/strings.xml,
    app/src/main/res/values-ro/strings.xml
  </files>
  <read_first>
    app/src/main/java/com/giftregistry/ui/auth/AuthViewModel.kt (just created -- state and action signatures),
    app/src/main/java/com/giftregistry/ui/auth/AuthUiState.kt (just created -- sealed interface),
    app/src/main/java/com/giftregistry/MainActivity.kt (existing -- needs AppNavigation wiring),
    app/src/main/res/values/strings.xml (existing strings to preserve and expand),
    app/src/main/res/values-ro/strings.xml (existing Romanian strings to preserve and expand),
    .planning/phases/02-android-core-auth/02-UI-SPEC.md (Auth Screen Layout, Component Contract, Copywriting Contract, Spacing, Color, Accessibility Contract),
    .planning/phases/02-android-core-auth/02-RESEARCH.md (Pattern 1: Navigation3 Auth Gating, Pitfall 2: NavEntry ViewModel decorator, Pitfall 3: Credential Manager needs Activity context)
  </read_first>
  <action>
    Create `ui/navigation/AppDestinations.kt`:
    ```kotlin
    package com.giftregistry.ui.navigation

    sealed class AppDestination {
        data object Auth : AppDestination()
        data object Home : AppDestination()
        data object Settings : AppDestination()
    }
    ```

    Create `ui/navigation/AppNavigation.kt` following RESEARCH.md Pattern 1:
    - `@Composable fun AppNavigation(authViewModel: AuthViewModel = hiltViewModel())`
    - Observe `authViewModel.authState.collectAsStateWithLifecycle()` -- NOT `collectAsState()`
    - When `AuthUiState.Loading`: show centered `CircularProgressIndicator` and return early (do not render NavDisplay)
    - `val backStack = rememberNavBackStack(initialKey = AppDestination.Auth)`
    - `LaunchedEffect(authUiState)`:
      - `Authenticated` -> clear backStack, add Home
      - `Unauthenticated` -> clear backStack, add Auth
      - `Loading` -> no-op
    - `NavDisplay(backStack, onBack = { if (backStack.size > 1) backStack.removeLast() }, entryDecorators = listOf(rememberViewModelStoreNavEntryDecorator()), entryProvider = entryProvider { ... })`
    - CRITICAL: `rememberViewModelStoreNavEntryDecorator()` prevents ViewModel recreation on nav (Pitfall 2)
    - Auth entry: `entry<AppDestination.Auth> { AuthScreen() }`
    - Home entry: `entry<AppDestination.Home> { HomeScaffold(onSettingsClick = { backStack.add(AppDestination.Settings) }, onSignOut = { authViewModel.signOut() }) }` -- define inline HomeScaffold composable: Scaffold + TopAppBar("Gift Registry" from R.string.app_name) + IconButton(Icons.Default.Settings) + body with "Sign Out" TextButton for testing
    - Settings entry: `entry<AppDestination.Settings> { SettingsScreen(onBack = { backStack.removeLast() }) }` -- SettingsScreen created in Plan 04. If not yet available, create a minimal placeholder in settings package: `@Composable fun SettingsScreen(onBack: () -> Unit) { Text("Settings") }`

    Create `ui/auth/AuthScreen.kt` following UI-SPEC Component Contract:
    - `@Composable fun AuthScreen(viewModel: AuthViewModel = hiltViewModel())`
    - Collect formState via `viewModel.formState.collectAsStateWithLifecycle()`
    - Wrap in `Scaffold(snackbarHost = { SnackbarHost(snackbarHostState) })`
    - Vertically scrollable Column, horizontal padding 16.dp
    - App title: `Text(stringResource(R.string.app_name), style = headlineLarge, modifier = Modifier.semantics { heading() })` -- top padding 64.dp, centered
    - Spacer 24.dp
    - `var selectedTabIndex by rememberSaveable { mutableIntStateOf(0) }`
    - `TabRow(selectedTabIndex)` with 2 tabs: stringResource(R.string.auth_sign_in_title) and stringResource(R.string.auth_sign_up_title)
    - Spacer 24.dp
    - `AnimatedContent(targetState = selectedTabIndex)` for tab content:
      - **Tab 0 (Sign In)**: Email OutlinedTextField (full width, label auth_email_label, keyboard type Email), Password OutlinedTextField with visibility toggle icon (contentDescription from auth_password_show/auth_password_hide), 24.dp spacing between fields. "Sign In" filled Button full width -- loading state: CircularProgressIndicator(Modifier.size(24.dp)) replaces label. Spacer 16.dp. "OR" divider (HorizontalDivider + Text + HorizontalDivider). Spacer 16.dp. "Sign in with Google" OutlinedButton full width with icon prefix.
      - **Tab 1 (Create Account)**: Same fields plus Confirm Password OutlinedTextField. Button = auth_sign_up_button, onClick = viewModel::signUp.
    - Error display: inline via OutlinedTextField `supportingText` with error color. Network/generic errors via Snackbar.
    - Spacer 32.dp
    - "Continue as Guest" TextButton: centered, labelLarge, primary color, `Modifier.fillMaxWidth().defaultMinSize(minHeight = 44.dp)` per Accessibility Contract, onClick = viewModel::continueAsGuest
    - Bottom padding 24.dp
    - Google Sign-In: On button tap, get Activity context via `LocalContext.current`, create `CredentialManager.create(context)`, build `GetGoogleIdOption` with `setServerClientId(context.getString(R.string.default_web_client_id))` and `setFilterByAuthorizedAccounts(false)`, launch in rememberCoroutineScope. On success extract `GoogleIdTokenCredential.createFrom(result.credential.data).idToken`, call `viewModel.signInWithGoogle(idToken)`. On failure show error Snackbar. Note: `default_web_client_id` is auto-generated by google-services plugin.

    Update `MainActivity.kt`:
    - Replace placeholder content in setContent with `AppNavigation()`
    - Keep `GiftRegistryTheme { AppNavigation() }`
    - Keep `enableEdgeToEdge()`

    Update `app/src/main/res/values/strings.xml` -- PRESERVE all existing strings, ADD new ones per UI-SPEC Copywriting Contract:
    ```xml
    <string name="auth_sign_in_button">Sign In</string>
    <string name="auth_sign_up_button">Create Account</string>
    <string name="auth_google_sign_in_button">Sign in with Google</string>
    <string name="auth_or_divider">OR</string>
    <string name="auth_confirm_password_label">Confirm Password</string>
    <string name="auth_password_show">Show password</string>
    <string name="auth_password_hide">Hide password</string>
    <string name="auth_sign_in_no_account_hint">New here? Switch to Create Account to get started.</string>
    <string name="auth_error_invalid_credentials">Incorrect email or password. Check your details and try again.</string>
    <string name="auth_error_email_exists">An account with this email already exists. Try signing in.</string>
    <string name="auth_error_weak_password">Password must be at least 8 characters.</string>
    <string name="auth_error_network">No connection. Check your internet and try again.</string>
    <string name="auth_guest_conversion_title">Save your reservation?</string>
    <string name="auth_guest_conversion_body">Create a free account to track your reservation and receive updates.</string>
    <string name="auth_guest_conversion_create">Create Account</string>
    <string name="auth_guest_conversion_dismiss">Maybe later</string>
    <string name="auth_settings_title">Settings</string>
    <string name="auth_settings_language_label">Language</string>
    <string name="auth_settings_language_english">English</string>
    <string name="auth_settings_language_romanian">Romana</string>
    <string name="settings_language_confirm">Change Language</string>
    ```

    Update `app/src/main/res/values-ro/strings.xml` -- PRESERVE all existing strings, ADD Romanian translations:
    ```xml
    <string name="auth_sign_in_button">Autentificare</string>
    <string name="auth_sign_up_button">Creeaz\u0103 Cont</string>
    <string name="auth_google_sign_in_button">Autentificare cu Google</string>
    <string name="auth_or_divider">SAU</string>
    <string name="auth_confirm_password_label">Confirm\u0103 Parola</string>
    <string name="auth_password_show">Arat\u0103 parola</string>
    <string name="auth_password_hide">Ascunde parola</string>
    <string name="auth_sign_in_no_account_hint">Nou aici? Treci la Creeaz\u0103 Cont pentru a \u00eencepe.</string>
    <string name="auth_error_invalid_credentials">Email sau parol\u0103 incorecte. Verific\u0103 datele \u0219i \u00eencearc\u0103 din nou.</string>
    <string name="auth_error_email_exists">Un cont cu acest email exist\u0103 deja. \u00cencearc\u0103 s\u0103 te autentifici.</string>
    <string name="auth_error_weak_password">Parola trebuie s\u0103 aib\u0103 cel pu\u021bin 8 caractere.</string>
    <string name="auth_error_network">Nu exist\u0103 conexiune. Verific\u0103 internetul \u0219i \u00eencearc\u0103 din nou.</string>
    <string name="auth_guest_conversion_title">Salvezi rezervarea?</string>
    <string name="auth_guest_conversion_body">Creeaz\u0103 un cont gratuit pentru a-\u021bi urm\u0103ri rezervarea \u0219i a primi actualiz\u0103ri.</string>
    <string name="auth_guest_conversion_create">Creeaz\u0103 Cont</string>
    <string name="auth_guest_conversion_dismiss">Poate mai t\u00e2rziu</string>
    <string name="auth_settings_title">Set\u0103ri</string>
    <string name="auth_settings_language_label">Limb\u0103</string>
    <string name="auth_settings_language_english">English</string>
    <string name="auth_settings_language_romanian">Rom\u00e2n\u0103</string>
    <string name="settings_language_confirm">Schimb\u0103 Limba</string>
    ```
  </action>
  <verify>
    <automated>cd /Users/victorpop/ai-projects/gift-registry && ls app/src/main/java/com/giftregistry/ui/auth/AuthScreen.kt app/src/main/java/com/giftregistry/ui/navigation/AppDestinations.kt app/src/main/java/com/giftregistry/ui/navigation/AppNavigation.kt && grep -c "auth_sign_in_button\|auth_sign_up_button\|auth_google_sign_in_button\|auth_or_divider\|auth_confirm_password_label\|auth_password_show\|auth_password_hide\|auth_error_invalid_credentials\|auth_guest_conversion_title\|auth_settings_title\|settings_language_confirm" app/src/main/res/values/strings.xml</automated>
  </verify>
  <acceptance_criteria>
    - AuthScreen.kt contains `package com.giftregistry.ui.auth`
    - AuthScreen.kt contains `@Composable fun AuthScreen`
    - AuthScreen.kt contains `TabRow`
    - AuthScreen.kt contains `AnimatedContent`
    - AuthScreen.kt contains `OutlinedTextField`
    - AuthScreen.kt contains `CredentialManager` for Google Sign-In
    - AuthScreen.kt contains `CircularProgressIndicator` for loading state
    - AuthScreen.kt contains `defaultMinSize(minHeight = 44.dp)` on guest link
    - AuthScreen.kt contains `semantics` with heading on title
    - AppDestinations.kt contains `sealed class AppDestination`
    - AppDestinations.kt contains `data object Auth`, `data object Home`, `data object Settings`
    - AppNavigation.kt contains `NavDisplay`
    - AppNavigation.kt contains `rememberViewModelStoreNavEntryDecorator()`
    - AppNavigation.kt contains `collectAsStateWithLifecycle`
    - AppNavigation.kt does NOT contain `collectAsState()` without lifecycle
    - MainActivity.kt contains `AppNavigation()`
    - strings.xml contains at least 11 new auth_ string keys
    - values-ro/strings.xml contains at least 11 new auth_ string keys
  </acceptance_criteria>
  <done>Auth screen renders with tabbed login/signup, Google sign-in, guest link. Navigation3 gates on auth state. MainActivity wires to AppNavigation. All string resources in English and Romanian.</done>
</task>

</tasks>

<verification>
After both tasks:
```bash
cd /Users/victorpop/ai-projects/gift-registry && ./gradlew testDebugUnitTest --tests "*.AuthViewModelTest" 2>&1 | tail -10
```
</verification>

<success_criteria>
- AuthViewModel manages auth state transitions (Loading -> Unauthenticated or Authenticated)
- Auth screen matches UI-SPEC layout (tabbed, fields, Google button, guest link)
- Navigation3 gates auth screen and auto-redirects authenticated users
- All string resources added in both English and Romanian
- No LiveData, no KTX imports, no Navigation Compose 2.x
- ViewModel unit tests pass with FakeAuthRepository
</success_criteria>

<output>
After completion, create `.planning/phases/02-android-core-auth/02-03-SUMMARY.md`
</output>
