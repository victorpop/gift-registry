---
phase: 02-android-core-auth
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - app/src/main/java/com/giftregistry/ui/auth/AuthUiState.kt
  - app/src/main/java/com/giftregistry/ui/auth/AuthViewModel.kt
  - app/src/main/java/com/giftregistry/ui/auth/AuthScreen.kt
  - app/src/main/java/com/giftregistry/ui/navigation/AppNavKeys.kt
  - app/src/main/java/com/giftregistry/ui/navigation/AppNavigation.kt
  - app/src/main/java/com/giftregistry/MainActivity.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/res/values-ro/strings.xml
  - app/src/test/java/com/giftregistry/MainDispatcherRule.kt
  - app/src/test/java/com/giftregistry/ui/auth/AuthViewModelTest.kt
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
must_haves:
  truths:
    - "User sees a tabbed auth screen with Sign In and Create Account tabs"
    - "User can enter email and password to sign up or sign in"
    - "User can tap Sign in with Google button"
    - "User can tap Continue as Guest to enter anonymous mode"
    - "Authenticated user is auto-redirected past auth screen"
    - "Auth state persists across process death (no flash of auth screen on restart)"
  artifacts:
    - path: "app/src/main/java/com/giftregistry/ui/auth/AuthScreen.kt"
      provides: "Tabbed auth UI with sign in, sign up, Google, guest"
      min_lines: 100
    - path: "app/src/main/java/com/giftregistry/ui/auth/AuthViewModel.kt"
      provides: "Auth state management and action dispatch"
      contains: "@HiltViewModel"
    - path: "app/src/main/java/com/giftregistry/ui/navigation/AppNavigation.kt"
      provides: "Navigation3 NavDisplay with auth gating"
      contains: "NavDisplay"
    - path: "app/src/test/java/com/giftregistry/ui/auth/AuthViewModelTest.kt"
      provides: "ViewModel unit tests with FakeAuthRepository"
      min_lines: 50
  key_links:
    - from: "AppNavigation.kt"
      to: "AuthViewModel"
      via: "hiltViewModel() + auth state observation"
      pattern: "collectAsStateWithLifecycle"
    - from: "AuthScreen.kt"
      to: "AuthViewModel"
      via: "event callbacks"
      pattern: "viewModel\\."
    - from: "MainActivity.kt"
      to: "AppNavigation"
      via: "setContent composable call"
      pattern: "AppNavigation"
---

<objective>
Build the auth UI screen, ViewModel, Navigation3 with auth gating, ViewModel unit tests, and string resources.

Purpose: This is the primary user-facing deliverable of Phase 2 -- the auth flow users interact with. Depends on domain/data layers from Plan 02.
Output: Working auth screen with email/password sign-up and sign-in, Google sign-in, guest access, navigation gating, and ViewModel tests.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-android-core-auth/02-RESEARCH.md
@.planning/phases/02-android-core-auth/02-UI-SPEC.md
@.planning/phases/02-android-core-auth/02-CONTEXT.md

@app/src/main/res/values/strings.xml
@app/src/main/res/values-ro/strings.xml

<interfaces>
<!-- Key types from Plan 02 that this plan depends on -->

From domain/model/User.kt:
```kotlin
data class User(
    val uid: String,
    val email: String?,
    val displayName: String?,
    val isAnonymous: Boolean
)
```

From domain/auth/AuthRepository.kt:
```kotlin
interface AuthRepository {
    val authState: Flow<User?>
    val currentUser: User?
    suspend fun signUpWithEmail(email: String, password: String): Result<User>
    suspend fun signInWithEmail(email: String, password: String): Result<User>
    suspend fun signInWithGoogle(idToken: String): Result<User>
    suspend fun signInAnonymously(): Result<User>
    suspend fun linkEmailToAnonymous(email: String, password: String): Result<User>
    fun signOut()
}
```

From domain/auth use cases (all @Inject constructor with operator fun invoke):
- SignUpUseCase(email, password): Result<User>
- SignInEmailUseCase(email, password): Result<User>
- SignInGoogleUseCase(idToken): Result<User>
- SignInAnonymousUseCase(): Result<User>
- ObserveAuthStateUseCase(): Flow<User?>

From data/auth/FakeAuthRepository.kt:
```kotlin
class FakeAuthRepository : AuthRepository {
    var shouldFail = false
    var failureException: Exception = Exception("Auth failed")
    fun emitUser(user: User?)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AuthUiState, AuthViewModel, MainDispatcherRule, and ViewModel unit tests</name>
  <files>
    app/src/main/java/com/giftregistry/ui/auth/AuthUiState.kt,
    app/src/main/java/com/giftregistry/ui/auth/AuthViewModel.kt,
    app/src/test/java/com/giftregistry/MainDispatcherRule.kt,
    app/src/test/java/com/giftregistry/ui/auth/AuthViewModelTest.kt
  </files>
  <read_first>
    app/src/main/java/com/giftregistry/domain/auth/AuthRepository.kt (interface contract),
    app/src/main/java/com/giftregistry/domain/model/User.kt (domain model),
    app/src/test/java/com/giftregistry/data/auth/FakeAuthRepository.kt (test double),
    app/src/main/java/com/giftregistry/domain/auth/SignUpUseCase.kt (use case signature),
    app/src/main/java/com/giftregistry/domain/auth/SignInEmailUseCase.kt (use case signature),
    app/src/main/java/com/giftregistry/domain/auth/ObserveAuthStateUseCase.kt (use case signature),
    .planning/phases/02-android-core-auth/02-UI-SPEC.md (error messages from Copywriting Contract),
    .planning/phases/02-android-core-auth/02-RESEARCH.md (Pitfall 5 for ViewModel scoping)
  </read_first>
  <behavior>
    - Test 1: Initial state is Loading (not Unauthenticated) to prevent auth screen flash on app restart
    - Test 2: When auth state emits non-null User, authUiState transitions to Authenticated
    - Test 3: When auth state emits null after init, authUiState transitions to Unauthenticated
    - Test 4: signUp() success sets state to Authenticated
    - Test 5: signUp() failure sets error message in formState
    - Test 6: signIn() success sets state to Authenticated
    - Test 7: signIn() failure sets error message in formState
    - Test 8: continueAsGuest() success sets state to Authenticated with isAnonymous=true
    - Test 9: signOut() resets state to Unauthenticated
  </behavior>
  <action>
    Create `ui/auth/AuthUiState.kt`:
    ```kotlin
    package com.giftregistry.ui.auth

    sealed interface AuthUiState {
        data object Loading : AuthUiState
        data object Unauthenticated : AuthUiState
        data class Authenticated(val uid: String, val isAnonymous: Boolean) : AuthUiState
    }

    data class AuthFormState(
        val email: String = "",
        val password: String = "",
        val confirmPassword: String = "",
        val isLoading: Boolean = false,
        val errorMessage: String? = null
    )
    ```

    Create `ui/auth/AuthViewModel.kt` annotated with `@HiltViewModel`:
    - Inject all use cases: `SignUpUseCase`, `SignInEmailUseCase`, `SignInGoogleUseCase`, `SignInAnonymousUseCase`, `ObserveAuthStateUseCase`
    - `private val _authState = MutableStateFlow<AuthUiState>(AuthUiState.Loading)` -- starts Loading to prevent auth screen flash on restart
    - `val authState: StateFlow<AuthUiState> = _authState.asStateFlow()`
    - `private val _formState = MutableStateFlow(AuthFormState())`
    - `val formState: StateFlow<AuthFormState> = _formState.asStateFlow()`
    - In `init` block: launch coroutine collecting `observeAuthStateUseCase()`. Map non-null User to `Authenticated(uid, isAnonymous)` and null to `Unauthenticated`. To handle Firebase's initial null before session restoration: use a small delay (300ms) or `drop(1)` approach so a brief null does not flash the auth screen.
    - `fun updateEmail(email: String)` -- updates formState.email, clears error
    - `fun updatePassword(password: String)` -- updates formState.password, clears error
    - `fun updateConfirmPassword(confirmPassword: String)` -- updates formState.confirmPassword
    - `fun clearError()` -- sets formState.errorMessage to null
    - `fun signUp()` -- validates: email not blank, password >= 8 chars, password == confirmPassword. If invalid, set errorMessage and return. Sets isLoading=true, calls signUpUseCase, on failure sets errorMessage, sets isLoading=false.
    - `fun signIn()` -- validates: email not blank, password not blank. Sets isLoading=true, calls signInEmailUseCase, on failure sets errorMessage, sets isLoading=false.
    - `fun signInWithGoogle(idToken: String)` -- sets isLoading=true, calls signInGoogleUseCase, on failure sets errorMessage, sets isLoading=false.
    - `fun continueAsGuest()` -- sets isLoading=true, calls signInAnonymousUseCase, on failure sets errorMessage, sets isLoading=false.
    - `fun signOut()` -- calls repository signOut via ObserveAuthStateUseCase repo or add a SignOutUseCase.

    Create `MainDispatcherRule.kt` test helper:
    ```kotlin
    package com.giftregistry

    import kotlinx.coroutines.Dispatchers
    import kotlinx.coroutines.ExperimentalCoroutinesApi
    import kotlinx.coroutines.test.UnconfinedTestDispatcher
    import kotlinx.coroutines.test.resetMain
    import kotlinx.coroutines.test.setMain
    import org.junit.rules.TestWatcher
    import org.junit.runner.Description

    @OptIn(ExperimentalCoroutinesApi::class)
    class MainDispatcherRule : TestWatcher() {
        val testDispatcher = UnconfinedTestDispatcher()

        override fun starting(description: Description) {
            Dispatchers.setMain(testDispatcher)
        }

        override fun finished(description: Description) {
            Dispatchers.resetMain()
        }
    }
    ```

    Create `AuthViewModelTest.kt` using JUnit 4 + kotlinx-coroutines-test + Turbine:
    - Use `FakeAuthRepository` directly, construct real use cases with the fake repo
    - Use `@get:Rule val mainDispatcherRule = MainDispatcherRule()`
    - 9 test functions covering each behavior above
    - Use Turbine `.test {}` for Flow assertions on authState and formState
  </action>
  <verify>
    <automated>cd /Users/victorpop/ai-projects/gift-registry && ./gradlew testDebugUnitTest --tests "*.AuthViewModelTest" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - AuthUiState.kt contains `sealed interface AuthUiState`
    - AuthUiState.kt contains `data object Loading`
    - AuthUiState.kt contains `data object Unauthenticated`
    - AuthUiState.kt contains `data class Authenticated`
    - AuthUiState.kt contains `data class AuthFormState`
    - AuthViewModel.kt contains `@HiltViewModel`
    - AuthViewModel.kt contains `MutableStateFlow<AuthUiState>(AuthUiState.Loading)`
    - AuthViewModel.kt contains `fun signUp()`
    - AuthViewModel.kt contains `fun signIn()`
    - AuthViewModel.kt contains `fun signInWithGoogle(idToken: String)`
    - AuthViewModel.kt contains `fun continueAsGuest()`
    - AuthViewModel.kt does NOT contain `MutableLiveData`
    - AuthViewModel.kt does NOT contain `import com.google.firebase`
    - AuthViewModelTest.kt contains `FakeAuthRepository`
    - AuthViewModelTest.kt contains at least 5 `@Test` functions
    - MainDispatcherRule.kt contains `Dispatchers.setMain`
    - All tests pass via ./gradlew testDebugUnitTest --tests "*.AuthViewModelTest"
  </acceptance_criteria>
  <done>AuthViewModel manages auth state and form state. All 9 test cases pass using FakeAuthRepository. ViewModel has zero Firebase imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create AuthScreen, Navigation3 with auth gating, wire MainActivity, and add string resources</name>
  <files>
    app/src/main/java/com/giftregistry/ui/auth/AuthScreen.kt,
    app/src/main/java/com/giftregistry/ui/navigation/AppNavKeys.kt,
    app/src/main/java/com/giftregistry/ui/navigation/AppNavigation.kt,
    app/src/main/java/com/giftregistry/MainActivity.kt,
    app/src/main/res/values/strings.xml,
    app/src/main/res/values-ro/strings.xml
  </files>
  <read_first>
    app/src/main/java/com/giftregistry/ui/auth/AuthViewModel.kt (just created -- state and action signatures),
    app/src/main/java/com/giftregistry/ui/auth/AuthUiState.kt (just created -- sealed interface),
    app/src/main/java/com/giftregistry/MainActivity.kt (existing -- needs AppNavigation wiring),
    app/src/main/res/values/strings.xml (existing strings to preserve and expand),
    app/src/main/res/values-ro/strings.xml (existing Romanian strings to preserve and expand),
    .planning/phases/02-android-core-auth/02-UI-SPEC.md (Auth Screen Layout, Component Contract, Copywriting Contract, Spacing Scale, Color, Accessibility Contract),
    .planning/phases/02-android-core-auth/02-RESEARCH.md (Pattern 2: Navigation3 auth gating, Pattern 4: Google Sign-In via Credential Manager, Pitfall 2: Activity context for CredentialManager, Pitfall 5: NavDisplay entryDecorators)
  </read_first>
  <action>
    Create `ui/navigation/AppNavKeys.kt` -- Navigation3 uses @Serializable key objects:
    ```kotlin
    package com.giftregistry.ui.navigation

    import kotlinx.serialization.Serializable

    @Serializable data object AuthKey
    @Serializable data object HomeKey
    @Serializable data object SettingsKey
    ```

    Create `ui/navigation/AppNavigation.kt` following RESEARCH.md Pattern 2:
    - `@Composable fun AppNavigation()`
    - Get `authViewModel: AuthViewModel` via `hiltViewModel()` (requires `hilt-navigation-compose`)
    - Observe `authViewModel.authState.collectAsStateWithLifecycle()` -- NOT `collectAsState()`
    - `val backStack = remember { mutableStateListOf<Any>(AuthKey) }`
    - `LaunchedEffect(authUiState)`:
      - When `AuthUiState.Authenticated` -> clear backStack, add HomeKey
      - When `AuthUiState.Unauthenticated` -> clear backStack, add AuthKey
      - When `AuthUiState.Loading` -> do nothing (show loading indicator)
    - When authUiState is Loading, show centered `CircularProgressIndicator` instead of NavDisplay
    - `NavDisplay(backStack = backStack, onBack = { if (backStack.size > 1) backStack.removeLast() }, entryDecorators = listOf(rememberSaveableStateHolderNavEntryDecorator(), rememberViewModelStoreNavEntryDecorator()), entryProvider = entryProvider { ... })`
    - CRITICAL: Include BOTH decorators -- without `rememberViewModelStoreNavEntryDecorator()`, `hiltViewModel()` breaks (Pitfall 5)
    - Entry for AuthKey: `entry<AuthKey> { AuthScreen() }`
    - Entry for HomeKey: placeholder `Scaffold` with `TopAppBar` showing "Gift Registry" title and settings `IconButton` that adds SettingsKey to backStack. Body shows centered `Text("Home -- Phase 3")`.
    - Entry for SettingsKey: placeholder `Text("Settings -- Plan 04")`. Will be replaced in Plan 04.

    Create `ui/auth/AuthScreen.kt` following UI-SPEC Component Contract:
    - `@Composable fun AuthScreen(viewModel: AuthViewModel = hiltViewModel())`
    - Collect `viewModel.formState.collectAsStateWithLifecycle()`
    - Wrap in `Scaffold(snackbarHost = { SnackbarHost(snackbarHostState) })`
    - Column with `Modifier.fillMaxSize().verticalScroll(rememberScrollState()).padding(horizontal = 16.dp)`:
      1. App title: `Text(stringResource(R.string.app_name), style = headlineLarge)` -- top padding 64.dp, centered, `Modifier.semantics { heading() }` per Accessibility Contract
      2. `TabRow` with 2 tabs: index 0 = stringResource(R.string.auth_sign_in_title), index 1 = stringResource(R.string.auth_sign_up_title). Selected indicator uses `primary` color.
      3. `AnimatedContent(targetState = selectedTabIndex)` for tab switching
      4. **Sign In tab (index 0)**:
         - Email `OutlinedTextField` -- full width, label from `auth_email_label`, value from formState.email, onValueChange calls viewModel.updateEmail()
         - Password `OutlinedTextField` with visibility toggle -- trailing `IconButton` with contentDescription from `auth_password_show`/`auth_password_hide`, toggling `Icons.Default.Visibility` / `Icons.Default.VisibilityOff`
         - 24.dp spacing between fields
         - Error: if formState.errorMessage != null, show in `supportingText` styled with `colorScheme.error`
         - "Sign In" `Button` (filled) -- full width, labelLarge, text from `auth_sign_in_button`. When isLoading, show `CircularProgressIndicator(Modifier.size(24.dp))` instead of text.
         - Spacer 16.dp
         - "OR" divider: Row with HorizontalDivider + Text(auth_or_divider) + HorizontalDivider
         - Spacer 16.dp
         - "Sign in with Google" `OutlinedButton` -- full width, from `auth_google_sign_in_button`
      5. **Create Account tab (index 1)**:
         - Email, Password, Confirm Password OutlinedTextFields (confirm password label from `auth_confirm_password_label`)
         - 24.dp spacing
         - "Create Account" Button from `auth_sign_up_button`
         - Same OR divider + Google button
      6. Spacer 32.dp
      7. "Continue as Guest" TextButton -- centered, labelLarge, primary color, `Modifier.defaultMinSize(minHeight = 44.dp)`, text from `auth_continue_as_guest`
      8. Bottom padding 24.dp

    - Google Sign-In: On tap, get `LocalContext.current` (Activity context per Pitfall 2). Create `CredentialManager.create(context)`. Build `GetGoogleIdOption.Builder().setFilterByAuthorizedAccounts(false).setServerClientId(context.getString(R.string.default_web_client_id)).build()`. Launch in `rememberCoroutineScope`. On success, extract `GoogleIdTokenCredential.createFrom(result.credential.data).idToken`, call `viewModel.signInWithGoogle(idToken)`. On failure, show error via Snackbar. `default_web_client_id` is auto-generated by google-services plugin.

    Update `MainActivity.kt`:
    - Replace placeholder content in `setContent` with `GiftRegistryTheme { AppNavigation() }`
    - Keep `enableEdgeToEdge()` call
    - Add import for `com.giftregistry.ui.navigation.AppNavigation`

    Update `app/src/main/res/values/strings.xml` -- PRESERVE all existing Phase 1 strings, ADD per UI-SPEC:
    ```xml
    <string name="auth_sign_in_button">Sign In</string>
    <string name="auth_sign_up_button">Create Account</string>
    <string name="auth_google_sign_in_button">Sign in with Google</string>
    <string name="auth_or_divider">OR</string>
    <string name="auth_confirm_password_label">Confirm Password</string>
    <string name="auth_password_show">Show password</string>
    <string name="auth_password_hide">Hide password</string>
    <string name="auth_sign_in_no_account_hint">New here? Switch to Create Account to get started.</string>
    <string name="auth_error_invalid_credentials">Incorrect email or password. Check your details and try again.</string>
    <string name="auth_error_email_exists">An account with this email already exists. Try signing in.</string>
    <string name="auth_error_weak_password">Password must be at least 8 characters.</string>
    <string name="auth_error_network">No connection. Check your internet and try again.</string>
    <string name="auth_guest_conversion_title">Save your reservation?</string>
    <string name="auth_guest_conversion_body">Create a free account to track your reservation and receive updates.</string>
    <string name="auth_guest_conversion_create">Create Account</string>
    <string name="auth_guest_conversion_dismiss">Maybe later</string>
    <string name="auth_settings_title">Settings</string>
    <string name="auth_settings_language_label">Language</string>
    <string name="auth_settings_language_english">English</string>
    <string name="auth_settings_language_romanian">Romana</string>
    <string name="settings_language_confirm">Change Language</string>
    ```

    Update `app/src/main/res/values-ro/strings.xml` -- PRESERVE all existing, ADD Romanian:
    ```xml
    <string name="auth_sign_in_button">Autentificare</string>
    <string name="auth_sign_up_button">Creeaz&#259; Cont</string>
    <string name="auth_google_sign_in_button">Autentificare cu Google</string>
    <string name="auth_or_divider">SAU</string>
    <string name="auth_confirm_password_label">Confirm&#259; Parola</string>
    <string name="auth_password_show">Arat&#259; parola</string>
    <string name="auth_password_hide">Ascunde parola</string>
    <string name="auth_sign_in_no_account_hint">Nou aici? Treci la Creeaz&#259; Cont pentru a &#238;ncepe.</string>
    <string name="auth_error_invalid_credentials">Email sau parol&#259; incorecte. Verific&#259; datele &#537;i &#238;ncearc&#259; din nou.</string>
    <string name="auth_error_email_exists">Un cont cu acest email exist&#259; deja. &#206;ncearc&#259; s&#259; te autentifici.</string>
    <string name="auth_error_weak_password">Parola trebuie s&#259; aib&#259; cel pu&#539;in 8 caractere.</string>
    <string name="auth_error_network">Nu exist&#259; conexiune. Verific&#259; internetul &#537;i &#238;ncearc&#259; din nou.</string>
    <string name="auth_guest_conversion_title">Salvezi rezervarea?</string>
    <string name="auth_guest_conversion_body">Creeaz&#259; un cont gratuit pentru a-&#539;i urm&#259;ri rezervarea &#537;i a primi actualiz&#259;ri.</string>
    <string name="auth_guest_conversion_create">Creeaz&#259; Cont</string>
    <string name="auth_guest_conversion_dismiss">Poate mai t&#226;rziu</string>
    <string name="auth_settings_title">Set&#259;ri</string>
    <string name="auth_settings_language_label">Limb&#259;</string>
    <string name="auth_settings_language_english">English</string>
    <string name="auth_settings_language_romanian">Rom&#226;n&#259;</string>
    <string name="settings_language_confirm">Schimb&#259; Limba</string>
    ```
  </action>
  <verify>
    <automated>cd /Users/victorpop/ai-projects/gift-registry && ls app/src/main/java/com/giftregistry/ui/auth/AuthScreen.kt app/src/main/java/com/giftregistry/ui/navigation/AppNavKeys.kt app/src/main/java/com/giftregistry/ui/navigation/AppNavigation.kt && grep -c "auth_sign_in_button\|auth_sign_up_button\|auth_google_sign_in_button\|auth_or_divider\|auth_confirm_password_label\|auth_password_show\|auth_password_hide\|auth_error_invalid_credentials\|auth_guest_conversion_title\|auth_settings_title\|settings_language_confirm" app/src/main/res/values/strings.xml</automated>
  </verify>
  <acceptance_criteria>
    - AuthScreen.kt contains `@Composable fun AuthScreen`
    - AuthScreen.kt contains `TabRow`
    - AuthScreen.kt contains `AnimatedContent`
    - AuthScreen.kt contains `OutlinedTextField` (at least 2)
    - AuthScreen.kt contains `stringResource(R.string.auth_sign_in_button)`
    - AuthScreen.kt contains `stringResource(R.string.auth_continue_as_guest)`
    - AuthScreen.kt contains `CredentialManager`
    - AuthScreen.kt contains `GetGoogleIdOption`
    - AuthScreen.kt contains `GoogleIdTokenCredential`
    - AuthScreen.kt contains `CircularProgressIndicator` for loading state
    - AuthScreen.kt contains `Modifier.defaultMinSize(minHeight = 44.dp)` on guest link
    - AuthScreen.kt contains `semantics` with `heading()`
    - AuthScreen.kt contains `Scaffold` and `SnackbarHost`
    - AppNavKeys.kt contains `@Serializable data object AuthKey`
    - AppNavKeys.kt contains `@Serializable data object HomeKey`
    - AppNavKeys.kt contains `@Serializable data object SettingsKey`
    - AppNavigation.kt contains `NavDisplay`
    - AppNavigation.kt contains `rememberViewModelStoreNavEntryDecorator()`
    - AppNavigation.kt contains `rememberSaveableStateHolderNavEntryDecorator()`
    - AppNavigation.kt contains `collectAsStateWithLifecycle`
    - AppNavigation.kt contains `LaunchedEffect`
    - AppNavigation.kt does NOT contain `collectAsState()` (must use lifecycle-aware)
    - MainActivity.kt contains `AppNavigation()`
    - strings.xml contains all 17 new string keys
    - values-ro/strings.xml contains all 17 new string keys in Romanian
  </acceptance_criteria>
  <done>Auth screen renders with tabbed login/signup, Google sign-in via Credential Manager, guest link. Navigation3 gates on auth state. MainActivity wires AppNavigation. All string resources in English and Romanian.</done>
</task>

</tasks>

<verification>
After both tasks:
```bash
cd /Users/victorpop/ai-projects/gift-registry && ./gradlew testDebugUnitTest --tests "*.AuthViewModelTest" 2>&1 | tail -15
```
Expected: All ViewModel tests pass.
</verification>

<success_criteria>
- AuthViewModel manages auth state transitions (Loading -> Unauthenticated or Authenticated)
- Auth screen matches UI-SPEC layout (tabbed, email/password, Google button, guest link)
- Navigation3 gates auth screen with both entryDecorators
- All 17+ new string resources in English and Romanian
- No LiveData, no KTX imports, no Navigation Compose 2.x
- ViewModel unit tests pass with FakeAuthRepository
- Google Sign-In uses Credential Manager (not deprecated play-services-auth)
</success_criteria>

<output>
After completion, create `.planning/phases/02-android-core-auth/02-03-SUMMARY.md`
</output>
