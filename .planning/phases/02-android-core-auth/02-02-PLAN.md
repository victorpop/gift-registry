---
phase: 02-android-core-auth
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/main/java/com/giftregistry/domain/model/User.kt
  - app/src/main/java/com/giftregistry/domain/model/GuestUser.kt
  - app/src/main/java/com/giftregistry/domain/auth/AuthRepository.kt
  - app/src/main/java/com/giftregistry/domain/auth/SignUpUseCase.kt
  - app/src/main/java/com/giftregistry/domain/auth/SignInEmailUseCase.kt
  - app/src/main/java/com/giftregistry/domain/auth/SignInGoogleUseCase.kt
  - app/src/main/java/com/giftregistry/domain/auth/SignInAnonymousUseCase.kt
  - app/src/main/java/com/giftregistry/domain/auth/LinkAccountUseCase.kt
  - app/src/main/java/com/giftregistry/domain/auth/ObserveAuthStateUseCase.kt
  - app/src/main/java/com/giftregistry/domain/preferences/LanguagePreferencesRepository.kt
  - app/src/main/java/com/giftregistry/data/auth/FirebaseAuthDataSource.kt
  - app/src/main/java/com/giftregistry/data/auth/AuthRepositoryImpl.kt
  - app/src/main/java/com/giftregistry/data/preferences/LanguagePreferencesDataStore.kt
  - app/src/main/java/com/giftregistry/di/DataModule.kt
  - app/src/test/java/com/giftregistry/data/auth/FakeAuthRepository.kt
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-05
  - AUTH-06
must_haves:
  truths:
    - "AuthRepository interface defines all auth operations (signup, signin-email, signin-google, anonymous, link, observe)"
    - "FirebaseAuthDataSource wraps Firebase SDK with no KTX imports"
    - "AuthRepositoryImpl maps FirebaseUser to domain User model"
    - "Use cases provide single-responsibility wrappers with @Inject"
    - "FakeAuthRepository exists for ViewModel unit tests with configurable success/failure"
    - "Hilt DI binds AuthRepository to AuthRepositoryImpl"
  artifacts:
    - path: "app/src/main/java/com/giftregistry/domain/auth/AuthRepository.kt"
      provides: "Auth contract for all operations"
      exports: ["AuthRepository"]
    - path: "app/src/main/java/com/giftregistry/data/auth/AuthRepositoryImpl.kt"
      provides: "Firebase-backed auth implementation"
      contains: "AuthRepositoryImpl"
    - path: "app/src/main/java/com/giftregistry/domain/model/User.kt"
      provides: "Pure Kotlin user model"
      contains: "data class User"
    - path: "app/src/test/java/com/giftregistry/data/auth/FakeAuthRepository.kt"
      provides: "Test double for ViewModel unit tests"
      contains: "FakeAuthRepository"
  key_links:
    - from: "di/DataModule.kt"
      to: "AuthRepository.kt"
      via: "@Binds annotation"
      pattern: "@Binds.*AuthRepository"
    - from: "AuthRepositoryImpl.kt"
      to: "FirebaseAuthDataSource.kt"
      via: "constructor injection"
      pattern: "class AuthRepositoryImpl.*Inject.*FirebaseAuthDataSource"
    - from: "AuthRepositoryImpl.kt"
      to: "User.kt"
      via: "FirebaseUser.toDomain() extension"
      pattern: "toDomain"
---

<objective>
Create the complete auth domain and data layers: repository interface, Firebase data source, repository implementation, all use cases, locale DataStore, and test infrastructure.

Purpose: Establishes the auth contract that all UI code depends on. Separating domain from data ensures testability (per ARCH-01, ARCH-04 decisions).
Output: Domain interfaces, data implementations, use cases, DI bindings, and FakeAuthRepository for testing.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-android-core-auth/02-RESEARCH.md
@.planning/phases/02-android-core-auth/02-CONTEXT.md

@app/src/main/java/com/giftregistry/di/AppModule.kt
@app/src/main/java/com/giftregistry/di/DataModule.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create domain layer -- User model, GuestUser model, AuthRepository interface, LanguagePreferencesRepository interface, and all use cases</name>
  <files>
    app/src/main/java/com/giftregistry/domain/model/User.kt,
    app/src/main/java/com/giftregistry/domain/model/GuestUser.kt,
    app/src/main/java/com/giftregistry/domain/auth/AuthRepository.kt,
    app/src/main/java/com/giftregistry/domain/auth/SignUpUseCase.kt,
    app/src/main/java/com/giftregistry/domain/auth/SignInEmailUseCase.kt,
    app/src/main/java/com/giftregistry/domain/auth/SignInGoogleUseCase.kt,
    app/src/main/java/com/giftregistry/domain/auth/SignInAnonymousUseCase.kt,
    app/src/main/java/com/giftregistry/domain/auth/LinkAccountUseCase.kt,
    app/src/main/java/com/giftregistry/domain/auth/ObserveAuthStateUseCase.kt,
    app/src/main/java/com/giftregistry/domain/preferences/LanguagePreferencesRepository.kt
  </files>
  <read_first>
    .planning/phases/02-android-core-auth/02-RESEARCH.md (Pattern 6 for domain contracts -- AuthRepository interface, AuthState sealed interface, User data class, GuestUser data class),
    .planning/phases/02-android-core-auth/02-CONTEXT.md (ARCH-01: domain holds use cases with zero Firebase imports; ARCH-04: fake repos for ViewModel tests; AUTH-UX-03: guest provides name/email on reservation not at entry),
    app/src/main/java/com/giftregistry/di/AppModule.kt (existing FirebaseAuth provider)
  </read_first>
  <action>
    Create `domain/model/User.kt` -- a pure Kotlin data class with NO Firebase imports:
    ```kotlin
    package com.giftregistry.domain.model

    data class User(
        val uid: String,
        val email: String?,
        val displayName: String?,
        val isAnonymous: Boolean
    )
    ```

    Create `domain/model/GuestUser.kt` -- per AUTH-05, guest provides name and email on reservation action:
    ```kotlin
    package com.giftregistry.domain.model

    data class GuestUser(
        val firstName: String,
        val lastName: String,
        val email: String
    )
    ```

    Create `domain/auth/AuthRepository.kt` interface per RESEARCH.md Pattern 6:
    ```kotlin
    package com.giftregistry.domain.auth

    import com.giftregistry.domain.model.User
    import kotlinx.coroutines.flow.Flow

    interface AuthRepository {
        val authState: Flow<User?>
        val currentUser: User?
        suspend fun signUpWithEmail(email: String, password: String): Result<User>
        suspend fun signInWithEmail(email: String, password: String): Result<User>
        suspend fun signInWithGoogle(idToken: String): Result<User>
        suspend fun signInAnonymously(): Result<User>
        suspend fun linkEmailToAnonymous(email: String, password: String): Result<User>
        fun signOut()
    }
    ```

    Create `domain/preferences/LanguagePreferencesRepository.kt` interface:
    ```kotlin
    package com.giftregistry.domain.preferences

    import kotlinx.coroutines.flow.Flow

    interface LanguagePreferencesRepository {
        fun observeLanguageTag(): Flow<String?>
        suspend fun setLanguageTag(tag: String)
        suspend fun getLanguageTag(): String?
    }
    ```

    Create each use case in `domain/auth/` package. Each has `@Inject constructor` taking `AuthRepository`, and a single `operator fun invoke(...)`:

    `SignUpUseCase.kt` (per AUTH-01):
    ```kotlin
    package com.giftregistry.domain.auth
    import com.giftregistry.domain.model.User
    import javax.inject.Inject

    class SignUpUseCase @Inject constructor(private val repository: AuthRepository) {
        suspend operator fun invoke(email: String, password: String): Result<User> =
            repository.signUpWithEmail(email, password)
    }
    ```

    `SignInEmailUseCase.kt` (per AUTH-02):
    ```kotlin
    package com.giftregistry.domain.auth
    import com.giftregistry.domain.model.User
    import javax.inject.Inject

    class SignInEmailUseCase @Inject constructor(private val repository: AuthRepository) {
        suspend operator fun invoke(email: String, password: String): Result<User> =
            repository.signInWithEmail(email, password)
    }
    ```

    `SignInGoogleUseCase.kt` (per AUTH-03):
    ```kotlin
    package com.giftregistry.domain.auth
    import com.giftregistry.domain.model.User
    import javax.inject.Inject

    class SignInGoogleUseCase @Inject constructor(private val repository: AuthRepository) {
        suspend operator fun invoke(idToken: String): Result<User> =
            repository.signInWithGoogle(idToken)
    }
    ```

    `SignInAnonymousUseCase.kt` (per AUTH-05):
    ```kotlin
    package com.giftregistry.domain.auth
    import com.giftregistry.domain.model.User
    import javax.inject.Inject

    class SignInAnonymousUseCase @Inject constructor(private val repository: AuthRepository) {
        suspend operator fun invoke(): Result<User> = repository.signInAnonymously()
    }
    ```

    `LinkAccountUseCase.kt` (per AUTH-06):
    ```kotlin
    package com.giftregistry.domain.auth
    import com.giftregistry.domain.model.User
    import javax.inject.Inject

    class LinkAccountUseCase @Inject constructor(private val repository: AuthRepository) {
        suspend operator fun invoke(email: String, password: String): Result<User> =
            repository.linkEmailToAnonymous(email, password)
    }
    ```

    `ObserveAuthStateUseCase.kt` (per AUTH-04):
    ```kotlin
    package com.giftregistry.domain.auth
    import com.giftregistry.domain.model.User
    import kotlinx.coroutines.flow.Flow
    import javax.inject.Inject

    class ObserveAuthStateUseCase @Inject constructor(private val repository: AuthRepository) {
        operator fun invoke(): Flow<User?> = repository.authState
    }
    ```

    CRITICAL: No domain file may contain `import com.google.firebase`.
  </action>
  <verify>
    <automated>cd /Users/victorpop/ai-projects/gift-registry && ls app/src/main/java/com/giftregistry/domain/model/User.kt app/src/main/java/com/giftregistry/domain/model/GuestUser.kt app/src/main/java/com/giftregistry/domain/auth/AuthRepository.kt app/src/main/java/com/giftregistry/domain/auth/SignUpUseCase.kt app/src/main/java/com/giftregistry/domain/auth/SignInEmailUseCase.kt app/src/main/java/com/giftregistry/domain/auth/SignInGoogleUseCase.kt app/src/main/java/com/giftregistry/domain/auth/SignInAnonymousUseCase.kt app/src/main/java/com/giftregistry/domain/auth/LinkAccountUseCase.kt app/src/main/java/com/giftregistry/domain/auth/ObserveAuthStateUseCase.kt app/src/main/java/com/giftregistry/domain/preferences/LanguagePreferencesRepository.kt && grep -rl "com.google.firebase" app/src/main/java/com/giftregistry/domain/ 2>/dev/null; echo "Firebase check done"</automated>
  </verify>
  <acceptance_criteria>
    - User.kt contains `data class User(` with fields uid, email, displayName, isAnonymous
    - User.kt does NOT contain `import com.google.firebase`
    - GuestUser.kt contains `data class GuestUser(` with fields firstName, lastName, email
    - AuthRepository.kt contains `interface AuthRepository`
    - AuthRepository.kt contains `val authState: Flow<User?>`
    - AuthRepository.kt contains `suspend fun signUpWithEmail(email: String, password: String): Result<User>`
    - AuthRepository.kt contains `suspend fun signInWithEmail(email: String, password: String): Result<User>`
    - AuthRepository.kt contains `suspend fun signInWithGoogle(idToken: String): Result<User>`
    - AuthRepository.kt contains `suspend fun signInAnonymously(): Result<User>`
    - AuthRepository.kt contains `suspend fun linkEmailToAnonymous(email: String, password: String): Result<User>`
    - AuthRepository.kt contains `fun signOut()`
    - LanguagePreferencesRepository.kt contains `interface LanguagePreferencesRepository`
    - LanguagePreferencesRepository.kt contains `fun observeLanguageTag(): Flow<String?>`
    - LanguagePreferencesRepository.kt contains `suspend fun getLanguageTag(): String?`
    - SignUpUseCase.kt contains `@Inject constructor` and `operator fun invoke`
    - SignInEmailUseCase.kt contains `@Inject constructor` and `operator fun invoke`
    - SignInGoogleUseCase.kt contains `@Inject constructor` and `operator fun invoke`
    - SignInAnonymousUseCase.kt contains `@Inject constructor` and `operator fun invoke`
    - LinkAccountUseCase.kt contains `@Inject constructor` and `operator fun invoke`
    - ObserveAuthStateUseCase.kt contains `@Inject constructor` and `operator fun invoke`
    - No file under domain/ contains `import com.google.firebase`
  </acceptance_criteria>
  <done>Domain layer complete: User model, GuestUser model, AuthRepository interface, LanguagePreferencesRepository interface, and 6 use cases -- all with correct signatures, Hilt injection, and zero Firebase imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create data layer -- FirebaseAuthDataSource, AuthRepositoryImpl, LanguagePreferencesDataStore, DI bindings, and FakeAuthRepository</name>
  <files>
    app/src/main/java/com/giftregistry/data/auth/FirebaseAuthDataSource.kt,
    app/src/main/java/com/giftregistry/data/auth/AuthRepositoryImpl.kt,
    app/src/main/java/com/giftregistry/data/preferences/LanguagePreferencesDataStore.kt,
    app/src/main/java/com/giftregistry/di/DataModule.kt,
    app/src/test/java/com/giftregistry/data/auth/FakeAuthRepository.kt
  </files>
  <read_first>
    app/src/main/java/com/giftregistry/domain/auth/AuthRepository.kt (just created -- interface to implement),
    app/src/main/java/com/giftregistry/domain/model/User.kt (just created -- model to map to),
    app/src/main/java/com/giftregistry/domain/preferences/LanguagePreferencesRepository.kt (just created -- interface to implement),
    app/src/main/java/com/giftregistry/di/AppModule.kt (existing -- FirebaseAuth provider),
    app/src/main/java/com/giftregistry/di/DataModule.kt (existing placeholder -- will be overwritten),
    .planning/phases/02-android-core-auth/02-RESEARCH.md (Pattern 4 for Google Sign-In via Credential Manager, DataStore Language Preference code example, Pitfall 6 about KTX)
  </read_first>
  <action>
    Create `data/auth/FirebaseAuthDataSource.kt`:
    ```kotlin
    package com.giftregistry.data.auth

    import com.google.firebase.auth.AuthResult
    import com.google.firebase.auth.EmailAuthProvider
    import com.google.firebase.auth.FirebaseAuth
    import com.google.firebase.auth.FirebaseUser
    import com.google.firebase.auth.GoogleAuthProvider
    import kotlinx.coroutines.channels.awaitClose
    import kotlinx.coroutines.flow.Flow
    import kotlinx.coroutines.flow.callbackFlow
    import kotlinx.coroutines.tasks.await
    import javax.inject.Inject
    import javax.inject.Singleton

    @Singleton
    class FirebaseAuthDataSource @Inject constructor(
        private val firebaseAuth: FirebaseAuth
    ) {
        val authStateFlow: Flow<FirebaseUser?> = callbackFlow {
            val listener = FirebaseAuth.AuthStateListener { auth ->
                trySend(auth.currentUser)
            }
            firebaseAuth.addAuthStateListener(listener)
            awaitClose { firebaseAuth.removeAuthStateListener(listener) }
        }

        val currentUser: FirebaseUser? get() = firebaseAuth.currentUser

        suspend fun createUserWithEmail(email: String, password: String): AuthResult =
            firebaseAuth.createUserWithEmailAndPassword(email, password).await()

        suspend fun signInWithEmail(email: String, password: String): AuthResult =
            firebaseAuth.signInWithEmailAndPassword(email, password).await()

        suspend fun signInWithGoogleCredential(idToken: String): AuthResult {
            val credential = GoogleAuthProvider.getCredential(idToken, null)
            return firebaseAuth.signInWithCredential(credential).await()
        }

        suspend fun signInAnonymously(): AuthResult =
            firebaseAuth.signInAnonymously().await()

        suspend fun linkWithEmail(email: String, password: String): AuthResult {
            val credential = EmailAuthProvider.getCredential(email, password)
            return firebaseAuth.currentUser!!.linkWithCredential(credential).await()
        }

        fun signOut() = firebaseAuth.signOut()
    }
    ```
    CRITICAL: Import `com.google.firebase.auth.FirebaseAuth` -- NOT `com.google.firebase.auth.ktx`. KTX modules removed in BoM 34.0.0.

    Create `data/auth/AuthRepositoryImpl.kt`:
    ```kotlin
    package com.giftregistry.data.auth

    import com.giftregistry.domain.auth.AuthRepository
    import com.giftregistry.domain.model.User
    import com.google.firebase.auth.FirebaseUser
    import kotlinx.coroutines.flow.Flow
    import kotlinx.coroutines.flow.map
    import javax.inject.Inject
    import javax.inject.Singleton

    @Singleton
    class AuthRepositoryImpl @Inject constructor(
        private val dataSource: FirebaseAuthDataSource
    ) : AuthRepository {

        override val authState: Flow<User?> = dataSource.authStateFlow.map { it?.toDomain() }

        override val currentUser: User? get() = dataSource.currentUser?.toDomain()

        override suspend fun signUpWithEmail(email: String, password: String): Result<User> =
            runCatching { dataSource.createUserWithEmail(email, password).user!!.toDomain() }

        override suspend fun signInWithEmail(email: String, password: String): Result<User> =
            runCatching { dataSource.signInWithEmail(email, password).user!!.toDomain() }

        override suspend fun signInWithGoogle(idToken: String): Result<User> =
            runCatching { dataSource.signInWithGoogleCredential(idToken).user!!.toDomain() }

        override suspend fun signInAnonymously(): Result<User> =
            runCatching { dataSource.signInAnonymously().user!!.toDomain() }

        override suspend fun linkEmailToAnonymous(email: String, password: String): Result<User> =
            runCatching { dataSource.linkWithEmail(email, password).user!!.toDomain() }

        override fun signOut() = dataSource.signOut()

        private fun FirebaseUser.toDomain(): User = User(
            uid = uid,
            email = email,
            displayName = displayName,
            isAnonymous = isAnonymous
        )
    }
    ```

    Create `data/preferences/LanguagePreferencesDataStore.kt` per RESEARCH.md DataStore example:
    ```kotlin
    package com.giftregistry.data.preferences

    import android.content.Context
    import androidx.datastore.core.DataStore
    import androidx.datastore.preferences.core.Preferences
    import androidx.datastore.preferences.core.edit
    import androidx.datastore.preferences.core.stringPreferencesKey
    import androidx.datastore.preferences.preferencesDataStore
    import com.giftregistry.domain.preferences.LanguagePreferencesRepository
    import dagger.hilt.android.qualifiers.ApplicationContext
    import kotlinx.coroutines.flow.Flow
    import kotlinx.coroutines.flow.first
    import kotlinx.coroutines.flow.map
    import javax.inject.Inject
    import javax.inject.Singleton

    private val Context.dataStore: DataStore<Preferences> by preferencesDataStore(name = "user_prefs")

    @Singleton
    class LanguagePreferencesDataStore @Inject constructor(
        @ApplicationContext private val context: Context
    ) : LanguagePreferencesRepository {

        private val languageTagKey = stringPreferencesKey("language_tag")

        override fun observeLanguageTag(): Flow<String?> =
            context.dataStore.data.map { prefs -> prefs[languageTagKey] }

        override suspend fun setLanguageTag(tag: String) {
            context.dataStore.edit { prefs -> prefs[languageTagKey] = tag }
        }

        override suspend fun getLanguageTag(): String? =
            context.dataStore.data.first()[languageTagKey]
    }
    ```

    Update `di/DataModule.kt` to bind both repositories:
    ```kotlin
    package com.giftregistry.di

    import com.giftregistry.data.auth.AuthRepositoryImpl
    import com.giftregistry.data.preferences.LanguagePreferencesDataStore
    import com.giftregistry.domain.auth.AuthRepository
    import com.giftregistry.domain.preferences.LanguagePreferencesRepository
    import dagger.Binds
    import dagger.Module
    import dagger.hilt.InstallIn
    import dagger.hilt.components.SingletonComponent
    import javax.inject.Singleton

    @Module
    @InstallIn(SingletonComponent::class)
    abstract class DataModule {

        @Binds
        @Singleton
        abstract fun bindAuthRepository(impl: AuthRepositoryImpl): AuthRepository

        @Binds
        @Singleton
        abstract fun bindLanguagePreferences(impl: LanguagePreferencesDataStore): LanguagePreferencesRepository
    }
    ```

    Create `app/src/test/java/com/giftregistry/data/auth/FakeAuthRepository.kt` per ARCH-04:
    ```kotlin
    package com.giftregistry.data.auth

    import com.giftregistry.domain.auth.AuthRepository
    import com.giftregistry.domain.model.User
    import kotlinx.coroutines.flow.Flow
    import kotlinx.coroutines.flow.MutableStateFlow

    class FakeAuthRepository : AuthRepository {
        private val _authState = MutableStateFlow<User?>(null)
        override val authState: Flow<User?> = _authState
        override val currentUser: User? get() = _authState.value

        var shouldFail = false
        var failureException: Exception = Exception("Auth failed")

        override suspend fun signUpWithEmail(email: String, password: String): Result<User> =
            fakeResult(User(uid = "fake-uid", email = email, displayName = null, isAnonymous = false))

        override suspend fun signInWithEmail(email: String, password: String): Result<User> =
            fakeResult(User(uid = "fake-uid", email = email, displayName = null, isAnonymous = false))

        override suspend fun signInWithGoogle(idToken: String): Result<User> =
            fakeResult(User(uid = "google-uid", email = "google@test.com", displayName = "Test User", isAnonymous = false))

        override suspend fun signInAnonymously(): Result<User> =
            fakeResult(User(uid = "anon-uid", email = null, displayName = null, isAnonymous = true))

        override suspend fun linkEmailToAnonymous(email: String, password: String): Result<User> =
            fakeResult(User(uid = "anon-uid", email = email, displayName = null, isAnonymous = false))

        override fun signOut() { _authState.value = null }

        fun emitUser(user: User?) { _authState.value = user }

        private fun <T> fakeResult(value: T): Result<T> =
            if (shouldFail) Result.failure(failureException)
            else Result.success(value).also { if (value is User) _authState.value = value }
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/victorpop/ai-projects/gift-registry && ls app/src/main/java/com/giftregistry/data/auth/FirebaseAuthDataSource.kt app/src/main/java/com/giftregistry/data/auth/AuthRepositoryImpl.kt app/src/main/java/com/giftregistry/data/preferences/LanguagePreferencesDataStore.kt app/src/main/java/com/giftregistry/di/DataModule.kt app/src/test/java/com/giftregistry/data/auth/FakeAuthRepository.kt && grep -rl "com.google.firebase.auth.ktx" app/src/main/java/com/giftregistry/ 2>/dev/null; echo "KTX check done"</automated>
  </verify>
  <acceptance_criteria>
    - FirebaseAuthDataSource.kt contains `@Singleton` and `@Inject constructor`
    - FirebaseAuthDataSource.kt contains `callbackFlow` for auth state listener
    - FirebaseAuthDataSource.kt contains `GoogleAuthProvider.getCredential(idToken, null)`
    - FirebaseAuthDataSource.kt contains `linkWithCredential`
    - FirebaseAuthDataSource.kt does NOT contain `import com.google.firebase.auth.ktx`
    - AuthRepositoryImpl.kt contains `class AuthRepositoryImpl @Inject constructor`
    - AuthRepositoryImpl.kt contains `: AuthRepository`
    - AuthRepositoryImpl.kt contains `fun FirebaseUser.toDomain(): User`
    - AuthRepositoryImpl.kt contains `runCatching`
    - LanguagePreferencesDataStore.kt contains `preferencesDataStore(name = "user_prefs")`
    - LanguagePreferencesDataStore.kt contains `stringPreferencesKey("language_tag")`
    - LanguagePreferencesDataStore.kt contains `@ApplicationContext`
    - LanguagePreferencesDataStore.kt contains `: LanguagePreferencesRepository`
    - DataModule.kt contains `@Binds` and `abstract fun bindAuthRepository`
    - DataModule.kt contains `AuthRepositoryImpl): AuthRepository`
    - DataModule.kt contains `abstract fun bindLanguagePreferences`
    - DataModule.kt contains `LanguagePreferencesDataStore): LanguagePreferencesRepository`
    - FakeAuthRepository.kt contains `class FakeAuthRepository : AuthRepository`
    - FakeAuthRepository.kt contains `var shouldFail = false`
    - FakeAuthRepository.kt contains `MutableStateFlow<User?>(null)`
    - FakeAuthRepository.kt contains `fun emitUser(user: User?)`
  </acceptance_criteria>
  <done>Data layer complete: FirebaseAuthDataSource wraps Firebase SDK, AuthRepositoryImpl maps to domain User, LanguagePreferencesDataStore persists locale, DI binds interfaces to implementations, FakeAuthRepository enables ViewModel unit tests.</done>
</task>

</tasks>

<verification>
After both tasks:
```bash
cd /Users/victorpop/ai-projects/gift-registry && grep -rl "com.google.firebase" app/src/main/java/com/giftregistry/domain/ 2>/dev/null && echo "FAIL: Firebase in domain" || echo "PASS: Domain is Firebase-free"
grep -rl "firebase.auth.ktx" app/src/main/java/com/giftregistry/ 2>/dev/null && echo "FAIL: KTX imports found" || echo "PASS: No KTX imports"
```
</verification>

<success_criteria>
- Domain layer has zero Firebase imports (pure Kotlin)
- AuthRepository covers all auth operations needed (signup, signin-email, signin-google, anonymous, link, observe)
- FirebaseAuthDataSource wraps Firebase SDK through data layer boundary
- AuthRepositoryImpl maps FirebaseUser to domain User via toDomain() extension
- LanguagePreferencesDataStore persists locale via DataStore
- DI binds both AuthRepository and LanguagePreferencesRepository
- FakeAuthRepository supports configurable success/failure for ViewModel tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-android-core-auth/02-02-SUMMARY.md`
</output>
