---
phase: 01-firebase-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - firestore.rules
  - tests/rules/firestore.rules.test.ts
  - tests/rules/package.json
  - tests/rules/tsconfig.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Public registries are readable by unauthenticated users"
    - "Private registries are readable only by owner and invited users"
    - "Reservations collection denies all direct client reads and writes"
    - "Items subcollection is writable only by the registry owner"
    - "Users can only read and write their own user document"
    - "Invited users (in invitedUsers map) can read private registries"
  artifacts:
    - path: "firestore.rules"
      provides: "Complete Firestore security rules with all access patterns"
      contains: "canReadRegistry"
      min_lines: 40
    - path: "tests/rules/firestore.rules.test.ts"
      provides: "Automated security rules tests for all 4 required scenarios"
      contains: "assertSucceeds"
      min_lines: 80
    - path: "tests/rules/package.json"
      provides: "Test dependencies"
      contains: "@firebase/rules-unit-testing"
  key_links:
    - from: "tests/rules/firestore.rules.test.ts"
      to: "firestore.rules"
      via: "fs.readFileSync loads rules into test environment"
      pattern: "readFileSync.*firestore.rules"
    - from: "tests/rules/firestore.rules.test.ts"
      to: "Firebase Emulator Suite"
      via: "initializeTestEnvironment connects to emulator on port 8080"
      pattern: "port: 8080"
---

<objective>
Write complete Firestore security rules and automated tests covering all four required access patterns: public registry read, private registry owner-only read, guest reservation write denial, and owner-only item write.

Purpose: Security rules are the sole access control boundary for Firestore. If rules are wrong, all subsequent phases build on an insecure foundation. Testing rules now prevents silent access control failures in Phases 2-7.
Output: firestore.rules (complete), tests/rules/ directory with automated tests.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-firebase-foundation/01-RESEARCH.md
@.planning/phases/01-firebase-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create security rules test scaffold</name>
  <read_first>
    - .planning/phases/01-firebase-foundation/01-RESEARCH.md (Pattern 2: security rules tests with @firebase/rules-unit-testing v5)
  </read_first>
  <files>tests/rules/package.json, tests/rules/tsconfig.json, tests/rules/firestore.rules.test.ts</files>
  <behavior>
    - Test: Public registry allows unauthenticated read (assertSucceeds on getDoc for visibility="public" registry)
    - Test: Private registry denies non-owner non-invited read (assertFails on getDoc for visibility="private" registry by other-user)
    - Test: Private registry allows owner read (assertSucceeds on getDoc by owner UID)
    - Test: Private registry allows invited user read (assertSucceeds on getDoc by UID in invitedUsers map)
    - Test: Reservation collection denies all direct client writes (assertFails on setDoc to reservations/ by any user)
    - Test: Reservation collection denies all direct client reads (assertFails on getDoc from reservations/ by any user)
    - Test: Items subcollection denies non-owner write (assertFails on setDoc to registries/{id}/items/{id} by non-owner)
    - Test: Items subcollection allows owner write (assertSucceeds on setDoc to registries/{id}/items/{id} by owner)
    - Test: Users collection allows user to read own document (assertSucceeds on getDoc users/{ownUid})
    - Test: Users collection denies reading other user's document (assertFails on getDoc users/{otherUid})
    - Test: Registry create requires auth and matching ownerId (assertFails if ownerId != auth.uid)
  </behavior>
  <action>
**1. Create `tests/rules/package.json`:**
```json
{
  "name": "gift-registry-rules-tests",
  "private": true,
  "scripts": {
    "test": "jest --forceExit --detectOpenHandles",
    "test:emulator": "firebase emulators:exec --only firestore,auth \"cd tests/rules && npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.7.0",
    "@types/jest": "^29.5.0"
  }
}
```

**2. Create `tests/rules/tsconfig.json`:**
```json
{
  "compilerOptions": {
    "target": "es2022",
    "module": "commonjs",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "lib",
    "rootDir": "."
  },
  "include": ["."]
}
```

**3. Create `tests/rules/jest.config.js`:**
```javascript
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  testTimeout: 30000,
};
```

**4. Create `tests/rules/firestore.rules.test.ts`** with ALL test scenarios from the research. The test file must:

- Import from `@firebase/rules-unit-testing`: `assertFails`, `assertSucceeds`, `initializeTestEnvironment`, `RulesTestEnvironment`
- Import from `firebase/firestore`: `doc`, `getDoc`, `setDoc`, `deleteDoc`
- Read rules file via `fs.readFileSync("../../firestore.rules", "utf8")`
- Connect to emulator at host `127.0.0.1`, port `8080`
- Use `testEnv.withSecurityRulesDisabled()` to seed test data
- Call `testEnv.clearFirestore()` in `afterEach`
- Call `testEnv.cleanup()` in `afterAll`

**Test groups (11 tests across 6 describe blocks):**

**describe("Public registry read"):**
- Seed: registries/pub1 with `{ ownerId: "owner1", visibility: "public", title: "Public", invitedUsers: {} }`
- Test: unauthenticated context `assertSucceeds(getDoc(doc(db, "registries/pub1")))`

**describe("Private registry access"):**
- Seed: registries/priv1 with `{ ownerId: "owner2", visibility: "private", title: "Private", invitedUsers: { "invited-user": true } }`
- Test: non-owner non-invited `assertFails(getDoc(...))`  (auth context "random-user")
- Test: owner reads own `assertSucceeds(getDoc(...))` (auth context "owner2")
- Test: invited user reads `assertSucceeds(getDoc(...))` (auth context "invited-user")

**describe("Reservation collection"):**
- Test: unauthenticated `assertFails(setDoc(doc(db, "reservations/res1"), {...}))`
- Test: authenticated `assertFails(getDoc(doc(db, "reservations/res1")))` (auth context "any-user")

**describe("Items subcollection"):**
- Seed: registries/pub1 (ownerId: "owner1", visibility: "public", invitedUsers: {})
- Test: non-owner `assertFails(setDoc(doc(db, "registries/pub1/items/item1"), {...}))` (auth "other-user")
- Test: owner `assertSucceeds(setDoc(doc(db, "registries/pub1/items/item1"), {...}))` (auth "owner1")

**describe("Users collection"):**
- Seed: users/user1 with `{ email: "test@example.com", displayName: "Test" }`
- Test: own doc read `assertSucceeds(getDoc(doc(db, "users/user1")))` (auth "user1")
- Test: other's doc read `assertFails(getDoc(doc(db, "users/user1")))` (auth "user2")

**describe("Registry creation"):**
- Test: create with matching ownerId `assertSucceeds(setDoc(doc(db, "registries/new1"), { ownerId: "creator1", ... }))` (auth "creator1")
- Test: create with mismatched ownerId `assertFails(setDoc(doc(db, "registries/new2"), { ownerId: "someone-else", ... }))` (auth "creator1")

After creating files, install dependencies:
```bash
cd tests/rules && npm install
```
  </action>
  <verify>
    <automated>cd tests/rules && npx tsc --noEmit && echo "TESTS COMPILE"</automated>
  </verify>
  <acceptance_criteria>
    - tests/rules/package.json contains `"@firebase/rules-unit-testing": "^5.0.0"`
    - tests/rules/package.json contains `"firebase": "^12.11.0"`
    - tests/rules/package.json contains `"jest"`
    - tests/rules/firestore.rules.test.ts contains `assertSucceeds`
    - tests/rules/firestore.rules.test.ts contains `assertFails`
    - tests/rules/firestore.rules.test.ts contains `initializeTestEnvironment`
    - tests/rules/firestore.rules.test.ts contains `readFileSync`
    - tests/rules/firestore.rules.test.ts contains `port: 8080`
    - tests/rules/firestore.rules.test.ts contains `describe("Public registry read"`
    - tests/rules/firestore.rules.test.ts contains `describe("Private registry access"`
    - tests/rules/firestore.rules.test.ts contains `describe("Reservation collection"`
    - tests/rules/firestore.rules.test.ts contains `describe("Items subcollection"`
    - tests/rules/firestore.rules.test.ts contains `describe("Users collection"`
    - tests/rules/firestore.rules.test.ts contains `describe("Registry creation"`
    - `cd tests/rules && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Test scaffold exists with 11+ tests across 6 describe blocks, all tests compile, dependencies installed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write complete Firestore security rules (RED then GREEN)</name>
  <read_first>
    - firestore.rules (current stub from Plan 01)
    - tests/rules/firestore.rules.test.ts (tests from Task 1)
    - .planning/phases/01-firebase-foundation/01-RESEARCH.md (Pattern 1: security rules, schema with invitedUsers map)
  </read_first>
  <files>firestore.rules</files>
  <behavior>
    - RED: Run tests against the stub rules (all deny) — tests that expect assertFails should pass, tests that expect assertSucceeds should fail
    - GREEN: Write complete rules so ALL tests pass
  </behavior>
  <action>
**Step 1 (RED):** Run the tests against the current stub firestore.rules (which denies everything). Verify that at least the assertFails tests pass and assertSucceeds tests fail. This confirms tests are correctly wired.

```bash
firebase emulators:exec --only firestore,auth "cd tests/rules && npm test" || true
```

**Step 2 (GREEN):** Replace `firestore.rules` with the complete security rules:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(registryData) {
      return isSignedIn() && request.auth.uid == registryData.ownerId;
    }

    function isPublicRegistry(registryData) {
      return registryData.visibility == 'public';
    }

    function isInvited(registryData) {
      return isSignedIn() &&
             registryData.visibility == 'private' &&
             registryData.invitedUsers[request.auth.uid] == true;
    }

    function canReadRegistry(registryData) {
      return isPublicRegistry(registryData) ||
             isOwner(registryData) ||
             isInvited(registryData);
    }

    // Registries collection
    match /registries/{registryId} {
      allow read: if canReadRegistry(resource.data);
      allow create: if isSignedIn() && request.resource.data.ownerId == request.auth.uid;
      allow update, delete: if isOwner(resource.data);

      // Items subcollection — inherits registry visibility for reads
      match /items/{itemId} {
        allow read: if canReadRegistry(
          get(/databases/$(database)/documents/registries/$(registryId)).data
        );
        allow create, update, delete: if isOwner(
          get(/databases/$(database)/documents/registries/$(registryId)).data
        );
      }

      // Invites subcollection — owner-only read/write
      match /invites/{inviteId} {
        allow read, write: if isOwner(
          get(/databases/$(database)/documents/registries/$(registryId)).data
        );
      }
    }

    // Reservations — Cloud Functions write only (Admin SDK bypasses rules)
    match /reservations/{reservationId} {
      allow read, write: if false;
    }

    // Users — owner reads/writes their own document only
    match /users/{userId} {
      allow read, update: if isSignedIn() && request.auth.uid == userId;
      allow create: if isSignedIn() && request.auth.uid == userId;
      allow delete: if false;
    }
  }
}
```

**Step 3:** Run all tests again — ALL must pass:

```bash
firebase emulators:exec --only firestore,auth "cd tests/rules && npm test"
```

If any test fails, fix the rules (not the tests) and re-run until all pass.
  </action>
  <verify>
    <automated>firebase emulators:exec --only firestore,auth "cd tests/rules && npm test"</automated>
  </verify>
  <acceptance_criteria>
    - firestore.rules contains `rules_version = '2'`
    - firestore.rules contains `function isSignedIn()`
    - firestore.rules contains `function isOwner(registryData)`
    - firestore.rules contains `function isPublicRegistry(registryData)`
    - firestore.rules contains `function isInvited(registryData)`
    - firestore.rules contains `function canReadRegistry(registryData)`
    - firestore.rules contains `match /registries/{registryId}`
    - firestore.rules contains `match /items/{itemId}`
    - firestore.rules contains `match /invites/{inviteId}`
    - firestore.rules contains `match /reservations/{reservationId}`
    - firestore.rules contains `match /users/{userId}`
    - firestore.rules contains `allow read, write: if false` (reservations)
    - firestore.rules contains `allow delete: if false` (users)
    - firestore.rules contains `request.resource.data.ownerId == request.auth.uid` (registry create)
    - firestore.rules contains `invitedUsers[request.auth.uid] == true`
    - `firebase emulators:exec --only firestore,auth "cd tests/rules && npm test"` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 11+ security rules tests pass against the complete firestore.rules. Public registry read, private owner-only read, invited user read, reservation denial, owner-only item write, user document isolation all verified.</done>
</task>

</tasks>

<verification>
- `firebase emulators:exec --only firestore,auth "cd tests/rules && npm test"` — all tests pass
- `firestore.rules` contains all 5 helper functions and all 5 collection match blocks
- Tests cover all 4 required scenarios from Phase 1 Success Criteria #3
</verification>

<success_criteria>
- Security rules enforce: public registry unauthenticated read, private registry owner+invited only, reservation client denial, owner-only item write, user document isolation
- All test scenarios pass against Firebase Emulator Suite
- TDD cycle completed: RED (stub rules fail) then GREEN (complete rules pass)
</success_criteria>

<output>
After completion, create `.planning/phases/01-firebase-foundation/01-02-SUMMARY.md`
</output>
