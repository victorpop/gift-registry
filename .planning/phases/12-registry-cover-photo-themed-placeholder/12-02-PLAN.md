---
phase: 12-registry-cover-photo-themed-placeholder
plan: 02
type: execute
wave: 1
depends_on:
  - 12-01
files_modified:
  - app/src/main/java/com/giftregistry/data/model/RegistryDto.kt
  - app/src/main/java/com/giftregistry/data/registry/RegistryRepositoryImpl.kt
  - app/src/main/java/com/giftregistry/data/storage/StorageDataSource.kt
  - app/src/main/java/com/giftregistry/data/storage/StorageRepositoryImpl.kt
  - app/src/main/java/com/giftregistry/data/storage/CoverImageProcessorImpl.kt
  - app/src/main/java/com/giftregistry/di/StorageModule.kt
  - app/src/main/java/com/giftregistry/di/DataModule.kt
  - app/src/main/java/com/giftregistry/ui/registry/cover/PresetCatalog.kt
  - app/src/main/res/drawable-xxhdpi/preset_housewarming_1.jpg
  - app/src/main/res/drawable-xxhdpi/preset_housewarming_2.jpg
  - app/src/main/res/drawable-xxhdpi/preset_housewarming_3.jpg
  - app/src/main/res/drawable-xxhdpi/preset_housewarming_4.jpg
  - app/src/main/res/drawable-xxhdpi/preset_housewarming_5.jpg
  - app/src/main/res/drawable-xxhdpi/preset_housewarming_6.jpg
  - app/src/main/res/drawable-xxhdpi/preset_wedding_1.jpg
  - app/src/main/res/drawable-xxhdpi/preset_wedding_2.jpg
  - app/src/main/res/drawable-xxhdpi/preset_wedding_3.jpg
  - app/src/main/res/drawable-xxhdpi/preset_wedding_4.jpg
  - app/src/main/res/drawable-xxhdpi/preset_wedding_5.jpg
  - app/src/main/res/drawable-xxhdpi/preset_wedding_6.jpg
  - app/src/main/res/drawable-xxhdpi/preset_baby_1.jpg
  - app/src/main/res/drawable-xxhdpi/preset_baby_2.jpg
  - app/src/main/res/drawable-xxhdpi/preset_baby_3.jpg
  - app/src/main/res/drawable-xxhdpi/preset_baby_4.jpg
  - app/src/main/res/drawable-xxhdpi/preset_baby_5.jpg
  - app/src/main/res/drawable-xxhdpi/preset_baby_6.jpg
  - app/src/main/res/drawable-xxhdpi/preset_birthday_1.jpg
  - app/src/main/res/drawable-xxhdpi/preset_birthday_2.jpg
  - app/src/main/res/drawable-xxhdpi/preset_birthday_3.jpg
  - app/src/main/res/drawable-xxhdpi/preset_birthday_4.jpg
  - app/src/main/res/drawable-xxhdpi/preset_birthday_5.jpg
  - app/src/main/res/drawable-xxhdpi/preset_birthday_6.jpg
  - app/src/main/res/drawable-xxhdpi/preset_christmas_1.jpg
  - app/src/main/res/drawable-xxhdpi/preset_christmas_2.jpg
  - app/src/main/res/drawable-xxhdpi/preset_christmas_3.jpg
  - app/src/main/res/drawable-xxhdpi/preset_christmas_4.jpg
  - app/src/main/res/drawable-xxhdpi/preset_christmas_5.jpg
  - app/src/main/res/drawable-xxhdpi/preset_christmas_6.jpg
  - app/src/main/res/drawable-xxhdpi/preset_custom_1.jpg
  - app/src/main/res/drawable-xxhdpi/preset_custom_2.jpg
  - app/src/main/res/drawable-xxhdpi/preset_custom_3.jpg
  - app/src/main/res/drawable-xxhdpi/preset_custom_4.jpg
  - app/src/main/res/drawable-xxhdpi/preset_custom_5.jpg
  - app/src/main/res/drawable-xxhdpi/preset_custom_6.jpg
  - storage.rules
  - firebase.json
autonomous: true
requirements:
  - D-02
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
user_setup:
  - service: firebase-storage
    why: "Cover photo upload + cross-service rules require Storage enabled in Firebase Console; first-time deploy prompts cross-service permission grant"
    env_vars: []
    dashboard_config:
      - task: "Confirm Storage enabled for project gift-registry-ro"
        location: "Firebase Console -> Storage -> Get Started (auto-provisioned with project; verify default bucket exists)"
      - task: "Accept cross-service permissions prompt on first `firebase deploy --only storage`"
        location: "Firebase CLI / Console — one-time grant linking Storage Rules with Firestore reads"
requirements_traceability: |
  This plan flips the following Wave 0 tests from RED to GREEN:
  - PresetCatalogTest (D-02, D-05) — drawables + lookup table populated
  - RegistryRepositoryImplCoverTest (Pitfall 1) — RegistryDto.imageUrl + toMap/toUpdateMap include the field
  - StorageRepositoryImplTest (D-04, D-05, D-07) — real impl using putBytes + downloadUrl
  - CoverImageProcessorTest (D-06) — real impl using two-pass BitmapFactory + JPEG compress

must_haves:
  truths:
    - "RegistryDto round-trips imageUrl through Firestore (no data loss)"
    - "RegistryRepositoryImpl.toMap and toUpdateMap include imageUrl"
    - "StorageRepository.uploadCover writes bytes to /users/{uid}/registries/{registryId}/cover.jpg and returns the public download URL"
    - "PresetCatalog.resolve('preset:Wedding:3') returns a non-null R.drawable.* Int"
    - "PresetCatalog.presetsFor('Wedding') returns 6 drawable IDs"
    - "Wave 0 RED tests for D-02/05/06/07 + Pitfall 1 are now GREEN"
    - "storage.rules is wired in firebase.json AND deployable via firebase deploy --only storage"
    - "36 placeholder JPEGs ship in drawable-xxhdpi/ — APK builds with no missing-resource errors"
  artifacts:
    - path: "app/src/main/java/com/giftregistry/data/model/RegistryDto.kt"
      provides: "imageUrl field (Pitfall 1 fix)"
      contains: "val imageUrl: String? = null"
    - path: "app/src/main/java/com/giftregistry/data/registry/RegistryRepositoryImpl.kt"
      provides: "toMap + toUpdateMap include imageUrl key + toDomain copies imageUrl through (Pitfall 1 fix)"
    - path: "app/src/main/java/com/giftregistry/data/storage/StorageRepositoryImpl.kt"
      provides: "Real upload impl — putBytes + downloadUrl roundtrip via runCatching"
    - path: "app/src/main/java/com/giftregistry/data/storage/CoverImageProcessorImpl.kt"
      provides: "Two-pass BitmapFactory.decodeAndCompress to 1280x720 JPEG q=85"
    - path: "app/src/main/java/com/giftregistry/di/StorageModule.kt"
      provides: "@Provides FirebaseStorage with emulator wiring + StorageRepository binding"
    - path: "app/src/main/java/com/giftregistry/ui/registry/cover/PresetCatalog.kt"
      provides: "byOccasion map, presetsFor, encode, resolve — populated"
    - path: "storage.rules"
      provides: "Cross-service Storage rules: write=owner, read=owner|public|invited"
    - path: "firebase.json"
      provides: "storage.rules wired at top-level (not just emulator)"
    - path: "app/src/main/res/drawable-xxhdpi/preset_*.jpg (36 files)"
      provides: "Bundled per-occasion JPEGs (placeholder set acceptable for Wave 1 per CONTEXT D-02)"
  key_links:
    - from: "data/registry/RegistryRepositoryImpl.kt::toMap"
      to: "data/registry/RegistryRepositoryImpl.kt::toUpdateMap"
      via: "shared imageUrl field key"
      pattern: "\"imageUrl\" to imageUrl"
    - from: "data/storage/StorageRepositoryImpl.kt"
      to: "domain/storage/StorageRepository.kt"
      via: "interface implementation"
      pattern: "class StorageRepositoryImpl @Inject .* : StorageRepository"
    - from: "di/StorageModule.kt"
      to: "data/storage/StorageRepositoryImpl.kt"
      via: "@Provides FirebaseStorage + @Binds StorageRepository"
      pattern: "FirebaseStorage|bindStorageRepository"
    - from: "storage.rules"
      to: "firestore (cross-service)"
      via: "firestore.get(/databases/(default)/documents/registries/$(registryId))"
      pattern: "firestore\\.get"
---

<objective>
Ship the data + storage layer for Phase 12: fix the imageUrl persistence bug (Pitfall 1), wire `firebase-storage` end-to-end, populate `PresetCatalog` with 36 bundled drawables, and author the cross-service `storage.rules` file.

Purpose: Flip 4 of the 8 Wave 0 RED suites GREEN (PresetCatalog, RegistryRepositoryImplCover, StorageRepositoryImpl, CoverImageProcessor). After this plan ships, Plan 03 (UI primitives) and Plan 04 (ViewModel + screens) have a working data foundation to build against.

Output: Real PresetCatalog + 36 JPEGs + StorageDataSource + StorageRepositoryImpl + CoverImageProcessorImpl + StorageModule (Hilt) + storage.rules + firebase.json wiring + RegistryDto/RegistryRepositoryImpl bug fix.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-CONTEXT.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-RESEARCH.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-VALIDATION.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-01-PLAN.md
@.planning/phases/01-firebase-foundation/01-02-PLAN.md
@CLAUDE.md
@firestore.rules

<interfaces>
<!-- Symbols Plan 02 must produce or consume. Embedded so executor needs no codebase scavenger hunt. -->

From app/src/main/java/com/giftregistry/data/registry/RegistryRepositoryImpl.kt (CURRENT — Pitfall 1):
```kotlin
private fun Registry.toMap(): Map<String, Any?> = mapOf(
    "ownerId" to ownerId, "title" to title, "occasion" to occasion,
    "visibility" to visibility, "eventDateMs" to eventDateMs,
    "eventLocation" to eventLocation, "description" to description,
    "locale" to locale, "notificationsEnabled" to notificationsEnabled,
    "invitedUsers" to invitedUsers.ifEmpty { emptyMap<String, Boolean>() },
    "createdAt" to System.currentTimeMillis(), "updatedAt" to System.currentTimeMillis()
)
private fun Registry.toUpdateMap(): Map<String, Any?> = mapOf(
    "title" to title, "occasion" to occasion, "visibility" to visibility,
    "eventDateMs" to eventDateMs, "eventLocation" to eventLocation,
    "description" to description, "notificationsEnabled" to notificationsEnabled,
    "updatedAt" to System.currentTimeMillis()
)
// BUG: imageUrl missing from BOTH maps; toDomain doesn't propagate it either.
```

From app/src/main/java/com/giftregistry/data/model/RegistryDto.kt (CURRENT):
```kotlin
data class RegistryDto(
    val id: String = "",
    val ownerId: String = "",
    val title: String = "",
    val occasion: String = "",
    val visibility: String = "public",
    val eventDateMs: Long? = null,
    val eventLocation: String? = null,
    val description: String? = null,
    val locale: String = "en",
    val notificationsEnabled: Boolean = true,
    val invitedUsers: Map<String, Any?> = emptyMap(),
    val createdAt: Long = 0L,
    val updatedAt: Long = 0L
    // BUG: no imageUrl field — Firestore reads silently strip it.
)
```

From RESEARCH.md Pattern 6 — storage.rules cross-service template (USE VERBATIM, just inline into the new file):
```
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function registryDoc(registryId) {
      return firestore.get(/databases/(default)/documents/registries/$(registryId));
    }
    function isOwnerOfRegistry(registryId) {
      return request.auth != null
          && registryDoc(registryId).data.ownerId == request.auth.uid;
    }
    function isPublicOrInvited(registryId) {
      let data = registryDoc(registryId).data;
      return data.get('visibility', 'public') == 'public'
          || (request.auth != null
              && data.get('invitedUsers', {})[request.auth.uid] == true);
    }
    match /users/{uid}/registries/{registryId}/cover.jpg {
      allow write: if request.auth != null
                   && request.auth.uid == uid
                   && isOwnerOfRegistry(registryId);
      allow read:  if isOwnerOfRegistry(registryId)
                   || isPublicOrInvited(registryId);
    }
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
```

From firestore.rules (CURRENT — defaults pattern that storage.rules MUST mirror):
- Legacy docs predate `visibility`/`invitedUsers` — defaults `'public'` and `{}` keep them readable. storage.rules uses the SAME defaults via `data.get('visibility', 'public')`.

From RESEARCH.md Pattern 2 — decodeAndCompressForCover (USE VERBATIM in CoverImageProcessorImpl):
- Two-pass BitmapFactory: bounds-only with `inJustDecodeBounds=true`, then sampled decode.
- Power-of-2 inSampleSize loop.
- Final fit to 1280×720 via `Bitmap.createScaledBitmap` preserving aspect.
- `Bitmap.compress(JPEG, 85, ByteArrayOutputStream)` — return `ByteArray`.
- Run on `Dispatchers.IO` — never main thread.

From RESEARCH.md Pattern 3 — StorageRepositoryImpl (USE VERBATIM, except inject the StorageDataSource if you split):
```kotlin
override suspend fun uploadCover(uid: String, registryId: String, jpegBytes: ByteArray): Result<String> = runCatching {
    val ref = storage.reference.child("users/$uid/registries/$registryId/cover.jpg")
    ref.putBytes(jpegBytes).await()
    ref.downloadUrl.await().toString()
}
```

From RESEARCH.md StorageModule template:
```kotlin
@Provides @Singleton
fun provideFirebaseStorage(): FirebaseStorage {
    val instance = FirebaseStorage.getInstance()
    if (BuildConfig.USE_FIREBASE_EMULATOR) {
        instance.useEmulator("10.0.2.2", 9199)
    }
    return instance
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Pitfall 1 — add imageUrl to RegistryDto + RegistryRepositoryImpl mappers (flips RegistryRepositoryImplCoverTest GREEN)</name>
  <files>
    app/src/main/java/com/giftregistry/data/model/RegistryDto.kt,
    app/src/main/java/com/giftregistry/data/registry/RegistryRepositoryImpl.kt
  </files>
  <read_first>
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-RESEARCH.md (Pitfall 1 — exact line numbers + reproduction)
    - app/src/main/java/com/giftregistry/data/model/RegistryDto.kt (current state — read fully before editing)
    - app/src/main/java/com/giftregistry/data/registry/RegistryRepositoryImpl.kt (current state — toMap/toUpdateMap/toDomain at lines 57-89)
    - app/src/test/java/com/giftregistry/data/registry/RegistryRepositoryImplCoverTest.kt (Wave 0 RED tests this task must flip GREEN)
    - app/src/main/java/com/giftregistry/domain/model/Registry.kt (already has `imageUrl: String? = null`)
  </read_first>
  <action>
    1. In `RegistryDto.kt`, add `val imageUrl: String? = null,` between `notificationsEnabled` and `invitedUsers` (alphabetical-ish; preserves field grouping). Final shape:
       ```kotlin
       data class RegistryDto(
           val id: String = "",
           val ownerId: String = "",
           val title: String = "",
           val occasion: String = "",
           val visibility: String = "public",
           val eventDateMs: Long? = null,
           val eventLocation: String? = null,
           val description: String? = null,
           val locale: String = "en",
           val notificationsEnabled: Boolean = true,
           val imageUrl: String? = null,           // NEW (Phase 12 Pitfall 1 fix; Plan 02 Task 1)
           val invitedUsers: Map<String, Any?> = emptyMap(),
           val createdAt: Long = 0L,
           val updatedAt: Long = 0L,
       )
       ```
    2. In `RegistryRepositoryImpl.kt`:
       a) In `toDomain()` (line 57), add `imageUrl = imageUrl,` between `notificationsEnabled` and `invitedUsers`:
          ```kotlin
          notificationsEnabled = notificationsEnabled,
          imageUrl = imageUrl,                                 // NEW
          invitedUsers = invitedUsers.mapValues { ... },
          ```
       b) In `toMap()` (line 75), add `"imageUrl" to imageUrl,` between `notificationsEnabled` and `invitedUsers`:
          ```kotlin
          "locale" to locale, "notificationsEnabled" to notificationsEnabled,
          "imageUrl" to imageUrl,                              // NEW (Pitfall 1 fix)
          "invitedUsers" to invitedUsers.ifEmpty { emptyMap<String, Boolean>() },
          ```
       c) In `toUpdateMap()` (line 84), add `"imageUrl" to imageUrl,` after `notificationsEnabled`:
          ```kotlin
          "description" to description, "notificationsEnabled" to notificationsEnabled,
          "imageUrl" to imageUrl,                              // NEW (Pitfall 1 fix)
          "updatedAt" to System.currentTimeMillis(),
          ```
    3. NO changes to `FirestoreDataSource` (it serializes the map verbatim — adding a key in the map automatically writes the field).
    4. Confirm by reading the file diff: `imageUrl` appears EXACTLY 4 times in `RegistryRepositoryImpl.kt` (1 in toDomain, 1 each in toMap & toUpdateMap key + value pair, plus the existing 0 baseline). And `imageUrl` appears EXACTLY 1 time in `RegistryDto.kt`.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests 'com.giftregistry.data.registry.RegistryRepositoryImplCoverTest' -x lint -x lintAnalyzeDebug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"imageUrl"' app/src/main/java/com/giftregistry/data/registry/RegistryRepositoryImpl.kt` returns >= 2 (one in each map)
    - `grep -c 'imageUrl =' app/src/main/java/com/giftregistry/data/registry/RegistryRepositoryImpl.kt` returns >= 1 (toDomain copy-through)
    - `grep -c 'val imageUrl: String' app/src/main/java/com/giftregistry/data/model/RegistryDto.kt` returns 1
    - `./gradlew :app:testDebugUnitTest --tests 'com.giftregistry.data.registry.RegistryRepositoryImplCoverTest' -x lint` exits 0 (all 4 RegistryRepositoryImplCoverTest cases pass)
    - `./gradlew :app:testDebugUnitTest --tests 'com.giftregistry.data.registry.*' -x lint` exits 0 (no regression on existing RegistryRepositoryImpl tests)
  </acceptance_criteria>
  <done>RegistryDto + RegistryRepositoryImpl roundtrip imageUrl through createRegistry, updateRegistry, and observeRegistry. RegistryRepositoryImplCoverTest is GREEN. No regressions in existing data-layer tests.</done>
</task>

<task type="auto">
  <name>Task 2: Ship 36 placeholder JPEGs + populate PresetCatalog (flips PresetCatalogTest + ResolveImageModelTest GREEN)</name>
  <files>
    app/src/main/res/drawable-xxhdpi/preset_housewarming_1.jpg,
    app/src/main/res/drawable-xxhdpi/preset_housewarming_2.jpg,
    app/src/main/res/drawable-xxhdpi/preset_housewarming_3.jpg,
    app/src/main/res/drawable-xxhdpi/preset_housewarming_4.jpg,
    app/src/main/res/drawable-xxhdpi/preset_housewarming_5.jpg,
    app/src/main/res/drawable-xxhdpi/preset_housewarming_6.jpg,
    app/src/main/res/drawable-xxhdpi/preset_wedding_1.jpg,
    app/src/main/res/drawable-xxhdpi/preset_wedding_2.jpg,
    app/src/main/res/drawable-xxhdpi/preset_wedding_3.jpg,
    app/src/main/res/drawable-xxhdpi/preset_wedding_4.jpg,
    app/src/main/res/drawable-xxhdpi/preset_wedding_5.jpg,
    app/src/main/res/drawable-xxhdpi/preset_wedding_6.jpg,
    app/src/main/res/drawable-xxhdpi/preset_baby_1.jpg,
    app/src/main/res/drawable-xxhdpi/preset_baby_2.jpg,
    app/src/main/res/drawable-xxhdpi/preset_baby_3.jpg,
    app/src/main/res/drawable-xxhdpi/preset_baby_4.jpg,
    app/src/main/res/drawable-xxhdpi/preset_baby_5.jpg,
    app/src/main/res/drawable-xxhdpi/preset_baby_6.jpg,
    app/src/main/res/drawable-xxhdpi/preset_birthday_1.jpg,
    app/src/main/res/drawable-xxhdpi/preset_birthday_2.jpg,
    app/src/main/res/drawable-xxhdpi/preset_birthday_3.jpg,
    app/src/main/res/drawable-xxhdpi/preset_birthday_4.jpg,
    app/src/main/res/drawable-xxhdpi/preset_birthday_5.jpg,
    app/src/main/res/drawable-xxhdpi/preset_birthday_6.jpg,
    app/src/main/res/drawable-xxhdpi/preset_christmas_1.jpg,
    app/src/main/res/drawable-xxhdpi/preset_christmas_2.jpg,
    app/src/main/res/drawable-xxhdpi/preset_christmas_3.jpg,
    app/src/main/res/drawable-xxhdpi/preset_christmas_4.jpg,
    app/src/main/res/drawable-xxhdpi/preset_christmas_5.jpg,
    app/src/main/res/drawable-xxhdpi/preset_christmas_6.jpg,
    app/src/main/res/drawable-xxhdpi/preset_custom_1.jpg,
    app/src/main/res/drawable-xxhdpi/preset_custom_2.jpg,
    app/src/main/res/drawable-xxhdpi/preset_custom_3.jpg,
    app/src/main/res/drawable-xxhdpi/preset_custom_4.jpg,
    app/src/main/res/drawable-xxhdpi/preset_custom_5.jpg,
    app/src/main/res/drawable-xxhdpi/preset_custom_6.jpg,
    app/src/main/java/com/giftregistry/ui/registry/cover/PresetCatalog.kt,
    app/src/main/java/com/giftregistry/ui/registry/cover/ResolveImageModel.kt,
    .planning/todos/pending/2026-04-27-curate-real-preset-jpegs-for-phase-12.md
  </files>
  <read_first>
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-CONTEXT.md (D-02 — placeholder filler images explicitly accepted for Wave 1)
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-RESEARCH.md (Pattern 4 + Pattern 5 — encode/resolve/presetsFor; Open Question 1 web-fallback degradation accepted)
    - app/src/main/java/com/giftregistry/ui/registry/cover/PresetCatalog.kt (Wave 0 stub — replace body)
    - app/src/main/java/com/giftregistry/ui/registry/create/OccasionCatalog.kt (storageKey set MUST match: Housewarming, Wedding, Baby, Birthday, Christmas, Custom)
    - app/src/test/java/com/giftregistry/ui/registry/cover/PresetCatalogTest.kt (assertions to satisfy)
    - app/src/test/java/com/giftregistry/ui/registry/cover/ResolveImageModelTest.kt (assertions to satisfy)
    - .planning/phases/07-romanian-store-browser/07-00-PLAN.md (precedent for shipping placeholder bundled assets — Phase 7 store logos)
  </read_first>
  <action>
    1. Generate 36 placeholder JPEGs (1280×720 each) using a deterministic per-occasion solid colour from the GiftMaison palette + the occasion glyph + index number rendered in white. Acceptable strategies (executor picks the least friction):
       - **Recommended:** use ImageMagick (`convert -size 1280x720 xc:'#E07A4F' -gravity center -font 'Helvetica' -pointsize 240 -fill white label:'⌂\n1' preset_housewarming_1.jpg`). The accent colour for Housewarming is approximately `#E07A4F`; for the other 5 occasions any GiftMaison-toned solid is acceptable for the placeholder set.
       - **Fallback:** copy a 1×1 transparent JPEG via `printf '\xff\xd8\xff\xe0\x00\x10JFIF\x00\x01\x01\x00\x00\x01\x00\x01\x00\x00\xff\xdb...\xff\xd9' > preset_housewarming_1.jpg` (Phase 7 used this strategy for store_*.webp logos — see 07-00-PLAN.md).
       Per-occasion suggested fill colour:
       - Housewarming: `#E07A4F` (accent)
       - Wedding: `#9C9477` (second)
       - Baby: `#D9CABF` (paperDeep — desaturated)
       - Birthday: `#E07A4F` (accent)
       - Christmas: `#5A4F40` (ink)
       - Custom: `#9C9477` (second)
       Each file MUST be a valid JPEG that BitmapFactory can decode (Coil 3 reads from `R.drawable.*` Int via the resources system, so any valid JPEG works). File size target: < 50 KB each (placeholders) — avoid bloating the APK.
    2. Replace `PresetCatalog.kt` Wave 0 stub with the full Pattern 5 implementation from RESEARCH.md (lines 351–384). Concrete final shape:
       ```kotlin
       package com.giftregistry.ui.registry.cover
       import com.giftregistry.R
       import com.giftregistry.ui.registry.create.OccasionCatalog

       object PresetCatalog {
           private val byOccasion: Map<String, List<Int>> = mapOf(
               "Housewarming" to listOf(
                   R.drawable.preset_housewarming_1, R.drawable.preset_housewarming_2,
                   R.drawable.preset_housewarming_3, R.drawable.preset_housewarming_4,
                   R.drawable.preset_housewarming_5, R.drawable.preset_housewarming_6,
               ),
               "Wedding" to listOf(
                   R.drawable.preset_wedding_1, R.drawable.preset_wedding_2,
                   R.drawable.preset_wedding_3, R.drawable.preset_wedding_4,
                   R.drawable.preset_wedding_5, R.drawable.preset_wedding_6,
               ),
               "Baby" to listOf(
                   R.drawable.preset_baby_1, R.drawable.preset_baby_2,
                   R.drawable.preset_baby_3, R.drawable.preset_baby_4,
                   R.drawable.preset_baby_5, R.drawable.preset_baby_6,
               ),
               "Birthday" to listOf(
                   R.drawable.preset_birthday_1, R.drawable.preset_birthday_2,
                   R.drawable.preset_birthday_3, R.drawable.preset_birthday_4,
                   R.drawable.preset_birthday_5, R.drawable.preset_birthday_6,
               ),
               "Christmas" to listOf(
                   R.drawable.preset_christmas_1, R.drawable.preset_christmas_2,
                   R.drawable.preset_christmas_3, R.drawable.preset_christmas_4,
                   R.drawable.preset_christmas_5, R.drawable.preset_christmas_6,
               ),
               "Custom" to listOf(
                   R.drawable.preset_custom_1, R.drawable.preset_custom_2,
                   R.drawable.preset_custom_3, R.drawable.preset_custom_4,
                   R.drawable.preset_custom_5, R.drawable.preset_custom_6,
               ),
           )

           /** 6 preset drawable IDs for [occasion]; canonicalised via OccasionCatalog. (D-02, D-11) */
           fun presetsFor(occasion: String?): List<Int> =
               byOccasion[OccasionCatalog.storageKeyFor(occasion)] ?: emptyList()

           /** Encodes (occasion, 1-based index) -> "preset:Wedding:3". (D-05) */
           fun encode(occasion: String, index1Based: Int): String = "preset:$occasion:$index1Based"

           /** Resolves "preset:Wedding:3" back to its R.drawable.* Int; null if malformed. (D-05) */
           fun resolve(sentinel: String): Int? {
               if (!sentinel.startsWith("preset:")) return null
               val parts = sentinel.removePrefix("preset:").split(":")
               if (parts.size != 2) return null
               val (occasion, indexStr) = parts
               val idx = indexStr.toIntOrNull() ?: return null
               val list = byOccasion[OccasionCatalog.storageKeyFor(occasion)] ?: return null
               return list.getOrNull(idx - 1)
           }
       }
       ```
    3. Replace `ResolveImageModel.kt` Wave 0 stub with:
       ```kotlin
       package com.giftregistry.ui.registry.cover

       /** Resolves a Registry.imageUrl into a Coil-friendly model. (RESEARCH.md Pattern 4) */
       fun resolveImageModel(imageUrl: String?): Any? = when {
           imageUrl == null -> null
           imageUrl.startsWith("preset:") -> PresetCatalog.resolve(imageUrl)
           else -> imageUrl
       }
       ```
    4. Drop a follow-up todo: create `.planning/todos/pending/2026-04-27-curate-real-preset-jpegs-for-phase-12.md` with body explaining placeholder JPEGs ship in Wave 1 and 36 stock-licensed 1280×720 owner-flow images (6 per occasion) need curating before v1.1 GA. Cite CONTEXT.md D-02 acceptance.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests 'com.giftregistry.ui.registry.cover.PresetCatalogTest' --tests 'com.giftregistry.ui.registry.cover.ResolveImageModelTest' -x lint -x lintAnalyzeDebug</automated>
  </verify>
  <acceptance_criteria>
    - `ls app/src/main/res/drawable-xxhdpi/preset_*.jpg | wc -l` returns 36
    - `find app/src/main/res/drawable-xxhdpi -name 'preset_*.jpg' -size 0c | wc -l` returns 0 (no zero-byte files)
    - `file app/src/main/res/drawable-xxhdpi/preset_housewarming_1.jpg | grep -c JPEG` returns >= 1 (each file is a valid JPEG)
    - `grep -c 'R.drawable.preset_' app/src/main/java/com/giftregistry/ui/registry/cover/PresetCatalog.kt` returns 36
    - `grep -c 'PresetCatalog.resolve' app/src/main/java/com/giftregistry/ui/registry/cover/ResolveImageModel.kt` returns 1
    - `./gradlew :app:assembleDebug -x lint -x lintAnalyzeDebug` exits 0 (all 36 drawable references resolve at compile-time)
    - `./gradlew :app:testDebugUnitTest --tests 'com.giftregistry.ui.registry.cover.PresetCatalogTest' --tests 'com.giftregistry.ui.registry.cover.ResolveImageModelTest' -x lint` exits 0 (all RED tests now GREEN)
    - File exists: `.planning/todos/pending/2026-04-27-curate-real-preset-jpegs-for-phase-12.md`
  </acceptance_criteria>
  <done>36 valid JPEG drawables; PresetCatalog populated; ResolveImageModel routes preset/URL/null correctly; PresetCatalogTest + ResolveImageModelTest are GREEN; APK builds without missing-resource errors; curation follow-up todo logged.</done>
</task>

<task type="auto">
  <name>Task 3: Ship StorageDataSource + StorageRepositoryImpl + CoverImageProcessorImpl + StorageModule (flips StorageRepositoryImplTest GREEN; CoverImageProcessorTest stays @Ignored)</name>
  <files>
    app/src/main/java/com/giftregistry/data/storage/StorageDataSource.kt,
    app/src/main/java/com/giftregistry/data/storage/StorageRepositoryImpl.kt,
    app/src/main/java/com/giftregistry/data/storage/CoverImageProcessorImpl.kt,
    app/src/main/java/com/giftregistry/di/StorageModule.kt,
    app/src/main/java/com/giftregistry/di/DataModule.kt
  </files>
  <read_first>
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-RESEARCH.md (Pattern 2: decodeAndCompressForCover; Pattern 3: putBytes + downloadUrl + runCatching; StorageModule template; Pitfall 3 URI-revoked-on-process-death; "Don't `await()` uploadTask inside a transaction" anti-pattern)
    - app/src/main/java/com/giftregistry/data/auth/FirebaseAuthDataSource.kt (existing callbackFlow + await pattern reference)
    - app/src/main/java/com/giftregistry/data/reservation/ReservationRepositoryImpl.kt (existing runCatching + tasks.await pattern reference)
    - app/src/main/java/com/giftregistry/di/DataModule.kt (current binding pattern — copy the Binds idiom for StorageRepository)
    - app/src/main/java/com/giftregistry/di/StoresModule.kt (canonical example of a separate Hilt module — copy structure)
    - app/src/main/java/com/giftregistry/domain/storage/StorageRepository.kt (Plan 01 stub — implement against this interface)
    - app/src/main/java/com/giftregistry/data/storage/StorageRepositoryImpl.kt (Plan 01 empty stub — replace body; do NOT change @Singleton/@Inject)
    - app/src/main/java/com/giftregistry/data/storage/CoverImageProcessor.kt (Plan 01 interface — implement)
  </read_first>
  <action>
    1. Create `StorageDataSource.kt` — thin wrapper around `FirebaseStorage` so StorageRepositoryImpl is unit-testable without mocking `StorageReference` directly. Optional split — RESEARCH.md says either direct (Pattern 3) or via DataSource is acceptable. Use direct injection for simplicity:
       ```kotlin
       package com.giftregistry.data.storage
       import com.google.firebase.storage.FirebaseStorage
       import javax.inject.Inject
       import javax.inject.Singleton

       /**
        * Thin abstraction over FirebaseStorage so StorageRepositoryImpl is mockable.
        * (RESEARCH.md Pattern 3)
        */
       @Singleton
       class StorageDataSource @Inject constructor(
           private val storage: FirebaseStorage,
       ) {
           suspend fun uploadCoverBytes(uid: String, registryId: String, jpegBytes: ByteArray): String {
               val ref = storage.reference.child("users/$uid/registries/$registryId/cover.jpg")
               ref.putBytes(jpegBytes).await()
               return ref.downloadUrl.await().toString()
           }
       }
       ```
       Use `import kotlinx.coroutines.tasks.await` (already on classpath via firebase main modules).
    2. Replace the Plan 01 stub `StorageRepositoryImpl.kt` body with the real impl:
       ```kotlin
       package com.giftregistry.data.storage
       import com.giftregistry.domain.storage.StorageRepository
       import javax.inject.Inject
       import javax.inject.Singleton

       @Singleton
       class StorageRepositoryImpl @Inject constructor(
           private val dataSource: StorageDataSource,
       ) : StorageRepository {
           override suspend fun uploadCover(
               uid: String,
               registryId: String,
               jpegBytes: ByteArray,
           ): Result<String> = runCatching {
               dataSource.uploadCoverBytes(uid, registryId, jpegBytes)
           }
       }
       ```
       NOTE: Update StorageRepositoryImplTest in `app/src/test/java/com/giftregistry/data/storage/StorageRepositoryImplTest.kt` if necessary so it mocks `StorageDataSource` instead of FirebaseStorage directly — this is a test refactor, not a contract change. The test still asserts the same three behaviours (happy/failure/path). If the test was authored to mock `FirebaseStorage`, refactor it to mock `StorageDataSource.uploadCoverBytes` instead. Path schema assertion: verify the dataSource is called with `uid="user1", registryId="reg1"` and that the dataSource's internal child(...) path matches `users/user1/registries/reg1/cover.jpg` — for that, also write a `StorageDataSourceTest.kt` (NEW) that mocks `FirebaseStorage` directly and slot-captures the `child(path)` argument.
    3. Create `CoverImageProcessorImpl.kt` — verbatim Pattern 2 with `Dispatchers.IO`:
       ```kotlin
       package com.giftregistry.data.storage
       import android.content.ContentResolver
       import android.graphics.Bitmap
       import android.graphics.BitmapFactory
       import android.net.Uri
       import kotlinx.coroutines.Dispatchers
       import kotlinx.coroutines.withContext
       import java.io.ByteArrayOutputStream
       import java.io.IOException
       import javax.inject.Inject
       import javax.inject.Singleton

       /**
        * Two-pass BitmapFactory decode + downscale to 1280x720 + JPEG q=85.
        * Runs on Dispatchers.IO. (D-06; RESEARCH.md Pattern 2)
        *
        * Pitfall 3 caveat: callers must not persist the source Uri across process death.
        */
       @Singleton
       class CoverImageProcessorImpl @Inject constructor(
           private val contentResolver: ContentResolver,
       ) : CoverImageProcessor {
           override suspend fun compress(uri: Uri): ByteArray = withContext(Dispatchers.IO) {
               val bounds = BitmapFactory.Options().apply { inJustDecodeBounds = true }
               contentResolver.openInputStream(uri)?.use {
                   BitmapFactory.decodeStream(it, null, bounds)
               } ?: throw IOException("Cannot read URI: $uri")

               var sample = 1
               while (
                   bounds.outWidth / (sample * 2) >= TARGET_WIDTH ||
                   bounds.outHeight / (sample * 2) >= TARGET_HEIGHT
               ) sample *= 2

               val decoded = contentResolver.openInputStream(uri)?.use {
                   BitmapFactory.decodeStream(
                       it, null,
                       BitmapFactory.Options().apply { inSampleSize = sample },
                   )
               } ?: throw IOException("Cannot decode URI: $uri")

               val scaled = decoded.scaleToFit(TARGET_WIDTH, TARGET_HEIGHT)
               if (scaled !== decoded) decoded.recycle()

               ByteArrayOutputStream().also {
                   scaled.compress(Bitmap.CompressFormat.JPEG, JPEG_QUALITY, it)
                   scaled.recycle()
               }.toByteArray()
           }

           private fun Bitmap.scaleToFit(maxW: Int, maxH: Int): Bitmap {
               if (width <= maxW && height <= maxH) return this
               val scale = minOf(maxW.toFloat() / width, maxH.toFloat() / height)
               return Bitmap.createScaledBitmap(this, (width * scale).toInt(), (height * scale).toInt(), true)
           }

           companion object {
               internal const val TARGET_WIDTH = 1280
               internal const val TARGET_HEIGHT = 720
               internal const val JPEG_QUALITY = 85
           }
       }
       ```
       The ContentResolver is provided by Hilt via `@ApplicationContext context.contentResolver`. Add this `@Provides` to StorageModule (Task 3.4).
    4. Create `StorageModule.kt` — provides `FirebaseStorage` (with emulator wiring), `ContentResolver`, and binds `StorageRepository` + `CoverImageProcessor`:
       ```kotlin
       package com.giftregistry.di
       import android.content.ContentResolver
       import android.content.Context
       import com.giftregistry.BuildConfig
       import com.giftregistry.data.storage.CoverImageProcessor
       import com.giftregistry.data.storage.CoverImageProcessorImpl
       import com.giftregistry.data.storage.StorageRepositoryImpl
       import com.giftregistry.domain.storage.StorageRepository
       import com.google.firebase.storage.FirebaseStorage
       import dagger.Binds
       import dagger.Module
       import dagger.Provides
       import dagger.hilt.InstallIn
       import dagger.hilt.android.qualifiers.ApplicationContext
       import dagger.hilt.components.SingletonComponent
       import javax.inject.Singleton

       @Module
       @InstallIn(SingletonComponent::class)
       abstract class StorageModule {

           @Binds
           @Singleton
           abstract fun bindStorageRepository(impl: StorageRepositoryImpl): StorageRepository

           @Binds
           @Singleton
           abstract fun bindCoverImageProcessor(impl: CoverImageProcessorImpl): CoverImageProcessor

           companion object {
               @Provides
               @Singleton
               fun provideFirebaseStorage(): FirebaseStorage {
                   val instance = FirebaseStorage.getInstance()
                   if (BuildConfig.USE_FIREBASE_EMULATOR) {
                       instance.useEmulator("10.0.2.2", 9199)
                   }
                   return instance
               }

               @Provides
               fun provideContentResolver(@ApplicationContext context: Context): ContentResolver =
                   context.contentResolver
           }
       }
       ```
       Place this file alongside the other modules in `di/` (sibling to `DataModule.kt`, `StoresModule.kt`).
    5. Do NOT add `StorageRepository` binding to `DataModule.kt` — keep it in the dedicated `StorageModule` (mirrors Phase 7's `StoresModule` precedent — see STATE.md Phase 7 entry "StoresModule created as separate @Module"). DataModule remains untouched in this task.
       NOTE: The frontmatter `files_modified` lists `DataModule.kt` defensively in case the executor finds a binding conflict. If conflict-free, leave it untouched.
    6. Verify Hilt graph compiles: `./gradlew :app:kspDebugKotlin -x lint` should succeed. KSP errors here usually mean a missing `@Provides` for `ContentResolver` or a duplicate binding.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug -x lint -x lintAnalyzeDebug && ./gradlew :app:testDebugUnitTest --tests 'com.giftregistry.data.storage.StorageRepositoryImplTest' --tests 'com.giftregistry.data.storage.StorageDataSourceTest' -x lint -x lintAnalyzeDebug</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `app/src/main/java/com/giftregistry/data/storage/StorageDataSource.kt`
    - File exists: `app/src/main/java/com/giftregistry/data/storage/StorageRepositoryImpl.kt` (replaced from Plan 01 stub)
    - File exists: `app/src/main/java/com/giftregistry/data/storage/CoverImageProcessorImpl.kt`
    - File exists: `app/src/main/java/com/giftregistry/di/StorageModule.kt`
    - `grep -c 'firebase-storage-ktx\|firebase\.storage\.ktx' app/src/main/java/com/giftregistry/data/storage/*.kt app/src/main/java/com/giftregistry/di/StorageModule.kt` returns 0 (CLAUDE.md NO-KTX rule)
    - `grep -c 'runCatching' app/src/main/java/com/giftregistry/data/storage/StorageRepositoryImpl.kt` returns 1 (Phase 02 D-08 pattern)
    - `grep -c 'useEmulator.*9199' app/src/main/java/com/giftregistry/di/StorageModule.kt` returns 1 (storage emulator wiring)
    - `grep -c 'inJustDecodeBounds\|inSampleSize\|JPEG, 85' app/src/main/java/com/giftregistry/data/storage/CoverImageProcessorImpl.kt` returns >= 3 (Pattern 2 fidelity)
    - `./gradlew :app:assembleDebug -x lint` exits 0 (Hilt graph + Kotlin compile succeed)
    - `./gradlew :app:testDebugUnitTest --tests 'com.giftregistry.data.storage.*' -x lint` exits 0 (StorageRepositoryImplTest GREEN; CoverImageProcessorTest @Ignored is fine)
  </acceptance_criteria>
  <done>FirebaseStorage wired with emulator support; StorageRepository is bound; uploadCover roundtrips bytes -> download URL via runCatching; CoverImageProcessor compiles (instrumented test left as Plan-05 follow-up); APK assembles cleanly.</done>
</task>

<task type="auto">
  <name>Task 4: Author storage.rules + wire into firebase.json</name>
  <files>
    storage.rules,
    firebase.json
  </files>
  <read_first>
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-RESEARCH.md (Pattern 6: cross-service Storage rules; Pitfall 5: Firestore-read cost; Open Question 2 emulator wiring)
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-CONTEXT.md (D-08 baseline)
    - firestore.rules (existing — `canReadRegistry` pattern with .get() defaults; storage.rules MUST mirror this for invitedUsers/visibility)
    - firebase.json (Plan 01 added emulators.storage.port=9199; Task 4 adds top-level "storage": { "rules": ... })
    - .planning/quick/260420-gat-harden-firestore-rules-against-missing-f/ (precedent for legacy-doc defaults)
  </read_first>
  <action>
    1. Create `storage.rules` at the repo root (sibling to `firestore.rules`) with the verbatim Pattern 6 template from RESEARCH.md (already inlined in `<interfaces>` above). MUST include:
       - `rules_version = '2';`
       - `service firebase.storage { match /b/{bucket}/o { ... } }`
       - Helper `registryDoc(registryId)` using `firestore.get(/databases/(default)/documents/registries/$(registryId))`
       - Helper `isOwnerOfRegistry(registryId)` — `request.auth.uid == registryDoc(registryId).data.ownerId`
       - Helper `isPublicOrInvited(registryId)` — uses `data.get('visibility', 'public')` and `data.get('invitedUsers', {})[uid]` (legacy-doc defensive defaults — same idiom as firestore.rules)
       - `match /users/{uid}/registries/{registryId}/cover.jpg` — write requires `request.auth.uid == uid` AND `isOwnerOfRegistry(registryId)`; read requires `isOwnerOfRegistry || isPublicOrInvited`
       - Default deny: `match /{allPaths=**} { allow read, write: if false; }`
       Include comment block at the top citing Phase 12 D-08 + RESEARCH.md Pattern 6 + Pitfall 5 (cost note: each evaluation is 1 Firestore read; max 2 reads per evaluation cap respected).
    2. In `firebase.json`, add top-level `"storage": { "rules": "storage.rules" },` peer to the existing `"firestore": { "rules": ... }` block:
       ```json
       "firestore": {
         "rules": "firestore.rules",
         "indexes": "firestore.indexes.json"
       },
       "storage": {
         "rules": "storage.rules"
       },
       "functions": [ ... ]
       ```
       Watch trailing-comma JSON syntax. Validate: `node -e 'JSON.parse(require("fs").readFileSync("firebase.json","utf8"))'`.
    3. Do NOT deploy — Plan 05 surfaces the deploy as a human checkpoint. This task only wires the file so `firebase deploy --only storage` from Plan 05 picks it up.
    4. Add a comment in storage.rules pointing at firestore.rules so the legacy-doc default-handling consistency is obvious: the `data.get('visibility', 'public')` and `data.get('invitedUsers', {})` defaults exactly mirror firestore.rules `canReadRegistry`.
  </action>
  <verify>
    <automated>node -e 'JSON.parse(require("fs").readFileSync("firebase.json","utf8"))' && grep -c 'firestore.get\|isOwnerOfRegistry' storage.rules</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `storage.rules` at repo root
    - `grep -c 'rules_version' storage.rules` returns >= 1
    - `grep -c "firestore.get" storage.rules` returns >= 1 (cross-service lookup wired)
    - `grep -c "isOwnerOfRegistry\|isPublicOrInvited" storage.rules` returns >= 2 (helpers defined)
    - `grep -c 'data.get..visibility..*public' storage.rules` returns >= 1 (legacy default matching firestore.rules)
    - `grep -c 'allPaths=\*\*' storage.rules` returns 1 (default-deny)
    - `firebase.json` parses as JSON
    - `cat firebase.json | python3 -c 'import json,sys; d=json.load(sys.stdin); assert d["storage"]["rules"] == "storage.rules", d["storage"]'` exits 0
    - `firebase.json` contains BOTH `"storage": { "rules": ... }` (top-level) AND `"emulators": { ..., "storage": { "port": 9199 }, ... }` (Plan 01 contribution preserved)
  </acceptance_criteria>
  <done>storage.rules exists, mirrors firestore.rules legacy-doc defaults, denies non-owner write + non-member read; firebase.json wires both top-level rules path and emulator port; rules ready for Plan 05 deploy.</done>
</task>

</tasks>

<verification>
1. `./gradlew :app:assembleDebug -x lint -x lintAnalyzeDebug` — exits 0 (full APK builds with 36 drawables + new Hilt graph).
2. `./gradlew :app:testDebugUnitTest --tests 'com.giftregistry.ui.registry.cover.PresetCatalogTest' --tests 'com.giftregistry.ui.registry.cover.ResolveImageModelTest' --tests 'com.giftregistry.data.registry.RegistryRepositoryImplCoverTest' --tests 'com.giftregistry.data.storage.*' -x lint` — exits 0 (Wave 0 RED → GREEN for Plan 02 scope; CoverImageProcessorTest @Ignored is acceptable).
3. `./gradlew :app:testDebugUnitTest -x lint -x lintAnalyzeDebug` — exits 0 (full unit suite stays green; no regressions).
4. `node -e 'JSON.parse(require("fs").readFileSync("firebase.json","utf8"))'` — exits 0.
5. `find app/src/main/res/drawable-xxhdpi -name 'preset_*.jpg' | wc -l` — returns 36.
6. Read the diff for `RegistryRepositoryImpl.kt` and confirm `imageUrl` appears in `toDomain`, `toMap`, AND `toUpdateMap` (4+ total references).
</verification>

<success_criteria>
- Pitfall 1 fix shipped: RegistryDto + RegistryRepositoryImpl now persist + read imageUrl through Firestore.
- 36 placeholder JPEGs in drawable-xxhdpi/, all valid JPEGs, APK builds without missing-resource errors.
- PresetCatalog + ResolveImageModel populated — sentinel encoding/resolving works.
- StorageRepository interface (domain, zero Firebase imports) + StorageRepositoryImpl (data, runCatching wrapper) shipped and bound via Hilt.
- CoverImageProcessor interface + Impl (two-pass BitmapFactory + JPEG q=85 on Dispatchers.IO) shipped.
- StorageModule with emulator wiring (10.0.2.2:9199) — gated on BuildConfig.USE_FIREBASE_EMULATOR.
- storage.rules cross-service rules file created + wired in firebase.json (deploy is Plan 05's job).
- Wave 0 RED suites GREEN: PresetCatalogTest, ResolveImageModelTest, RegistryRepositoryImplCoverTest, StorageRepositoryImplTest. Remaining RED suites (CoverPhotoSelectionTest equality, CoverPhotoPickerEnabledTest, CreateRegistryViewModelCoverTest) deliberately stay RED until Plan 03 + Plan 04.
- One follow-up todo logged for real preset curation (CONTEXT D-02 acceptance).
- Storage emulator config + first-deploy cross-service prompt are surfaced via `user_setup` so execute-plan tells the user.
</success_criteria>

<output>
After completion, create `.planning/phases/12-registry-cover-photo-themed-placeholder/12-02-SUMMARY.md` per `.claude/get-shit-done/templates/summary.md`. Include:
- Pitfall 1 fix verified (link to RegistryRepositoryImplCoverTest passing)
- Wave 0 RED → GREEN mapping (4 suites flipped this plan; 4 still RED awaiting Plan 03 + 04)
- 36 drawables checksum or file listing
- Notes on whether ImageMagick/identicon/transparent-stub strategy was used for placeholder JPEGs (so Plan 05 UAT knows what visual is expected)
- StorageRepositoryImplTest refactor notes if the test was rewritten to mock StorageDataSource instead of FirebaseStorage
- Confirmation that domain/storage/StorageRepository.kt has zero Firebase imports
- storage.rules deploy instructions deferred to Plan 05
</output>
