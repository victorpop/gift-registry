---
phase: 12-registry-cover-photo-themed-placeholder
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/test/java/com/giftregistry/ui/registry/cover/PresetCatalogTest.kt
  - app/src/test/java/com/giftregistry/ui/registry/cover/ResolveImageModelTest.kt
  - app/src/test/java/com/giftregistry/ui/registry/cover/CoverPhotoSelectionTest.kt
  - app/src/test/java/com/giftregistry/ui/registry/cover/CoverPhotoPickerEnabledTest.kt
  - app/src/test/java/com/giftregistry/ui/registry/create/CreateRegistryViewModelCoverTest.kt
  - app/src/test/java/com/giftregistry/data/registry/RegistryRepositoryImplCoverTest.kt
  - app/src/test/java/com/giftregistry/data/storage/StorageRepositoryImplTest.kt
  - app/src/test/java/com/giftregistry/data/storage/CoverImageProcessorTest.kt
  - app/src/main/java/com/giftregistry/ui/registry/cover/CoverPhotoSelection.kt
  - app/src/main/java/com/giftregistry/ui/registry/cover/PresetCatalog.kt
  - app/src/main/java/com/giftregistry/ui/registry/cover/ResolveImageModel.kt
  - app/src/main/java/com/giftregistry/ui/registry/cover/CoverPhotoPickerEnabled.kt
  - app/src/main/java/com/giftregistry/domain/storage/StorageRepository.kt
  - app/src/main/java/com/giftregistry/data/storage/CoverImageProcessor.kt
  - app/src/main/java/com/giftregistry/data/storage/StorageRepositoryImpl.kt
autonomous: true
requirements:
  - D-02
  - D-05
  - D-07
  - D-11
  - D-12
  - D-14
  - D-16

must_haves:
  truths:
    - "All 8 Wave 0 test files exist on disk and reference symbols that compile"
    - "./gradlew :app:compileDebugUnitTestKotlin succeeds (test sources compile against stubs)"
    - "./gradlew :app:testDebugUnitTest --tests '*Phase12*' fails with clearly named RED test failures (not compile errors)"
    - "Each failing test maps to a Phase 12 decision ID in its KDoc"
  artifacts:
    - path: "app/src/test/java/com/giftregistry/ui/registry/cover/PresetCatalogTest.kt"
      provides: "RED tests for D-02 / D-05 sentinel encoding & roundtrip"
    - path: "app/src/test/java/com/giftregistry/ui/registry/cover/ResolveImageModelTest.kt"
      provides: "RED tests for resolveImageModel String/Int/null branching"
    - path: "app/src/test/java/com/giftregistry/ui/registry/cover/CoverPhotoSelectionTest.kt"
      provides: "RED tests for sealed-interface equality/default state (D-11 supports)"
    - path: "app/src/test/java/com/giftregistry/ui/registry/cover/CoverPhotoPickerEnabledTest.kt"
      provides: "RED tests for D-12 occasion-gated picker logic"
    - path: "app/src/test/java/com/giftregistry/ui/registry/create/CreateRegistryViewModelCoverTest.kt"
      provides: "RED tests for D-07 upload-before-save ordering + D-11 occasion-change-clears-preset (Pitfall 2 guard)"
    - path: "app/src/test/java/com/giftregistry/data/registry/RegistryRepositoryImplCoverTest.kt"
      provides: "RED tests for Pitfall 1 (RegistryDto + toMap/toUpdateMap roundtrip imageUrl)"
    - path: "app/src/test/java/com/giftregistry/data/storage/StorageRepositoryImplTest.kt"
      provides: "RED tests for uploadCover happy path + runCatching failure path"
    - path: "app/src/test/java/com/giftregistry/data/storage/CoverImageProcessorTest.kt"
      provides: "RED tests for decodeAndCompressForCover size invariant (D-06)"
    - path: "app/src/main/java/com/giftregistry/ui/registry/cover/CoverPhotoSelection.kt"
      provides: "Stub sealed interface so test files compile"
    - path: "app/src/main/java/com/giftregistry/ui/registry/cover/PresetCatalog.kt"
      provides: "Stub object with no-op encode/resolve so tests fail on assertions, not compile"
    - path: "app/src/main/java/com/giftregistry/ui/registry/cover/ResolveImageModel.kt"
      provides: "Stub top-level resolveImageModel returning null"
    - path: "app/src/main/java/com/giftregistry/ui/registry/cover/CoverPhotoPickerEnabled.kt"
      provides: "Stub isCoverPickerEnabled(occasion) predicate returning true (test asserts false-when-blank)"
    - path: "app/src/main/java/com/giftregistry/domain/storage/StorageRepository.kt"
      provides: "Domain-layer interface (no Firebase imports per Phase 02 D-08)"
    - path: "app/src/main/java/com/giftregistry/data/storage/CoverImageProcessor.kt"
      provides: "Stub interface so VM tests can mock the bitmap pipeline (impl arrives Plan 02)"
    - path: "app/src/main/java/com/giftregistry/data/storage/StorageRepositoryImpl.kt"
      provides: "Empty stub class so StorageRepositoryImplTest compiles in Wave 0 (real impl arrives Plan 02 Task 3)"
  key_links:
    - from: "test/.../cover/*Test.kt"
      to: "main/.../cover/*.kt"
      via: "import statements at top of each test file"
      pattern: "import com\\.giftregistry\\.ui\\.registry\\.cover\\."
    - from: "test/.../create/CreateRegistryViewModelCoverTest.kt"
      to: "domain/storage/StorageRepository.kt"
      via: "mockk<StorageRepository>() in @Before"
      pattern: "mockk<StorageRepository>"
---

<objective>
Ship Wave 0 RED test scaffolding for Phase 12 — failing pure-Kotlin unit tests that pin the contract for every D-02, D-05, D-06, D-07, D-11, D-12, D-14, D-16 decision plus the imageUrl-roundtrip bug surfaced in RESEARCH.md (Pitfall 1). Each test references a stub implementation that compiles but fails its assertions, so Plan 02 + Plan 03 can flip RED → GREEN as they ship the real code.

Purpose: Lock the executable contract before any feature code is written. Every later wave must keep these tests passing or explicitly update them with rationale. Pattern proven across Phases 8–11 (8 plans shipped this way already).

Output: 8 new test files + 7 minimal stub files (6 cover-package stubs + 1 StorageRepositoryImpl skeleton). Zero feature behaviour. The full-suite gradle command stays green for everything except the 8 Phase 12 tests, which fail with intentional `fail("Wave 0 stub")` markers or assertion mismatches that name the decision being violated.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-CONTEXT.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-RESEARCH.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-VALIDATION.md
@.planning/phases/11-registry-detail-create-add-item-redesign/11-01-PLAN.md
@.planning/phases/10-onboarding-home-redesign/10-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Symbols Wave 0 stubs MUST expose so test files compile. Plan 02 + Plan 03 replace stub bodies with real logic. -->

From app/src/main/java/com/giftregistry/ui/registry/create/OccasionCatalog.kt (already exists — DO NOT touch in this plan):
```kotlin
object OccasionCatalog {
    val entries: List<OccasionEntry>           // 6 entries; storageKey ∈ {Housewarming, Wedding, Baby, Birthday, Christmas, Custom}
    fun glyphFor(occasion: String?): String     // returns the tile glyph
    fun storageKeyFor(occasion: String?): String // canonicalises legacy values
}
data class OccasionEntry(val storageKey: String, val glyph: String)
```

From app/src/main/java/com/giftregistry/domain/model/Registry.kt (already exists):
```kotlin
data class Registry(
    val id: String = "",
    val ownerId: String = "",
    // ...
    val imageUrl: String? = null,                // <-- already in domain (Phase 10), but RegistryDto + toMap drop it (Pitfall 1)
    val createdAt: Long = 0L,
    val updatedAt: Long = 0L,
)
```

From app/src/main/java/com/giftregistry/data/model/RegistryDto.kt (already exists — Pitfall 1 BUG; Plan 02 fixes):
```kotlin
data class RegistryDto(
    val id: String = "",
    // ... NO imageUrl FIELD — must be added in Plan 02
)
```

Existing test infrastructure (verified in libs.versions.toml):
- JUnit 4.13.2 (`testImplementation(libs.junit)`)
- MockK 1.13.17 (`testImplementation(libs.mockk)`)
- Turbine 1.2.0 (`testImplementation(libs.turbine)`)
- kotlinx-coroutines-test 1.9.0 (`testImplementation(libs.coroutines.test)`)

NO new test dependencies required for this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author 4 pure-Kotlin RED test files for cover-photo helpers + matching stub implementations</name>
  <files>
    app/src/test/java/com/giftregistry/ui/registry/cover/PresetCatalogTest.kt,
    app/src/test/java/com/giftregistry/ui/registry/cover/ResolveImageModelTest.kt,
    app/src/test/java/com/giftregistry/ui/registry/cover/CoverPhotoSelectionTest.kt,
    app/src/test/java/com/giftregistry/ui/registry/cover/CoverPhotoPickerEnabledTest.kt,
    app/src/main/java/com/giftregistry/ui/registry/cover/CoverPhotoSelection.kt,
    app/src/main/java/com/giftregistry/ui/registry/cover/PresetCatalog.kt,
    app/src/main/java/com/giftregistry/ui/registry/cover/ResolveImageModel.kt,
    app/src/main/java/com/giftregistry/ui/registry/cover/CoverPhotoPickerEnabled.kt
  </files>
  <read_first>
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-CONTEXT.md (D-02, D-05, D-11, D-12, D-14)
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-RESEARCH.md (Patterns 4 + 5; Open Question 1; Wave 0 Gaps)
    - .planning/phases/11-registry-detail-create-add-item-redesign/11-01-PLAN.md (Wave 0 RED pattern reference)
    - app/src/main/java/com/giftregistry/ui/registry/create/OccasionCatalog.kt (storageKey set the tests assume)
    - app/src/test/java/com/giftregistry/ui/registry/detail/FilterChipStateTest.kt (canonical Phase 11 RED test style — discover via `find app/src/test -name 'FilterChipStateTest.kt'`)
  </read_first>
  <behavior>
    - PresetCatalogTest:
      • `presetsFor("Wedding")` returns a 6-element `List<Int>` (drawable IDs).
      • `presetsFor("baby shower")` (legacy alias from OccasionCatalog) resolves to the Baby presets via OccasionCatalog.storageKeyFor.
      • `presetsFor(null)` returns `emptyList()`.
      • `encode("Wedding", 3)` returns `"preset:Wedding:3"`.
      • `resolve("preset:Wedding:3")` returns a non-null `Int` equal to `presetsFor("Wedding")[2]`.
      • `resolve("preset:NotARealOccasion:1")` returns null.
      • `resolve("preset:Wedding:99")` returns null (out-of-range index).
      • `resolve("https://example.com/x.jpg")` returns null (not a sentinel).
      • `resolve("")` returns null.
    - ResolveImageModelTest:
      • `resolveImageModel(null) == null`
      • `resolveImageModel("https://x") is String` and equals the URL
      • `resolveImageModel("preset:Wedding:3") is Int` and equals `PresetCatalog.resolve("preset:Wedding:3")`
      • `resolveImageModel("preset:Bogus:9") == null` (malformed sentinel falls through to null, NOT to the raw string)
    - CoverPhotoSelectionTest:
      • `CoverPhotoSelection.None` is a singleton (`===` comparison)
      • `Preset("Wedding", 3) == Preset("Wedding", 3)` (data-class equality)
      • `Preset("Wedding", 3) != Preset("Wedding", 4)`
      • `Gallery(uri)` carries the Uri value (Uri instance constructed via `mockk<Uri>()`)
    - CoverPhotoPickerEnabledTest (D-12):
      • `isCoverPickerEnabled(occasion = "")` returns false
      • `isCoverPickerEnabled(occasion = "   ")` returns false (whitespace)
      • `isCoverPickerEnabled(occasion = null)` returns false
      • `isCoverPickerEnabled(occasion = "Wedding")` returns true
  </behavior>
  <action>
    1. Create stub `CoverPhotoSelection.kt` (the only stub that's already final-shape — sealed interface is straightforward):
       ```kotlin
       package com.giftregistry.ui.registry.cover
       import android.net.Uri
       sealed interface CoverPhotoSelection {
           data object None : CoverPhotoSelection
           data class Preset(val occasion: String, val index: Int) : CoverPhotoSelection
           data class Gallery(val uri: Uri) : CoverPhotoSelection
       }
       ```
    2. Create stub `PresetCatalog.kt` — empty mappings + `TODO("Plan 02 wires R.drawable.* IDs")` in `presetsFor`/`resolve`. Tests will FAIL on the empty-list / null returns, not on compile errors.
       ```kotlin
       package com.giftregistry.ui.registry.cover
       object PresetCatalog {
           fun presetsFor(occasion: String?): List<Int> = emptyList()    // STUB — Plan 02 fills
           fun encode(occasion: String, index1Based: Int): String = "preset:$occasion:$index1Based" // already final shape
           fun resolve(sentinel: String): Int? = null                     // STUB
       }
       ```
       NOTE: `encode` is final-shape because it has no Android dependencies; tests for it pass on Wave 0. The point is to keep the API surface intact while `presetsFor`/`resolve` fail.
    3. Create stub `ResolveImageModel.kt`:
       ```kotlin
       package com.giftregistry.ui.registry.cover
       fun resolveImageModel(imageUrl: String?): Any? = null              // STUB — Plan 03 fills
       ```
    4. Create stub `CoverPhotoPickerEnabled.kt`:
       ```kotlin
       package com.giftregistry.ui.registry.cover
       fun isCoverPickerEnabled(occasion: String?): Boolean = true       // STUB inverted on purpose so the D-12 RED tests fail
       ```
    5. Author the four test files. Use JUnit 4 (`@Test`, `org.junit.Assert.*`). Test class names must end in `Test`. Each `@Test` method KDoc must reference its Decision ID. Place all four in package `com.giftregistry.ui.registry.cover` (test source set mirrors main).
    6. For `CoverPhotoSelectionTest.testGallerySelection_carriesUri()`, use `mockk<Uri>()` (relaxed=true) — `Uri` is part of `android.net` so mock it; do NOT use `Uri.parse()` (Robolectric not on classpath).
    7. Each test file MUST start with the package declaration `package com.giftregistry.ui.registry.cover` so the existing OccasionCatalog import (`com.giftregistry.ui.registry.create.OccasionCatalog`) resolves cleanly.
    8. Do NOT add `@RunWith(AndroidJUnit4::class)` — these are pure-JVM tests.
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugUnitTestKotlin -x lint -x lintAnalyzeDebug 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `app/src/test/java/com/giftregistry/ui/registry/cover/PresetCatalogTest.kt`
    - File exists: `app/src/test/java/com/giftregistry/ui/registry/cover/ResolveImageModelTest.kt`
    - File exists: `app/src/test/java/com/giftregistry/ui/registry/cover/CoverPhotoSelectionTest.kt`
    - File exists: `app/src/test/java/com/giftregistry/ui/registry/cover/CoverPhotoPickerEnabledTest.kt`
    - File exists: `app/src/main/java/com/giftregistry/ui/registry/cover/CoverPhotoSelection.kt`
    - File exists: `app/src/main/java/com/giftregistry/ui/registry/cover/PresetCatalog.kt`
    - File exists: `app/src/main/java/com/giftregistry/ui/registry/cover/ResolveImageModel.kt`
    - File exists: `app/src/main/java/com/giftregistry/ui/registry/cover/CoverPhotoPickerEnabled.kt`
    - `grep -c '@Test' app/src/test/java/com/giftregistry/ui/registry/cover/PresetCatalogTest.kt` returns >= 7
    - `grep -l 'D-02\|D-05' app/src/test/java/com/giftregistry/ui/registry/cover/PresetCatalogTest.kt` returns the path (decision IDs cited in KDoc)
    - `grep -l 'D-12' app/src/test/java/com/giftregistry/ui/registry/cover/CoverPhotoPickerEnabledTest.kt` returns the path
    - `./gradlew :app:compileDebugUnitTestKotlin -x lint` exits 0
    - `./gradlew :app:testDebugUnitTest --tests 'com.giftregistry.ui.registry.cover.*' -x lint` exits NON-ZERO with at least 8 failed tests (RED expected)
  </acceptance_criteria>
  <done>4 test files compile, 4 stub files compile, run reports >= 8 failing tests across the cover package, no compilation errors anywhere in the test source set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author 4 RED test files for ViewModel + data + storage layers (Pitfall 1 + Pitfall 2 + D-06 + D-07)</name>
  <files>
    app/src/test/java/com/giftregistry/ui/registry/create/CreateRegistryViewModelCoverTest.kt,
    app/src/test/java/com/giftregistry/data/registry/RegistryRepositoryImplCoverTest.kt,
    app/src/test/java/com/giftregistry/data/storage/StorageRepositoryImplTest.kt,
    app/src/test/java/com/giftregistry/data/storage/CoverImageProcessorTest.kt,
    app/src/main/java/com/giftregistry/domain/storage/StorageRepository.kt,
    app/src/main/java/com/giftregistry/data/storage/CoverImageProcessor.kt,
    app/src/main/java/com/giftregistry/data/storage/StorageRepositoryImpl.kt
  </files>
  <read_first>
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-RESEARCH.md (Pitfall 1 + Pitfall 2 + Pattern 2 + Pattern 3; Validation Architecture section)
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-CONTEXT.md (D-06, D-07, D-08, D-11)
    - app/src/main/java/com/giftregistry/data/registry/RegistryRepositoryImpl.kt (Pitfall 1 reference — `toMap` lines 75-82, `toUpdateMap` lines 84-89 silently drop imageUrl)
    - app/src/main/java/com/giftregistry/data/model/RegistryDto.kt (missing imageUrl field — Pitfall 1)
    - app/src/main/java/com/giftregistry/ui/registry/create/CreateRegistryViewModel.kt (existing onSave structure)
    - .planning/phases/02-android-core-auth/02-02-PLAN.md (`runCatching` pattern in domain<->data boundary — D-08 zero Firebase imports in domain)
    - app/src/test/java/com/giftregistry/data/auth/AuthRepositoryImplTest.kt (canonical mockk pattern — discover via `find app/src/test -name 'AuthRepositoryImplTest.kt'`)
  </read_first>
  <behavior>
    - CreateRegistryViewModelCoverTest:
      • `onSave_uploadsBeforeSavingRegistry`: Given coverPhotoSelection=Gallery(uri), when storageRepository.uploadCover succeeds with "https://download/x.jpg", then createRegistryUseCase is called with `registry.imageUrl == "https://download/x.jpg"` AND savedRegistryId emits AFTER uploadCover suspend completes (verified via mockk's `coVerifyOrder { uploadCover(...); createRegistry(...) }`). [Pitfall 2 + D-07]
        **CRITICAL ORDER (D-07 + Pitfall 2):** the test asserts uploadCover is invoked BEFORE createRegistryUseCase. Plan 12-04 must flip this RED → GREEN by changing the ViewModel implementation order, NEVER by editing this test. The test stays as authored here.
      • `uploadFailure_surfacesError_no_navigation`: Given uploadCover returns Result.failure, then `error.value` is non-null AND `savedRegistryId.value` stays null AND `isSaving.value` is false. [D-07 — failure must NOT emit savedRegistryId]
      • `presetSelection_skipsUpload_encodesAsString`: Given coverPhotoSelection=Preset("Wedding", 3), when onSave fires, then uploadCover is NEVER called AND createRegistryUseCase receives `registry.imageUrl == "preset:Wedding:3"`.
      • `noneSelection_emitsNullImageUrl`: Given coverPhotoSelection=None, when onSave fires, then `registry.imageUrl == null` AND uploadCover is never called.
      • `occasionChange_clearsPresetSelection`: Given current=Preset("Wedding", 3), when occasion.value changes to "Birthday", then coverPhotoSelection.value becomes None. [D-11]
    - RegistryRepositoryImplCoverTest (Pitfall 1):
      • `toMap_includesImageUrl`: Given a Registry with `imageUrl = "preset:Wedding:3"`, when createRegistry is called, then the FirestoreDataSource receives a map containing key `"imageUrl"` with that value (mockk slot capture).
      • `toUpdateMap_includesImageUrl`: Given a Registry with `imageUrl = "https://x.jpg"`, when updateRegistry is called, then the dataSource update map contains key `"imageUrl"` with that value.
      • `toDomain_propagatesImageUrl`: Given a RegistryDto with `imageUrl = "preset:Birthday:1"`, when observed, then the emitted Registry has `imageUrl == "preset:Birthday:1"`.
      • `toDomain_nullImageUrl_whenAbsent`: Given a RegistryDto without an imageUrl value (default null), the emitted Registry.imageUrl is null.
      • `newRegistryId_returnsNonBlankString`: Given the repository, when `newRegistryId()` is called, it returns a non-blank String (Plan 02 plumbs Firestore's client-side ID generation `firestore.collection("registries").document().id`). [D-07 upload-then-write enabler]
    - StorageRepositoryImplTest (depends on Plan 02 implementation; this is RED until then):
      • `uploadCover_happyPath_returnsDownloadUrl`: Mock FirebaseStorage so putBytes().await() succeeds and downloadUrl.await() returns Uri pointing at "https://download/x.jpg"; assert Result.success("https://download/x.jpg").
      • `uploadCover_failure_returnsResultFailure`: Mock putBytes().await() to throw; assert Result.isFailure with the same exception.
      • `uploadCover_pathSchema`: Verify the StorageReference path resolves to `users/{uid}/registries/{registryId}/cover.jpg` per D-05 (mockk slot on `storage.reference.child(...)`).
    - CoverImageProcessorTest:
      • `compress_smallImage_under300kb`: Use a synthetic Bitmap (640×480) created inline; assert returned ByteArray.size < 300_000.
      • `compress_largeImage_downscalesAndCompresses`: Use a 4032×3024 Bitmap; assert returned ByteArray.size < 300_000 AND when re-decoded via BitmapFactory the decoded bitmap has width <= 1280 AND height <= 720.
      Note: this test MUST be `@RunWith(RobolectricTestRunner::class)` IF Robolectric is available, OR fall back to a fake Bitmap mock if not. **DETERMINE during this task** by running `./gradlew :app:dependencies --configuration testRuntimeClasspath | grep robolectric` — if absent, mark the test class with `@Ignore("Plan 02: needs Robolectric or instrumented variant")` and document the gap. RESEARCH.md notes Robolectric is NOT in the toolchain — assume `@Ignore` path; the impl will either use a fake or move to instrumented tests. Tests still compile against the interface, so Wave 0 contract holds.
  </behavior>
  <action>
    1. Create domain stub `app/src/main/java/com/giftregistry/domain/storage/StorageRepository.kt`:
       ```kotlin
       package com.giftregistry.domain.storage
       interface StorageRepository {
           /**
            * Upload a JPEG cover photo to /users/{uid}/registries/{registryId}/cover.jpg
            * and return the public download URL string. Failures wrapped per Phase 02 D-08.
            * (D-04, D-05, D-07)
            */
           suspend fun uploadCover(uid: String, registryId: String, jpegBytes: ByteArray): Result<String>
       }
       ```
       MUST have ZERO Firebase imports (Phase 02 decision — domain is pure Kotlin).
    2. Create data stub `app/src/main/java/com/giftregistry/data/storage/CoverImageProcessor.kt` as an interface (not impl — Plan 02 ships impl):
       ```kotlin
       package com.giftregistry.data.storage
       import android.net.Uri
       interface CoverImageProcessor {
           /** Decode + downscale to 1280×720 max + JPEG q=85. Throws on failure. (D-06) */
           suspend fun compress(uri: Uri): ByteArray
       }
       ```
    3. Author `CreateRegistryViewModelCoverTest.kt` using existing test patterns: mockk relaxed=true for `AuthRepository`, `CreateRegistryUseCase`, `UpdateRegistryUseCase`, `ObserveRegistryUseCase`, `StorageRepository`, `CoverImageProcessor`, `RegistryRepository` (for `newRegistryId()` if injected directly). Use `coEvery`, `coVerify`, `coVerifyOrder`. Use `runTest { }` from `kotlinx-coroutines-test`. Use `Dispatchers.setMain(StandardTestDispatcher())` in `@Before` (existing pattern in `AuthViewModelTest.kt`).
       The `onSave_uploadsBeforeSavingRegistry` test MUST use `coVerifyOrder { storageRepository.uploadCover(any(), any(), any()); createRegistryUseCase.invoke(any()) }` — uploadCover FIRST, createRegistry SECOND. This is the D-07 + Pitfall 2 contract that Plan 12-04 must satisfy via implementation, NOT by editing this test.
    4. Author `RegistryRepositoryImplCoverTest.kt`. Mock `FirestoreDataSource` via mockk; capture the `Map<String, Any?>` argument to `createRegistry`/`updateRegistry`; assert `slot.captured["imageUrl"]` matches. Add the `newRegistryId_returnsNonBlankString` test that verifies `RegistryRepositoryImpl.newRegistryId()` delegates to a Firestore-backed ID generator and returns a non-blank string.
    5. Author `StorageRepositoryImplTest.kt`. Reference symbol `StorageRepositoryImpl` — Plan 02 will create the real impl. The test compiles only if Plan 02 also provides a stub; **for Wave 0 compilation, additionally create** `app/src/main/java/com/giftregistry/data/storage/StorageRepositoryImpl.kt` as an empty stub:
       ```kotlin
       package com.giftregistry.data.storage
       import com.giftregistry.domain.storage.StorageRepository
       import javax.inject.Inject
       import javax.inject.Singleton
       @Singleton
       class StorageRepositoryImpl @Inject constructor() : StorageRepository {
           override suspend fun uploadCover(uid: String, registryId: String, jpegBytes: ByteArray): Result<String> =
               Result.failure(NotImplementedError("Plan 02"))
       }
       ```
       This file is included in the task's `<files>` element above and in the frontmatter `files_modified`. Plan 02 Task 3 replaces the body with the real implementation.
    6. Author `CoverImageProcessorTest.kt` with `@Ignore("Robolectric required — Plan 02 will switch to fake-Bitmap or instrumented test")` on the class until Plan 02 decides. Tests still reference the `CoverImageProcessor` interface so contract is locked.
    7. Each test file's KDoc references its Decision IDs (D-06, D-07, D-11, Pitfall 1, Pitfall 2). Test method names mirror the validation table in 12-RESEARCH.md so the V&V trace is automatic.
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugUnitTestKotlin -x lint -x lintAnalyzeDebug 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `app/src/test/java/com/giftregistry/ui/registry/create/CreateRegistryViewModelCoverTest.kt`
    - File exists: `app/src/test/java/com/giftregistry/data/registry/RegistryRepositoryImplCoverTest.kt`
    - File exists: `app/src/test/java/com/giftregistry/data/storage/StorageRepositoryImplTest.kt`
    - File exists: `app/src/test/java/com/giftregistry/data/storage/CoverImageProcessorTest.kt`
    - File exists: `app/src/main/java/com/giftregistry/domain/storage/StorageRepository.kt`
    - File exists: `app/src/main/java/com/giftregistry/data/storage/CoverImageProcessor.kt`
    - File exists: `app/src/main/java/com/giftregistry/data/storage/StorageRepositoryImpl.kt` (empty stub)
    - `grep -c 'firebase\|FirebaseStorage' app/src/main/java/com/giftregistry/domain/storage/StorageRepository.kt` returns 0 (zero Firebase imports in domain — Phase 02 D-08)
    - `grep -c 'Pitfall 1\|imageUrl' app/src/test/java/com/giftregistry/data/registry/RegistryRepositoryImplCoverTest.kt` returns >= 4
    - `grep -c 'newRegistryId' app/src/test/java/com/giftregistry/data/registry/RegistryRepositoryImplCoverTest.kt` returns >= 1 (Plan 02 plumbs the helper for upload-then-write)
    - `grep -c 'coVerifyOrder\|uploadsBeforeSavingRegistry' app/src/test/java/com/giftregistry/ui/registry/create/CreateRegistryViewModelCoverTest.kt` returns >= 2
    - `grep -B2 -A6 'coVerifyOrder' app/src/test/java/com/giftregistry/ui/registry/create/CreateRegistryViewModelCoverTest.kt | grep -c 'uploadCover'` returns >= 1 (uploadCover named first in the verify order block)
    - `./gradlew :app:compileDebugUnitTestKotlin -x lint` exits 0
    - `./gradlew :app:testDebugUnitTest --tests 'com.giftregistry.ui.registry.create.CreateRegistryViewModelCoverTest' --tests 'com.giftregistry.data.registry.RegistryRepositoryImplCoverTest' --tests 'com.giftregistry.data.storage.StorageRepositoryImplTest' -x lint` exits NON-ZERO (RED expected — Plan 02 + Plan 04 will flip GREEN)
  </acceptance_criteria>
  <done>4 test files + StorageRepository interface + CoverImageProcessor interface + StorageRepositoryImpl stub all compile; full Wave 0 RED suite shows 13+ intentional failing tests (8 from Task 1 + 5 from Task 2 including newRegistryId; Robolectric-gated processor tests are @Ignored, not failing).</done>
</task>

<task type="auto">
  <name>Task 3: Add firebase-storage dep + storage emulator wiring (no impl yet)</name>
  <files>
    gradle/libs.versions.toml,
    app/build.gradle.kts,
    firebase.json
  </files>
  <read_first>
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-RESEARCH.md (Standard Stack section + Open Question 2 emulator wiring)
    - CLAUDE.md (Firebase BoM 34.11.0 main modules, NO `*-ktx`)
    - gradle/libs.versions.toml (existing firebase-* aliases — add alongside)
    - app/build.gradle.kts lines 78-82 (existing Firebase main-module block)
    - firebase.json (existing emulators block — add `storage` peer)
  </read_first>
  <action>
    1. In `gradle/libs.versions.toml`, under `[libraries]` immediately after `firebase-messaging`, add:
       ```toml
       firebase-storage = { group = "com.google.firebase", name = "firebase-storage" }
       ```
       NOTE: no `version.ref` because the BoM (34.11.0) resolves the version (22.0.1 per RESEARCH.md). MUST NOT use `firebase-storage-ktx` — KTX modules removed in BoM v34 per CLAUDE.md.
    2. In `app/build.gradle.kts`, in the Firebase main-module block (after `implementation(libs.firebase.messaging)` near line 83), append:
       ```kotlin
       implementation(libs.firebase.storage)
       ```
       Keep alphabetic-by-module-name ordering if the existing file uses it; otherwise append at end of the block.
    3. In `firebase.json`, inside the `"emulators"` block (after the existing `"firestore"` entry), add:
       ```json
       "storage": {
         "port": 9199
       },
       ```
       Watch trailing-comma JSON syntax — `firestore` block ends with `},` then `storage` block ends with `},` then existing `"hosting"` follows. Validate with `node -e 'JSON.parse(require("fs").readFileSync("firebase.json","utf8"))'`.
    4. Do NOT create `storage.rules` here (Plan 02 ships it). Do NOT add `"storage": { "rules": "storage.rules" }` to the top-level firebase.json — Plan 02 wires that. This task only adds the emulator port so Plan 02's tests can target it.
  </action>
  <verify>
    <automated>node -e 'JSON.parse(require("fs").readFileSync("firebase.json","utf8"))' && ./gradlew :app:dependencies --configuration debugRuntimeClasspath 2>&1 | grep -c 'firebase-storage:' </automated>
  </verify>
  <acceptance_criteria>
    - `gradle/libs.versions.toml` contains `firebase-storage = { group = "com.google.firebase", name = "firebase-storage" }`
    - `gradle/libs.versions.toml` does NOT contain `firebase-storage-ktx` (negative grep)
    - `app/build.gradle.kts` contains `implementation(libs.firebase.storage)`
    - `firebase.json` parses as valid JSON
    - `firebase.json` contains `"storage": { "port": 9199 }` inside the emulators block (verify via `cat firebase.json | python3 -c 'import json,sys; d=json.load(sys.stdin); assert d["emulators"]["storage"]["port"] == 9199'`)
    - `./gradlew :app:dependencies --configuration debugRuntimeClasspath 2>&1 | grep 'firebase-storage:'` returns at least one line (Gradle resolved 22.0.1 via BoM)
    - `firebase.json` does NOT yet contain top-level `"storage": { "rules": ... }` (that wiring is Plan 02's job)
  </acceptance_criteria>
  <done>firebase-storage 22.0.1 resolves via BoM 34.11.0; firebase.json parses; Storage Emulator port 9199 declared.</done>
</task>

</tasks>

<verification>
1. Run `./gradlew :app:compileDebugUnitTestKotlin -x lint -x lintAnalyzeDebug` — exits 0 (all 8 test files + 7 stubs compile).
2. Run `./gradlew :app:testDebugUnitTest --tests 'com.giftregistry.ui.registry.cover.*' --tests 'com.giftregistry.ui.registry.create.CreateRegistryViewModelCoverTest' --tests 'com.giftregistry.data.registry.RegistryRepositoryImplCoverTest' --tests 'com.giftregistry.data.storage.StorageRepositoryImplTest' -x lint -x lintAnalyzeDebug` — exits NON-ZERO with 13+ failing tests (RED expected).
3. Run `./gradlew :app:dependencies --configuration debugRuntimeClasspath 2>&1 | grep 'firebase-storage:22'` — at least one match (BoM 34.11.0 resolves to 22.0.1).
4. Run `node -e 'JSON.parse(require("fs").readFileSync("firebase.json","utf8"))'` — exits 0.
5. Manually skim `git diff` to confirm: no `firebase-storage-ktx`, no edits to existing `OccasionCatalog.kt` / `Registry.kt` / `RegistryDto.kt` (Plan 02's job), no edits to `RegistryRepositoryImpl.kt` (Plan 02's job).
</verification>

<success_criteria>
- All 8 Wave 0 RED test files exist on disk and compile against stubs.
- Stubs intentionally produce assertion failures (not compile errors), so Plan 02/03/04 can flip RED → GREEN by replacing stub bodies.
- `firebase-storage` (no KTX) is on the classpath via BoM 34.11.0.
- Firebase Storage emulator port 9199 is declared in `firebase.json`.
- Domain-layer `StorageRepository` has zero Firebase imports (Phase 02 architecture invariant preserved).
- Decision IDs D-02, D-05, D-06, D-07, D-11, D-12, D-14, D-16 each map to at least one named test method via KDoc references.
- The Wave 0 RED test `onSave_uploadsBeforeSavingRegistry` asserts uploadCover-BEFORE-createRegistry order via `coVerifyOrder` — this is the D-07 + Pitfall 2 contract Plan 12-04 must satisfy via implementation. Downstream plans MUST flip the test GREEN by changing the ViewModel implementation, not by editing the test.
- No edits to existing implementation files (RegistryRepositoryImpl, RegistryDto, CreateRegistryViewModel, RegistryDetailHero, RegistryCard) — those are Plan 02 + Plan 03 + Plan 04 territory.
</success_criteria>

<output>
After completion, create `.planning/phases/12-registry-cover-photo-themed-placeholder/12-01-SUMMARY.md` per `.claude/get-shit-done/templates/summary.md`. Include:
- Wave 0 RED count (intentionally failing tests)
- Decision-ID → test-method coverage table (mirror 12-RESEARCH.md Validation Architecture)
- Stub-file → impl-plan-target mapping (so Plan 02/03/04 know what to replace)
- Confirmation that no Firebase imports leaked into the domain layer
- Confirmation that `onSave_uploadsBeforeSavingRegistry` asserts upload-then-create order (downstream contract)
</output>
</output>
