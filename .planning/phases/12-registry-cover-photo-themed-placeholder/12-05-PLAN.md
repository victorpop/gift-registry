---
phase: 12-registry-cover-photo-themed-placeholder
plan: 05
type: execute
wave: 3
depends_on:
  - 12-04
files_modified:
  - app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt
  - .planning/phases/12-registry-cover-photo-themed-placeholder/12-VALIDATION.md
autonomous: false
requirements:
  - D-08
  - D-09
  - D-10
  - D-13
  - D-14
  - D-15
  - D-16
user_setup:
  - service: firebase-storage
    why: "Storage rules deploy is human-action — first-time deploy prompts cross-service permissions grant"
    env_vars: []
    dashboard_config:
      - task: "Run `firebase deploy --only storage` (one-time cross-service permissions prompt; accept it)"
        location: "Firebase CLI (terminal)"
      - task: "Verify default Storage bucket exists for gift-registry-ro"
        location: "Firebase Console -> Storage"

must_haves:
  truths:
    - "StyleGuidePreview shows the 4 new Phase 12 sections rendering correctly"
    - "On-device UAT verifies all manual-only checks from 12-VALIDATION.md"
    - "storage.rules is deployed to gift-registry-ro and the cross-service permissions grant has been accepted"
    - "Non-owner cannot upload to /users/{otherUid}/registries/{registryId}/cover.jpg (rules verified via deployed environment)"
    - "Romanian locale string parity verified — no English fallback in cover_photo_* keys"
  artifacts:
    - path: "app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt"
      provides: "Phase 12 @Preview sections — HeroImageOrPlaceholder, CoverPhotoPickerInline, CoverPhotoPickerSheet, RegistryCard placeholder variants"
    - path: ".planning/phases/12-registry-cover-photo-themed-placeholder/12-VALIDATION.md"
      provides: "Per-Task Verification Map filled in + Wave 0 checklist marked complete + Approval signed"
  key_links:
    - from: "StyleGuidePreview"
      to: "ui/registry/cover/HeroImageOrPlaceholder.kt"
      via: "Compose @Preview composables"
      pattern: "HeroImageOrPlaceholder"
    - from: "12-VALIDATION.md"
      to: "all Phase 12 RED tests"
      via: "Per-Task Verification Map"
      pattern: "Test Type"
---

<objective>
Verify Phase 12 visually + on-device. Add 4 new StyleGuidePreview entries so the new composables are reviewable in Android Studio's Preview pane. Run the on-device UAT covering every Manual-Only Verification from 12-VALIDATION.md (visual fidelity, owner-only tap, RO locale, storage rules deny non-owner). Deploy storage.rules to the live Firebase project (one-time cross-service permissions prompt).

Purpose: Phase 12 is invisible to the user until UAT confirms the visible bug is fixed (cards render placeholder when imageUrl == null) AND the picker visually matches the GiftMaison design system AND non-owner write is denied at the security boundary. This is the gate for v1.1 GA inclusion.

Output: 4 StyleGuidePreview entries + completed VALIDATION.md + 1 human checkpoint covering 12 UAT checks + storage rules deployed.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-CONTEXT.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-RESEARCH.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-VALIDATION.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-01-PLAN.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-02-PLAN.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-03-PLAN.md
@.planning/phases/12-registry-cover-photo-themed-placeholder/12-04-PLAN.md
@.planning/phases/11-registry-detail-create-add-item-redesign/11-06-PLAN.md
@app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt

<interfaces>
<!-- Composables to preview + UAT-verify. Embedded so executor needs no codebase scavenger hunt. -->

From Plan 03 (composables under preview):
```kotlin
@Composable fun HeroImageOrPlaceholder(imageUrl: String?, occasion: String?, glyphSize: TextUnit = 32.sp, colorFilter: ColorFilter? = null, modifier: Modifier = Modifier)
@Composable fun CoverPhotoPickerInline(occasion: String?, selection: CoverPhotoSelection, onTap: () -> Unit, disabledHint: String, modifier: Modifier = Modifier)
@Composable fun CoverPhotoPickerSheet(occasion: String, currentSelection: CoverPhotoSelection, onSelectionChanged: (CoverPhotoSelection) -> Unit, onDismiss: () -> Unit, headerText: String, pickFromGalleryText: String, removeText: String)
```

From existing StyleGuidePreview.kt (verified pattern — Phase 11 added 7 sections; Phase 12 adds 4):
- Each preview is a `private @Composable fun XxxPreview()` with `@Preview(name = ..., showBackground = true, backgroundColor = 0xFFF7F2E9, widthDp = 360, ...)` annotation
- Wraps content in `GiftRegistryTheme { ... }`

Manual-only UAT items from 12-VALIDATION.md (Wave 3 must verify all):
1. Bottom-sheet visual matches GiftMaison tokens (D-09, D-10) — StyleGuidePreview + on-device
2. Hero gradient placeholder pixel parity (D-14, D-16) — Phase 11 hero unchanged
3. Card placeholder on home grid (D-15) — Primary + Secondary both render gradient
4. Photo Picker → Storage upload roundtrip (D-04, D-05, D-07) — Storage Emulator
5. Owner-only tap-to-change on Detail hero (D-13) — owner sees ripple, guest does not
6. Storage rules deny non-owner write (D-08) — emulator OR deployed environment
7. Romanian locale strings (I18N-02) — RO device locale shows translated keys
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 4 Phase 12 StyleGuidePreview sections</name>
  <files>
    app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt
  </files>
  <read_first>
    - app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt (CURRENT — full file; mirror existing pattern at the end)
    - app/src/main/java/com/giftregistry/ui/registry/cover/HeroImageOrPlaceholder.kt (Plan 03)
    - app/src/main/java/com/giftregistry/ui/registry/cover/CoverPhotoPickerInline.kt (Plan 03)
    - app/src/main/java/com/giftregistry/ui/registry/cover/CoverPhotoPickerSheet.kt (Plan 03)
    - app/src/main/java/com/giftregistry/ui/registry/list/RegistryCard.kt (Plan 03 refactored — render with imageUrl=null to verify the visible bug fix)
    - .planning/phases/11-registry-detail-create-add-item-redesign/11-06-PLAN.md (precedent for StyleGuidePreview Wave 3 work)
  </read_first>
  <action>
    Append these 4 preview sections AT THE END of the file (after existing `ItemPreviewCardPreview()`). Use existing `@Preview(showBackground = true, backgroundColor = 0xFFF7F2E9, widthDp = 360)` style. Wrap each in `GiftRegistryTheme { ... }`. Surround with `Box(modifier = Modifier.padding(16.dp)) { ... }` if the composable is a single block.

    1. **HeroImageOrPlaceholderPreview** — show 2 states side-by-side in a `Column` with `Spacer`s:
       - Top: `HeroImageOrPlaceholder(imageUrl = null, occasion = "Wedding", glyphSize = 40.sp, modifier = Modifier.fillMaxWidth().height(180.dp))` (hero placeholder reference)
       - Bottom: `HeroImageOrPlaceholder(imageUrl = null, occasion = "Birthday", glyphSize = 32.sp, modifier = Modifier.fillMaxWidth().aspectRatio(16f/9f))` (card placeholder reference)
       Name: `name = "HeroImageOrPlaceholder — hero (40 sp) + card (32 sp)"`. Width 360, height ~400.

    2. **CoverPhotoPickerInlinePreview** — show 3 states stacked in a `Column`:
       - Disabled: `CoverPhotoPickerInline(occasion = null, selection = CoverPhotoSelection.None, onTap = {}, disabledHint = "Pick an occasion to see suggested covers", modifier = Modifier.fillMaxWidth())`
       - Enabled + None: `CoverPhotoPickerInline(occasion = "Wedding", selection = CoverPhotoSelection.None, onTap = {}, disabledHint = "...", modifier = Modifier.fillMaxWidth())`
       - Enabled + Preset: `CoverPhotoPickerInline(occasion = "Wedding", selection = CoverPhotoSelection.Preset("Wedding", 1), onTap = {}, disabledHint = "...", modifier = Modifier.fillMaxWidth())`
       Wrap each in a labelled Row so the preview pane shows what each state looks like. Name: `"CoverPhotoPickerInline — 3 states"`. Width 360, height 720.

    3. **CoverPhotoPickerSheetPreview** — render the sheet body inline as a Column (NOT a real ModalBottomSheet, which doesn't render in @Preview). Reproduce the sheet's 3×2 grid + "Pick from gallery" pill + "Remove" button using the same composables (PresetThumbnail) so the visual contract is reviewable:
       ```kotlin
       Column(modifier = Modifier.padding(20.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
           Text("CHOOSE A COVER", style = GiftMaisonTheme.typography.monoCaps, color = GiftMaisonTheme.colors.inkFaint)
           LazyVerticalGrid(
               columns = GridCells.Fixed(3),
               horizontalArrangement = Arrangement.spacedBy(10.dp),
               verticalArrangement = Arrangement.spacedBy(10.dp),
               modifier = Modifier.fillMaxWidth().heightIn(max = 240.dp),
               userScrollEnabled = false,
           ) {
               itemsIndexed(PresetCatalog.presetsFor("Wedding")) { index, drawableId ->
                   PresetThumbnail(drawableId = drawableId, selected = index == 0, onClick = {})
               }
           }
           Button(onClick = {}, modifier = Modifier.fillMaxWidth(), shape = GiftMaisonTheme.shapes.pill,
               colors = ButtonDefaults.buttonColors(containerColor = GiftMaisonTheme.colors.ink, contentColor = GiftMaisonTheme.colors.paper)) {
               Text("Pick from gallery")
           }
           TextButton(onClick = {}, modifier = Modifier.align(Alignment.End)) {
               Text("Remove cover photo", color = GiftMaisonTheme.colors.inkSoft)
           }
       }
       ```
       Name: `"CoverPhotoPickerSheet (Wedding, 6 presets, first selected)"`. Width 360, height 600. **NOTE:** PresetThumbnail must be public or `internal` accessible from the same module — Plan 03 declared it `internal`, which is fine for this preview because StyleGuidePreview is in the same compilation unit.

    4. **RegistryCardPlaceholdersPreview** — render BOTH card variants with `imageUrl = null` so the visible bug fix is verifiable in the Preview pane:
       ```kotlin
       Column(modifier = Modifier.padding(16.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
           RegistryCardPrimary(
               registry = Registry(id = "1", title = "Sarah's Wedding", occasion = "Wedding", imageUrl = null),
               onClick = {}, onLongClick = {},
           )
           RegistryCardSecondary(
               registry = Registry(id = "2", title = "Welcome Baby Lia", occasion = "Baby", imageUrl = null),
               onClick = {}, onLongClick = {},
           )
       }
       ```
       Name: `"RegistryCard placeholder — Primary (Wedding) + Secondary (Baby)"`. Width 360, height 600. This preview IS the visible-bug regression check.

    5. Imports to add at the top of StyleGuidePreview.kt:
       - `androidx.compose.foundation.layout.aspectRatio`
       - `androidx.compose.foundation.layout.heightIn`
       - `androidx.compose.foundation.lazy.grid.GridCells`
       - `androidx.compose.foundation.lazy.grid.LazyVerticalGrid`
       - `androidx.compose.foundation.lazy.grid.itemsIndexed`
       - `androidx.compose.material3.Button`
       - `androidx.compose.material3.ButtonDefaults`
       - `androidx.compose.material3.TextButton`
       - `com.giftregistry.ui.registry.cover.CoverPhotoPickerInline`
       - `com.giftregistry.ui.registry.cover.CoverPhotoSelection`
       - `com.giftregistry.ui.registry.cover.HeroImageOrPlaceholder`
       - `com.giftregistry.ui.registry.cover.PresetCatalog`
       - `com.giftregistry.ui.registry.cover.PresetThumbnail`
    6. Each `@Preview` annotation should set `name`, `showBackground = true`, `backgroundColor = 0xFFF7F2E9`, `widthDp = 360`, and a sensible `heightDp`. Match the existing file's annotation style.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug -x lint -x lintAnalyzeDebug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@Preview' app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` increases by 4 (compared to baseline before this task)
    - `grep -c 'HeroImageOrPlaceholder\|CoverPhotoPickerInline\|PresetThumbnail' app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` returns >= 4
    - `grep -c 'imageUrl = null' app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` returns >= 2 (RegistryCard placeholder regression check)
    - `grep -c 'CoverPhotoSelection.Preset\|CoverPhotoSelection.None' app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` returns >= 2
    - `./gradlew :app:assembleDebug -x lint` exits 0 (preview composables compile)
  </acceptance_criteria>
  <done>4 new preview sections render in Android Studio Preview pane; the bug-regression check (placeholder cards) is reviewable; APK builds cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy storage.rules + on-device UAT (12-check checklist + manual upload roundtrip)</name>
  <files>
    storage.rules,
    app/src/main/res/values-ro/strings.xml
  </files>
  <action>
    Run the human checkpoint workflow described in <how-to-verify> below — Storage rules deploy, Storage Emulator smoke test, Visual UAT in Android Studio Preview pane, On-device UAT (12 checks), and the optional Storage rules security simulator test. The user types the resume-signal once verification is complete; Claude consumes it and proceeds to Task 3.
  </action>
  <verify>
    <automated>echo "Manual checkpoint - automated verification not applicable. Resume signal from user determines outcome (see acceptance_criteria below)."</automated>
  </verify>
  <acceptance_criteria>
    - User typed an explicit resume-signal: `approved`, `partial - ...`, or `blocked - ...`
    - All 4 visual UAT checks (Android Studio Preview pane) pass OR are explicitly waived
    - All 8 functional UAT checks (on-device, checks 5-12) pass OR are explicitly triaged via the partial signal
    - Storage rules deployed to gift-registry-ro (or deferred via blocked signal with a logged follow-up todo)
  </acceptance_criteria>
  <done>User typed `approved` (or `partial - ...` with all unaddressed issues triaged into follow-up todos / a Plan 06 gap closure). Phase is fit for v1.1 GA.</done>

  <what-built>
    Phase 12 ships:
    - 36 bundled placeholder preset JPEGs in drawable-xxhdpi/ (per CONTEXT.md D-02 — production curation deferred to follow-up todo)
    - StorageRepository + CoverImageProcessor + StorageModule (firebase-storage 22.0.1 via BoM 34.11.0, NO KTX)
    - storage.rules (cross-service Firestore lookup; not yet deployed)
    - HeroImageOrPlaceholder shared composable; RegistryCardPrimary + RegistryCardSecondary + RegistryDetailHero refactored to consume it
    - CoverPhotoPickerInline + CoverPhotoPickerSheet + PresetThumbnail
    - CreateRegistryViewModel + Screen wiring (inline picker above OccasionTileGrid; sheet host)
    - RegistryDetailViewModel + Screen wiring (owner-only tap target on hero)
    - 10 cover_photo_* string keys × 2 locales (EN + RO)
    - 4 StyleGuidePreview sections
    - Pitfall 1 fix shipped (RegistryDto.imageUrl + RegistryRepositoryImpl.toMap/toUpdateMap roundtrip)
    - All 8 Wave 0 RED unit tests are now GREEN (or @Ignored with reason for CoverImageProcessorTest)
  </what-built>
  <how-to-verify>
    **STEP A — Storage rules deploy (~5 min, one-time):**
    1. From repo root, run `firebase deploy --only storage --project gift-registry-ro` (substitute with the correct project alias if different — check `.firebaserc`).
    2. **First-time prompt expected:** Firebase CLI will ask permission to enable cross-service rules between Storage and Firestore. **Accept** (`Y`).
    3. Deploy completes. Verify in Firebase Console → Storage → Rules tab that `storage.rules` content shows the new rules with `firestore.get(...)` cross-service lookup.

    **STEP B — Storage Emulator wiring smoke test (~3 min):**
    1. Run `firebase emulators:start --only auth,firestore,storage`. Verify Storage emulator UI loads at `http://localhost:4000` (Storage tab visible alongside Auth + Firestore).
    2. Run the app with `-Puse_emulator=true` (`./gradlew :app:installDebug -Puse_emulator=true` then launch). Confirm `BuildConfig.USE_FIREBASE_EMULATOR = true` routes Storage to `10.0.2.2:9199`.

    **STEP C — Visual UAT — open Android Studio Preview pane (~5 min):**
    1. Open `app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt`.
    2. Verify the 4 new preview sections render without errors:
       □ Check 1: `HeroImageOrPlaceholder — hero (40 sp) + card (32 sp)` shows TWO gradient blocks; the top has a 40 sp glyph (large), the bottom has a 32 sp glyph (smaller). Both gradients flow accentSoft (top) → accent (bottom). Glyph is paper-coloured Instrument Serif italic.
       □ Check 2: `CoverPhotoPickerInline — 3 states` shows three stacked 16:9 blocks: disabled has the dark scrim + caption "Pick an occasion to see suggested covers"; enabled+None has plain gradient placeholder; enabled+Preset shows the first wedding preset image (or placeholder JPEG colour for the placeholder asset set).
       □ Check 3: `CoverPhotoPickerSheet (Wedding, 6 presets, first selected)` shows the 3×2 grid + ink "Pick from gallery" pill + low-emphasis "Remove cover photo" text button. First tile has 1.5 dp accent border (selected); others have 1 dp line border.
       □ Check 4: `RegistryCard placeholder — Primary (Wedding) + Secondary (Baby)` shows TWO cards: dark Primary with the Wedding glyph on a darker (70%-brightness) gradient; lighter Secondary with the Baby glyph on a full-brightness gradient. **This is the visible-bug regression check.**

    **STEP D — On-device UAT (~20 min) — install debug build with emulator, run through all 12 checks:**
    1. Boot emulator with `-Puse_emulator=true`. Sign in as test user.
    2. Create a new registry without picking a cover photo (skip the picker entirely):
       □ Check 5: After save, the registry card on Home shows the gradient + glyph placeholder (NOT empty). [D-15 visible-bug fix]
       □ Check 6: Open the registry detail. The 180 dp hero shows the gradient + 40 sp glyph placeholder. [D-14, D-16]
    3. Edit the same registry. Tap the cover-photo block above the occasion grid:
       □ Check 7: Sheet opens with 6 Wedding presets. Tap the second preset; sheet dismisses. Save.
       □ Check 8: Both Home card AND Detail hero now show the chosen preset (the placeholder JPEG colour for the placeholder asset). `Registry.imageUrl` in Firestore (Emulator UI) shows `"preset:Wedding:2"`. [D-02, D-05, Pitfall 1 fix]
    4. Edit again, tap the cover, tap "Pick from gallery":
       □ Check 9: System Photo Picker opens. Pick any image. Sheet dismisses; preview updates.
       □ Check 10: Save. The Storage emulator UI shows a new file at `users/{uid}/registries/{registryId}/cover.jpg`. The Detail hero re-loads with the chosen image. `Registry.imageUrl` in Firestore is the download URL. [D-04, D-05, D-07]
    5. Sign in as a DIFFERENT test user (or sign out and view the registry as a guest if visibility is public):
       □ Check 11: Open the same registry detail. The hero displays the cover. Tap on the hero — NOTHING happens (no ripple, no sheet). [D-13 owner-only]
    6. Switch device language to Romanian (Settings → Language → Română):
       □ Check 12: Re-open Create Registry. The cover-photo label says "Fotografie de copertă". The disabled-state hint says "Alege o ocazie pentru a vedea coperți sugerate". The sheet header says "ALEGE O COPERTĂ". The "Pick from gallery" pill says "Alege din galerie". The "Remove" text button says "Elimină coperta". No fallback to English. [I18N-02]

    **STEP E — Storage rules security smoke test (~5 min, optional but recommended):**
    1. With the deployed environment (or emulator), attempt to upload to `/users/{otherUid}/registries/{someRegistryId}/cover.jpg` as the wrong user. Use the Firebase Console Storage Rules simulator (Rules tab → Simulator) with auth.uid set to a non-owner, request type "create": expect DENY.
    2. As a non-invited guest on a private registry, try to READ the cover via the public download URL — expect rule deny (Firebase Console → Rules → Simulator with auth.uid as a non-invited user).

    **CONFIRM each check by typing one of:**
    - `approved — all 12 checks pass + storage rules deployed` (proceed to commit)
    - `partial — checks 1/2/...12 passed; check N failed because ___` (Claude triages: small fixes get a follow-up commit; large issues get a Plan 06 gap-closure)
    - `blocked — cannot deploy storage rules because ___` (Claude advises: defer deploy to follow-up; verify locally with emulator only)
  </how-to-verify>
  <resume-signal>Type `approved` (with optional notes), `partial — ...`, or `blocked — ...`</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize 12-VALIDATION.md — fill Per-Task Verification Map + sign off</name>
  <files>
    .planning/phases/12-registry-cover-photo-themed-placeholder/12-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-VALIDATION.md (CURRENT — frontmatter has `nyquist_compliant: false`, `wave_0_complete: false`, `status: draft`; Per-Task Verification Map has only TBD row)
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-01-SUMMARY.md (Plan 01 outcome — RED test count + decision-ID coverage)
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-02-SUMMARY.md (Plan 02 outcome — Pitfall 1 fix verified, RED → GREEN tally)
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-03-SUMMARY.md (Plan 03 outcome)
    - .planning/phases/12-registry-cover-photo-themed-placeholder/12-04-SUMMARY.md (Plan 04 outcome — final RED → GREEN)
  </read_first>
  <action>
    1. Update frontmatter:
       ```yaml
       ---
       phase: 12
       slug: registry-cover-photo-themed-placeholder
       status: complete
       nyquist_compliant: true
       wave_0_complete: true
       created: 2026-04-27
       completed: <today>
       ---
       ```
    2. Replace the "Per-Task Verification Map" section TBD row with a real table covering every Phase 12 task (one row per task across all 5 plans). For each row provide: Task ID (e.g., `12-01-T1`), Plan (`12-01`), Wave (0/1/2/3), Requirement (D-IDs separated by `, `), Test Type (`unit`/`integration`/`manual`), Automated Command (the gradle filter or `manual UAT`), File Exists (✅), Status (✅ green / ❌ red / ⚠ flaky / 🟦 manual). Aim for ~14 rows total (3 in 12-01, 4 in 12-02, 3 in 12-03, 3 in 12-04, 1 in 12-05).
    3. In "Wave 0 Requirements" section, mark all 6 list items `[x]` (complete).
    4. In "Manual-Only Verifications" section, append a "Status" column listing verification result per row (UAT pass/fail). Reference the resume-signal from Task 2.
    5. Sign off: change "Approval: pending" to "Approval: approved <date> via /gsd:execute-phase Wave 3 human checkpoint".
  </action>
  <verify>
    <automated>grep -c 'nyquist_compliant: true\|wave_0_complete: true\|status: complete' .planning/phases/12-registry-cover-photo-themed-placeholder/12-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true` (3 keys flipped)
    - Per-Task Verification Map has at least 14 rows (one per Phase 12 task)
    - All 6 Wave 0 Requirements bullets are checked `[x]`
    - Manual-Only Verifications table has a Status column
    - Approval section reads `approved` with the date
    - `grep -c 'TBD' .planning/phases/12-registry-cover-photo-themed-placeholder/12-VALIDATION.md` returns 0 (all placeholders resolved)
  </acceptance_criteria>
  <done>VALIDATION.md is signed off; nyquist_compliant flips to true; phase is fully traced from decision IDs to passing tests + UAT.</done>
</task>

</tasks>

<verification>
1. `./gradlew :app:assembleDebug -x lint -x lintAnalyzeDebug` — exits 0 (StyleGuidePreview compiles).
2. `./gradlew :app:testDebugUnitTest -x lint -x lintAnalyzeDebug` — exits 0 (full Phase 12 unit suite GREEN).
3. Open StyleGuidePreview in Android Studio — confirm 4 new preview sections render.
4. Human checkpoint Task 2 returns `approved` with all 12 checks passing.
5. `firebase deploy --only storage --project gift-registry-ro` succeeded (or `--dry-run` if user chose to defer; surfaced as a follow-up todo).
6. `12-VALIDATION.md` shows `nyquist_compliant: true` and `Approval: approved`.
7. ROADMAP.md Phase 12 entry will be marked `[x]` by the orchestrator's `update_roadmap` step.
</verification>

<success_criteria>
- 4 new StyleGuidePreview sections shipped — phase composables are visually reviewable in Android Studio.
- On-device UAT: all 12 manual checks pass (visible-bug fix, gradient parity, owner-only tap, RO locale, Photo Picker → Storage roundtrip).
- storage.rules deployed to gift-registry-ro with cross-service permissions accepted.
- Phase 12 VALIDATION.md is fully populated and signed off.
- All Wave 0 RED tests GREEN; all manual UAT items addressed.
- The visible bug from the user's screenshot (cards rendering empty when imageUrl == null) is RESOLVED — both Primary and Secondary card variants render the gradient + glyph placeholder.
</success_criteria>

<output>
After completion, create `.planning/phases/12-registry-cover-photo-themed-placeholder/12-05-SUMMARY.md` per `.claude/get-shit-done/templates/summary.md`. Include:
- 4 StyleGuidePreview entry names + a screenshot caption inventory (what each preview demonstrates)
- 12-check UAT outcome table (which checks passed; any that didn't and how triaged)
- Storage rules deploy confirmation (deployed vs deferred + why)
- Romanian locale verification result
- Phase 12 final tally: 8 Wave 0 RED suites GREEN; 0 unaddressed decisions; 1 follow-up todo logged (real preset JPEG curation)
- Suggested ROADMAP.md entry update text for the orchestrator
</output>
