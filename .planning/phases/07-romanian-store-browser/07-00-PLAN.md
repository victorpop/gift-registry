---
phase: 07-romanian-store-browser
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/data/stores.seed.json
  - functions/scripts/seedStores.ts
  - functions/package.json
  - firestore.rules
  - tests/rules/firestore.rules.test.ts
  - app/proguard-rules.pro
  - app/src/main/res/drawable-nodpi/store_emag.webp
  - app/src/main/res/drawable-nodpi/store_altex.webp
  - app/src/main/res/drawable-nodpi/store_flanco.webp
  - app/src/main/res/drawable-nodpi/store_libris.webp
  - app/src/main/res/drawable-nodpi/store_carturesti.webp
  - app/src/main/res/drawable-nodpi/store_ikea.webp
  - app/src/main/res/drawable-nodpi/store_dedeman.webp
  - app/src/main/res/drawable-nodpi/store_elefant.webp
  - app/src/main/res/drawable-nodpi/store_generic.webp
  - app/src/main/res/values/strings.xml
  - app/src/main/res/values-ro/strings.xml
autonomous: false
requirements:
  - STORE-01
  - STORE-04

must_haves:
  truths:
    - "Firestore config/stores document is readable by unauthenticated client after seed script runs"
    - "Firestore config/stores document is NOT writable by any client (admin-only via Admin SDK)"
    - "All 15 stores_* string keys resolve in both values/strings.xml and values-ro/strings.xml"
    - "9 bundled store logo drawables exist in drawable-nodpi/ (8 retailers + 1 generic fallback)"
    - "R8 keep rule present for R$drawable so getIdentifier-resolved logos survive release minification"
  artifacts:
    - path: "functions/data/stores.seed.json"
      provides: "Curated list of 8 Romanian retailers in the exact Firestore shape"
      contains: "emag"
    - path: "functions/scripts/seedStores.ts"
      provides: "Idempotent Admin SDK seed script that writes config/stores doc"
      contains: ".set("
    - path: "firestore.rules"
      provides: "Rule allowing world-read and denying all writes on /config/{configId}"
      contains: "match /config/{configId}"
    - path: "tests/rules/firestore.rules.test.ts"
      provides: "Rule tests for config/stores read (allowed) + write (denied)"
      contains: "config/stores"
    - path: "app/proguard-rules.pro"
      provides: "Keep rule preventing R8 from stripping drawables accessed via getIdentifier"
      contains: "-keep class **.R$drawable"
    - path: "app/src/main/res/values/strings.xml"
      provides: "15 new stores_* English string keys"
      contains: "stores_add_to_list_cta"
    - path: "app/src/main/res/values-ro/strings.xml"
      provides: "15 new stores_* Romanian string keys"
      contains: "stores_add_to_list_cta"
    - path: "app/src/main/res/drawable-nodpi/store_generic.webp"
      provides: "Fallback logo used when getIdentifier returns 0"
  key_links:
    - from: "functions/scripts/seedStores.ts"
      to: "functions/data/stores.seed.json"
      via: "fs.readFileSync import of the JSON payload"
      pattern: "stores.seed.json"
    - from: "firestore.rules"
      to: "tests/rules/firestore.rules.test.ts"
      via: "rules tests load firestore.rules from disk and assert config/stores read/write"
      pattern: "config"
---

<objective>
Provision the static foundation Phase 7 requires before any Kotlin code is written:
- Seed data file + idempotent admin seed script for `config/stores`
- Firestore security rules opening `config/{configId}` for read-only public access + matching rule tests
- 9 bundled store logo drawables (8 retailers + 1 generic fallback)
- ProGuard keep rule so R8 release minification does not strip drawables resolved via `context.resources.getIdentifier`
- 15 new `stores_*` string keys in English and Romanian strings files

Purpose: Every downstream plan (domain layer, Store List, WebView) assumes these artifacts exist. Shipping them first unblocks parallel work and satisfies Nyquist (rule tests exist before Wave 2 data-layer tests reference them).

Output: Firestore config doc ready to seed, rules tests green, ProGuard hardened, i18n strings live, and logo drawables on disk. Committed to git. Checkpoint at the end confirms logos were supplied by the human (designer task — not an automatable step).
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-romanian-store-browser/07-CONTEXT.md
@.planning/phases/07-romanian-store-browser/07-UI-SPEC.md
@.planning/phases/07-romanian-store-browser/07-RESEARCH.md

<interfaces>
<!-- Existing firestore.rules structure (tail — add new match block before closing `}`) -->
From firestore.rules (existing Phase 6 rules end at line 84):
```
    // Phase 6: FCM tokens subcollection — each user owns their own tokens.
    match /users/{userId}/fcmTokens/{tokenId} {
      allow read, write, delete: if isSignedIn() && request.auth.uid == userId;
    }
  }
}
```

From tests/rules/firestore.rules.test.ts:
- Uses @firebase/rules-unit-testing `initializeTestEnvironment`
- Helpers: `testEnv.unauthenticatedContext().firestore()` and `testEnv.authenticatedContext(uid).firestore()`
- Assertions: `assertSucceeds`, `assertFails`
- Test runner: Jest via `cd tests/rules && npm test`

From existing strings.xml structure (comment groupings, last entry):
```
    <!-- Phase 6: Email language setting -->
    <string name="settings_email_language_label">Email language</string>
</resources>
```
Romanian variant file is `app/src/main/res/values-ro/strings.xml` and mirrors key set.

From functions/package.json existing deps:
- `firebase-admin: ^13.7.0` (already installed — used by seed script)
- Node 22 runtime
- No `ts-node` dev dep — script will be executable via `npx tsx` OR compiled+run via `tsc && node lib/scripts/seedStores.js`. Use the compiled-then-run approach to match existing `functions/lib` output convention.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed data, seed script, and Firestore security rules</name>
  <read_first>
    - /Users/victorpop/ai-projects/gift-registry/firestore.rules (lines 80-86 — insertion point after Phase 6 fcmTokens rule)
    - /Users/victorpop/ai-projects/gift-registry/tests/rules/firestore.rules.test.ts (full file — copy describe/helper pattern for new config tests)
    - /Users/victorpop/ai-projects/gift-registry/functions/package.json (existing scripts + deps — add `seed:stores` script)
    - /Users/victorpop/ai-projects/gift-registry/functions/src/index.ts (Admin SDK init pattern — `initializeApp` with default credentials)
    - /Users/victorpop/ai-projects/gift-registry/.planning/phases/07-romanian-store-browser/07-RESEARCH.md (sections "Seed Script Shape", "Firestore Rule Addition")
    - /Users/victorpop/ai-projects/gift-registry/.planning/phases/07-romanian-store-browser/07-CONTEXT.md (D-01, D-02, D-04, D-21, D-22)
  </read_first>
  <files>
    - functions/data/stores.seed.json (new)
    - functions/scripts/seedStores.ts (new)
    - functions/package.json (modified — add `seed:stores` npm script)
    - firestore.rules (modified — add /config/{configId} match block)
    - tests/rules/firestore.rules.test.ts (modified — add describe("config/stores") block)
  </files>
  <action>
Create the seed + rules foundation. NO Kotlin in this task.

**Step 1 — `functions/data/stores.seed.json`** (verbatim — the exact 8 stores with displayOrder 10/20/30/... per D-04):
```json
{
  "stores": [
    { "id": "emag", "name": "eMAG", "homepageUrl": "https://www.emag.ro", "displayOrder": 10, "logoAsset": "store_emag" },
    { "id": "altex", "name": "Altex", "homepageUrl": "https://altex.ro", "displayOrder": 20, "logoAsset": "store_altex" },
    { "id": "flanco", "name": "Flanco", "homepageUrl": "https://www.flanco.ro", "displayOrder": 30, "logoAsset": "store_flanco" },
    { "id": "libris", "name": "Libris", "homepageUrl": "https://www.libris.ro", "displayOrder": 40, "logoAsset": "store_libris" },
    { "id": "carturesti", "name": "Cărturești", "homepageUrl": "https://carturesti.ro", "displayOrder": 50, "logoAsset": "store_carturesti" },
    { "id": "ikea", "name": "IKEA", "homepageUrl": "https://www.ikea.com/ro/ro/", "displayOrder": 60, "logoAsset": "store_ikea" },
    { "id": "dedeman", "name": "Dedeman", "homepageUrl": "https://www.dedeman.ro", "displayOrder": 70, "logoAsset": "store_dedeman" },
    { "id": "elefant", "name": "Elefant", "homepageUrl": "https://www.elefant.ro", "displayOrder": 80, "logoAsset": "store_elefant" }
  ]
}
```

**Step 2 — `functions/scripts/seedStores.ts`** (idempotent `.set` per D-22). Use `firebase-admin` already in deps. Must read the JSON payload and write to `config/stores`:
```ts
import * as admin from "firebase-admin";
import * as fs from "fs";
import * as path from "path";

// Initialize Admin SDK. When run via `firebase emulators:exec` or with
// GOOGLE_APPLICATION_CREDENTIALS set, default credentials work. When run
// directly against prod, user must have ADC set up (`gcloud auth application-default login`).
if (admin.apps.length === 0) {
  admin.initializeApp();
}

async function main(): Promise<void> {
  const jsonPath = path.resolve(__dirname, "../data/stores.seed.json");
  const raw = fs.readFileSync(jsonPath, "utf8");
  const payload = JSON.parse(raw) as { stores: unknown[] };

  // Idempotent: `.set` overwrites the doc every run with the canonical seed
  // (D-22). This is intentional — the JSON file is the single source of truth
  // for the store list; running the script twice yields the same result.
  await admin
    .firestore()
    .collection("config")
    .doc("stores")
    .set(payload);

  console.log(`Seeded config/stores with ${payload.stores.length} entries`);
}

main()
  .then(() => process.exit(0))
  .catch((err) => {
    console.error("Seed failed:", err);
    process.exit(1);
  });
```

**Step 3 — `functions/package.json`**: Add `"seed:stores": "npm run build && node lib/scripts/seedStores.js"` under `scripts`. Do NOT add any new `dependencies` — `firebase-admin` is already present. Preserve all existing fields exactly as-is.

**Step 4 — `firestore.rules`**: Add the `/config/{configId}` match block after the existing `users/{userId}/fcmTokens/{tokenId}` rule (line 83), BEFORE the closing `}` at line 84. Block text:
```
    // Phase 7 (STORE-01 / D-21): config collection — world-readable so the
    // Store Browser can load the store list even for unauthenticated
    // onboarding states. No client writes — admin only via seed script
    // (Admin SDK bypasses rules).
    match /config/{configId} {
      allow read: if true;
      allow write: if false;
    }
```

**Step 5 — `tests/rules/firestore.rules.test.ts`**: Add a new `describe("config/stores rules", ...)` block after the last existing describe. Reuse existing `testEnv` / `assertSucceeds` / `assertFails` imports. Tests MUST cover these cases (minimum 3 tests):
```ts
describe("config/stores rules", () => {
  it("allows unauthenticated read of config/stores", async () => {
    await testEnv.withSecurityRulesDisabled(async (ctx) => {
      await setDoc(doc(ctx.firestore(), "config", "stores"), { stores: [] });
    });
    const unauthDb = testEnv.unauthenticatedContext().firestore();
    await assertSucceeds(getDoc(doc(unauthDb, "config", "stores")));
  });

  it("allows authenticated read of config/stores", async () => {
    await testEnv.withSecurityRulesDisabled(async (ctx) => {
      await setDoc(doc(ctx.firestore(), "config", "stores"), { stores: [] });
    });
    const authDb = testEnv.authenticatedContext("user1").firestore();
    await assertSucceeds(getDoc(doc(authDb, "config", "stores")));
  });

  it("denies unauthenticated write to config/stores", async () => {
    const unauthDb = testEnv.unauthenticatedContext().firestore();
    await assertFails(setDoc(doc(unauthDb, "config", "stores"), { stores: [] }));
  });

  it("denies authenticated write to config/stores", async () => {
    const authDb = testEnv.authenticatedContext("user1").firestore();
    await assertFails(setDoc(doc(authDb, "config", "stores"), { stores: [] }));
  });
});
```
Keep the existing tests untouched — append the new describe only.

**Do NOT**:
- Run the seed script against prod from this task (deploy is deferred)
- Modify `firebase-admin` version
- Change Node runtime
- Touch unrelated describe blocks in the rules test file
  </action>
  <acceptance_criteria>
    - `ls functions/data/stores.seed.json` succeeds
    - `grep "emag" functions/data/stores.seed.json` returns a match
    - `grep "displayOrder" functions/data/stores.seed.json` returns exactly 8 matches
    - `ls functions/scripts/seedStores.ts` succeeds
    - `grep ".set(" functions/scripts/seedStores.ts` returns a match
    - `grep "seed:stores" functions/package.json` returns a match
    - `grep "match /config/{configId}" firestore.rules` returns a match
    - `grep "allow read: if true" firestore.rules` returns a match (within config block)
    - `grep "config/stores rules" tests/rules/firestore.rules.test.ts` returns a match
    - `cd functions && npm run build` exits 0 (the new .ts compiles with existing tsconfig)
    - `cd tests/rules && npm test` exits 0 with all new `config/stores` tests green (requires Firestore emulator running on :8080 per existing test infra)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/victorpop/ai-projects/gift-registry/functions && npm run build && cd /Users/victorpop/ai-projects/gift-registry/tests/rules && npm test</automated>
  </verify>
  <done>Seed JSON + script exist; firestore.rules has the config block; rules tests include 4 config/stores tests and all pass against the emulator.</done>
</task>

<task type="auto">
  <name>Task 2: ProGuard keep rule + stores_* strings (en + ro)</name>
  <read_first>
    - /Users/victorpop/ai-projects/gift-registry/app/proguard-rules.pro (currently 3-line stub)
    - /Users/victorpop/ai-projects/gift-registry/app/src/main/res/values/strings.xml (last block — Phase 6 email language; append new Phase 7 comment + keys here)
    - /Users/victorpop/ai-projects/gift-registry/app/src/main/res/values-ro/strings.xml (Romanian mirror)
    - /Users/victorpop/ai-projects/gift-registry/.planning/phases/07-romanian-store-browser/07-UI-SPEC.md ("Copywriting Contract" table, "Android String Keys" section — copy XML verbatim)
    - /Users/victorpop/ai-projects/gift-registry/.planning/phases/07-romanian-store-browser/07-RESEARCH.md ("Pitfall 2: getIdentifier R8 Stripping")
  </read_first>
  <files>
    - app/proguard-rules.pro (modified — add drawable keep rule)
    - app/src/main/res/values/strings.xml (modified — append stores_* English keys)
    - app/src/main/res/values-ro/strings.xml (modified — append stores_* Romanian keys)
  </files>
  <action>
**Step 1 — `app/proguard-rules.pro`**: Append the following block (per Research Pitfall 2 and D-03). This is a no-op in debug builds and critical in release builds:
```
# Phase 7 (STORE-01): Store logos are loaded at runtime via
# context.resources.getIdentifier(logoAsset, "drawable", packageName).
# R8 resource shrinking otherwise considers these drawables "unused" because
# there is no direct R.drawable.store_* reference in compiled code and
# removes them from the release APK. Keep the R$drawable inner class so
# identifier resolution continues to find them.
-keep class **.R$drawable { *; }
```

**Step 2 — `app/src/main/res/values/strings.xml`**: Append BEFORE the closing `</resources>` tag. Copy verbatim from UI-SPEC Copywriting Contract — 15 keys:
```xml
    <!-- Phase 7: Store Browser entry points -->
    <string name="stores_fab_label">New</string>
    <string name="stores_browse_label">Browse stores</string>
    <string name="stores_create_registry_label">Create registry</string>

    <!-- Phase 7: Store List screen -->
    <string name="stores_list_title">Browse stores</string>
    <string name="stores_list_error_heading">Couldn\'t load stores</string>
    <string name="stores_list_error_body">Check your connection and try again.</string>
    <string name="stores_retry">Try again</string>
    <string name="stores_logo_content_description">Store</string>

    <!-- Phase 7: Store Browser (WebView) screen -->
    <string name="stores_webview_close">Close</string>
    <string name="stores_webview_error_heading">Page didn\'t load</string>
    <string name="stores_webview_error_body">Check your connection and try again.</string>
    <string name="stores_add_to_list_cta">Add to list</string>
    <string name="stores_external_link_blocked">This link opens in an external app</string>

    <!-- Phase 7: Registry picker (add-item screen extension) -->
    <string name="stores_registry_picker_label">Add to registry:</string>
```

**Step 3 — `app/src/main/res/values-ro/strings.xml`**: Mirror the same 15 keys with Romanian copy from UI-SPEC Copywriting Contract. Append BEFORE closing `</resources>`:
```xml
    <!-- Phase 7: Store Browser entry points -->
    <string name="stores_fab_label">Nou</string>
    <string name="stores_browse_label">Răsfoiește magazine</string>
    <string name="stores_create_registry_label">Creează o listă</string>

    <!-- Phase 7: Store List screen -->
    <string name="stores_list_title">Răsfoiește magazine</string>
    <string name="stores_list_error_heading">Nu s-au putut încărca magazinele</string>
    <string name="stores_list_error_body">Verifică conexiunea și încearcă din nou.</string>
    <string name="stores_retry">Încearcă din nou</string>
    <string name="stores_logo_content_description">Magazin</string>

    <!-- Phase 7: Store Browser (WebView) screen -->
    <string name="stores_webview_close">Închide</string>
    <string name="stores_webview_error_heading">Pagina nu s-a încărcat</string>
    <string name="stores_webview_error_body">Verifică conexiunea și încearcă din nou.</string>
    <string name="stores_add_to_list_cta">Adaugă la listă</string>
    <string name="stores_external_link_blocked">Acest link se deschide într-o altă aplicație</string>

    <!-- Phase 7: Registry picker (add-item screen extension) -->
    <string name="stores_registry_picker_label">Adaugă în lista:</string>
```

**Do NOT**:
- Hardcode any of these strings in Kotlin files (Kotlin consumption lands in later plans)
- Translate store names — "eMAG", "IKEA", "Cărturești" etc are proper nouns and live in Firestore, not strings.xml (per D-18)
- Remove or reorder existing strings — append-only
  </action>
  <acceptance_criteria>
    - `grep "R\$drawable" app/proguard-rules.pro` returns a match
    - `grep -c "stores_" app/src/main/res/values/strings.xml` returns at least 15
    - `grep -c "stores_" app/src/main/res/values-ro/strings.xml` returns at least 15
    - `grep "stores_add_to_list_cta" app/src/main/res/values/strings.xml` returns a match
    - `grep "stores_add_to_list_cta" app/src/main/res/values-ro/strings.xml` returns a match
    - `grep "Adaugă la listă" app/src/main/res/values-ro/strings.xml` returns a match
    - `grep "Răsfoiește magazine" app/src/main/res/values-ro/strings.xml` returns a match
    - `./gradlew :app:processDebugResources` exits 0 (no duplicate-string or malformed-XML errors)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/victorpop/ai-projects/gift-registry && ./gradlew :app:processDebugResources</automated>
  </verify>
  <done>ProGuard has the drawable keep rule; both strings.xml files compile and contain all 15 stores_* keys in the correct language.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Supply 9 store logo .webp files</name>
  <files>
    - app/src/main/res/drawable-nodpi/store_emag.webp
    - app/src/main/res/drawable-nodpi/store_altex.webp
    - app/src/main/res/drawable-nodpi/store_flanco.webp
    - app/src/main/res/drawable-nodpi/store_libris.webp
    - app/src/main/res/drawable-nodpi/store_carturesti.webp
    - app/src/main/res/drawable-nodpi/store_ikea.webp
    - app/src/main/res/drawable-nodpi/store_dedeman.webp
    - app/src/main/res/drawable-nodpi/store_elefant.webp
    - app/src/main/res/drawable-nodpi/store_generic.webp
  </files>
  <action>
Claude cannot perform this step — the retailers' logos are trademarked assets, and no public CLI issues them. Automated tasks 1–2 have already landed the Wave 1 scaffolding; the only remaining artifact is 9 WebP logo files that must be supplied by the human operator.

**Place these 9 files in `app/src/main/res/drawable-nodpi/`** (create the directory first — it does NOT currently exist in the repo):

  1. `store_emag.webp`
  2. `store_altex.webp`
  3. `store_flanco.webp`
  4. `store_libris.webp`
  5. `store_carturesti.webp`
  6. `store_ikea.webp`
  7. `store_dedeman.webp`
  8. `store_elefant.webp`
  9. `store_generic.webp` — fallback (grey placeholder + storefront icon per CONTEXT `<specifics>`)

**Constraints:**
- Format: WebP (not PNG, not SVG) — matches the `.webp` filenames referenced by Firestore `logoAsset` strings
- Size target: each logo renders at 64dp in the store card; supply ~192–256px source images so density-independent scaling looks crisp
- Filenames MUST match exactly (lowercase, underscores) — these become resource identifiers resolved at runtime via `context.resources.getIdentifier("store_emag", "drawable", packageName)`
- Dir is `drawable-nodpi/` (not `drawable/`) so the same asset is used at all densities — logos are vector-ish brand marks, no density multiplication needed
- Do NOT commit large (>100 KB) images — WebP lossy should keep each file under 30 KB

If real brand logos are not yet available, placeholder grey-with-storefront images (same filenames) are acceptable for intermediate development — Plan 03 UAT will flag placeholders as a follow-up concern.
  </action>
  <verify>
    <automated>ls app/src/main/res/drawable-nodpi/store_emag.webp app/src/main/res/drawable-nodpi/store_altex.webp app/src/main/res/drawable-nodpi/store_flanco.webp app/src/main/res/drawable-nodpi/store_libris.webp app/src/main/res/drawable-nodpi/store_carturesti.webp app/src/main/res/drawable-nodpi/store_ikea.webp app/src/main/res/drawable-nodpi/store_dedeman.webp app/src/main/res/drawable-nodpi/store_elefant.webp app/src/main/res/drawable-nodpi/store_generic.webp && ./gradlew :app:assembleDebug</automated>
  </verify>
  <done>All 9 WebP files exist in `app/src/main/res/drawable-nodpi/`; `./gradlew :app:assembleDebug` exits 0 with no resource warnings about drawable-nodpi.</done>
  <resume-signal>Type "logos placed" once all 9 .webp files are in `app/src/main/res/drawable-nodpi/` and `./gradlew :app:assembleDebug` passes. If you choose to proceed without real retailer logos (grey placeholders for all 8), say "placeholders only" so downstream plans can flag this for UAT follow-up.</resume-signal>
</task>

</tasks>

<verification>
**Task 1 gate:** `cd functions && npm run build` + `cd tests/rules && npm test` both exit 0 with new config/stores tests green.

**Task 2 gate:** `./gradlew :app:processDebugResources` exits 0; both strings.xml files contain 15 new `stores_*` keys; proguard-rules.pro has the keep rule.

**Task 3 gate:** Human confirms 9 .webp files live in `drawable-nodpi/`; `./gradlew :app:assembleDebug` exits 0.

**Plan-level gate:** All artifacts required by Plans 07-01..03 exist on disk and are committed.
</verification>

<success_criteria>
- [ ] `functions/data/stores.seed.json` contains exactly 8 stores with displayOrder 10/20/30/40/50/60/70/80
- [ ] `functions/scripts/seedStores.ts` compiles (`cd functions && npm run build` exits 0) and uses `.set` (idempotent)
- [ ] `firestore.rules` has `match /config/{configId}` with `allow read: if true; allow write: if false;`
- [ ] `tests/rules/firestore.rules.test.ts` contains `describe("config/stores rules", ...)` with ≥4 tests; all pass
- [ ] `app/proguard-rules.pro` contains `-keep class **.R$drawable { *; }`
- [ ] `app/src/main/res/values/strings.xml` and `values-ro/strings.xml` each contain all 15 `stores_*` keys with exact copy from UI-SPEC
- [ ] `app/src/main/res/drawable-nodpi/` exists and contains 9 `store_*.webp` files (8 retailers + generic fallback)
- [ ] `./gradlew :app:assembleDebug` exits 0 after logos are placed
</success_criteria>

<output>
After completion, create `.planning/phases/07-romanian-store-browser/07-00-SUMMARY.md` recording:
- Seed JSON + script paths
- Rule block added (line range)
- ProGuard rule added
- Count of new string keys + languages covered
- Logo file list + whether real brand logos or placeholders were supplied
- Any deviations from the plan (and why)
</output>
