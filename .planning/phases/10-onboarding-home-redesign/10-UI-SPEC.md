---
phase: 10
slug: onboarding-home-redesign
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-21
---

# Phase 10 — UI Design Contract: Onboarding + Home Redesign

> Visual and interaction contract for the two owner-facing screens being re-skinned in Phase 10:
> Screen 06 (AuthScreen) and Screen 07 (RegistryListScreen).
> Generated by gsd-ui-researcher. Consumed by gsd-planner and gsd-executor.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | Compose Material3 + GiftMaison custom theme (Phase 8) |
| Preset | not applicable — native Android Compose |
| Component library | Jetpack Compose BOM 2026.03.00, Material3 |
| Icon library | Material Icons Extended (existing project dependency) |
| Font | Instrument Serif (display/wordmark), Inter (body/UI), JetBrains Mono (caps labels) — all via Compose Google Fonts, Phase 8 |

Source: CLAUDE.md, GiftMaisonFonts.kt, GiftMaisonTypography.kt

---

## Design System — Primitive Reuse Contract

All Phase 8 and Phase 9 primitives are shipped and must be consumed as-is. Do not re-declare or shadow any of the following:

| Primitive | Access Path | Notes |
|-----------|-------------|-------|
| Colour tokens | `GiftMaisonTheme.colors.*` | 13 Housewarming tokens — see GiftMaisonColors.kt |
| Typography roles | `GiftMaisonTheme.typography.*` | 10 roles — see GiftMaisonTypography.kt |
| Shape tokens | `GiftMaisonTheme.shapes.*` | radii 8/10/12/14/16/22/pill — see GiftMaisonShapes.kt |
| Spacing tokens | `GiftMaisonTheme.spacing.*` | edge/edgeWide/gap4..gap20 — see GiftMaisonSpacing.kt |
| Shadow modifiers | `.fabShadow()` / `.googleBannerShadow()` / `.bottomSheetShadow()` | see GiftMaisonShadows.kt |
| Wordmark | `GiftMaisonWordmark(modifier, fontSize)` | see GiftMaisonWordmark.kt |
| Bottom nav + FAB | `GiftMaisonBottomNav` + `GiftMaisonFab` | Phase 9, wired in AppNavigation — no Phase 10 work |
| Status chips | `StatusChip` + `PulsingDot` | Phase 9 — not used on Home cards per handoff |

Source: GiftMaisonColors.kt, GiftMaisonTypography.kt, GiftMaisonShapes.kt, GiftMaisonSpacing.kt, GiftMaisonShadows.kt, GiftMaisonWordmark.kt

---

## Spacing Scale

GiftMaison spacing tokens (Phase 8) are the spacing system. Phase 10 uses only named tokens — no raw `dp` literals on new composables.

| Token | Value | Phase 10 Usage |
|-------|-------|----------------|
| `spacing.gap4` | 4 dp | Bullet separators in stats line, icon-to-text micro gaps |
| `spacing.gap8` | 8 dp | Intra-field group gaps, divider margin, card-section spacing |
| `spacing.gap10` | 10 dp | Inter-card vertical gap in LazyColumn |
| `spacing.gap12` | 12 dp | Vertical gap between form field pairs on Auth screen |
| `spacing.gap14` | 14 dp | Vertical gap between field groups (first/last name → email) |
| `spacing.gap16` | 16 dp | Section-to-section vertical gap on Auth; default padding |
| `spacing.gap20` | 20 dp | Hero bottom padding; Google banner internal padding |
| `spacing.edge` | 16 dp | Screen horizontal edge padding (Auth content, card horizontal) |
| `spacing.edgeWide` | 20 dp | Wider horizontal edge (Google banner side padding per handoff) |

**Touch-target exception:** Avatar button on Home top bar — minimum 44 dp tap target (`Modifier.size(44.dp)` wrapping 30 dp circle). Consistent with Android accessibility minimum.

Source: GiftMaisonSpacing.kt, CONTEXT.md, handoff § 06 + § 07

---

## Typography

**All size, weight, and line-height values are fixed in Phase 8 `GiftMaisonTypography.kt` and re-used unchanged. Phase 10 does not declare or override any typography scale values — this table only documents which existing Phase 8 roles each Phase 10 element consumes.**

| Role | Token | Phase 10 Usage |
|------|-------|----------------|
| Display XL | `typography.displayXL` | Home "Your registries" headline |
| Display L | `typography.displayL` | Auth headline lines ("Start your", "first registry.") |
| Display S | `typography.displayS` | Registry card title (primary card) |
| Body L | `typography.bodyL` | Auth CTA button labels; primary action labels |
| Body M | `typography.bodyM` | Auth field labels, helper text, divider text; card secondary text |
| Body M Emphasis | `typography.bodyMEmphasis` | Auth "Log in" footer ghost pill label; field label emphasis |
| Body S | `typography.bodyS` | Registry card date, location, stats line on secondary cards |
| Body XS | `typography.bodyXS` | Terms line on Auth; occasion pill label |
| Mono Caps | `typography.monoCaps` | Home stats caption ("N active · M items total"); tab labels |

**Special: Headline italic-accent construction on Auth screen**

The Auth headline is NOT a single Text. It is two lines stacked:
- Line 1: "Start your" — `typography.displayL`, `FontStyle.Italic`, `colors.inkSoft`
- Line 2: "first registry." — `typography.displayL`, `FontStyle.Italic`; word "first registry" in `colors.ink`, period in `colors.accent`

Implemented as `buildAnnotatedString` or separate `Text` composables in a `Column`. The period colour uses the same `wordmarkAnnotatedString` pattern from `GiftMaisonWordmark.kt`.

Source: GiftMaisonTypography.kt, CONTEXT.md, handoff § 06

---

## Color

Full palette is from `housewarmingColors()` in GiftMaisonColors.kt. Phase 10 60/30/10 usage:

| Role | Token | Hex | Phase 10 Usage |
|------|-------|-----|----------------|
| Dominant (60%) | `colors.paper` | `#F7F2E9` | Auth screen background; Home screen background |
| Secondary (30%) | `colors.paperDeep` | `#EDE5D5` | Auth input field backgrounds; segmented tab track; secondary registry cards |
| Accent (10%) | `colors.accent` | `#C8623A` | Reserved elements only (see below) |
| Destructive | `colors.ink` on errorContainer | — | Delete registry confirmation (existing pattern, no new destructive actions in Phase 10) |

**Accent (`colors.accent` #C8623A) is reserved for exactly these elements in Phase 10:**
1. Auth screen — period in "first registry." headline
2. Auth screen — wordmark period in top bar
3. Auth screen — caret color on focused email field (AnimatedContent on focus state)
4. Auth screen — "→" italic serif arrow inside Google banner
5. Auth screen — concentric accentInk rings in Google banner top-right
6. Auth screen — Sign up CTA button background (ink pill = `colors.ink` fill; accent accent only on the secondary "Log in" ghost pill border)
7. Home screen — wordmark period in top bar
8. Home screen — selected segmented tab state (accentSoft pill + accent stroke on selected label)
9. Home screen — occasion pill border/background on primary card only

**Additional semantic colours:**
| Token | Hex | Phase 10 Usage |
|-------|-----|----------------|
| `colors.ink` | `#2A2420` | Primary card background; Sign up CTA fill; primary text |
| `colors.paper` | `#F7F2E9` | Text on primary card (ink surface); avatar initials ring if needed |
| `colors.inkSoft` | `#6A5E52` | Auth headline line 1 "Start your"; secondary body text |
| `colors.inkFaint` | `#9C8E7F` | Placeholder text in fields; per-tab empty state copy |
| `colors.line` | `#DDD4C4` | Secondary card border; divider lines; field border (unfocused) |
| `colors.accentSoft` | `#F3DED0` | Selected tab pill background |
| `colors.accentInk` | `#FCF8EF` | Text on accent/accentSoft surfaces |
| `colors.second` | `#4F7050` | Avatar circle background (olive) |
| `colors.secondSoft` | `#D7E2CE` | Avatar text on second |
| `colors.warn` | `#D29447` | Error banner background at 0.15 alpha (`colors.warn.copy(alpha = 0.15f)`) |

Source: GiftMaisonColors.kt, CONTEXT.md § Home screen + Auth screen decisions

---

## Screen 06 — Onboarding / Auth Screen Contract

### Layout

Full-screen `Column` with `verticalScroll`. Background: `colors.paper`. No `Scaffold` top bar (wordmark is inline, not in a Material3 TopAppBar).

```
┌─────────────────────────────────┐
│  [wordmark]          spacing.edge horizontal   │
│                                 │
│  "Start your          displayL italic inkSoft  │
│   first registry."    displayL italic + accent period │
│                                 │
│  [Google banner]      radius16, googleBannerShadow │
│                                 │
│  ─── or sign up with email ───  │
│                                 │
│  [First name field]   (sign-up mode only)      │
│  [Last name field]    (sign-up mode only)      │
│  [Email field]        accent caret on focus    │
│  [Password field]     eye-toggle trailing icon │
│  [Confirm pwd field]  (sign-up mode only)      │
│                                 │
│  [Sign up CTA]        ink pill, full-width     │
│                                 │
│  Terms line                     │
│                                 │
│  [Log in footer pill] ghost border, centered   │
└─────────────────────────────────┘
```

### Top Bar (inline, not TopAppBar)

- `GiftMaisonWordmark(fontSize = 20.sp)` left-aligned
- Padding: `spacing.edge` horizontal, `spacing.gap16` vertical top
- No back arrow, no actions

### Headline

```kotlin
Column(modifier = Modifier.padding(horizontal = spacing.edge)) {
    Text("Start your", style = typography.displayL, fontStyle = Italic, color = colors.inkSoft)
    // Second line as AnnotatedString: "first registry" in colors.ink + "." in colors.accent
    Text(annotated("first registry."), style = typography.displayL, fontStyle = Italic)
}
```

Vertical gap before Google banner: `spacing.gap20`

### Google Banner

Shape: `shapes.radius16`. Shadow: `Modifier.googleBannerShadow(colors.accent)`.
Background: `colors.paperDeep`. Padding: `spacing.gap14` vertical, `spacing.edgeWide` horizontal.

Internal layout:
- Left: 20×20 dp white circle with Google "G" icon (Material Icons: `Icons.Default.`... or a drawn `Canvas` circle). Background white, icon `#4285F4`/`#EA4335`/`#FBBC05`/`#34A853` sectors — or use a single-colour "G" in `colors.ink` as acceptable placeholder.
- Centre: `typography.bodyL` "Continue with Google" in `colors.ink`
- Right: `→` symbol in `FontStyle.Italic`, `InstrumentSerifFamily`, 18.sp, `colors.accent`
- Top-right corner decoration: 3 concentric circles drawn with `Canvas` (or layered `Box` circles using `Modifier.border`):
  - Outer ring: ~60 dp diameter, `colors.accentInk` at alpha 0.08, stroke 1 dp
  - Middle ring: ~40 dp, alpha 0.12, stroke 1 dp
  - Inner ring: ~24 dp, alpha 0.18, stroke 1 dp
  - Positioned with `Modifier.align(Alignment.TopEnd).offset(x = 8.dp, y = (-8).dp)` or `Canvas` absolute coords

Full banner is tappable (`Modifier.clickable`), calls existing `viewModel.signInWithGoogle()`.

### Divider

`Row` with `HorizontalDivider` + `colors.line`, centred `typography.bodyS` "or sign up with email" in `colors.inkFaint`.

### Input Fields

Field style: `OutlinedTextField`, `shape = shapes.radius12`, `colors = TextFieldDefaults.outlinedTextFieldColors` with:
- `containerColor = colors.paperDeep`
- `focusedBorderColor = colors.accent`
- `unfocusedBorderColor = colors.line`
- `cursorColor = colors.accent`
- `focusedLabelColor = colors.accent`
- `unfocusedLabelColor = colors.inkFaint`
- `placeholderColor = colors.inkFaint`
- `textColor = colors.ink`

Fields in sign-up mode (in order): First name, Last name, Email, Password, Confirm password.
Fields in sign-in mode: Email, Password.

First name + Last name in sign-up: placed in a `Row` with `spacing.gap8` gap between them (each `Modifier.weight(1f)`), matching handoff 2-column layout. Gap above: `spacing.gap12`.

Email field: `KeyboardType.Email`, `ImeAction.Next`. No leading icon (handoff has none — drop the existing `Icons.Default.Email` leading icon).

Password field: trailing eye-toggle `IconButton` (existing implementation kept). The eye `IconButton` uses `contentDescription = stringResource(R.string.auth_password_show)` when the field is masked and `contentDescription = stringResource(R.string.auth_password_hide)` when revealed — add these keys if missing from strings.xml. No leading icon.

Confirm password: sign-up mode only, same style as password field.

### Sign Up CTA

Shape: `shapes.pill`. Background: `colors.ink`. Text: `typography.bodyL`, `colors.paper`, "Sign up" (sign-up mode) / "Log in" (sign-in mode).

Height: 52 dp (full-width). Gap above: `spacing.gap20`. Loading state: `CircularProgressIndicator` centred, `colors.paper`, size 20 dp.

### Terms Line

Centred single line, `typography.bodyXS`, `colors.inkFaint`:
`"By signing up you agree to our [Terms] and [Privacy Policy]"`
Terms and Privacy Policy as tappable spans using `AnnotatedString` with `StringAnnotation`, each in `colors.accent`, `TextDecoration.Underline`. Opens `Intent.ACTION_VIEW` to placeholder URLs from string keys `auth_terms_url` / `auth_privacy_url`. Gap above: `spacing.gap12`.

### Footer "Log in" Pill

Centred, `shapes.pill` border, `colors.line` stroke 1 dp, transparent background.
Label: `typography.bodyMEmphasis`, `colors.ink`, "Already have an account? Log in"
Alternatively: separate `Text("Already have an account?")` + underlined `TextButton("Log in")` in a `Row` — Claude's choice per CONTEXT.md.
Gap above: `spacing.gap16`. Bottom screen padding: `spacing.gap20`.

### Mode Toggle Behaviour

Footer pill / "Log in" button calls `isSignUpMode = false` (and `viewModel.clearError()`). Header footer text switches via existing `auth_have_account_prompt` / `auth_no_account_prompt` keys. Default mode: sign-up (`isSignUpMode = true` as initial state — matches CONTEXT.md decision).

Source: CONTEXT.md § Onboarding + Auth decisions, handoff § 06

---

## Screen 07 — Home / All Registries Screen Contract

### Layout

`Scaffold` with custom top bar (inline `Row`, not `TopAppBar`). Background: `colors.paper`. Bottom padding accounts for Phase 9 `GiftMaisonBottomNav` height (already handled by Scaffold + nav padding in AppNavigation).

```
┌─────────────────────────────────┐
│  [wordmark]          [avatar]   │  top bar inline Row
│                                 │
│  Your registries      displayXL │
│  3 active · 12 items total  monoCaps inkFaint │
│                                 │
│  [Active][Drafts][Past]  segmented tabs │
│                                 │
│  ┌──────────────────────┐       │
│  │ [16:9 hero image]    │  PRIMARY card (ink bg) │
│  │ [occasion pill] [date]│       │
│  │ Registry title        │       │
│  │ N items · M reserved  │       │
│  └──────────────────────┘       │
│                                 │
│  ┌──────────────────────┐       │
│  │ [16:9 hero image]    │  secondary card │
│  └──────────────────────┘       │
│  ...                            │
└─────────────────────────────────┘
```

### Top Bar (inline Row, not TopAppBar)

Full-width `Row`, `Modifier.padding(horizontal = spacing.edge, vertical = spacing.gap16)`, `verticalAlignment = Alignment.CenterVertically`.

- Left: `GiftMaisonWordmark(fontSize = 20.sp)`
- Spacer `Modifier.weight(1f)`
- Right: `AvatarButton` composable (30 dp circle, `colors.second` background, initials `typography.bodyMEmphasis` `colors.paper`, tappable at min 44 dp, navigates to `SettingsKey`)

Avatar initials derivation: `displayName.split(" ").take(2).map { it.first().uppercaseChar() }.joinToString("")`. Fallback: first char of email. Final fallback: `"?"`.

### Headline + Caption

`Column`, `Modifier.padding(horizontal = spacing.edge)`:
- `Text("Your registries", style = typography.displayXL, color = colors.ink)`
- Gap: `spacing.gap4`
- `Text("N active · M items total", style = typography.monoCaps, color = colors.inkFaint)` — stats derived client-side from filtered lists counts

Gap below: `spacing.gap16`

### Segmented Tabs

Custom `SegmentedTabs` composable (NOT Material3 `SegmentedButton`). Lives in `ui/registry/list/SegmentedTabs.kt` or `ui/common/SegmentedTabs.kt` — executor's choice.

Track: full-width pill shape (`shapes.pill`), `colors.paperDeep` background, `spacing.gap8` vertical padding, `spacing.edge` horizontal padding on the outer container.

Each tab: equal-width `Box(Modifier.weight(1f))`, centred label `typography.monoCaps`.

Selected state: white (`colors.paper`) pill with `colors.line` drop shadow at 2 dp elevation drawn behind the pill. Label: `colors.ink`. Animated: `animateColorAsState` for background, no slide animation needed.

Unselected state: transparent background. Label: `colors.inkFaint`.

Tabs: "Active" / "Drafts" / "Past". Keys: `home_tab_active`, `home_tab_drafts`, `home_tab_past`.

Tab state: `var selectedTab by rememberSaveable { mutableStateOf(0) }` — persists rotation, resets on cold start (CONTEXT.md).

### Tab Filtering Logic (client-side, no ViewModel change)

```
Active  = eventDateMs == null || eventDateMs >= startOfTodayMs
Past    = eventDateMs != null && eventDateMs < startOfTodayMs
Drafts  = title.isBlank() || items.isEmpty()  // heuristic, CONTEXT.md approved
```

`startOfTodayMs` computed once per composition: `LocalDate.now().atStartOfDay().toEpochMilli()` (Java 8 time API, available on API 26+; use `Calendar.getInstance()` for minSdk 23 compat).

### Per-Tab Empty State

When filtered list is empty: centred `Text(style = typography.bodyM, color = colors.inkFaint)` with `spacing.gap20` bottom padding. No illustration, no CTA (CONTEXT.md + REQUIREMENTS.md § Out of Scope).

### Registry Card

Shape: `shapes.radius16`. Horizontal edge: `spacing.edge`. Inter-card gap: `spacing.gap10`.

**Primary card** (`isPrimary = registries.maxByOrNull { it.updatedAt }?.id == registry.id`):
- Background: `colors.ink`
- Image: `AsyncImage(Coil)`, aspect ratio 16:9, `ColorFilter.colorMatrix(ColorMatrix().apply { setToScale(0.7f, 0.7f, 0.7f, 1f) })` for 70% brightness darken
- Occasion pill: top-left overlay, `shapes.pill`, `colors.accent` background, `colors.paper` text, `typography.bodyXS`, `spacing.gap4` vertical + `spacing.gap8` horizontal padding
- Date: bottom-right overlay, `typography.bodyXS`, `colors.paper`, `spacing.gap8` margin from corner
- Title: `typography.displayS`, `colors.paper`, `spacing.gap8` top margin below image area
- Stats line: `typography.bodyS`, `colors.paper` at 0.6 alpha, monoCaps bullet `\u2022` separators
- Padding: `spacing.gap16` on all text content edges

**Secondary card** (all non-primary):
- Background: `colors.paperDeep`
- Border: `Modifier.border(1.dp, colors.line, shapes.radius16)`
- Image: full-brightness (no filter)
- Occasion pill: top-left overlay, `shapes.pill`, `colors.paperDeep` background, `colors.inkSoft` text
- Date: bottom-right overlay, `typography.bodyXS`, `colors.inkSoft`
- Title: `typography.displayS`, `colors.ink`
- Stats line: `typography.bodyS`, `colors.inkFaint`, monoCaps bullet `\u2022` separators

Image loading: `AsyncImage(model = registry.imageUrl, contentScale = ContentScale.Crop)`. Placeholder: `colors.paperDeep` fill. Error: `colors.paperDeep` fill.

Card tap: `Modifier.clickable { onNavigateToDetail(registry.id) }` — existing nav path preserved.

### Stats Line Construction

```kotlin
"${itemCount} items \u2022 ${reservedCount} reserved \u2022 ${givenCount} given"
```

If per-registry stats not available (Phase 10 deferral per CONTEXT.md), render "N items" where N = 0 as a known approximation. Mark with `// TODO: Phase 10 stats aggregation — see CONTEXT.md deferred section`.

Source: CONTEXT.md § Home screen decisions, handoff § 07

---

## Component Inventory — New Composables

Phase 10 introduces these new composable files:

| Composable | File | Description |
|------------|------|-------------|
| `GoogleBanner` | `ui/auth/GoogleBanner.kt` | Terracotta pill with Google G, arrow, concentric rings, `googleBannerShadow` |
| `AuthHeadline` | `ui/auth/AuthHeadline.kt` | Two-line stacked italic serif headline with accent period on line 2 |
| `SegmentedTabs` | `ui/registry/list/SegmentedTabs.kt` | Custom pill-track segmented control, 3 tabs, animated selected pill |
| `RegistryCardPrimary` | `ui/registry/list/RegistryCard.kt` | Dark ink card with 16:9 hero, darkened image, occasion pill, date overlay |
| `RegistryCardSecondary` | `ui/registry/list/RegistryCard.kt` | PaperDeep card with line border, full-brightness image |
| `AvatarButton` | `ui/common/AvatarButton.kt` | 30 dp circle with initials, second bg, 44 dp tap target, navigates to SettingsKey |
| `HomeTopBar` | `ui/registry/list/HomeTopBar.kt` | Wordmark + AvatarButton inline row — NOT a Material3 TopAppBar |

All composables are `internal` to their package except where consumed across packages.

---

## Animation Contract

| Animation | Spec | Where |
|-----------|------|-------|
| Segmented tab selected pill | `animateColorAsState(durationMillis = 200)` for label colour; no positional animation | `SegmentedTabs` |
| Auth field focus ring | `BorderStroke` driven by `isFocused` interaction source; `animateColorAsState` `line → accent`, 150 ms | All `OutlinedTextField` in Auth |
| Google banner tap | Material3 ripple (`indication = rememberRipple()`) on `Modifier.clickable` | `GoogleBanner` |
| Card tap | Material3 ripple | `RegistryCardPrimary` + `RegistryCardSecondary` |

No new pulsing animations in Phase 10 (PulsingDot from Phase 9 is not used on Home cards).

---

## Copywriting Contract

### New String Keys — Auth Screen (English / Romanian)

All new keys follow `auth_` prefix. Add to `values/strings.xml` AND `values-ro/strings.xml`.

| Key | English | Romanian |
|-----|---------|----------|
| `auth_headline_prefix` | "Start your" | "Începe-ți" |
| `auth_headline_accent` | "first registry." | "primul registru." |
| `auth_google_cta` | "Continue with Google" | "Continuă cu Google" |
| `auth_or_email_divider` | "or sign up with email" | "sau înregistrează-te cu email" |
| `auth_first_name_label` | "First name" | "Prenume" |
| `auth_last_name_label` | "Last name" | "Nume" |
| `auth_email_label` | "Email" | "Email" (exists, reuse) |
| `auth_password_label` | "Password" | "Parolă" (exists, reuse) |
| `auth_password_helper` | "At least 8 characters" | "Cel puțin 8 caractere" |
| `auth_signup_cta` | "Sign up" | "Înregistrează-te" |
| `auth_login_cta` | "Log in" | "Autentifică-te" |
| `auth_terms_line` | "By signing up you agree to our %1$s and %2$s" | "Prin înregistrare ești de acord cu %1$s și %2$s" |
| `auth_terms_link` | "Terms" | "Termeni" |
| `auth_privacy_link` | "Privacy Policy" | "Politică de confidențialitate" |
| `auth_login_footer` | "Already have an account? Log in" | "Ai deja un cont? Autentifică-te" |
| `auth_terms_url` | "" (TODO placeholder) | "" (TODO placeholder) |
| `auth_privacy_url` | "" (TODO placeholder) | "" (TODO placeholder) |
| `auth_password_show` | "Show password" | "Arată parola" |
| `auth_password_hide` | "Hide password" | "Ascunde parola" |

**Note:** `auth_signup_cta` ("Sign up") and `auth_login_cta` ("Log in") are conventional two-word auth labels (industry standard). Full-sentence variants like "Sign up for GiftMaison" were considered and rejected for visual density on the pill CTA.

**Existing auth keys reused without change:** `auth_email_label`, `auth_password_label`, `auth_confirm_password_label`, `auth_error_invalid_credentials`, `auth_error_email_exists`, `auth_error_weak_password`, `auth_error_network`.

### New String Keys — Home Screen (English / Romanian)

All new keys follow `home_` prefix.

| Key | English | Romanian |
|-----|---------|----------|
| `home_headline` | "Your registries" | "Registrele tale" |
| `home_stats_caption` | "%1$d active · %2$d items total" | "%1$d active · %2$d obiecte total" |
| `home_tab_active` | "ACTIVE" | "ACTIVE" |
| `home_tab_drafts` | "DRAFTS" | "SCHIȚE" |
| `home_tab_past` | "PAST" | "TRECUTE" |
| `home_empty_active` | "No active registries yet" | "Nu există registre active" |
| `home_empty_drafts` | "No drafts" | "Nu există schițe" |
| `home_empty_past` | "No past registries" | "Nu există registre trecute" |
| `home_avatar_content_desc` | "Open settings" | "Deschide setări" |

### Primary CTAs

| Screen | Primary CTA | Copy |
|--------|-------------|------|
| Auth (sign-up mode) | Sign up button | "Sign up" — `auth_signup_cta` |
| Auth (sign-in mode) | Log in button | "Log in" — `auth_login_cta` |
| Home | (none on screen — CTA is FAB from Phase 9) | — |

### Empty State Copy (Phase 10 minimum, no illustration)

| State | Copy |
|-------|------|
| Home / Active tab empty | "No active registries yet" (`home_empty_active`) |
| Home / Drafts tab empty | "No drafts" (`home_empty_drafts`) |
| Home / Past tab empty | "No past registries" (`home_empty_past`) |
| Auth loading | `CircularProgressIndicator` + disabled CTA, no text copy |

### Error State Copy

| Error | Copy (existing keys reused) | Treatment |
|-------|------------------------------|-----------|
| Auth invalid credentials | `auth_error_invalid_credentials`: "Incorrect email or password. Check your details and try again." | Inline error banner, `colors.warn.copy(alpha = 0.15f)` background, `typography.bodyM` |
| Auth email exists | `auth_error_email_exists`: "An account with this email already exists. Try signing in." | Same inline banner |
| Auth weak password | `auth_error_weak_password`: "Password must be at least 8 characters." | Same inline banner |
| Auth network error | `auth_error_network`: "No connection. Check your internet and try again." | Same inline banner |
| Home list load error | `common_error_generic` + `common_retry` (existing) | Centred `Column`, `colors.ink` text, `TextButton` retry |

Error banner style: `Box`, `shapes.radius12`, `colors.warn.copy(alpha = 0.15f)` background, `colors.inkSoft` text, `spacing.gap12` internal padding.

### Destructive Actions

Phase 10 introduces NO new destructive actions. The existing registry delete `AlertDialog` (from `RegistryListScreen`) uses existing `registry_delete_confirm_title` / `registry_delete_confirm_body` / `common_delete` (in `MaterialTheme.colorScheme.error` red) / `common_cancel` — all unchanged.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | not applicable — native Android Compose | not required |
| Third-party | none | not applicable |

This phase is native Android Compose + Jetpack + Coil. No web component registry involved.

---

## StyleGuidePreview Appendix

Phase 10 adds two preview sections to `StyleGuidePreview.kt` (existing Phase 8/9 file):

1. **Auth Form Preview** — shows `AuthScreen` content in sign-up mode and sign-in mode side-by-side, `GiftMaisonTheme` wrapper, `@Preview(showBackground = true, widthDp = 360)`
2. **Home Cards Preview** — shows primary card + two secondary cards in a `LazyColumn` mock, with mock `SegmentedTabs` above
3. **SegmentedTabs Preview** — isolated `SegmentedTabs` composable in all three selected states

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
