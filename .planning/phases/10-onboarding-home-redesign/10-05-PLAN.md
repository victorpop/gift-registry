---
phase: 10-onboarding-home-redesign
plan: 05
type: execute
wave: 2
depends_on:
  - 10-03
  - 10-04
files_modified:
  - app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt
autonomous: false
requirements:
  - SCR-06
  - SCR-07

must_haves:
  truths:
    - "StyleGuidePreview.kt gains 5 new @Preview composables: AuthHeadline preview, GoogleBanner preview (in-isolation), SegmentedTabs preview (3 selected states), RegistryCardPrimary preview (ink bg + 70% image), RegistryCardSecondary preview (paperDeep bg + line border)"
    - "Human checkpoint confirms on-device: Auth screen default mode is Sign up, headline renders ink/inkSoft/accent split, Google banner shows concentric rings + shadow, email field caret pulses at ≈1.1 s on focus, tertiary guest link is reachable and invokes continueAsGuest(), Home screen wordmark + avatar render correctly, 'Your registries' headline + N active caption, SegmentedTabs switches between Active/Drafts/Past with filtered cards, primary card is darkest (ink bg) with dimmed image, secondary cards are paperDeep with line border, empty state shows per-tab copy"
    - "Full unit suite GREEN: 6 Phase 10 Wave 0 tests (40 @Test methods) + Phase 8/9 tests all pass"
    - "Debug APK builds + installs + launches successfully on a real device / emulator running Android 31+"
    - "Romanian locale verified — 38 Phase 10 string keys (23 auth_ + 15 home_/registry_) render correctly in values-ro"
  artifacts:
    - path: "app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt"
      provides: "5 new Phase 10 @Preview composables appended"
      contains: "AuthHeadlinePreview"
  key_links:
    - from: "StyleGuidePreview.kt"
      to: "Plan 02/03/04 composables"
      via: "Direct @Composable invocation inside @Preview blocks wrapped in GiftRegistryTheme"
      pattern: "@Preview"
---

<objective>
Ship the last-mile verification: append 5 new @Preview composables to StyleGuidePreview.kt so the v1.1 Phase 10 primitives can be inspected in Android Studio's preview pane without running the app, then run an on-device human checkpoint covering every acceptance criterion from SCR-06 + SCR-07 against the live debug APK.

Purpose: Phase 8/9 precedent — visual surfaces that aren't unit-testable (Canvas rings, colour contrast, image brightness, typography rendering, locale correctness) ship verification via StyleGuidePreview + a human checkpoint. This plan mirrors Phase 9 Plan 04 Task 3 (21-check on-device UAT) scaled to Phase 10's two screens. Plans 03 + 04 wired everything; Plan 05 proves it end-to-end before Phase 10 is declared complete.

Output: 1 file modified (StyleGuidePreview.kt gains 5 previews) + human-verified UAT report. If the human flags issues, each is triaged to (a) surgical fix applied immediately in this plan or captured as a Phase 10 gap-closure todo for a follow-up plan, (b) out-of-scope recorded in SUMMARY, or (c) invalid with rationale.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-onboarding-home-redesign/10-UI-SPEC.md
@.planning/phases/10-onboarding-home-redesign/10-VALIDATION.md
@.planning/phases/10-onboarding-home-redesign/10-CONTEXT.md
@.planning/phases/10-onboarding-home-redesign/10-RESEARCH.md
@.planning/phases/10-onboarding-home-redesign/10-03-PLAN.md
@.planning/phases/10-onboarding-home-redesign/10-04-PLAN.md

<interfaces>
<!-- Plans 02/03/04 outputs consumed in StyleGuidePreview -->

From app/src/main/java/com/giftregistry/ui/auth/AuthHeadline.kt:
```kotlin
@Composable fun AuthHeadline(modifier: Modifier = Modifier)
```

From app/src/main/java/com/giftregistry/ui/auth/GoogleBanner.kt:
```kotlin
@Composable fun GoogleBanner(onClick: () -> Unit, modifier: Modifier = Modifier)
```

From app/src/main/java/com/giftregistry/ui/registry/list/SegmentedTabs.kt:
```kotlin
@Composable fun SegmentedTabs(tabs: List<String>, selectedIndex: Int, onTabSelected: (Int) -> Unit, modifier: Modifier = Modifier)
```

From app/src/main/java/com/giftregistry/ui/registry/list/RegistryCard.kt:
```kotlin
@Composable fun RegistryCardPrimary(registry: Registry, onClick: () -> Unit, onLongClick: () -> Unit, modifier: Modifier = Modifier)
@Composable fun RegistryCardSecondary(registry: Registry, onClick: () -> Unit, onLongClick: () -> Unit, modifier: Modifier = Modifier)
```

<!-- Existing StyleGuidePreview.kt pattern (Phase 8/9) — new previews mirror the style -->

```kotlin
@Preview(name = "...", showBackground = true, backgroundColor = 0xFFF7F2E9, widthDp = 360, heightDp = ...)
@Composable
private fun SomePreview() {
    GiftRegistryTheme {
        Box(Modifier.background(GiftMaisonTheme.colors.paper).padding(16.dp)) {
            SomeComposable(...)
        }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 5 Phase 10 @Preview composables to StyleGuidePreview.kt</name>
  <files>
    app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt
  </files>
  <read_first>
    - app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt (existing file — understand Phase 8/9 preview pattern: private fn, @Preview with name + showBackground + backgroundColor 0xFFF7F2E9 + widthDp 360, GiftRegistryTheme wrapper, Box with paper bg + padding)
    - app/src/main/java/com/giftregistry/ui/auth/AuthHeadline.kt (Plan 03 output — @Composable wrapper reads strings from resources, so preview works out of the box)
    - app/src/main/java/com/giftregistry/ui/auth/GoogleBanner.kt (Plan 02/03 output)
    - app/src/main/java/com/giftregistry/ui/registry/list/SegmentedTabs.kt (Plan 02 output)
    - app/src/main/java/com/giftregistry/ui/registry/list/RegistryCard.kt (Plan 04 output)
    - app/src/main/java/com/giftregistry/domain/model/Registry.kt (to construct mock Registry instances for card previews)
  </read_first>
  <action>
    Append the 5 new @Preview composables to the END of `StyleGuidePreview.kt` (before the file's final closing brace if the file is wrapped in an object / class; otherwise at file end). Mirror the existing preview patterns.

    **Add imports if not already present:**
    ```kotlin
    import androidx.compose.foundation.layout.Arrangement
    import androidx.compose.foundation.layout.Column
    import com.giftregistry.domain.model.Registry
    import com.giftregistry.ui.auth.AuthHeadline
    import com.giftregistry.ui.auth.GoogleBanner
    import com.giftregistry.ui.registry.list.RegistryCardPrimary
    import com.giftregistry.ui.registry.list.RegistryCardSecondary
    import com.giftregistry.ui.registry.list.SegmentedTabs
    ```

    **Append the 5 previews:**
    ```kotlin
    @Preview(name = "Auth headline", showBackground = true, backgroundColor = 0xFFF7F2E9, widthDp = 360, heightDp = 140)
    @Composable
    private fun AuthHeadlinePreview() {
        GiftRegistryTheme {
            Box(
                modifier = Modifier
                    .background(GiftMaisonTheme.colors.paper)
                    .padding(16.dp),
            ) {
                AuthHeadline()
            }
        }
    }

    @Preview(name = "Google banner (SCR-06)", showBackground = true, backgroundColor = 0xFFF7F2E9, widthDp = 360, heightDp = 120)
    @Composable
    private fun GoogleBannerPreview() {
        GiftRegistryTheme {
            Box(
                modifier = Modifier
                    .background(GiftMaisonTheme.colors.paper)
                    .padding(16.dp),
            ) {
                GoogleBanner(onClick = {})
            }
        }
    }

    @Preview(name = "Segmented tabs — all 3 selected states", showBackground = true, backgroundColor = 0xFFF7F2E9, widthDp = 360, heightDp = 220)
    @Composable
    private fun SegmentedTabsPreview() {
        GiftRegistryTheme {
            val tabs = listOf("ACTIVE", "DRAFTS", "PAST")
            Column(
                modifier = Modifier
                    .background(GiftMaisonTheme.colors.paper)
                    .padding(16.dp),
                verticalArrangement = Arrangement.spacedBy(12.dp),
            ) {
                SegmentedTabs(tabs = tabs, selectedIndex = 0, onTabSelected = {})
                SegmentedTabs(tabs = tabs, selectedIndex = 1, onTabSelected = {})
                SegmentedTabs(tabs = tabs, selectedIndex = 2, onTabSelected = {})
            }
        }
    }

    private val previewRegistry: Registry = Registry(
        id = "preview-1",
        ownerId = "owner-1",
        title = "Ana's housewarming",
        occasion = "Housewarming",
        visibility = "public",
        eventDateMs = 1_780_000_000_000L,
        eventLocation = "Bucharest",
        description = null,
        imageUrl = null,  // null exercises the paperDeep placeholder path
        createdAt = 1_740_000_000_000L,
        updatedAt = 1_745_000_000_000L,
    )

    @Preview(name = "Registry card — primary (ink bg, 70% image)", showBackground = true, backgroundColor = 0xFFF7F2E9, widthDp = 360, heightDp = 320)
    @Composable
    private fun RegistryCardPrimaryPreview() {
        GiftRegistryTheme {
            Box(
                modifier = Modifier
                    .background(GiftMaisonTheme.colors.paper)
                    .padding(16.dp),
            ) {
                RegistryCardPrimary(
                    registry = previewRegistry,
                    onClick = {},
                    onLongClick = {},
                )
            }
        }
    }

    @Preview(name = "Registry card — secondary (paperDeep + line border)", showBackground = true, backgroundColor = 0xFFF7F2E9, widthDp = 360, heightDp = 320)
    @Composable
    private fun RegistryCardSecondaryPreview() {
        GiftRegistryTheme {
            Box(
                modifier = Modifier
                    .background(GiftMaisonTheme.colors.paper)
                    .padding(16.dp),
            ) {
                RegistryCardSecondary(
                    registry = previewRegistry,
                    onClick = {},
                    onLongClick = {},
                )
            }
        }
    }
    ```

    Note: AuthScreen-as-a-whole is intentionally NOT previewed because it consumes `hiltViewModel()` which fails to instantiate in @Preview scope (Hilt requires ActivityScope). The individual components (AuthHeadline + GoogleBanner) are previewable — the full assembly is verified via the on-device checkpoint in Task 2.

    **Critical constraints:**
    - All previews wrap in `GiftRegistryTheme { ... }` so the `LocalGiftMaisonColors` providers populate.
    - All previews use `showBackground = true, backgroundColor = 0xFFF7F2E9` (paper) per Phase 8/9 convention.
    - `widthDp = 360` matches the handoff's "360 dp width" preview target.
    - Preview functions are `private` (encapsulation) and don't appear in the app binary.
    - The hand-constructed `previewRegistry` has `imageUrl = null` — exercises the paperDeep placeholder path in AsyncImage.
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin && ./gradlew :app:testDebugUnitTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@Preview" app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` → increased by exactly 5 vs pre-edit count
    - `grep "fun AuthHeadlinePreview" app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` → matches
    - `grep "fun GoogleBannerPreview" app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` → matches
    - `grep "fun SegmentedTabsPreview" app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` → matches
    - `grep "fun RegistryCardPrimaryPreview" app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` → matches
    - `grep "fun RegistryCardSecondaryPreview" app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` → matches
    - `grep "GiftRegistryTheme" app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` → matches (existing + new previews wrap correctly)
    - `./gradlew :app:compileDebugKotlin` succeeds (previews don't break compilation)
    - `./gradlew :app:testDebugUnitTest` — full unit suite GREEN (40 Phase 10 Wave 0 tests + Phase 8/9 tests)
  </acceptance_criteria>
  <done>5 new @Preview composables appended to StyleGuidePreview.kt covering AuthHeadline, GoogleBanner, SegmentedTabs (3 states), RegistryCardPrimary, and RegistryCardSecondary. All wrap in GiftRegistryTheme. File compiles cleanly; full unit suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human checkpoint — on-device verification of SCR-06 + SCR-07</name>
  <files>
    app/src/main/java/com/giftregistry/ui/auth/AuthScreen.kt
    app/src/main/java/com/giftregistry/ui/registry/list/RegistryListScreen.kt
    app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt
  </files>
  <read_first>
    - .planning/phases/10-onboarding-home-redesign/10-UI-SPEC.md (full pixel spec to verify against)
    - .planning/phases/10-onboarding-home-redesign/10-CONTEXT.md (locked decisions to confirm on-device)
    - design_handoff_android_owner_flow/README.md § 06 + § 07 (visual contract)
    - .planning/phases/10-onboarding-home-redesign/10-VALIDATION.md (Manual-Only Verifications table — drives the checkpoint list)
  </read_first>
  <what-built>
    Full Phase 10 integration:
    - SCR-06 AuthScreen re-skinned: wordmark top bar / italic-accent headline / GoogleBanner with concentric rings + accent shadow / mode-aware divider / first-last-email-password field stack (sign-up) or email-password (sign-in) / FocusedFieldCaret on email focus (1.1 s opacity pulse) / ink pill CTA / Terms line with tappable spans / ghost `Log in` footer pill / tertiary `Continue as guest` TextButton (AUTH-05) / inline warn-banner error
    - SCR-07 Home re-skinned: wordmark + 30 dp avatar top bar (avatar taps → SettingsKey) / "Your registries" displayXL + monoCaps caption / SegmentedTabs (Active / Drafts / Past) / registry card list with 1 dark primary (ink bg, 70% image) + paperDeep secondary (line border, full-brightness image) / 16:9 hero + occasion pill + date + title + stats line / per-tab empty state
    - 5 new StyleGuidePreview sections in Android Studio preview pane
  </what-built>
  <action>
    No Claude implementation action — this task blocks on human visual review.

    Before signalling, Claude should:
    1. Confirm `./gradlew :app:installDebug` completes without error and reports the installed package.
    2. Present the checklist below to the user with the exact 17 verification steps (mirrors Phase 9 Plan 04 Task 3 approach).
    3. Wait for the `resume-signal` before proceeding.
  </action>
  <how-to-verify>
    Run the debug APK on a physical device or emulator (API 31+ preferred):
    ```bash
    ./gradlew :app:installDebug
    adb shell am start -n com.giftregistry/.MainActivity
    ```
    Then sign OUT (if authenticated) to land on the Auth screen. For Home-screen checks, sign back in.

    **SCR-06 Onboarding / Auth screen:**
    1. On first launch (or after sign-out), the Auth screen opens in SIGN-UP mode by default — First name / Last name / Email / Password / Confirm password fields all visible; primary CTA reads "Sign up". Expected: no "Forgot password?" link visible (that was sign-in-only legacy).
    2. Wordmark `giftmaison.` renders top-left in Instrument Serif italic with accent-coloured period (terracotta — `#C8623A`).
    3. Headline reads "Start your" on line 1 (italic, inkSoft `#6A5E52`) and "first registry." on line 2 (italic; "first registry" in ink `#2A2420`, trailing period in accent `#C8623A`).
    4. Subline reads "A warm, shareable list — set up in under a minute." (bodyM, inkSoft).
    5. Google banner: terracotta pill with `googleBannerShadow` (soft accent shadow below), 20×20 white circle with "G" glyph on left, "Continue with Google" centre (accentInk text), italic serif "→" arrow on right at 0.9 alpha. Top-right corner shows 3 subtle concentric rings (ascending opacity 0.08 / 0.12 / 0.18). Tapping the banner triggers Google sign-in flow.
    6. Divider label reads "or sign up with email" in sign-up mode (bodyS, inkFaint) flanked by 1 dp line dividers.
    7. Tap into the Email field (empty). A thin 2×20 dp accent caret pulses at ≈1.1 s cadence (opacity only — NOT scaling — distinct from the reserved-chip dot at 1.4 s that also scales). Start typing: caret disappears while typing.
    8. Password field shows the eye toggle on the right — tapping reveals / masks the password. Helper text below in sign-up mode reads "At least 8 characters with one number." (bodyXS, inkFaint).
    9. Tap "Sign up" CTA with invalid input (blank name) — inline warn banner appears (warn @ 0.15 alpha bg, inkSoft text) with "First and last name are required."
    10. Tap the footer ghost pill "Already have an account? Log in". Mode flips to sign-in: First/Last name + Confirm password fields hide; primary CTA now reads "Log in"; divider label switches to "or sign in with email".
    11. Tap "New here? Sign up" footer pill — mode flips back to sign-up.
    12. Tap the tertiary "Continue as guest" TextButton (below the footer pill, bodyXS inkFaint). Expected: anonymous Firebase sign-in succeeds; navigates to Home. This confirms AUTH-05 preservation.

    **SCR-07 Home / all registries screen:**
    13. Top bar: `giftmaison.` wordmark left, 30 dp olive-coloured circle on the right with user initials (e.g. "AP" for "Ana Popescu") in paper-coloured bodyMEmphasis. Tap the avatar — navigates to Settings screen.
    14. Below top bar: "Your registries" in displayXL (32 sp Instrument Serif, ink), followed by "N active · M items total" in monoCaps inkFaint. Correct counts: N = number of currently-Active registries (eventDateMs null OR future).
    15. SegmentedTabs (paperDeep pill track, paper selected pill) — 3 tabs "ACTIVE / DRAFTS / PAST". Tap DRAFTS, tap PAST, tap ACTIVE. Each tap animates the label colour (200 ms tween). Selected tab label = ink; unselected = inkFaint.
    16. Registry list: EXACTLY ONE card renders as "primary" — dark ink background, paper-coloured title/stats, image darkened to ~70 % brightness (visibly dimmer than secondary cards). The primary card is the registry with the most-recent `updatedAt`. All other cards render with paperDeep background + 1 dp line border + full-brightness image. Each card shows: 16:9 hero image + occasion pill top-left + date bottom-right + title (displayS) + stats line (`N items \u2022 M reserved \u2022 K given` with mono-caps bullets).
    17. Tap the ACTIVE tab then switch to a tab with no matching registries (e.g. DRAFTS on a fresh account). Empty state: centred single line in bodyM inkFaint — "No drafts" (EN) / "Nu există schițe" (RO). No illustration, no CTA. Bottom nav + FAB from Phase 9 still visible at the bottom of the screen.

    **Locale verification (RO):**
    Switch device locale to Romanian (Android Settings → System → Languages → Română as primary). Relaunch app.
    - SCR-06 renders: "Începe-ți / primul registru." headline; "Continuă cu Google" banner; "Începe-ți" sublines; "Înregistrează-te" / "Autentifică-te" CTAs; "Continuă ca oaspete" tertiary link.
    - SCR-07 renders: "Registrele tale" headline; tabs "ACTIVE / SCHIȚE / TRECUTE"; empty-state "Nu există registre active / schițe / trecute"; avatar content description "Deschide setări".

    **Regression guards:**
    - Tap a registry card → navigates to RegistryDetail (existing Phase 9 flow, not re-skinned in Phase 10 — layout is pre-v1.1).
    - Long-press a registry card → DropdownMenu with Edit + Delete options. Tap Edit → navigates to edit screen. Tap Delete → confirmation AlertDialog → Confirm → registry deletes via existing DeleteRegistryUseCase.
    - Sign out → AuthScreen returns with sign-up as default mode.
  </how-to-verify>
  <verify>
    <automated>./gradlew :app:installDebug</automated>
  </verify>
  <acceptance_criteria>
    - User reports "approved" after completing all 17 SCR-06/07 checks + locale verification + regression guards
    - If issues, each is triaged as (a) surgical fix applied within this plan (one-line edits OK; larger fixes captured as Plan 06 gap-closure todo), (b) out-of-scope (documented in SUMMARY with rationale), or (c) invalid (documented)
    - `./gradlew :app:installDebug` succeeds
  </acceptance_criteria>
  <done>User signs off "approved" on all 17 SCR-06 + SCR-07 verification checks + locale verification + regression guards. Any flagged issues are either surgically fixed in-plan or captured as follow-up todos in the Phase 10 SUMMARY.</done>
  <resume-signal>
    Reply "approved" if all 17 checks + locale + regression guards pass.

    If issues exist, list them by check number (e.g. "check 3: period renders in inkSoft instead of accent — AuthHeadline accent-colour span off-by-one" or "check 16: all cards render as primary — primaryRegistryIdOf not wired"). For each flagged issue, Claude proposes a surgical fix and executes OR captures the item as a gap-closure todo for Phase 10 Plan 06.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 10 end-state verification:

1. Both Phase 10 requirements proven in code + unit tests:
   ```bash
   # All 6 Phase 10 Wave 0 test files GREEN (40 @Test methods)
   ./gradlew :app:testDebugUnitTest \
     --tests "com.giftregistry.ui.registry.list.TabFilterPredicateTest" \
     --tests "com.giftregistry.ui.registry.list.DraftHeuristicTest" \
     --tests "com.giftregistry.ui.registry.list.IsPrimarySelectionTest" \
     --tests "com.giftregistry.ui.common.AvatarInitialsTest" \
     --tests "com.giftregistry.ui.auth.AuthHeadlineTest" \
     --tests "com.giftregistry.ui.auth.AuthFormStateTest"
   ```

2. StyleGuidePreview appendix complete:
   ```bash
   grep -c "AuthHeadlinePreview\\|GoogleBannerPreview\\|SegmentedTabsPreview\\|RegistryCardPrimaryPreview\\|RegistryCardSecondaryPreview" \
     app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt
   # expect 5
   ```

3. Phase 10 requirement mapping:
   ```
   SCR-06 → Plan 01 (RED tests) + Plan 02 (GoogleBanner + FocusedFieldCaret + ConcentricRings) + Plan 03 (AuthScreen re-skin + AuthHeadline + AuthFormState + 23 strings) + Plan 05 (preview + checkpoint)
   SCR-07 → Plan 01 (RED tests) + Plan 02 (TabFilters + primaryRegistryIdOf + AvatarButton + AvatarInitials + SegmentedTabs + Registry.imageUrl) + Plan 04 (HomeTopBar + RegistryCard + RegistryListScreen + AppNavigation wiring + 15 strings) + Plan 05 (preview + checkpoint)
   ```

4. Full debug APK builds + installs + passes human UAT:
   ```bash
   ./gradlew :app:testDebugUnitTest && ./gradlew :app:assembleDebug && ./gradlew :app:installDebug   # expect BUILD SUCCESSFUL
   ```

5. Human checkpoint signed off (Task 2 resume-signal = "approved").
</verification>

<success_criteria>
- StyleGuidePreview.kt gains 5 new @Preview composables (AuthHeadline, GoogleBanner, SegmentedTabs, RegistryCardPrimary, RegistryCardSecondary)
- Human checkpoint approves SCR-06 (12 checks) + SCR-07 (5 checks) + locale (RO) + regression guards
- All 6 Phase 10 Wave 0 tests GREEN; Phase 8/9 regression GREEN
- Full debug APK builds + installs
- Phase 10 complete: both SCR-06 and SCR-07 requirements satisfied per handoff pixel contract, AuthViewModel / RegistryListViewModel / CredentialManager / Firestore schema / navigation graph all preserved, AUTH-05 guest path intact, Phase 9 chrome untouched
- Commit message: `feat(10-05): Wave 2 — StyleGuidePreview sections + on-device UAT sign-off`
- Known follow-ups recorded in Phase SUMMARY (10-SUMMARY.md — next command creates it per standard workflow):
  - firstName/lastName → Firebase User.displayName via UserProfileChangeRequest (deferred non-visual enhancement)
  - Real Terms / Privacy / Support URLs (currently empty placeholders per CONTEXT.md discretion)
  - Per-registry stats aggregation (itemCount / reservedCount / givenCount) — Phase 10 renders zeros
  - Notifications bell placement (deferred to Phase 11)
  - Old string-key cleanup (`auth_email_placeholder`, `auth_or_email_divider`, `auth_subtitle`, `auth_forgot_password`, `auth_no_account_prompt`, `auth_have_account_prompt`, `auth_sign_in_no_account_hint`, `auth_sign_in_title`, `auth_sign_up_title`, `auth_sign_in_button`, `auth_sign_up_button`, `auth_or_divider`, `auth_support`, `auth_terms_of_service`, `auth_privacy_policy`, `registry_list_title`, `registry_list_empty`) — single follow-up commit post-phase
  - `Registry.imageUrl` Firestore backfill — current registries without imageUrl show paperDeep placeholder (acceptable; backfill improves visual polish)
  - Registry card isPrimary via owner-controlled pinning — out of v1.1 scope
</success_criteria>

<output>
After completion, create `.planning/phases/10-onboarding-home-redesign/10-05-SUMMARY.md` documenting:
- 1 file modified (StyleGuidePreview.kt) — 5 new @Preview composables appended
- 17 on-device checks + locale + regression guards completed
- Any issues flagged during UAT + disposition (fixed in-plan / follow-up todo / out-of-scope / invalid)
- Both Phase 10 requirements (SCR-06 + SCR-07) satisfied per handoff pixel contract
- Full Phase 10 diff footprint: 6 net-new files, 5 modified files, 0 deleted files, 38 new string-resource entries × 2 locales = 76 string additions
- Traceability matrix mapping each SCR-06 / SCR-07 handoff bullet → code location → test (if any)
- Follow-up todos for Phase 11 / post-phase cleanup
- Commit SHA + diff stats
</output>
</content>
</invoke>