---
phase: 16-android-notifications-inbox-invite-accept-decline
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-05-24T22:31:28.531Z
---

<current_state>
Mid-UAT for Plan 16-06. Tasks 1, 2, 4, 5 are committed and done. Task 3 (on-device UAT, 20 scenarios) is in progress: Section A (10 items) is **10/10 PASS** with A5 flagged for minor visual polish (no blocker). Paused before Section B (decline flow). UAT-RESULTS.md is up to date with per-item status.

Devices in play:
- **Emulator** (Pixel 36.1 AVD): Account A (registry owner). Built with `-Puse_emulator=false` → production backend.
- **Huawei physical**: Account B (invitee, uid `dioPXtGkGcNw7xSX4DYsll5xiIn1`). Built with `-Puse_emulator=false` → production backend. Debug App Check token registered in Firebase Console.

Both apps installed and signed in at pause time. B is now a member of the test registry (A8 promoted them from pendingInvitedUsers → invitedUsers).
</current_state>

<completed_work>
- Task 1 (App Check + Phase 14 todo close + D-14 stanza) — commit `f7a5a32`
- Task 2 (Deploy composite index + 3 callables to gift-registry-ro) — commit `7ff9b5e`
- Task 4 (Restore inbox bell entry point in HomeTopBar — UAT-surfaced gap fix) — commit `8e901fc`
- Task 5 (Register FCM token on auth state change — UAT-surfaced gap fix) — commit `cf79b51`
- Task 3 UAT Section A (10/10 PASS):
  - A1 invite send → PASS
  - A2 pending state in Firestore → PASS
  - A3 FCM push received (when app backgrounded) → PASS
  - A4 inbox card visible via bell + badge → PASS
  - A5 InviteResponseSheet renders → PASS-with-flag (minor visual polish flagged for v1.1, task #16)
  - A6 dismissal guard during loading → PASS
  - A7 auto-navigate to RegistryDetail on Accept → PASS
  - A8 Firestore state post-accept (promote) → PASS
  - A9 Owner sees invite_accepted notification → PASS
  - A10 Invitee sees JOINED card replacing INVITE → PASS
</completed_work>

<remaining_work>
- **Section B — Decline flow (6 items)**: B1 fresh invite, B2 decline opens dialog, B3 cancel keeps sheet, B4 confirm dismisses + removes card, B5 owner sees invite_declined, B6 Firestore state (pending GONE, invitedUsers unchanged)
- **Section C — Legacy + edge cases (2 items)**: C1 legacy invite (no pendingEntryKey) navigates directly without sheet, C2 re-invite already-member (D-16 short-circuit) — no pendingInvitedUsers entry, push fires, tap navigates directly
- **Section D — Visual + locale (2 items)**: D1 inbox visual contract (wordmark, paper bg, 1dp dividers, MonoCaps, 6dp accent dot), D2 Romanian locale parity (Acceptă/Refuză/Reîncearcă, dialog copy, owner notifications RO)
- **Post-UAT**: write `16-06-SUMMARY.md`, mark plan complete via `roadmap update-plan-progress`, advance STATE.md, then phase-level verification + completion + roadmap update
</remaining_work>

<decisions_made>
- Added Plan 16-06 Tasks 4 + 5 inline as scope deviations (rather than gap-closure phase 16.1). Both fixes were small (~30 lines) and necessary for Phase 16's inbox UX to work at all. Committed atomically with deviation notes in commit messages.
- Use emulator (A) + Huawei (B) for UAT, both pointing at production backend (`-Puse_emulator=false`). Original plan was emulator-only; physical device adds real GMS FCM validation. Production backend matches deployed Cloud Functions — no integration mismatch.
- A3 marked PASS once push verified arriving when app is backgrounded. Foreground push behavior (notification routed to onMessageReceived instead of system tray) is Android-standard; in-app banner for invites is a v1.1 polish (task #15).
</decisions_made>

<blockers>
- **Section B requires test setup**: B is already an invitedUsers member (from A8 accept). To test decline, need to manually clear `invitedUsers[dioPXtGkGcNw7xSX4DYsll5xiIn1]` in Firestore Console (`registries/{your-test-id}` → edit invitedUsers map → delete that key → save), then re-invite B's email from emulator. Re-inviting without clearing hits D-16 already-member branch and skips pendingInvitedUsers entirely, defeating the decline test.
- Pre-existing Huawei sign-in hang fixed by building with `-Puse_emulator=false` (root cause was the default `use_emulator=true` from commit 8523217 pointing at 10.0.2.2 AVD-only). Don't rebuild without that flag for physical devices.
</blockers>

<context>
This session ran long — several hours were spent diagnosing two ghost issues before reaching real UAT:

1. **Emulator "fails to launch" was actually `INSTALL_FAILED_INSUFFICIENT_STORAGE`** from a previously corrupted install. App Check init was a red herring; the original Phase 16 App Check code is fine. Diagnostic instrumented version confirmed all 5 init steps succeed cleanly.

2. **Huawei sign-in hang was `-Puse_emulator` default true** (commit 8523217 from April 2026 — pre-Phase-16). All Firebase calls were going to `10.0.2.2:9099/8080/5001/9199` (AVD-only loopback) which is unreachable from a physical device. Pre-Phase-16 sign-in on Huawei must have used `-Puse_emulator=false` explicitly.

Three memory entries saved capturing these gotchas for future sessions:
- `reference-android-emulator-flag` — the use_emulator flag trap
- `reference-appcheck-android-debug-tokens` — one UUID per device, pm clear regenerates it
- `feedback-fcm-token-signin` — onNewToken doesn't fire on sign-in; explicit registration needed

Two UAT-surfaced bugs were fixed inline as Plan 16-06 deviations (Tasks 4 + 5):
- Inbox bell removed during Phase 10 GiftMaison reskin with "placement deferred to Phase 11" — never re-added. Restored in HomeTopBar (commit 8e901fc).
- FCM token registered only via `MessagingService.onNewToken` which fires on first install + token rotation — NOT on subsequent sign-ins. Every post-install sign-in was leaving `users/{uid}/fcmTokens` empty. Fixed via FirebaseAuth.addAuthStateListener in GiftRegistryApp (commit cf79b51).

UAT-RESULTS.md is the authoritative source for per-item PASS/FAIL state. Task list (#1–#16) tracks all in-flight items including deferred polish tasks.

Plan 16-06 Task 3 success criteria: all 20 UAT items PASS or documented FLAG. After Task 3 completes, write 16-06-SUMMARY.md and roll up to phase-level verification.
</context>

<next_action>
Confirm with user: are both devices still signed in (emulator with A, Huawei with B)? Is the test registry still present in production Firestore with B in invitedUsers?

If yes → walk user through clearing invitedUsers[dioPXtGkGcNw7xSX4DYsll5xiIn1] in Firestore Console, then re-inviting B's email from emulator (B1). Continue with the B1–B6 table from the last assistant message before pause.

If devices were closed / lost session → may need to re-sign-in (still using `-Puse_emulator=false` build, which is already installed). New Huawei sign-in would need a fresh debug token registered (capture from logcat).
</next_action>
