---
phase: 13
plan: 01
type: execute
wave: 2
depends_on: [00]
files_modified:
  - web/src/components/giftmaison/Wordmark.tsx
  - web/src/components/giftmaison/Pill.tsx
  - web/src/components/giftmaison/Btn.tsx
  - web/src/components/giftmaison/Field.tsx
  - web/src/components/giftmaison/PulseDot.tsx
  - web/src/components/giftmaison/MonoCaption.tsx
  - web/src/components/giftmaison/index.ts
autonomous: true
requirements: [D-13, D-16, D-08]
requirements_addressed: [D-13, D-16, D-08]
objective: "Wave 2 atom layer — ship Wordmark, Pill, Btn, Field, PulseDot, MonoCaption as Tailwind-utility React components mirroring web-screens.jsx atom shapes, ready to be consumed by chrome (Plan 02) and screens (Plans 04/05/06)."
must_haves:
  truths:
    - "<Wordmark /> renders 'giftmaison.' in Instrument Serif italic with the period span coloured gm.accent"
    - "<Pill tone='accent'> renders accentSoft bg + accent fg + transparent border per UI-SPEC tone table"
    - "<Btn variant='primary' size='lg'> renders ink bg + paper fg + 14/22 px padding + 15 px text + lineHeight 1"
    - "<Field label='…'> renders a mono-caps 10 px gm.inkFaint label above an input row with 10 px radius and 1 px gm.line border"
    - "<PulseDot /> renders an 8 px circle in gm.accent with the gm-pulse animation class applied"
    - "All atoms accept className prop for layout overrides and consume only gm.* Tailwind tokens (no inline rgb/hex)"
  artifacts:
    - path: web/src/components/giftmaison/Wordmark.tsx
      provides: Reusable brand wordmark for top nav + footer + auth screen
    - path: web/src/components/giftmaison/Pill.tsx
      provides: Status pill with 5 tone presets (neutral/accent/second/ok/warn) + 2 sizes (sm/md)
    - path: web/src/components/giftmaison/Btn.tsx
      provides: Pill-shaped button with 4 variants (primary/accent/ghost/quiet) + 3 sizes (sm/md/lg)
    - path: web/src/components/giftmaison/Field.tsx
      provides: Form field wrapper with mono-caps label + input shell, react-hook-form compatible
    - path: web/src/components/giftmaison/PulseDot.tsx
      provides: Accent pulsing dot for sticky banner (10 px) + status pills (8 px)
    - path: web/src/components/giftmaison/MonoCaption.tsx
      provides: Mono-caps caption span for labels, meta, status pill copy
    - path: web/src/components/giftmaison/index.ts
      provides: Barrel export so consumers `import { Wordmark, Pill, Btn, Field, PulseDot, MonoCaption } from '../components/giftmaison'`
  key_links:
    - from: web/src/components/giftmaison/PulseDot.tsx
      to: web/src/index.css gm-pulse keyframe
      via: animation utility class
      pattern: "animate-gm-pulse"
    - from: web/src/components/giftmaison/Wordmark.tsx
      to: tailwind.config.ts font-display
      via: font-display Tailwind class
      pattern: "font-display"
---

<objective>
Hand-build the 6 atoms that the rest of Phase 13 depends on. Each atom is a pure-Tailwind React component (no inline styles), TypeScript-typed, accepting `className` for layout overrides. These atoms are the "verbs" — every screen plan calls them; if they're wrong, every screen is wrong.

Purpose: Lock the atom shapes to the handoff prototype (`web-screens.jsx` Logo / Pill / Btn / Field) and CONTEXT D-13 / D-16. Atoms ship in `web/src/components/giftmaison/` so they're visually segregated from Phase 5 components.

Output: 6 component files + 1 barrel export. ~250 lines total. No screens or pages touched.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-web-fallback-visual-refresh/13-CONTEXT.md
@.planning/phases/13-web-fallback-visual-refresh/13-UI-SPEC.md
@.planning/phases/13-web-fallback-visual-refresh/13-00-PLAN.md
@design_handoff_web_giver_flow/reference/web-screens.jsx
@web/tailwind.config.ts
@web/src/index.css
</context>

<interfaces>
Plan 00 has already exposed the Tailwind utilities the atoms consume. Executor uses them directly:

```
Color utilities:    bg-gm-paper, bg-gm-paperDeep, bg-gm-ink, bg-gm-accent, bg-gm-accentSoft, bg-gm-second, bg-gm-secondSoft
                    text-gm-ink, text-gm-inkSoft, text-gm-inkFaint, text-gm-paper, text-gm-accent, text-gm-accentInk, text-gm-second, text-gm-ok, text-gm-warn
                    border-gm-line, border-gm-ink, border-gm-accent
Font utilities:     font-display (Instrument Serif), font-body (Inter), font-mono (JetBrains Mono)
Radius utilities:   rounded-gm-card (14 px), rounded-gm-card-large (18 px), rounded-gm-modal (20 px), rounded-full (pill)
Animation utility:  animate-gm-pulse  (1.4 s ease-in-out infinite alternate)
Spacing extension:  p-7 / px-7 / py-7  → 28 px (handoff tablet page padding)
```

Reference atom shapes from `design_handoff_web_giver_flow/reference/web-screens.jsx` lines 7–89 (Logo / Pill / Btn / Field). The reference is in inline-style JSX — translate every property to a Tailwind utility class.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build Wordmark, Pill, MonoCaption, PulseDot atoms</name>
  <files>web/src/components/giftmaison/Wordmark.tsx, web/src/components/giftmaison/Pill.tsx, web/src/components/giftmaison/MonoCaption.tsx, web/src/components/giftmaison/PulseDot.tsx</files>
  <read_first>
    - design_handoff_web_giver_flow/reference/web-screens.jsx lines 7–44 (Logo + Pill atom shapes)
    - .planning/phases/13-web-fallback-visual-refresh/13-UI-SPEC.md "Component Inventory" → "Atoms" table + "Pill tone mapping" sub-table
    - .planning/phases/13-web-fallback-visual-refresh/13-UI-SPEC.md "Status Pill Set (CONTEXT D-16)" table
    - .planning/phases/13-web-fallback-visual-refresh/13-UI-SPEC.md "Animation" CSS block (gm-pulse + reduced-motion fallback)
    - .planning/phases/13-web-fallback-visual-refresh/13-UI-SPEC.md "Typography" → "Wordmark (CONTEXT D-13)" sub-paragraph (Instrument Serif italic 22/24, letter-spacing -0.2 px, terminal `.` span color gm.accent, optional withTag mono-caps 'gift registry' 10 px)
  </read_first>
  <action>
Create four files. Use ONLY Tailwind utilities — no `style={{}}` inline objects.

**`web/src/components/giftmaison/Wordmark.tsx`**

```tsx
import type { HTMLAttributes } from 'react'

export interface WordmarkProps extends HTMLAttributes<HTMLDivElement> {
  /** 22 px (default, body usage) or 24 px (top nav). */
  size?: 22 | 24
  /** Render the adjacent mono-caps "gift registry" tagline. Default false. */
  withTag?: boolean
}

/**
 * GiftMaison wordmark (CONTEXT D-13). Italic Instrument Serif `giftmaison.` with
 * the terminal period in `gm.accent`. Mirrors Android Phase 8 wordmark visually.
 *
 * Size 22 = body-level (footer, auth caption); size 24 = top nav.
 *
 * `withTag` adds " gift registry" mono-caps 10 px in `gm.inkFaint` (web-screens.jsx Logo).
 */
export function Wordmark({ size = 22, withTag = false, className = '', ...rest }: WordmarkProps) {
  const sizeClass = size === 24 ? 'text-[24px]' : 'text-[22px]'
  return (
    <div
      className={`flex items-baseline gap-2 ${className}`.trim()}
      {...rest}
    >
      <span
        className={`font-display italic text-gm-ink leading-none tracking-[-0.2px] ${sizeClass}`}
      >
        giftmaison<span className="text-gm-accent">.</span>
      </span>
      {withTag && (
        <span className="font-mono text-[10px] text-gm-inkFaint uppercase tracking-[1.5px] font-medium">
          gift registry
        </span>
      )}
    </div>
  )
}

export default Wordmark
```

**`web/src/components/giftmaison/Pill.tsx`**

```tsx
import type { HTMLAttributes, ReactNode } from 'react'

export type PillTone = 'neutral' | 'accent' | 'second' | 'ok' | 'warn'
export type PillSize = 'sm' | 'md'

export interface PillProps extends HTMLAttributes<HTMLSpanElement> {
  tone?: PillTone
  size?: PillSize
  children: ReactNode
}

/**
 * Mono-caps pill (CONTEXT D-16). 11 px (sm) / 12 px (md), letter-spacing 0.3 px,
 * weight 500, rounded-full, 1 px border. Token mapping per UI-SPEC "Pill tone mapping".
 *
 * Status pill copy is uppercased and white-space: nowrap so chips don't wrap.
 *
 * The `ok` tone uses an oklch-derived green soft bg via arbitrary value
 * (`bg-[oklch(0.94_0.04_150)]`) since this colour is not in the gm.* palette
 * (UI-SPEC explicitly notes it as a computed value).
 */
export function Pill({ tone = 'neutral', size = 'sm', className = '', children, ...rest }: PillProps) {
  const toneClasses: Record<PillTone, string> = {
    neutral: 'bg-gm-paperDeep  text-gm-inkSoft  border-gm-line',
    accent:  'bg-gm-accentSoft text-gm-accent   border-transparent',
    second:  'bg-gm-secondSoft text-gm-second   border-transparent',
    ok:      'bg-[oklch(0.94_0.04_150)] text-gm-ok border-transparent',
    warn:    'bg-[oklch(0.95_0.04_70)]  text-gm-warn border-transparent',
  }
  const sizeClasses: Record<PillSize, string> = {
    sm: 'px-2 py-[3px] text-[11px]',
    md: 'px-[10px] py-[5px] text-[12px]',
  }
  return (
    <span
      className={[
        'inline-flex items-center gap-[5px] rounded-full border font-mono font-medium uppercase whitespace-nowrap tracking-[0.3px]',
        toneClasses[tone],
        sizeClasses[size],
        className,
      ].join(' ')}
      {...rest}
    >
      {children}
    </span>
  )
}

export default Pill
```

**`web/src/components/giftmaison/MonoCaption.tsx`**

```tsx
import type { HTMLAttributes, ReactNode } from 'react'

export interface MonoCaptionProps extends HTMLAttributes<HTMLSpanElement> {
  tone?: 'soft' | 'faint'
  size?: 'micro' | 'sm' | 'md'
  children: ReactNode
}

/**
 * Mono-caps caption span (UI-SPEC Atoms table).
 * Sizes: micro (10 px) / sm (11 px, default) / md (12 px).
 * Tones: soft = inkSoft, faint = inkFaint (default).
 * Letter-spacing scales with size: 1.2 / 1.3 / 1.5 px.
 */
export function MonoCaption({
  tone = 'faint',
  size = 'sm',
  className = '',
  children,
  ...rest
}: MonoCaptionProps) {
  const toneClass = tone === 'soft' ? 'text-gm-inkSoft' : 'text-gm-inkFaint'
  const sizeClass =
    size === 'micro' ? 'text-[10px] tracking-[1.2px]'
    : size === 'md' ? 'text-[12px] tracking-[1.5px]'
    : 'text-[11px] tracking-[1.3px]'
  return (
    <span
      className={`font-mono uppercase font-medium ${toneClass} ${sizeClass} ${className}`.trim()}
      {...rest}
    >
      {children}
    </span>
  )
}

export default MonoCaption
```

**`web/src/components/giftmaison/PulseDot.tsx`**

```tsx
import type { HTMLAttributes } from 'react'

export interface PulseDotProps extends HTMLAttributes<HTMLSpanElement> {
  /** Diameter in px. 8 (default — status pill, in-card banner) or 10 (sticky banner). */
  size?: 8 | 10
}

/**
 * Pulsing accent dot (CONTEXT D-11, UI-SPEC Animation).
 * Renders a circle in gm.accent with a soft accentSoft halo and the shared
 * `gm-pulse` keyframe animation (1.4 s alternate). Reduced-motion fallback
 * is applied at the @keyframes level in web/src/index.css.
 *
 * Size 8 px = Reserved item-card pill leading dot, in-card reserved banner.
 * Size 10 px = sticky reservation banner (Plan 04).
 */
export function PulseDot({ size = 8, className = '', ...rest }: PulseDotProps) {
  const dimClass = size === 10 ? 'w-[10px] h-[10px] shadow-[0_0_0_5px_var(--gm-accentSoft)]' : 'w-2 h-2 shadow-[0_0_0_4px_var(--gm-accentSoft)]'
  return (
    <span
      aria-hidden="true"
      className={`inline-block rounded-full bg-gm-accent animate-gm-pulse ${dimClass} ${className}`.trim()}
      {...rest}
    />
  )
}

export default PulseDot
```

Notes:
- All four atoms are pure presentational components — no `useState`, no `useEffect`, no `useTranslation` (parents pass i18n strings as `children`).
- Wordmark uses `text-[22px]` / `text-[24px]` arbitrary values because the spec is precise to the px (UI-SPEC Typography "Wordmark"). Equivalent Tailwind named sizes round to 1.375rem / 1.5rem.
- Pill `bg-[oklch(...)]` must use Tailwind 3.4 arbitrary-value syntax (no spaces in arbitrary values; underscores → spaces in Tailwind). The CSS produced is the literal `oklch(0.94 0.04 150)` per UI-SPEC.
- PulseDot uses `box-shadow` arbitrary values pointing at the CSS variable `var(--gm-accentSoft)` for the halo — so the halo colour follows the v1.2 occasion-cascade (D-07 future).
  </action>
  <verify>
    <automated>test -f web/src/components/giftmaison/Wordmark.tsx &amp;&amp; test -f web/src/components/giftmaison/Pill.tsx &amp;&amp; test -f web/src/components/giftmaison/MonoCaption.tsx &amp;&amp; test -f web/src/components/giftmaison/PulseDot.tsx &amp;&amp; grep -q "font-display italic text-gm-ink" web/src/components/giftmaison/Wordmark.tsx &amp;&amp; grep -q "text-gm-accent" web/src/components/giftmaison/Wordmark.tsx &amp;&amp; grep -q "bg-gm-accentSoft text-gm-accent" web/src/components/giftmaison/Pill.tsx &amp;&amp; grep -q "uppercase whitespace-nowrap tracking-\[0.3px\]" web/src/components/giftmaison/Pill.tsx &amp;&amp; grep -q "animate-gm-pulse" web/src/components/giftmaison/PulseDot.tsx &amp;&amp; grep -q "bg-gm-accent" web/src/components/giftmaison/PulseDot.tsx &amp;&amp; grep -q "font-mono uppercase font-medium" web/src/components/giftmaison/MonoCaption.tsx &amp;&amp; npm --prefix web run typecheck</automated>
  </verify>
  <done>4 atom files exist; each consumes only `gm.*` Tailwind tokens (no inline rgb/hex except the documented `bg-[oklch(...)]` arbitrary values); typecheck passes; PulseDot has `animate-gm-pulse` class; Wordmark splits text + accent period into two spans.</done>
</task>

<task type="auto">
  <name>Task 2: Build Btn + Field atoms + barrel index export</name>
  <files>web/src/components/giftmaison/Btn.tsx, web/src/components/giftmaison/Field.tsx, web/src/components/giftmaison/index.ts</files>
  <read_first>
    - design_handoff_web_giver_flow/reference/web-screens.jsx lines 46–89 (Btn + Field atom shapes)
    - .planning/phases/13-web-fallback-visual-refresh/13-UI-SPEC.md "Component Inventory" → "Atoms" + "Btn variant mapping" sub-table + "Btn sizes" sub-paragraph
    - .planning/phases/13-web-fallback-visual-refresh/13-UI-SPEC.md "Spacing Scale" → "Component-internal padding exceptions" sub-paragraph (Btn paddings 7/12, 11/18, 14/22 verbatim from web-screens.jsx — these are intentional, not page grid values)
    - .planning/phases/13-web-fallback-visual-refresh/13-UI-SPEC.md "Accessibility" table → "Focus ring" row (2 px gm-accent outline + 2 px offset, :focus-visible only)
  </read_first>
  <action>
**`web/src/components/giftmaison/Btn.tsx`**

```tsx
import type { ButtonHTMLAttributes, ReactNode } from 'react'

export type BtnVariant = 'primary' | 'accent' | 'ghost' | 'quiet'
export type BtnSize = 'sm' | 'md' | 'lg'

export interface BtnProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: BtnVariant
  size?: BtnSize
  /** Optional leading icon node (lucide-react glyph). */
  icon?: ReactNode
  children: ReactNode
}

/**
 * Pill-shaped button (UI-SPEC Component Inventory > Atoms > Btn).
 *
 * Variants:
 *   primary = ink bg / paper fg (default page primary CTA)
 *   accent  = accent bg / accentInk fg (sticky banner Continue, confirm-back CTA)
 *   ghost   = transparent bg / ink fg / line border (Sign in, Google, Skip)
 *   quiet   = transparent bg / inkSoft fg / no border (Release reservation on dark banner — pass className to override fg/border for the dark surface)
 *
 * Sizes (vertical/horizontal padding · font size):
 *   sm = 7/12 px · 12 px   md = 11/18 px · 13.5 px   lg = 14/22 px · 15 px
 * All sizes lineHeight: 1, letter-spacing -0.1 px, font-medium Inter.
 *
 * Focus ring: 2 px gm-accent outline + 2 px offset on :focus-visible (UI-SPEC a11y).
 *
 * Always renders <button>. For link buttons (e.g. "Continue to {retailer}"),
 * the consuming screen uses a sibling <a> styled identically — Plan 04 inlines
 * the same Btn classes on its <a target="_blank"> element.
 */
export function Btn({
  variant = 'primary',
  size = 'md',
  icon,
  className = '',
  children,
  type = 'button',
  ...rest
}: BtnProps) {
  const variantClasses: Record<BtnVariant, string> = {
    primary: 'bg-gm-ink    text-gm-paper    border-gm-ink',
    accent:  'bg-gm-accent text-gm-accentInk border-gm-accent',
    ghost:   'bg-transparent text-gm-ink     border-gm-line',
    quiet:   'bg-transparent text-gm-inkSoft border-transparent',
  }
  const sizeClasses: Record<BtnSize, string> = {
    sm: 'px-3 py-[7px] text-[12px]',     // 12 / 7 px (web-screens.jsx Btn `sm`)
    md: 'px-[18px] py-[11px] text-[13.5px]', // 18 / 11 px
    lg: 'px-[22px] py-[14px] text-[15px]',   // 22 / 14 px
  }
  return (
    <button
      type={type}
      className={[
        'inline-flex items-center justify-center gap-2 rounded-full border font-body font-medium tracking-[-0.1px] leading-none cursor-pointer',
        'focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gm-accent',
        'disabled:opacity-50 disabled:cursor-not-allowed',
        variantClasses[variant],
        sizeClasses[size],
        className,
      ].join(' ')}
      {...rest}
    >
      {icon}
      {children}
    </button>
  )
}

export default Btn
```

**`web/src/components/giftmaison/Field.tsx`**

```tsx
import { forwardRef, type InputHTMLAttributes, type ReactNode } from 'react'

export interface FieldProps extends Omit<InputHTMLAttributes<HTMLInputElement>, 'prefix'> {
  /** Mono-caps label text. Required (a11y). */
  label: string
  /** Optional helper text below the input. */
  hint?: string
  /** Renders the "✓ auto-filled" tag next to the label (D-form auto-fill UX). */
  autofilled?: boolean
  /** Optional prefix node (e.g., currency, lock icon). */
  prefix?: ReactNode
  /** Optional suffix node (e.g., "show" toggle on password fields). */
  suffix?: ReactNode
  /** Optional error message under the field; uses gm.warn or destructive copy. */
  error?: string
}

/**
 * Form field wrapper (UI-SPEC Component Inventory > Atoms > Field).
 *
 * Mono-caps label 10 px in gm.inkFaint, then a bordered input row:
 *   - 11/14 px padding (web-screens.jsx Field literal — kept verbatim)
 *   - rounded-[10px], 1 px gm.line border, 1.5 px gm.accent on :focus-within
 *   - 14 px Inter body, gm.ink text, gm.inkFaint placeholder
 *
 * Compatible with react-hook-form via `{...register('email')}` spread → forwarded ref.
 *
 * Plan 06 (Auth screen restyle) is the primary consumer: email + password fields.
 */
export const Field = forwardRef<HTMLInputElement, FieldProps>(function Field(
  { label, hint, autofilled, prefix, suffix, error, className = '', ...inputProps },
  ref,
) {
  return (
    <label className={`flex flex-col gap-[6px] ${className}`.trim()}>
      <span className="font-mono text-[10px] uppercase tracking-[1.3px] text-gm-inkFaint font-medium">
        {label}
        {autofilled && <span className="text-gm-ok ml-2">✓ auto-filled</span>}
      </span>
      <div className="flex items-center gap-2 px-[14px] py-[11px] rounded-[10px] bg-gm-paper border border-gm-line focus-within:border-[1.5px] focus-within:border-gm-accent transition-colors">
        {prefix && <span className="text-gm-inkFaint text-[13px]">{prefix}</span>}
        <input
          ref={ref}
          className="flex-1 font-body text-[14px] text-gm-ink placeholder:text-gm-inkFaint bg-transparent outline-none"
          {...inputProps}
        />
        {suffix}
      </div>
      {hint && <span className="font-body text-[11.5px] text-gm-inkFaint">{hint}</span>}
      {error && (
        <span role="alert" className="font-body text-[12px] text-gm-warn">
          {error}
        </span>
      )}
    </label>
  )
})

export default Field
```

**`web/src/components/giftmaison/index.ts`**

```ts
export { Wordmark, type WordmarkProps } from './Wordmark'
export { Pill, type PillProps, type PillTone, type PillSize } from './Pill'
export { Btn, type BtnProps, type BtnVariant, type BtnSize } from './Btn'
export { Field, type FieldProps } from './Field'
export { PulseDot, type PulseDotProps } from './PulseDot'
export { MonoCaption, type MonoCaptionProps } from './MonoCaption'
```

Notes:
- Btn padding values (`px-3 py-[7px]`, `px-[18px] py-[11px]`, `px-[22px] py-[14px]`) are taken verbatim from `web-screens.jsx` line 54. UI-SPEC explicitly authorises these as component-internal exceptions to the ×4 spacing scale (optical alignment of pill text).
- Btn `quiet` variant on the dark sticky banner (Plan 04) needs `text-gm-paper border border-gm-paper/20` overrides — the consumer passes them via `className`. Btn does NOT special-case the dark surface.
- Field uses `forwardRef` because react-hook-form's `register('email')` spread requires forwarded refs for `setFocus`. The existing AuthModal in Phase 5 uses `{...form.register('email')}` on raw `<input>` — Plan 06 will swap to `<Field {...form.register('email')} label={t('auth.email_label')} />`.
- Field's focus-within rule uses `border-[1.5px] focus-within:border-gm-accent` — UI-SPEC says "1.5 px gm.accent on focus", and Tailwind's `focus-within` triggers when any descendant input is focused. Combined with the `:focus-visible` outline ring on Btn, this gives consistent keyboard-focus affordance.
- Btn intentionally omits the explicit `focus:ring-2 focus:ring-primary` Phase-5 pattern — UI-SPEC Accessibility row "Focus ring" replaces it with `focus-visible:outline-*` for `gm-accent`. Existing tests that probe `focus:ring-primary` on AuthModal buttons still pass because AuthModal isn't restyled until Plan 06.
  </action>
  <verify>
    <automated>test -f web/src/components/giftmaison/Btn.tsx &amp;&amp; test -f web/src/components/giftmaison/Field.tsx &amp;&amp; test -f web/src/components/giftmaison/index.ts &amp;&amp; grep -q "bg-gm-ink    text-gm-paper    border-gm-ink" web/src/components/giftmaison/Btn.tsx &amp;&amp; grep -q "bg-gm-accent text-gm-accentInk border-gm-accent" web/src/components/giftmaison/Btn.tsx &amp;&amp; grep -q "focus-visible:outline-gm-accent" web/src/components/giftmaison/Btn.tsx &amp;&amp; grep -q "px-\[22px\] py-\[14px\]" web/src/components/giftmaison/Btn.tsx &amp;&amp; grep -q "forwardRef<HTMLInputElement, FieldProps>" web/src/components/giftmaison/Field.tsx &amp;&amp; grep -q "focus-within:border-gm-accent" web/src/components/giftmaison/Field.tsx &amp;&amp; grep -q "rounded-\[10px\]" web/src/components/giftmaison/Field.tsx &amp;&amp; grep -q "export { Wordmark" web/src/components/giftmaison/index.ts &amp;&amp; grep -q "export { Btn" web/src/components/giftmaison/index.ts &amp;&amp; npm --prefix web run typecheck &amp;&amp; npm --prefix web run build</automated>
  </verify>
  <done>Btn renders 4 variants × 3 sizes with focus-visible outline ring; Field forwards ref to inner input + supports prefix/suffix/error/autofilled; barrel index re-exports all 6 atoms with their type exports; typecheck + build both succeed.</done>
</task>

</tasks>

<verification>
**Goal-backward checks:**
1. `npm --prefix web run typecheck` exits 0 — all 6 atoms type-check; no untyped props.
2. `npm --prefix web run build` exits 0 — Tailwind purge resolves every `bg-gm-*`, `text-gm-*`, `border-gm-*`, `font-*`, `rounded-gm-*`, `animate-gm-pulse` class used in the 6 atom files.
3. `grep -r "from '../components/giftmaison'" web/src/` returns nothing yet (no consumers — atoms are net-new; Plan 02+ wires them in).
4. The 92 existing tests still pass: `npm --prefix web run test:run` exits 0 (atoms are not yet imported by any production code, so they cannot regress anything).
5. The index barrel re-exports all 6 atoms by name + their published types — verified via grep on `export {` count = 6.
</verification>

<success_criteria>
6 GiftMaison atoms ship in `web/src/components/giftmaison/` with full TypeScript types, Tailwind-only styling (no inline rgb/hex), and a barrel `index.ts`. Plan 02 (chrome) and Plans 04/05/06 (screens) consume them via `import { Wordmark, Pill, Btn, Field, PulseDot, MonoCaption } from '../components/giftmaison'`. The 92 existing web tests are unaffected (atoms are net-new files, no existing consumers touched).
</success_criteria>

<output>
After completion, create `.planning/phases/13-web-fallback-visual-refresh/13-01-SUMMARY.md`.
</output>
