---
phase: 11-registry-detail-create-add-item-redesign
plan: 02
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - app/src/main/java/com/giftregistry/util/AffiliateUrlTransformer.kt
  - app/src/main/java/com/giftregistry/ui/registry/detail/FilterChipState.kt
  - app/src/main/java/com/giftregistry/ui/registry/detail/HeroToolbarAlpha.kt
  - app/src/main/java/com/giftregistry/ui/registry/detail/RegistryStats.kt
  - app/src/main/java/com/giftregistry/ui/registry/detail/ShareUrl.kt
  - app/src/main/java/com/giftregistry/ui/registry/create/OccasionCatalog.kt
  - app/src/main/java/com/giftregistry/ui/item/add/AddItemMode.kt
  - app/src/main/java/com/giftregistry/ui/item/add/AffiliateRowVisibility.kt
autonomous: true
requirements:
  - SCR-08
  - SCR-09
  - SCR-10

must_haves:
  truths:
    - "`AffiliateUrlTransformer` gains a new public `fun isAffiliateDomain(url: String): Boolean` that exposes the previously-private `merchantRules` domain check — necessary for SCR-10 affiliate row visibility"
    - "7 pure-Kotlin Phase 11 helper files ship: FilterChipState enum + matches(), heroToolbarAlpha() with firstVisibleItemIndex>=1→1f guard, RegistryStats + registryStatsOf(), shareUrlOf(), OccasionCatalog with 6 entries + legacy alias map, AddItemMode enum + ADD_ITEM_MODE_DEFAULT_ORDINAL, shouldShowAffiliateRow() predicate"
    - "All 7 Wave 0 RED tests flip GREEN: `./gradlew :app:testDebugUnitTest --tests 'com.giftregistry.ui.registry.detail.*' --tests 'com.giftregistry.ui.registry.create.OccasionCatalogTest' --tests 'com.giftregistry.ui.item.add.*'` passes with 51 @Test methods green"
    - "Existing Phase 3 affiliate behaviour preserved — AffiliateUrlTransformer.transform() unchanged; isAffiliateDomain is purely additive; full unit suite green; no Phase 3-10 test regressions"
    - "Zero UI changes in this plan — RegistryDetailScreen / CreateRegistryScreen / AddItemScreen are UNTOUCHED; integration lands in Plans 03, 04, 05 running in parallel"
  artifacts:
    - path: "app/src/main/java/com/giftregistry/util/AffiliateUrlTransformer.kt"
      provides: "Public isAffiliateDomain(url) accessor exposing merchantRules domain check"
      contains: "isAffiliateDomain"
    - path: "app/src/main/java/com/giftregistry/ui/registry/detail/FilterChipState.kt"
      provides: "SCR-08 FilterChipState enum + matches() extension — 4 chips / ItemStatus mapping"
      contains: "FilterChipState"
    - path: "app/src/main/java/com/giftregistry/ui/registry/detail/HeroToolbarAlpha.kt"
      provides: "SCR-08 toolbar alpha fade calc with Pitfall 1 guard"
      contains: "heroToolbarAlpha"
    - path: "app/src/main/java/com/giftregistry/ui/registry/detail/RegistryStats.kt"
      provides: "SCR-08 items/reserved/given/views derivation (pure Kotlin)"
      contains: "registryStatsOf"
    - path: "app/src/main/java/com/giftregistry/ui/registry/detail/ShareUrl.kt"
      provides: "SCR-08 canonical share URL builder"
      contains: "shareUrlOf"
    - path: "app/src/main/java/com/giftregistry/ui/registry/create/OccasionCatalog.kt"
      provides: "SCR-09 6-occasion catalog + legacy alias map + glyph lookup"
      contains: "OccasionCatalog"
    - path: "app/src/main/java/com/giftregistry/ui/item/add/AddItemMode.kt"
      provides: "SCR-10 AddItemMode enum + ADD_ITEM_MODE_DEFAULT_ORDINAL const"
      contains: "AddItemMode"
    - path: "app/src/main/java/com/giftregistry/ui/item/add/AffiliateRowVisibility.kt"
      provides: "SCR-10 shouldShowAffiliateRow(url, isAffiliateDomain, ogFetchSucceeded) predicate"
      contains: "shouldShowAffiliateRow"
  key_links:
    - from: "Wave 0 test files"
      to: "Plan 02 Kotlin helpers"
      via: "Import of com.giftregistry.ui.registry.detail.* + com.giftregistry.ui.registry.create.* + com.giftregistry.ui.item.add.*"
      pattern: "import com\\.giftregistry\\.ui\\."
    - from: "AddItemScreen (Plan 05)"
      to: "AffiliateUrlTransformer.isAffiliateDomain + shouldShowAffiliateRow"
      via: "ViewModel-side domain check + composable visibility gate"
      pattern: "isAffiliateDomain\\("
---

<objective>
Ship the pure-Kotlin helpers + the `AffiliateUrlTransformer.isAffiliateDomain` accessor that the three Wave 1 screen re-skin plans (03, 04, 05) all consume. Plan 02 flips all 7 Wave 0 tests RED → GREEN (51 @Test methods) and unblocks Plans 03/04/05 to run in parallel without redeclaring any shared logic.

Purpose: Per CONTEXT.md + RESEARCH.md, Phase 11 is a re-skin — existing VMs, repositories, Cloud Functions are preserved. Plan 02 lands every bit of pure-Kotlin logic the next three screen plans need, so each of 03/04/05 only has to edit its single screen + its strings. This mirrors the Phase 10 Plan 02 pattern (helpers first, screens after). `AffiliateUrlTransformer.isAffiliateDomain` is the one addition to existing production code — backward-compatible one-line method on a Kotlin `object`.

Output: 8 files (1 edit to AffiliateUrlTransformer.kt, 7 new helper files). Wave 0 full suite GREEN. No visual or behaviour change on-screen yet — integration is Plans 03/04/05.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-registry-detail-create-add-item-redesign/11-CONTEXT.md
@.planning/phases/11-registry-detail-create-add-item-redesign/11-RESEARCH.md
@.planning/phases/11-registry-detail-create-add-item-redesign/11-UI-SPEC.md
@.planning/phases/11-registry-detail-create-add-item-redesign/11-01-PLAN.md

<interfaces>
<!-- Existing AffiliateUrlTransformer surface (as of Phase 3) — Plan 02 ADDS isAffiliateDomain -->

From app/src/main/java/com/giftregistry/util/AffiliateUrlTransformer.kt:
```kotlin
data class TransformResult(
    val originalUrl: String,
    val affiliateUrl: String,
    val merchantName: String?,
    val wasTransformed: Boolean,
)

object AffiliateUrlTransformer {
    private const val AFFILIATE_UNIQUE_ID = "PLACEHOLDER_UNIQUE_ID"
    private const val AFFILIATE_CODE = "PLACEHOLDER_AFF_CODE"
    private const val EMAG_CAMPAIGN_ID = "PLACEHOLDER_CAMPAIGN_ID"

    private val merchantRules: Map<String, (String) -> String> = mapOf(
        "emag.ro" to ::buildEmagAffiliateUrl
    )

    fun transform(url: String): TransformResult { /* existing — unchanged */ }

    private fun extractDomain(url: String): String? {
        return try { URI(url).host?.lowercase() } catch (e: Exception) { null }
    }

    // NEW in Plan 02:
    //   fun isAffiliateDomain(url: String): Boolean
    //     — mirrors the transform() domain check without invoking the URL builder.
    //     extractDomain(url)?.let { d -> merchantRules.keys.any { d.endsWith(it) } } ?: false
}
```

<!-- Existing Item domain model — used by RegistryStats helper -->

From app/src/main/java/com/giftregistry/domain/model/Item.kt:
```kotlin
data class Item(
    val id: String = "",
    val registryId: String = "",
    val title: String = "",
    val originalUrl: String = "",
    val affiliateUrl: String = "",
    val imageUrl: String? = null,
    val price: String? = null,
    val notes: String? = null,
    val status: ItemStatus = ItemStatus.AVAILABLE,
    val createdAt: Long = 0L,
    val updatedAt: Long = 0L,
    val expiresAt: Long? = null,
)
enum class ItemStatus { AVAILABLE, RESERVED, PURCHASED }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AffiliateUrlTransformer.isAffiliateDomain + ship SCR-08 helpers (FilterChipState + HeroToolbarAlpha + RegistryStats + ShareUrl) — flips 4 Wave 0 tests GREEN</name>
  <files>
    app/src/main/java/com/giftregistry/util/AffiliateUrlTransformer.kt
    app/src/main/java/com/giftregistry/ui/registry/detail/FilterChipState.kt
    app/src/main/java/com/giftregistry/ui/registry/detail/HeroToolbarAlpha.kt
    app/src/main/java/com/giftregistry/ui/registry/detail/RegistryStats.kt
    app/src/main/java/com/giftregistry/ui/registry/detail/ShareUrl.kt
  </files>
  <read_first>
    - app/src/main/java/com/giftregistry/util/AffiliateUrlTransformer.kt (current 57-line object — add isAffiliateDomain between extractDomain and noMatch without touching existing behaviour)
    - app/src/test/java/com/giftregistry/ui/registry/detail/FilterChipStateTest.kt (pins FilterChipState enum order + matches() contract)
    - app/src/test/java/com/giftregistry/ui/registry/detail/HeroToolbarAlphaTest.kt (pins heroToolbarAlpha signature + Pitfall 1 guard semantics)
    - app/src/test/java/com/giftregistry/ui/registry/detail/RegistryStatsTest.kt (pins RegistryStats data class + registryStatsOf aggregation)
    - app/src/test/java/com/giftregistry/ui/registry/detail/ShareUrlTest.kt (pins shareUrlOf literal format)
    - .planning/phases/11-registry-detail-create-add-item-redesign/11-RESEARCH.md (Pattern 2 stats derivation; Pitfall 1 toolbar alpha guard; Pitfall 3 merchantRules accessor)
    - app/src/main/java/com/giftregistry/ui/registry/list/TabFilters.kt (Phase 10 pure-Kotlin helper precedent — same package layout, no Compose imports)
  </read_first>
  <action>
    **Edit 1 — `app/src/main/java/com/giftregistry/util/AffiliateUrlTransformer.kt`:** add `isAffiliateDomain(url: String): Boolean` as a new public method on the `object`. Do NOT change `transform()`, `extractDomain()`, `buildEmagAffiliateUrl()`, or `merchantRules`. Drop-in insertion between `extractDomain()` and `noMatch()`:

    ```kotlin
    /**
     * Phase 11 SCR-10 accessor: returns true when [url]'s host matches any entry
     * in [merchantRules] (currently `"emag.ro"`). Mirrors the domain check in
     * [transform] without invoking the URL builder — the Add item screen uses this
     * to decide whether to show the affiliate confirmation row before save.
     *
     * Returns false for blank / malformed URLs and for hosts that do not match
     * any merchant rule. Case-insensitive (extractDomain lowercases).
     */
    fun isAffiliateDomain(url: String): Boolean {
        if (url.isBlank()) return false
        val domain = extractDomain(url) ?: return false
        return merchantRules.keys.any { domain.endsWith(it) }
    }
    ```

    **Edit 2 — CREATE `app/src/main/java/com/giftregistry/ui/registry/detail/FilterChipState.kt`:**
    ```kotlin
    package com.giftregistry.ui.registry.detail

    import com.giftregistry.domain.model.ItemStatus

    /**
     * SCR-08: Filter chips on the Registry Detail screen — 4 states in fixed UI order.
     *
     * Domain → UI mapping (locked by CONTEXT.md § Registry Detail):
     *   All       → every ItemStatus
     *   Open      → ItemStatus.AVAILABLE
     *   Reserved  → ItemStatus.RESERVED
     *   Completed → ItemStatus.PURCHASED   ← handoff asymmetric alias, matches Phase 9 StatusChip
     *
     * Unit-tested by FilterChipStateTest (Wave 0).
     */
    enum class FilterChipState { All, Open, Reserved, Completed }

    /** True when [status] should be included under the [this] filter. */
    fun FilterChipState.matches(status: ItemStatus): Boolean = when (this) {
        FilterChipState.All       -> true
        FilterChipState.Open      -> status == ItemStatus.AVAILABLE
        FilterChipState.Reserved  -> status == ItemStatus.RESERVED
        FilterChipState.Completed -> status == ItemStatus.PURCHASED
    }
    ```

    **Edit 3 — CREATE `app/src/main/java/com/giftregistry/ui/registry/detail/HeroToolbarAlpha.kt`:**
    ```kotlin
    package com.giftregistry.ui.registry.detail

    /**
     * SCR-08: Pinned-toolbar alpha fade derivation.
     *
     * Contract (pure Kotlin — Compose call site converts `120.dp` to px via
     * `with(LocalDensity.current) { 120.dp.toPx() }` outside `derivedStateOf`,
     * per RESEARCH.md Pitfall 2):
     *
     *   if (firstVisibleItemIndex >= 1) 1f                               // PITFALL 1 GUARD
     *   else (firstVisibleItemScrollOffsetPx / heroThresholdPx).coerceIn(0f, 1f)
     *
     * The guard short-circuits to 1f once the hero (index 0) has scrolled off the
     * screen — `LazyListState.firstVisibleItemScrollOffset` resets to 0 when the
     * first visible item changes, which would otherwise flash the toolbar back to
     * transparent (RESEARCH.md Pitfall 1).
     *
     * Unit-tested by HeroToolbarAlphaTest (Wave 0).
     */
    fun heroToolbarAlpha(
        firstVisibleItemIndex: Int,
        firstVisibleItemScrollOffsetPx: Int,
        heroThresholdPx: Float,
    ): Float {
        if (firstVisibleItemIndex >= 1) return 1f
        return (firstVisibleItemScrollOffsetPx.toFloat() / heroThresholdPx).coerceIn(0f, 1f)
    }
    ```

    **Edit 4 — CREATE `app/src/main/java/com/giftregistry/ui/registry/detail/RegistryStats.kt`:**
    ```kotlin
    package com.giftregistry.ui.registry.detail

    import com.giftregistry.domain.model.Item
    import com.giftregistry.domain.model.ItemStatus

    /**
     * SCR-08 4-stat strip (items / reserved / given / views).
     *
     * `views` is always 0 in v1.1 per CONTEXT.md § Deferred — `Registry.viewCount`
     * is deferred to v1.2. The field stays in this data class so the strip layout
     * is render-ready when the backing field ships without touching the composable.
     */
    data class RegistryStats(
        val items: Int,
        val reserved: Int,
        val given: Int,
        val views: Int,
    )

    /**
     * Derives stats from the current items list.
     * Pure Kotlin — unit-tested by RegistryStatsTest (Wave 0).
     */
    fun registryStatsOf(items: List<Item>): RegistryStats = RegistryStats(
        items = items.size,
        reserved = items.count { it.status == ItemStatus.RESERVED },
        given = items.count { it.status == ItemStatus.PURCHASED },
        views = 0,  // TODO v1.2: Registry.viewCount
    )
    ```

    **Edit 5 — CREATE `app/src/main/java/com/giftregistry/ui/registry/detail/ShareUrl.kt`:**
    ```kotlin
    package com.giftregistry.ui.registry.detail

    /**
     * SCR-08: Share banner URL builder.
     *
     * Format is locked by CONTEXT.md § Share banner: the Firebase Hosting
     * production target is `gift-registry-ro.web.app` (confirmed by the quick-task
     * 260420-nh8 fix for the email invite URL). The web fallback router's /r/:id
     * param matches this path exactly.
     *
     * NOT URL-encoded — Firestore auto-generated IDs are base-62 and never contain
     * characters that need encoding. URLEncoder.encode() would break the web
     * fallback route match. Unit-tested by ShareUrlTest (Wave 0).
     */
    fun shareUrlOf(registryId: String): String =
        "https://gift-registry-ro.web.app/r/$registryId"
    ```

    **Critical constraints:**
    - Do NOT import `androidx.compose.*` in any of the 4 new detail/ files — they are pure Kotlin and must stay unit-testable without a Compose runtime.
    - Do NOT change the existing `transform()` / `extractDomain()` / `noMatch()` / `buildEmagAffiliateUrl()` logic in AffiliateUrlTransformer — Phase 3 behaviour is preserved.
    - Do NOT widen `merchantRules` to public or `internal` — the private map stays encapsulated; `isAffiliateDomain` is the only new external surface.
    - File package declarations must match the test packages verbatim: `com.giftregistry.ui.registry.detail` for all 4 detail files.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "com.giftregistry.ui.registry.detail.FilterChipStateTest" --tests "com.giftregistry.ui.registry.detail.HeroToolbarAlphaTest" --tests "com.giftregistry.ui.registry.detail.RegistryStatsTest" --tests "com.giftregistry.ui.registry.detail.ShareUrlTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "fun isAffiliateDomain" app/src/main/java/com/giftregistry/util/AffiliateUrlTransformer.kt` → matches
    - `grep -c "private val merchantRules" app/src/main/java/com/giftregistry/util/AffiliateUrlTransformer.kt` → 1 (still private — not widened)
    - `grep "fun transform" app/src/main/java/com/giftregistry/util/AffiliateUrlTransformer.kt` → matches (existing method preserved)
    - `ls app/src/main/java/com/giftregistry/ui/registry/detail/{FilterChipState,HeroToolbarAlpha,RegistryStats,ShareUrl}.kt` → all 4 exist
    - `grep -c "import androidx.compose" app/src/main/java/com/giftregistry/ui/registry/detail/FilterChipState.kt` → 0
    - `grep -c "import androidx.compose" app/src/main/java/com/giftregistry/ui/registry/detail/HeroToolbarAlpha.kt` → 0
    - `grep -c "import androidx.compose" app/src/main/java/com/giftregistry/ui/registry/detail/RegistryStats.kt` → 0
    - `grep -c "import androidx.compose" app/src/main/java/com/giftregistry/ui/registry/detail/ShareUrl.kt` → 0
    - `grep "if (firstVisibleItemIndex >= 1) return 1f" app/src/main/java/com/giftregistry/ui/registry/detail/HeroToolbarAlpha.kt` → matches (Pitfall 1 guard pinned in impl)
    - `grep "gift-registry-ro.web.app/r/" app/src/main/java/com/giftregistry/ui/registry/detail/ShareUrl.kt` → matches
    - `grep "views = 0" app/src/main/java/com/giftregistry/ui/registry/detail/RegistryStats.kt` → matches (v1.2 TODO)
    - `./gradlew :app:testDebugUnitTest --tests "com.giftregistry.ui.registry.detail.FilterChipStateTest"` → 5 GREEN
    - `./gradlew :app:testDebugUnitTest --tests "com.giftregistry.ui.registry.detail.HeroToolbarAlphaTest"` → 7 GREEN (Pitfall 1 guard test passes)
    - `./gradlew :app:testDebugUnitTest --tests "com.giftregistry.ui.registry.detail.RegistryStatsTest"` → 6 GREEN
    - `./gradlew :app:testDebugUnitTest --tests "com.giftregistry.ui.registry.detail.ShareUrlTest"` → 5 GREEN
  </acceptance_criteria>
  <done>AffiliateUrlTransformer gains isAffiliateDomain (additive, backward-compat). 4 SCR-08 pure-Kotlin helper files shipped. 4 Wave 0 tests flipped GREEN (23 @Test methods passing).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship SCR-09 + SCR-10 helpers (OccasionCatalog + AddItemMode + AffiliateRowVisibility) — flips remaining 3 Wave 0 tests GREEN</name>
  <files>
    app/src/main/java/com/giftregistry/ui/registry/create/OccasionCatalog.kt
    app/src/main/java/com/giftregistry/ui/item/add/AddItemMode.kt
    app/src/main/java/com/giftregistry/ui/item/add/AffiliateRowVisibility.kt
  </files>
  <read_first>
    - app/src/test/java/com/giftregistry/ui/registry/create/OccasionCatalogTest.kt (pins OccasionCatalog.entries order + glyph mapping + legacy alias contract)
    - app/src/test/java/com/giftregistry/ui/item/add/AddItemModeTest.kt (pins AddItemMode enum order + ADD_ITEM_MODE_DEFAULT_ORDINAL)
    - app/src/test/java/com/giftregistry/ui/item/add/AffiliateRowVisibilityTest.kt (pins shouldShowAffiliateRow contract)
    - .planning/phases/11-registry-detail-create-add-item-redesign/11-UI-SPEC.md (§ Screen 09 2×3 Occasion Tile Grid table — glyph chars + storage keys)
    - .planning/phases/11-registry-detail-create-add-item-redesign/11-RESEARCH.md (Open Question 1 — legacy alias map for pre-Phase-11 Firestore registry.occasion values)
    - .planning/phases/11-registry-detail-create-add-item-redesign/11-CONTEXT.md (§ Create Registry 2×3 occasion tile grid; § Add Item URL 3-mode + affiliate row visibility)
  </read_first>
  <action>
    Create 3 new pure-Kotlin helper files.

    **File 1 — `app/src/main/java/com/giftregistry/ui/registry/create/OccasionCatalog.kt`:**
    ```kotlin
    package com.giftregistry.ui.registry.create

    /**
     * SCR-09: Occasion catalog for the Create Registry 2×3 tile grid + hero
     * placeholder glyph lookup on the Registry Detail screen.
     *
     * The 6 v1.1 occasions in fixed handoff order (Housewarming / Wedding / Baby /
     * Birthday / Christmas / Custom). Each entry exposes the canonical
     * [storageKey] that goes into `Registry.occasion` and the [glyph] char that
     * renders inside the tile + hero placeholder.
     *
     * Legacy alias map for pre-Phase-11 Firestore documents (RESEARCH.md
     * Open Question 1): older docs may contain `"Baby shower"` / `"Anniversary"`
     * strings since the Phase 3 occasion set used different labels. `glyphFor`
     * + `storageKeyFor` surface the right v1.1 tile for those legacy values
     * without requiring a Firestore migration.
     */
    data class OccasionEntry(val storageKey: String, val glyph: String)

    object OccasionCatalog {

        val entries: List<OccasionEntry> = listOf(
            OccasionEntry(storageKey = "Housewarming", glyph = "⌂"),
            OccasionEntry(storageKey = "Wedding",      glyph = "♡"),
            OccasionEntry(storageKey = "Baby",         glyph = "◐"),
            OccasionEntry(storageKey = "Birthday",     glyph = "✦"),
            OccasionEntry(storageKey = "Christmas",    glyph = "❅"),
            OccasionEntry(storageKey = "Custom",       glyph = "+"),
        )

        /**
         * Legacy value → canonical v1.1 storageKey map.
         * Case-insensitive comparison via lowercase keys.
         */
        private val legacyAliases: Map<String, String> = mapOf(
            "baby shower" to "Baby",
            "anniversary" to "Housewarming",
        )

        /**
         * Returns the tile glyph for [occasion], applying case-insensitive matching
         * and the legacy alias map. Null / unknown → the Custom glyph "+".
         */
        fun glyphFor(occasion: String?): String {
            val canonical = storageKeyFor(occasion)
            return entries.firstOrNull { it.storageKey == canonical }?.glyph ?: "+"
        }

        /**
         * Canonicalises an arbitrary [occasion] string (potentially from a legacy
         * Firestore doc) to the v1.1 storage key. Null → "Custom". Unknown → "Custom".
         * Passes through any already-canonical v1.1 value unchanged.
         */
        fun storageKeyFor(occasion: String?): String {
            if (occasion == null) return "Custom"
            val normalised = occasion.trim()
            if (normalised.isEmpty()) return "Custom"
            val lower = normalised.lowercase()
            legacyAliases[lower]?.let { return it }
            return entries.firstOrNull { it.storageKey.equals(normalised, ignoreCase = true) }?.storageKey
                ?: "Custom"
        }
    }
    ```

    **File 2 — `app/src/main/java/com/giftregistry/ui/item/add/AddItemMode.kt`:**
    ```kotlin
    package com.giftregistry.ui.item.add

    /**
     * SCR-10: 3-mode segmented control on the Add Item screen (Paste URL / Browse
     * stores / Manual).
     *
     * Default = PasteUrl per CONTEXT.md § Add Item URL. The default ordinal is
     * exposed as a top-level const val so `rememberSaveable { mutableIntStateOf(ADD_ITEM_MODE_DEFAULT_ORDINAL) }`
     * can initialise without importing the enum (Phase 10 precedent from STATE.md:
     * "Tab index uses Int via rememberSaveable mutableIntStateOf(0), not sealed class").
     *
     * Unit-tested by AddItemModeTest (Wave 0).
     */
    enum class AddItemMode { PasteUrl, BrowseStores, Manual }

    /** Default AddItemMode ordinal for rememberSaveable initial value. */
    const val ADD_ITEM_MODE_DEFAULT_ORDINAL: Int = 0
    ```

    **File 3 — `app/src/main/java/com/giftregistry/ui/item/add/AffiliateRowVisibility.kt`:**
    ```kotlin
    package com.giftregistry.ui.item.add

    /**
     * SCR-10: Affiliate confirmation row visibility predicate.
     *
     * CONTEXT.md § Affiliate confirmation row: "shown only when OG fetch succeeded
     * AND the URL domain matches AffiliateUrlTransformer.merchantRules … Omitted
     * entirely for non-matching URLs — no misleading 'tag applied' for unknown
     * merchants."
     *
     * All three conditions must be true: url non-blank, OG fetch succeeded,
     * and isAffiliateDomain (from AffiliateUrlTransformer.isAffiliateDomain).
     *
     * Unit-tested by AffiliateRowVisibilityTest (Wave 0).
     */
    fun shouldShowAffiliateRow(
        url: String?,
        isAffiliateDomain: Boolean,
        ogFetchSucceeded: Boolean,
    ): Boolean = !url.isNullOrBlank() && ogFetchSucceeded && isAffiliateDomain
    ```

    **Critical constraints:**
    - No `androidx.compose.*` imports in any file — pure Kotlin, all three files live on the JVM classpath and are consumed by Compose call sites that are defined in Plans 03/04/05.
    - `OccasionCatalog.entries` list order must exactly match the Wave 0 test assertion (Housewarming / Wedding / Baby / Birthday / Christmas / Custom).
    - `OccasionCatalog` is an `object` (singleton) — no instantiation, `entries` is a `val` (immutable).
    - `legacyAliases` map keys MUST be lowercase since `storageKeyFor` looks up using `.lowercase()` — pins case-insensitivity across legacy docs.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "com.giftregistry.ui.registry.create.OccasionCatalogTest" --tests "com.giftregistry.ui.item.add.AddItemModeTest" --tests "com.giftregistry.ui.item.add.AffiliateRowVisibilityTest"</automated>
  </verify>
  <acceptance_criteria>
    - `ls app/src/main/java/com/giftregistry/ui/registry/create/OccasionCatalog.kt app/src/main/java/com/giftregistry/ui/item/add/AddItemMode.kt app/src/main/java/com/giftregistry/ui/item/add/AffiliateRowVisibility.kt` → all 3 exist
    - `grep "object OccasionCatalog" app/src/main/java/com/giftregistry/ui/registry/create/OccasionCatalog.kt` → matches
    - `grep -c "storageKey = \"Housewarming\"\|storageKey = \"Wedding\"\|storageKey = \"Baby\"\|storageKey = \"Birthday\"\|storageKey = \"Christmas\"\|storageKey = \"Custom\"" app/src/main/java/com/giftregistry/ui/registry/create/OccasionCatalog.kt` → 6 matches
    - `grep -c "\"baby shower\" to \"Baby\"\|\"anniversary\" to \"Housewarming\"" app/src/main/java/com/giftregistry/ui/registry/create/OccasionCatalog.kt` → 2 matches (legacy aliases)
    - `grep "enum class AddItemMode" app/src/main/java/com/giftregistry/ui/item/add/AddItemMode.kt` → matches
    - `grep "ADD_ITEM_MODE_DEFAULT_ORDINAL: Int = 0" app/src/main/java/com/giftregistry/ui/item/add/AddItemMode.kt` → matches
    - `grep "fun shouldShowAffiliateRow" app/src/main/java/com/giftregistry/ui/item/add/AffiliateRowVisibility.kt` → matches
    - `grep -c "import androidx.compose" app/src/main/java/com/giftregistry/ui/registry/create/OccasionCatalog.kt app/src/main/java/com/giftregistry/ui/item/add/AddItemMode.kt app/src/main/java/com/giftregistry/ui/item/add/AffiliateRowVisibility.kt` → 0 (pure Kotlin)
    - `./gradlew :app:testDebugUnitTest --tests "com.giftregistry.ui.registry.create.OccasionCatalogTest"` → 17 GREEN
    - `./gradlew :app:testDebugUnitTest --tests "com.giftregistry.ui.item.add.AddItemModeTest"` → 5 GREEN
    - `./gradlew :app:testDebugUnitTest --tests "com.giftregistry.ui.item.add.AffiliateRowVisibilityTest"` → 6 GREEN
    - `./gradlew :app:testDebugUnitTest` — full unit suite GREEN (no regressions in Phase 3-10 tests)
  </acceptance_criteria>
  <done>3 SCR-09/10 pure-Kotlin helper files shipped. 3 Wave 0 tests flipped GREEN (28 @Test methods passing). Total Phase 11 Wave 0 count: 51 / 51 GREEN.</done>
</task>

</tasks>

<verification>
Plan 02 completion checks:

1. AffiliateUrlTransformer gains isAffiliateDomain without regressing transform():
   ```bash
   grep "fun isAffiliateDomain" app/src/main/java/com/giftregistry/util/AffiliateUrlTransformer.kt   # expect match
   grep "private val merchantRules" app/src/main/java/com/giftregistry/util/AffiliateUrlTransformer.kt  # still private
   ```

2. All 7 Wave 0 tests GREEN (51 @Test methods):
   ```bash
   ./gradlew :app:testDebugUnitTest \
     --tests "com.giftregistry.ui.registry.detail.*" \
     --tests "com.giftregistry.ui.registry.create.OccasionCatalogTest" \
     --tests "com.giftregistry.ui.item.add.*"
   # expect: 51 tests, 0 failures
   ```

3. Full unit suite GREEN (no Phase 3-10 regressions):
   ```bash
   ./gradlew :app:testDebugUnitTest   # expect: BUILD SUCCESSFUL
   ```

4. No composables shipped in Plan 02:
   ```bash
   grep -r "@Composable" app/src/main/java/com/giftregistry/ui/registry/detail/FilterChipState.kt \
                        app/src/main/java/com/giftregistry/ui/registry/detail/HeroToolbarAlpha.kt \
                        app/src/main/java/com/giftregistry/ui/registry/detail/RegistryStats.kt \
                        app/src/main/java/com/giftregistry/ui/registry/detail/ShareUrl.kt \
                        app/src/main/java/com/giftregistry/ui/registry/create/OccasionCatalog.kt \
                        app/src/main/java/com/giftregistry/ui/item/add/AddItemMode.kt \
                        app/src/main/java/com/giftregistry/ui/item/add/AffiliateRowVisibility.kt
   # expect: no matches (pure Kotlin only)
   ```

5. Existing screens UNTOUCHED:
   ```bash
   git diff --name-only HEAD   # must NOT list RegistryDetailScreen.kt, CreateRegistryScreen.kt, AddItemScreen.kt, AppNavigation.kt
   ```

6. Debug APK still builds (sanity check — imports work, no missing refs):
   ```bash
   ./gradlew :app:assembleDebug   # expect: BUILD SUCCESSFUL
   ```
</verification>

<success_criteria>
- `AffiliateUrlTransformer.isAffiliateDomain(url)` shipped as a public method on the existing object (backward-compatible; `merchantRules` stays private; `transform()` unchanged)
- 7 pure-Kotlin helper files shipped across 3 packages:
  - `ui/registry/detail/` — FilterChipState.kt, HeroToolbarAlpha.kt, RegistryStats.kt, ShareUrl.kt
  - `ui/registry/create/` — OccasionCatalog.kt
  - `ui/item/add/` — AddItemMode.kt, AffiliateRowVisibility.kt
- All 7 Wave 0 tests flipped RED → GREEN (51 @Test methods passing)
- HeroToolbarAlpha.kt contains the `if (firstVisibleItemIndex >= 1) return 1f` Pitfall 1 guard on line 1 of the function body
- OccasionCatalog legacy alias map: `"baby shower" → "Baby"`, `"anniversary" → "Housewarming"` (case-insensitive via lowercased keys)
- No Compose imports in any Phase 11 helper file (pure-JVM, unit-testable without Compose runtime)
- No screen-level changes — RegistryDetailScreen.kt, CreateRegistryScreen.kt, AddItemScreen.kt, AppNavigation.kt UNTOUCHED
- Full unit suite GREEN; debug APK builds
- Commit message: `feat(11-02): Wave 1 — Phase 11 shared helpers + AffiliateUrlTransformer.isAffiliateDomain`
</success_criteria>

<output>
After completion, create `.planning/phases/11-registry-detail-create-add-item-redesign/11-02-SUMMARY.md` documenting:
- 8 files landed (1 edit to AffiliateUrlTransformer, 7 new helper files)
- 7 Wave 0 tests flipped RED → GREEN (51 @Test methods)
- Public API exposed for Plans 03 + 04 + 05:
  - `AffiliateUrlTransformer.isAffiliateDomain(url: String): Boolean` — new public method
  - `enum class FilterChipState { All, Open, Reserved, Completed }` + `fun FilterChipState.matches(status: ItemStatus): Boolean`
  - `fun heroToolbarAlpha(firstVisibleItemIndex: Int, firstVisibleItemScrollOffsetPx: Int, heroThresholdPx: Float): Float` with Pitfall 1 guard
  - `data class RegistryStats(items, reserved, given, views)` + `fun registryStatsOf(items: List<Item>): RegistryStats`
  - `fun shareUrlOf(registryId: String): String`
  - `object OccasionCatalog { val entries; fun glyphFor(String?); fun storageKeyFor(String?) }`
  - `enum class AddItemMode { PasteUrl, BrowseStores, Manual }` + `const val ADD_ITEM_MODE_DEFAULT_ORDINAL = 0`
  - `fun shouldShowAffiliateRow(url: String?, isAffiliateDomain: Boolean, ogFetchSucceeded: Boolean): Boolean`
- Commit SHA + diff stats
- Confirms Plans 03/04/05 can now run in parallel (each touches a different screen + its own strings.xml section)
</output>
