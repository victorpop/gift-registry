---
phase: 11-registry-detail-create-add-item-redesign
plan: 06
type: execute
wave: 2
depends_on:
  - 11-03
  - 11-04
  - 11-05
files_modified:
  - app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt
autonomous: false
requirements:
  - SCR-08
  - SCR-09
  - SCR-10

must_haves:
  truths:
    - "StyleGuidePreview.kt gains 7 new @Preview composables appended after the Phase 10 sections: HeroPlaceholderPreview (RegistryDetailHero imageUrl=null for Housewarming + Wedding), StatsStripPreview (StatsStrip with a mixed items list producing 12/2/1/0), FilterChipRowPreview (active=All), ShareBannerPreview, OccasionTileGridPreview (showing Housewarming selected + 5 unselected), AddItemSegmentedTabsPreview (3 tabs Paste URL selected), ItemPreviewCardPreview (with mock OG data)"
    - "All 7 previews wrap in GiftRegistryTheme and render without runtime errors in Android Studio's preview pane"
    - "Debug APK builds, installs, and launches on a real device / emulator running Android 31+"
    - "Full unit suite GREEN: 51 Phase 11 Wave 0 tests + Phase 8/9/10 tests all pass"
    - "Human checkpoint confirms all 20-25 on-device UAT checks pass covering SCR-08 (hero placeholder + gradient overlay + toolbar alpha fade + 4-stat strip + share banner tap copy+share + filter chips horizontal scroll + item row 58×58 + StatusChip + always-visible ⋯ + DropdownMenu Edit/Delete), SCR-09 (STEP 1 OF 2 indicator + Skip creates Draft + italic-accent headline + 2×3 tile grid with selected/unselected states + custom radio card + bottom CTA → AddItemKey on Continue / HomeKey on Skip), SCR-10 (× close + 3-tab segmented + PulsingDot at ~1 s cadence + affiliate row appears only on emag.ro + preview card with OG image + ✓ auto-filled tag + info pill with domain substitution + dual CTA with 1:1.5 flex + Browse stores tab navigates to StoreListScreen + Manual mode reveals form fields)"
    - "Romanian locale verified — 38+ Phase 11 string keys (13 detail + 13 create + 12-14 add item) render correctly in values-ro"
    - "Regression guards pass: reservation flow (reserve → confirm purchase) still works end-to-end; Phase 7 Store Browser → Add to list round-trip still pre-fills AddItem URL; invite bottom sheet still opens from RegistryDetail overflow"
  artifacts:
    - path: "app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt"
      provides: "7 new Phase 11 @Preview composables appended"
      contains: "HeroPlaceholderPreview"
  key_links:
    - from: "StyleGuidePreview.kt"
      to: "Plans 03/04/05 composables"
      via: "Direct @Composable invocation inside @Preview blocks wrapped in GiftRegistryTheme"
      pattern: "@Preview"
---

<objective>
Append 7 new `@Preview` composables to `StyleGuidePreview.kt` so the Phase 11 primitives can be inspected in Android Studio's preview pane without running the app, then run an on-device human checkpoint covering every acceptance criterion from SCR-08 + SCR-09 + SCR-10 against the live debug APK.

Purpose: Phase 8/9/10 precedent — visual surfaces that aren't unit-testable (Coil image loading, pulsing dot cadence, gradient overlay alpha, horizontal scroll gestures, share sheet launch, clipboard copy, locale switching) ship verification via StyleGuidePreview + a human checkpoint. Phase 10 Plan 05 shipped 5 previews + a 17-check UAT; Phase 11 scales to 7 previews + a 20-25-check UAT covering 3 screens. Plans 03-05 wired everything; Plan 06 proves it end-to-end before Phase 11 is declared complete.

Output: 1 file modified (StyleGuidePreview.kt gains 7 previews) + human-verified UAT report. If the human flags issues, each is triaged to (a) surgical fix applied immediately in this plan or captured as a Phase 11 gap-closure todo for a follow-up plan, (b) out-of-scope recorded in SUMMARY, or (c) invalid with rationale.
</objective>

<execution_context>
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorpop/ai-projects/gift-registry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-registry-detail-create-add-item-redesign/11-UI-SPEC.md
@.planning/phases/11-registry-detail-create-add-item-redesign/11-VALIDATION.md
@.planning/phases/11-registry-detail-create-add-item-redesign/11-CONTEXT.md
@.planning/phases/11-registry-detail-create-add-item-redesign/11-03-PLAN.md
@.planning/phases/11-registry-detail-create-add-item-redesign/11-04-PLAN.md
@.planning/phases/11-registry-detail-create-add-item-redesign/11-05-PLAN.md

<interfaces>
<!-- Plans 03/04/05 outputs consumed in StyleGuidePreview -->

From app/src/main/java/com/giftregistry/ui/registry/detail/RegistryDetailHero.kt:
```kotlin
@Composable internal fun RegistryDetailHero(registry: Registry?, listState: LazyListState, onBack: () -> Unit, onShare: () -> Unit, onOverflow: () -> Unit, modifier: Modifier = Modifier)
```

From app/src/main/java/com/giftregistry/ui/registry/detail/StatsStrip.kt:
```kotlin
@Composable internal fun StatsStrip(items: List<Item>, modifier: Modifier = Modifier)
```

From app/src/main/java/com/giftregistry/ui/registry/detail/ShareBanner.kt:
```kotlin
@Composable internal fun ShareBanner(registryId: String, onShared: () -> Unit, modifier: Modifier = Modifier)
```

From app/src/main/java/com/giftregistry/ui/registry/detail/FilterChipsRow.kt:
```kotlin
@Composable internal fun FilterChipsRow(items: List<Item>, activeFilter: FilterChipState, onFilterSelected: (FilterChipState) -> Unit, modifier: Modifier = Modifier)
```

From app/src/main/java/com/giftregistry/ui/registry/detail/RegistryItemRow.kt:
```kotlin
@Composable internal fun RegistryItemRow(item: Item, isLast: Boolean, onEdit: () -> Unit, onDelete: () -> Unit, modifier: Modifier = Modifier)
```

From app/src/main/java/com/giftregistry/ui/registry/create/OccasionTileGrid.kt:
```kotlin
@Composable internal fun OccasionTileGrid(selectedOccasion: String, onOccasionSelected: (String) -> Unit, modifier: Modifier = Modifier)
```

From app/src/main/java/com/giftregistry/ui/item/add/ItemPreviewCard.kt:
```kotlin
@Composable internal fun ItemPreviewCard(imageUrl: String, title: String, price: String, url: String, modifier: Modifier = Modifier)
```

From app/src/main/java/com/giftregistry/ui/registry/list/SegmentedTabs.kt (Phase 10 — reused):
```kotlin
@Composable fun SegmentedTabs(tabs: List<String>, selectedIndex: Int, onTabSelected: (Int) -> Unit, modifier: Modifier = Modifier)
```

<!-- Existing StyleGuidePreview.kt pattern (Phases 8/9/10) — new previews mirror the style -->
<!-- Preview composables are annotated with @Preview(showBackground = true, widthDp = <value>, heightDp = <value>) and wrapped in GiftRegistryTheme { ... } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 7 Phase 11 @Preview composables to StyleGuidePreview.kt</name>
  <files>
    app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt
  </files>
  <read_first>
    - app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt (full 355-line existing file — append after the last Phase 10 preview; confirm GiftRegistryTheme wrapper pattern + @Preview(widthDp = ..., heightDp = ...) annotation style)
    - .planning/phases/11-registry-detail-create-add-item-redesign/11-UI-SPEC.md (§ StyleGuidePreview Appendix — lists 8 preview sections; Plan 06 ships 7 since the `AddItemDualCtaBar` preview can be condensed or combined with another section)
    - app/src/main/java/com/giftregistry/ui/registry/detail/RegistryDetailHero.kt (Plan 03)
    - app/src/main/java/com/giftregistry/ui/registry/detail/StatsStrip.kt (Plan 03)
    - app/src/main/java/com/giftregistry/ui/registry/detail/ShareBanner.kt (Plan 03)
    - app/src/main/java/com/giftregistry/ui/registry/detail/FilterChipsRow.kt (Plan 03)
    - app/src/main/java/com/giftregistry/ui/registry/create/OccasionTileGrid.kt (Plan 04)
    - app/src/main/java/com/giftregistry/ui/item/add/ItemPreviewCard.kt (Plan 05)
    - app/src/main/java/com/giftregistry/ui/registry/list/SegmentedTabs.kt (Phase 10 — reused for AddItem tabs preview)
  </read_first>
  <action>
    Append 7 `@Preview` composables to the END of `StyleGuidePreview.kt` (before the final `}` if file has a wrapping class, or at bottom otherwise — match the existing file style). Each preview wraps content in `GiftRegistryTheme { ... }` matching the existing Phase 8/9/10 pattern.

    **Previews to add:**

    1. **HeroPlaceholderPreview** — shows `RegistryDetailHero` with `registry.imageUrl = null` + `occasion = "Housewarming"` (renders the accentSoft→accent gradient + ⌂ glyph). One preview for Housewarming; one for Wedding to verify glyph mapping.
       ```kotlin
       @Preview(showBackground = true, widthDp = 360, heightDp = 180)
       @Composable
       private fun HeroPlaceholderHousewarmingPreview() {
           GiftRegistryTheme {
               RegistryDetailHero(
                   registry = Registry(id = "r1", title = "Our new home", occasion = "Housewarming", imageUrl = null),
                   listState = rememberLazyListState(),
                   onBack = {},
                   onShare = {},
                   onOverflow = {},
               )
           }
       }

       @Preview(showBackground = true, widthDp = 360, heightDp = 180)
       @Composable
       private fun HeroPlaceholderWeddingPreview() {
           GiftRegistryTheme {
               RegistryDetailHero(
                   registry = Registry(id = "r2", title = "Ana & Radu", occasion = "Wedding", imageUrl = null),
                   listState = rememberLazyListState(),
                   onBack = {},
                   onShare = {},
                   onOverflow = {},
               )
           }
       }
       ```

    2. **StatsStripPreview** — `StatsStrip` with a mixed items list producing items=12 / reserved=2 / given=1 / views=0.
       ```kotlin
       @Preview(showBackground = true, widthDp = 360, heightDp = 80)
       @Composable
       private fun StatsStripPreview() {
           GiftRegistryTheme {
               StatsStrip(items = buildList {
                   repeat(9) { add(Item(id = "i$it", registryId = "r", title = "item $it", status = ItemStatus.AVAILABLE)) }
                   add(Item(id = "r1", registryId = "r", title = "res1", status = ItemStatus.RESERVED))
                   add(Item(id = "r2", registryId = "r", title = "res2", status = ItemStatus.RESERVED))
                   add(Item(id = "p1", registryId = "r", title = "given", status = ItemStatus.PURCHASED))
               })
           }
       }
       ```

    3. **ShareBannerPreview** — `ShareBanner(registryId = "abc123")`.
       ```kotlin
       @Preview(showBackground = true, widthDp = 360)
       @Composable
       private fun ShareBannerPreview() {
           GiftRegistryTheme {
               ShareBanner(registryId = "abc123", onShared = {})
           }
       }
       ```

    4. **FilterChipRowPreview** — `FilterChipsRow` with active=All.
       ```kotlin
       @Preview(showBackground = true, widthDp = 360)
       @Composable
       private fun FilterChipRowPreview() {
           GiftRegistryTheme {
               FilterChipsRow(
                   items = buildList {
                       repeat(9) { add(Item(id = "i$it", registryId = "r", title = "item $it", status = ItemStatus.AVAILABLE)) }
                       add(Item(id = "r1", registryId = "r", status = ItemStatus.RESERVED))
                       add(Item(id = "r2", registryId = "r", status = ItemStatus.RESERVED))
                       add(Item(id = "p1", registryId = "r", status = ItemStatus.PURCHASED))
                   },
                   activeFilter = FilterChipState.All,
                   onFilterSelected = {},
               )
           }
       }
       ```

    5. **OccasionTileGridPreview** — `OccasionTileGrid(selectedOccasion = "Housewarming")` rendering all 6 tiles with the first selected.
       ```kotlin
       @Preview(showBackground = true, widthDp = 360, heightDp = 400)
       @Composable
       private fun OccasionTileGridPreview() {
           GiftRegistryTheme {
               OccasionTileGrid(selectedOccasion = "Housewarming", onOccasionSelected = {})
           }
       }
       ```

    6. **AddItemSegmentedTabsPreview** — Phase 10 `SegmentedTabs` reused with 3 Phase 11 labels, Paste URL selected.
       ```kotlin
       @Preview(showBackground = true, widthDp = 360, heightDp = 80)
       @Composable
       private fun AddItemSegmentedTabsPreview() {
           GiftRegistryTheme {
               androidx.compose.foundation.layout.Box(modifier = androidx.compose.ui.Modifier.padding(16.dp)) {
                   SegmentedTabs(
                       tabs = listOf("Paste URL", "Browse stores", "Manual"),
                       selectedIndex = 0,
                       onTabSelected = {},
                   )
               }
           }
       }
       ```

    7. **ItemPreviewCardPreview** — `ItemPreviewCard` with mock OG data.
       ```kotlin
       @Preview(showBackground = true, widthDp = 360, heightDp = 140)
       @Composable
       private fun ItemPreviewCardPreview() {
           GiftRegistryTheme {
               androidx.compose.foundation.layout.Box(modifier = androidx.compose.ui.Modifier.padding(16.dp)) {
                   ItemPreviewCard(
                       imageUrl = "",  // preview-only: no network; placeholder fills the 80x80 box
                       title = "Philips HD9200/90 Airfryer",
                       price = "189",
                       url = "https://emag.ro/philips-airfryer-product/12345",
                   )
               }
           }
       }
       ```

    **Required imports** (add at top of StyleGuidePreview.kt, keeping alphabetical if the file uses that convention):
    - `androidx.compose.foundation.lazy.rememberLazyListState`
    - `androidx.compose.ui.unit.dp`
    - `com.giftregistry.domain.model.Registry`
    - `com.giftregistry.domain.model.Item`
    - `com.giftregistry.domain.model.ItemStatus`
    - `com.giftregistry.ui.registry.detail.RegistryDetailHero`
    - `com.giftregistry.ui.registry.detail.StatsStrip`
    - `com.giftregistry.ui.registry.detail.ShareBanner`
    - `com.giftregistry.ui.registry.detail.FilterChipsRow`
    - `com.giftregistry.ui.registry.detail.FilterChipState`
    - `com.giftregistry.ui.registry.create.OccasionTileGrid`
    - `com.giftregistry.ui.item.add.ItemPreviewCard`
    - `com.giftregistry.ui.registry.list.SegmentedTabs`

    **Note on `internal` visibility:** the Phase 11 sub-composables are `internal`, which means they're visible inside the same module (the `:app` module). Since `StyleGuidePreview.kt` is in `:app/src/main/java/com/giftregistry/ui/theme/preview/` — same module — the `internal` modifier doesn't block access. Preview composables can call them directly.

    **Do NOT:** add `@Composable` functions outside @Preview wrappers (StyleGuidePreview is preview-only, no production surfaces). Do NOT touch any other section of the file.
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@Preview" app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` → increased by 7 vs pre-plan baseline (Phase 10 count + 7)
    - `grep "HeroPlaceholderHousewarmingPreview\|HeroPlaceholderWeddingPreview\|StatsStripPreview\|ShareBannerPreview\|FilterChipRowPreview\|OccasionTileGridPreview\|AddItemSegmentedTabsPreview\|ItemPreviewCardPreview" app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` → ≥ 7 matches (the 8 names listed collapse to 7 entries — HeroPlaceholder counts as 2 separate previews but describes one component, counted as 1 "section")
    - `grep "RegistryDetailHero(\|StatsStrip(\|ShareBanner(\|FilterChipsRow(\|OccasionTileGrid(\|SegmentedTabs(\|ItemPreviewCard(" app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` → ≥ 7 invocations
    - `grep "GiftRegistryTheme {" app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt` → count increased by ≥ 7 vs Phase 10 baseline (each preview wraps in theme)
    - `./gradlew :app:compileDebugKotlin` → BUILD SUCCESSFUL
    - `./gradlew :app:assembleDebug` → BUILD SUCCESSFUL (full APK builds)
    - `./gradlew :app:testDebugUnitTest` → full unit suite green
  </acceptance_criteria>
  <done>7 Phase 11 @Preview composables appended to StyleGuidePreview.kt. All invoke live Phase 11 sub-composables with realistic mock data. Debug APK builds; full unit suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device human UAT — 41 checks covering SCR-08 + SCR-09 + SCR-10 + locale + regression guards</name>
  <files>
    app/src/main/java/com/giftregistry/ui/registry/detail/RegistryDetailScreen.kt
    app/src/main/java/com/giftregistry/ui/registry/create/CreateRegistryScreen.kt
    app/src/main/java/com/giftregistry/ui/item/add/AddItemScreen.kt
    app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt
    app/src/main/java/com/giftregistry/ui/navigation/AppNavigation.kt
  </files>
  <read_first>
    - .planning/phases/11-registry-detail-create-add-item-redesign/11-UI-SPEC.md (full pixel spec for all 3 screens — primary reference during human verification)
    - .planning/phases/11-registry-detail-create-add-item-redesign/11-CONTEXT.md (18 locked decisions — confirm each on-device)
    - .planning/phases/11-registry-detail-create-add-item-redesign/11-VALIDATION.md (Manual-Only Verifications table — drives the checkpoint list)
    - design_handoff/design_handoff_android_owner_flow/README.md § 08 + § 09 + § 10 (handoff visual contract)
    - .planning/phases/11-registry-detail-create-add-item-redesign/11-03-PLAN.md (SCR-08 acceptance criteria)
    - .planning/phases/11-registry-detail-create-add-item-redesign/11-04-PLAN.md (SCR-09 acceptance criteria)
    - .planning/phases/11-registry-detail-create-add-item-redesign/11-05-PLAN.md (SCR-10 acceptance criteria)
  </read_first>
  <what-built>
Phase 11 ships 3 re-skinned owner-facing screens (RegistryDetail, CreateRegistry, AddItem) + 13 new sub-composables + ~38 new string keys in EN + RO. Plans 01-05 completed the implementation; Plan 06 Task 1 appended preview harnesses to StyleGuidePreview.kt. This checkpoint runs a 41-check on-device UAT against a fresh debug APK install.

**SCR-08 Registry Detail:** 180 dp hero (Coil 3 AsyncImage or accentSoft→accent placeholder with occasion glyph), pinned toolbar with alpha-fade + Pitfall 1 guard, 4-stat strip, accentSoft share banner (clipboard copy + share intent + snackbar), horizontally scrolling filter chips (All/Open/Reserved/Completed) with ink-filled active state, full-width item rows with 58×58 thumbnail + shared StatusChip + always-visible 26×26 ⋯ button.

**SCR-09 Create Registry:** custom `← [STEP 1 OF 2] Skip` app bar, italic-accent "What's the / occasion?" headline, 2×3 OccasionTileGrid, form fields with GiftMaison colours, custom VisibilityRadioCard (18 dp ring + 8 dp accent dot), bottom CTA pushes AddItemKey (not RegistryDetailKey — Step 1→2 flow), Skip creates Draft via Phase 10 isDraft heuristic.

**SCR-10 Add Item:** × close bar, 3-tab SegmentedTabs (Paste URL / Browse stores / Manual), FetchingIndicator with PulsingDot(period=1_000.ms), AffiliateConfirmationRow gated on shouldShowAffiliateRow, ItemPreviewCard with 80×80 thumbnail, AutoFillTag, InfoPill with domain substitution, AddItemDualCtaBar (ghost 1f + primary 1.5f).
  </what-built>
  <action>
    No Claude implementation action — this task blocks on human visual review.

    Before signalling, Claude should:
    1. Confirm `./gradlew :app:installDebug` completes without error and reports the installed package `com.giftregistry`.
    2. Present the full 41-check list (in `<how-to-verify>` below) to the user.
    3. Wait for the `resume-signal` before proceeding.

    If the user reports any ❌ items, Claude triages each:
    - **(a) In-plan surgical fix:** apply the edit, verify the fix by running the specific UAT check again, commit on top of Plan 06 (small diffs only — 1-5 lines per issue).
    - **(b) Phase 11 gap-closure todo:** create `.planning/todos/pending/{date}-{slug}.md` describing the issue, the reproduction steps, and the expected Phase 11 spec reference. Link from 11-06-SUMMARY.md.
    - **(c) Out-of-scope:** record in 11-06-SUMMARY.md under "UAT findings deferred" with rationale (e.g., "deferred to v1.2 per CONTEXT.md Deferred Ideas").

    ANY regression-guard failure (checks 38-41) MUST be resolved in-plan — re-skin must not break existing behaviour.
  </action>
  <how-to-verify>
Run the debug APK on a physical device or emulator (API 31+ preferred):
```bash
./gradlew :app:installDebug
adb shell am start -n com.giftregistry/.MainActivity
```

Sign in (or create an account) with at least one existing registry + a few items (some AVAILABLE, some RESERVED, one PURCHASED) to exercise the filter chips and status treatments. Then step through the checklist below. For each check, mark ✅ / ❌ / ⚠️ (partial).

**SCR-08 — Registry Detail (Home → tap any card)**

1. Hero renders at 180 dp with either the registry image (Coil 3 loads from Firestore) OR the accentSoft→accent gradient placeholder with the correct occasion glyph (⌂ Housewarming, ♡ Wedding, ◐ Baby, ✦ Birthday, ❅ Christmas, + Custom/null)
2. Pinned toolbar at top-left shows ← Back, top-right shows ↗ Share + ⋯ Overflow; starts transparent and fades to opaque paper as the list scrolls
3. After scrolling far enough that the hero disappears, the toolbar STAYS fully opaque (Pitfall 1 guard — does NOT flash back to transparent)
4. 4-stat strip renders below hero with correct counts: "{N} ITEMS | {M} RESERVED | {K} GIVEN | 0 VIEWS" separated by 1 dp vertical dividers
5. Share banner (accentSoft pill) below stats shows the 26×26 accent square with ↗, the mono-caps URL in accent colour, and "Tap to copy or share" helper text in inkSoft
6. Tapping the share banner copies `https://gift-registry-ro.web.app/r/{yourRegistryId}` to clipboard AND launches the Android share sheet AND shows a "Link copied" snackbar at bottom
7. Filter chips row below banner: All / Open / Reserved / Completed, horizontally scrollable (LazyRow), with correct inline counts. Active chip is ink-filled with paper text; inactive chips have a line border and inkFaint text
8. Tapping Reserved chip filters the item list to only RESERVED items
9. Item row: 58×58 dp rounded thumbnail on left (with paperDeep placeholder if image absent), title + "{price} · {host}" below, shared Phase 9 StatusChip on right stacked above 26×26 circular ⋯ button with a 1 dp line border
10. Tapping ⋯ button opens the DropdownMenu with Edit and Delete; long-pressing an item row does NOT open the menu (verifies D-06 dual-gesture removal)
11. Toolbar ⋯ opens registry-level DropdownMenu with Edit / Share / Invite / Delete Registry
12. Legacy per-screen FAB NOT present; only the Phase 9 global bottom FAB is visible
13. PURCHASED items render with Phase 9 purchasedVisualTreatment: ~55 % opacity + grayscale thumbnail + strikethrough title (STAT-04 preserved)

**SCR-09 — Create Registry (Home → tap the Phase 9 centre FAB → New registry)**

14. Top bar shows `← [STEP 1 OF 2] Skip` — left back arrow, centred "STEP 1 OF 2" mono-caps text, right "Skip" TextButton
15. Headline reads "What's the" on line 1 (ink colour) and "occasion?" on line 2 (italic, accent terracotta colour)
16. Sub-headline below: "We'll set the tone, suggested categories, and email theme to match." in inkSoft
17. 2×3 tile grid: 6 tiles in order Housewarming ⌂ / Wedding ♡ / Baby ◐ / Birthday ✦ / Christmas ❅ / Custom +. Unselected = paperDeep bg + 1.5 dp line border + accent glyph + ink label. Tapping Housewarming sets it to selected state (accent bg + 1.5 dp accent border + accentInk glyph + accentInk label)
18. Form fields: Registry name (full width), Date (50%) + Time (50% — read-only placeholder), Place (full width). All fields render with paperDeep bg, line border unfocused, accent border focused, accent cursor. Tapping Date opens the DatePickerDialog
19. Visibility radio card below fields: paperDeep bg + line border, "VISIBILITY" mono-caps label, two rows (Public / Private). Selected radio = 18 dp circle with 2 dp accent border + 8 dp accent inner dot. Tapping either row switches selection (VM state updates)
20. Bottom CTA bar has 1 dp line border-top + full-width ink pill "Continue · add items →" with paper text. Tap Continue with valid title → save fires (CircularProgressIndicator appears briefly) → lands on Add Item screen (AddItemKey) with the new registryId (NOT RegistryDetailKey — Step 1 → Step 2 flow works)
21. Tapping Skip with a blank title: placeholder "Untitled draft" / "Registru nesalvat" (RO locale) saves silently → pops to Home. Phase 10 Drafts tab on Home shows the new draft

**SCR-10 — Add Item (arrive via Step 1 → Step 2 OR AddAction sheet → Item from URL)**

22. Top bar: × close icon left, centred "Add item" title, right spacer for balance. No Material3 TopAppBar styling — custom Row
23. 3-tab segmented control below top bar: Paste URL / Browse stores / Manual. Paste URL selected by default (paper pill with ink label); unselected tabs have inkFaint label on paperDeep track
24. Paste URL mode: URL field with paperDeep bg; paste `https://emag.ro/philips-airfryer-product/12345` (or any real emag.ro product URL) → field triggers OG fetch → mono-caps "⌕ FETCHING FROM EMAG.RO" appears below with a PulsingDot blinking at ≈1-second full cycle (visibly faster than the Phase 9 Reserved-chip dot at 1.4 s)
25. After OG fetch succeeds: affiliate row appears with "✓ Affiliate tag applied invisibly" (green ok colour) + "Clear" ghost button on right; preview card renders with 80×80 thumbnail + title + price + "emag.ro · via Open Graph" source line
26. Title field below has a "✓ auto-filled" green pill tag next to its label (only when OG populated the title); field value is the scraped OG title (editable)
27. Optional Note field appears below Title with "Optional notes" placeholder
28. Below Note: secondSoft info pill with ℹ glyph + "We recognized emag.ro and will add our affiliate tag. No impact on your guests." — domain substituted correctly via `%1$s` placeholder
29. Now paste an unknown domain URL (e.g. `https://example.com/product/1`). OG fetch runs but affiliate row + info pill DO NOT appear (CONTEXT.md rule: affiliate row only shown when isAffiliateDomain)
30. Bottom CTA bar shows TWO buttons: "Add another" (ghost outlined 1f flex) + "Save to registry ✓" (primary ink pill 1.5f flex). Primary button is noticeably wider than ghost button (1:1.5 ratio visible)
31. Tap "Add another" with valid title → item saves → form resets to empty → stays on Add Item screen for the next item
32. Tap "Save to registry ✓" with valid title → item saves → pops back to RegistryDetail → new item appears in the list
33. Tap Browse stores tab → immediately pushes StoreListScreen (Phase 7) with the current registryId pre-selected; tapping back returns to AddItem (Paste URL tab, not Browse stores — LaunchedEffect resets tab index before navigating)
34. Tap Manual tab → URL field + FetchingIndicator + AffiliateConfirmationRow + PreviewCard + InfoPill all HIDDEN; Title + Price + Image URL + Note fields exposed for direct entry. Tap Save saves via same AddItemUseCase path

**Romanian locale verification (Settings → Language → Romanian):**

35. RegistryDetail stats strip labels: OBIECTE / REZERVATE / DĂRUITE / VIZUALIZĂRI
36. CreateRegistry: "PASUL 1 DIN 2", "Omite", "Care este / ocazia?", tile labels "Casă nouă / Nuntă / Bebeluș / Zi de naștere / Crăciun / Personalizat", CTA "Continuă · adaugă obiecte →"
37. AddItem: "Lipește URL / Răsfoiește magazine / Manual" tabs, "Salvează în registru ✓", "Adaugă altul", info pill copy in RO

**Regression guards (ALL must pass):**

38. Reservation flow: tap Reserve on an AVAILABLE item → submit GuestIdentitySheet → retailer opens → item shows RESERVED with countdown; then tap "I completed the purchase" banner → item flips to PURCHASED (Phase 4 + Phase 6 preserved)
39. Invite flow: Registry detail overflow → Invite → InviteBottomSheet opens and sends invite (Phase 3)
40. Delete registry: Registry detail overflow → Delete Registry → AlertDialog → Confirm → registry deletes and pops to Home
41. Store Browser round-trip: Phase 9 FAB → Add action sheet → Browse stores → tap a store → WebView loads → tap "Add to list" bottom CTA → returns to AddItem with URL pre-filled via Phase 7 SavedStateHandle path

Reply with the marked-up checklist. Any ❌ on checks 1-37: triage per `<action>` block rules. Any ❌ on checks 38-41: blocking — must be fixed in this plan before Phase 11 can be declared complete.
  </how-to-verify>
  <verify>
    <automated>./gradlew :app:installDebug</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :app:installDebug` succeeds and reports `com.giftregistry` as the installed package
    - User reports "approved" after completing all 41 on-device checks (37 SCR-specific + 4 regression guards)
    - All 4 regression guards (checks 38-41) MUST be ✅ — any ❌ here is blocking and requires in-plan fix
    - Any ❌ on checks 1-37 is triaged: (a) surgical fix applied within this plan, (b) new Phase 11 gap-closure todo created under `.planning/todos/pending/`, or (c) out-of-scope rationale recorded in 11-06-SUMMARY.md
    - `⚠️` (partial) items are acceptable if documented in SUMMARY with rationale
  </acceptance_criteria>
  <done>User signs off "approved" on all 41 checks (or approves after surgical fixes for any flagged issues). All 4 regression guards pass. Any deferred issues are captured as gap-closure todos or out-of-scope notes in the Phase 11 SUMMARY.</done>
  <resume-signal>Paste the marked-up 41-item checklist and type "approved" or describe remaining issues that need in-plan fixes</resume-signal>
</task>

</tasks>

<verification>
Plan 06 completion checks:

1. 7 new @Preview composables in StyleGuidePreview.kt:
   ```bash
   grep -c "HeroPlaceholder\|StatsStripPreview\|ShareBannerPreview\|FilterChipRowPreview\|OccasionTileGridPreview\|AddItemSegmentedTabsPreview\|ItemPreviewCardPreview" app/src/main/java/com/giftregistry/ui/theme/preview/StyleGuidePreview.kt
   # expect ≥ 7
   ```

2. Debug APK builds:
   ```bash
   ./gradlew :app:assembleDebug   # BUILD SUCCESSFUL
   ```

3. Full unit suite green:
   ```bash
   ./gradlew :app:testDebugUnitTest   # 51 Phase 11 Wave 0 + Phase 8/9/10 tests green
   ```

4. Debug APK installs + launches:
   ```bash
   ./gradlew :app:installDebug
   adb shell am start -n com.giftregistry/.MainActivity
   # expect: app launches without crash
   ```

5. Human UAT completed with checklist and triage decisions recorded.
</verification>

<success_criteria>
- StyleGuidePreview.kt gains 7 Phase 11 @Preview composables (HeroPlaceholder × 2 locales, StatsStrip, ShareBanner, FilterChipRow, OccasionTileGrid, AddItemSegmentedTabs, ItemPreviewCard)
- Each preview wraps in GiftRegistryTheme and renders without runtime errors
- Debug APK builds + installs + launches on target device/emulator
- Full unit suite GREEN (51 Phase 11 Wave 0 tests + Phase 8/9/10 unchanged)
- Human UAT confirms all 41 on-device checks pass (or issues triaged to gap-closure todos / out-of-scope)
- Romanian locale verified for 38+ new keys across the 3 screens
- 4 regression guards PASS: reservation flow (reserve → confirm purchase), invite bottom sheet, delete flows, Store Browser → Add to list round-trip
- Commit message: `docs(11-06): Wave 2 StyleGuidePreview Phase 11 previews + human UAT`
</success_criteria>

<output>
After completion, create `.planning/phases/11-registry-detail-create-add-item-redesign/11-06-SUMMARY.md` documenting:
- 1 file changed (StyleGuidePreview.kt — 7 new @Preview composables)
- UAT results: N/41 ✅ / M ❌ / K ⚠️
- Each ❌ triaged: (a) surgical fix applied in this plan, (b) new Phase 11 gap-closure todo, or (c) out-of-scope rationale
- Romanian locale verified for 38+ new Phase 11 keys
- Regression guards pass: reservation flow, invite bottom sheet, delete flows, Store Browser round-trip
- Phase 11 DONE — 3 screens (SCR-08 + SCR-09 + SCR-10) shipped pixel-accurately with all Phase 3/4/6/7/9/10 behaviour preserved
- Commit SHA
</output>
