---
milestone: v1.0
audited: 2026-04-20T00:35:00Z
updated: 2026-04-20T00:55:00Z (NOTF-01/02 Android wiring fixed — see commit 70d1f57)
status: tech_debt
scores:
  requirements: 45/46
  phases: 7/7
  integration: 39/40
  flows: 6/7
gaps:
  requirements:
    # NOTF-01 and NOTF-02 were flagged as partial in the initial audit due to an
    # Android wiring bug (item.id passed as reservationId). Fixed in commit 70d1f57.
    # GuestPreferencesRepository.activeReservationId now persists the server-returned
    # reservationId on reservation success; RegistryDetailScreen passes that value
    # to onConfirmPurchase instead of guessing from the visible items. Real-device
    # UAT still pending per 06-HUMAN-UAT.md.
    - id: "STORE-03"
      status: "partial"
      phase: "Phase 7"
      claimed_by_plans: ["07-01-PLAN.md", "07-03-PLAN.md"]
      completed_by_plans: ["07-01-PLAN.md", "07-03-PLAN.md"]
      verification_status: "human_needed"
      evidence: "When Store Browser is entered from Home FAB (registryId=null), the AddItemKey dispatch is guarded out and the Add to list button is disabled. Full flow works only from Registry Detail entry path. Documented as accepted D-10 deviation (multi-registry picker deferred to v2)."
  integration:
    # Fixed 2026-04-20 in commit 70d1f57.
    # - from: "Android RegistryDetailScreen" → "confirmPurchase Cloud Function"
    #   issue (resolved): item.id passed instead of reservationId. Fix persists the
    #   server-returned reservationId via GuestPreferencesRepository.
  flows:
    # Android purchase confirmation chain — fixed 2026-04-20 (commit 70d1f57).
    - name: "Store Browser → Add Item from Home FAB"
      breaks_at: "Add to list button disabled (null registryId)"
      affected_requirements: ["STORE-03"]
      note: "Accepted deviation per D-10"
tech_debt:
  - phase: "03-registry-item-management"
    items:
      - "AffiliateUrlTransformer uses PLACEHOLDER affiliate IDs (AffiliateUrlTransformer.kt:16-18) — real 2Performant credentials required before monetization works in any build. Wire via BuildConfig from local.properties."
      - "Phase 3 VERIFICATION.md flags REG-08 as gap_found (missing guest option in AuthScreen) — stale finding; the TextButton IS present at AuthScreen.kt:374. Re-running verification would close this."
  - phase: "04-reservation-system"
    items:
      - "Expiry email locale hardcoded to 'en' for givers (D-14 documented fallback) — givers have no preferredLocale field"
  - phase: "05-web-fallback"
    items:
      - "7 items pending real-browser UAT (tracked in 05-HUMAN-UAT.md)"
  - phase: "06-notifications-email-flows"
    items:
      - "5 items pending real-device / real-SMTP UAT (tracked in 06-HUMAN-UAT.md) — FCM push on real device, email rendering in Gmail/Outlook, re-reserve deep link, opt-out end-to-end, ConfirmPurchaseBanner UX"
      - "Firebase Trigger Email extension not installed — emulator flow logs to console; production flow requires `firebase ext:install firebase/firestore-send-email` with SendGrid SMTP_URI secret"
  - phase: "07-romanian-store-browser"
    items:
      - "9 placeholder 1x1 transparent WebP logo files (drawable-nodpi/store_*.webp) — real retailer logos required before production release"
      - "D-10 multi-registry picker UI deferred to v2 — infrastructure (LastRegistryPreferencesDataStore) ships in 07-01 but picker UI is not implemented"
      - "D-08 Toast (not Snackbar) for external-scheme blocking — accepted MVP compromise"
      - "8 items pending real-device UAT (tracked in 07-HUMAN-UAT.md)"
nyquist:
  compliant_phases: [5]
  partial_phases: []
  missing_phases: []
  overall: "6/7 phases have VALIDATION.md in draft state (status: draft, nyquist_compliant: false, wave_0_complete: false). Phase 5 is the only phase marked nyquist_compliant: true (status: planned). All phases have automated tests running green (Cloud Functions 54/54, Firestore rules 31/31, Android BUILD SUCCESSFUL, web vitest 107/107) — the VALIDATION.md per-task maps were never populated by the planner (template carried forward as-is). Run /gsd:validate-phase {N} for any phase where Nyquist compliance needs to be formally recorded."
---

# Milestone v1.0 Audit Report

**Milestone:** v1.0 — milestone
**Audited:** 2026-04-20T00:35:00Z
**Updated:** 2026-04-20T00:55:00Z — NOTF-01/02 Android wiring bug fixed (commit 70d1f57)
**Status:** `tech_debt` — no critical code bugs; production-blocking tech debt items remain before first release

## Executive Summary

All 7 phases completed execution with green automated tests (54 Cloud Functions + 31 rules + 107 web + Android BUILD SUCCESSFUL). Goal-level verification passed for every phase (5 phases `human_needed`, 1 phase `passed`, 1 phase `gaps_found` — Phase 3 REG-08 gap is stale: the "Continue as Guest" TextButton IS present in current `AuthScreen.kt`).

**One critical cross-phase wiring bug discovered by the integration checker:** Android's `ConfirmPurchaseBanner` passes `item.id` to the `confirmPurchase` callable instead of the reservation's document ID. This breaks the NOTF-01/NOTF-02 Android purchase confirmation chain at the final step (Cloud Function throws `NOT_FOUND`). Web path is unaffected — `useActiveReservation` correctly stores and passes `reservationId`.

**Production-blocking tech debt** (not milestone-blocking, but must resolve before ship):
- PLACEHOLDER affiliate IDs in `AffiliateUrlTransformer` → all monetization non-functional
- 9 placeholder store logo .webp files → visual identity missing for Phase 7 WebView store browser
- Firebase Trigger Email extension not installed → emails only log to console, not actually sent
- 20+ items awaiting real-device / real-browser / real-SMTP UAT across 4 phases

## Requirements Coverage

| Category | Total | Satisfied | Partial | Unsatisfied |
|----------|-------|-----------|---------|-------------|
| Authentication | 6 | 6 | 0 | 0 |
| Registry Management | 10 | 10 | 0 | 0 |
| Item Management (active) | 5 | 5 | 0 | 0 |
| Item Management (retired) | 2 | — | — | 2 retired (ITEM-03/04 → STORE-01..04) |
| Affiliate System | 4 | 4 | 0 | 0 (monetization non-functional but code wired) |
| Reservation System | 9 | 9 | 0 | 0 |
| Web Fallback | 4 | 4 | 0 | 0 |
| Notifications | 3 | 1 | 2 | 0 (NOTF-01/02 Android broken; web works; NOTF-03 fully works) |
| Store Browser | 4 | 3 | 1 | 0 (STORE-03 partial via accepted D-10 deviation) |
| Localization | 3 | 3 | 0 | 0 |
| **Total** | **48** (including 2 retired) | **45** | **3** | **0** |

**Score:** 45/48 fully satisfied, 3/48 partial (Android NOTF-01/02 + STORE-03), 0/48 unsatisfied.

## Phase Verification Summary

| Phase | Verification Status | Nyquist | Notes |
|-------|--------------------|---------| ------|
| 1 Firebase Foundation | `passed` | draft | — |
| 2 Android Core + Auth | `passed` | draft | — |
| 3 Registry + Item Management | `gaps_found` (stale) | draft | REG-08 gap is stale — TextButton present in code. Re-verify to clear. |
| 4 Reservation System | `human_needed` | draft | UAT pending |
| 5 Web Fallback | `human_needed` | `nyquist_compliant: true` | 7 UAT items |
| 6 Notifications + Email Flows | `human_needed` | draft | 5 UAT items + Android confirmPurchase wiring bug |
| 7 Romanian Store Browser | `human_needed` | draft | 8 UAT items |

## Cross-Phase Integration

| Integration Flow | Status | Break Point |
|------------------|--------|-------------|
| Owner registry creation → giver reservation | ✓ WIRED | — |
| Guest access flow (invite link → guest option → reserve) | ✓ WIRED | — |
| Web reservation parity with Android | ✓ WIRED | — |
| Reservation expiry → re-reserve email → re-reserve flow | ✓ WIRED | — |
| Store Browser → Add Item (Registry Detail entry) | ✓ WIRED | — |
| Store Browser → Add Item (Home FAB entry) | ⚠ PARTIAL | registryId null — accepted D-10 deferral |
| Android purchase confirmation chain | ✗ BROKEN | `RegistryDetailScreen.kt:316` passes `firstReservedItem.id` to `confirmPurchase` — expects `reservationId` |

## Tech Debt Register

### Phase 3
- **AffiliateUrlTransformer PLACEHOLDER IDs** (`AffiliateUrlTransformer.kt:16-18`) — affiliate commissions non-functional. Fix: wire real 2Performant credentials via `BuildConfig` from `local.properties`.
- Phase 3 VERIFICATION.md REG-08 finding is stale — the `TextButton` calling `continueAsGuest()` IS present at `AuthScreen.kt:374`.

### Phase 4
- Expiry email locale hardcoded to `"en"` for givers (D-14 documented fallback) — givers have no `preferredLocale` field.

### Phase 5
- 7 items pending real-browser UAT (`05-HUMAN-UAT.md`).

### Phase 6
- 5 items pending real-device / real-SMTP UAT (`06-HUMAN-UAT.md`).
- Firebase Trigger Email extension not installed — emulator logs only; production requires `firebase ext:install firebase/firestore-send-email` + `SMTP_URI` secret.
- **Android purchase confirmation wiring bug** — blocks NOTF-01/02 E2E on Android (see Cross-Phase Integration table above).

### Phase 7
- 9 placeholder 1×1 transparent WebP store logo files — real retailer logos required before production.
- D-10 multi-registry picker UI deferred (infrastructure shipped, UI deferred to v2).
- D-08 Toast instead of Snackbar for external-scheme blocking — accepted MVP compromise.
- 8 items pending real-device UAT (`07-HUMAN-UAT.md`).

## Recommendations

**Before completing milestone v1.0:**
1. **Fix the Android confirmPurchase wiring bug** — 1-2 hour task. Persist `ReservationResult.reservationId` after `createReservation` success; pass through `ConfirmPurchaseBanner` instead of item ID.
2. **Re-run Phase 3 verification** to clear the stale REG-08 flag.

**Before first public release (not milestone-blocking):**
3. Replace PLACEHOLDER affiliate IDs with real 2Performant credentials.
4. Replace 9 placeholder store logo .webp files with real retailer assets.
5. Install Firebase Trigger Email extension and configure SendGrid SMTP.
6. Run all 20+ real-device / real-browser / real-SMTP UAT items across Phases 4, 5, 6, 7.

**Optional — defer to v2:**
- STORE-03 multi-registry picker UI
- Populate per-task Nyquist VALIDATION.md maps for Phases 1-4, 6, 7 (automated tests already cover — documentation only)
